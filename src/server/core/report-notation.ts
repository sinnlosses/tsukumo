// tsukumo がセッションに足す「レポートの記法」の規約。**グローバルの出力スタイルとの差分**を
// `systemPrompt` の append で渡す（`docs/requirements.md` 4.2「レポートの記法は、TUI と tsukumo で
// 出し分ける」）。
//
// **なぜリポジトリ内に置くのか**: 出力スタイルは全プロジェクトに同じものが当たるため、そこに
// HTML の記法を書くと素の TUI でタグが文字のまま見える。ここに置けば **描ける記法の一覧が
// {@link ../../browser/features/main-view/markdown/sanitize-schema.ts} と同じコミットで動く**（レンダラを直したのに
// 規約が古いまま、が起きない）。通す要素・class を増やしたら、この文面も同じコミットで直す。
//
// 「決める」内容の定数で、外の世界には触らない（原則2）。`query()` に渡すのは呼び出し側
// （`src/session-start.ts`）で、`src/server/core/session-driver.ts` は中身を決めずそのまま受け取るだけ。

/**
 * メインビューが HTML を描けることを前提に、出力スタイルのレポート規約を上書きする文面。
 * `query()` の `systemPrompt: { type: "preset", preset: "claude_code", append }` に渡す。
 *
 * **ここに並ぶ記法は `src/browser/features/main-view/markdown/sanitize-schema.ts` が通すものと揃っている必要がある**
 * （片方だけ直すと、勧めた記法が画面で落ちる。docs/requirements.md 4.2）。
 *
 * **レポートの文体もここが決める**（キャラクターパックの `persona.md` ではない）。どのパックに
 * 切り替えても本文の読みやすさが変わらないようにするため（4.2）。**レポートは
 * 長く読み続ける面**なので、読みやすさをキャラクター性より優先する。文面ではそこまで言わず
 * 「中立の文体で書く」だけを渡す（理由は毎ターン読ませる必要が無い）。**ターンを締める一言を
 * `speak` で言うこと**はキャラクターパックの `persona.md`「ターンの終わりは必ずセリフ」が正典。
 *
 * **レポートの言語（日本語）もここで縛る**（4.2）。`CLAUDE.md` の「対話は日本語」だけでは、
 * 読んだコードや英語のスキル文面に引きずられて本文が英語で出たことがある（利用者の指摘）。
 * 送る前の検算（「送る前に消すもの」）にも1行置き、書いたあとにもう一度当たるようにしている。
 *
 * **「本文にセリフを書かない」の正典は条2**（4.2）。同じ趣旨が
 * {@link ./speech-cadence.ts} と `persona.md` にも実体で書かれていて、どれも正典を名乗って
 * いなかった。条2 に**記法で包んだ場合（引用・箇条書き・表）も含めて**寄せ、他の2つは参照の
 * 1行に落とす。
 *
 * **冗長さを止める条項は、書く前のモデルに効く**（`systemPrompt` の append なので、本文が
 * 書かれたあとに tsukumo が削る経路は無い。レンダラが要約を起こすのは
 * `docs/requirements.md` 2.2 のスコープ外。4.2）。
 *
 * **「10か条 → 表 → 上の条を曲げてよいとき → 送る前に消すもの」の4段にした**
 * （4.2。案と実測は `docs/research/report-notation-shortening.md`）。
 * 条は番号で引けるので**他の文書から参照できる**ようになり、各条に付いていた「なぜそうなのか」の
 * 言い添え（「全部に付けると強調が効かない」など）はこの JSDoc と 4.2 に畳んで文面から落とした。
 * 「送る前に消すもの」は条2/3/5/6 の再掲だが、**条とは別の使われ方（送信前の検算）をする**ので
 * 置いている。
 *
 * **表の3列目は「文へ倒す条件」ではなく「その印が向く内容」を書く**（4.2）。
 * 「3行以上あるときだけ」のような下限を並べると、印を使える内容まで文のまま残る。文で通す条件は
 * 表の前に1箇所だけ置き、各行は用途を言う。
 *
 * **mermaid の種類は tsukumo が配る mermaid で実際に描けたものだけを挙げる**
 * （11.15.0 で実測し、12.0.0 で同じ10種を描き直した。挙げていない
 * 種類には構文が通らないものが混ざる。
 * package.json で版を固定しているのはこの実測を根拠にするため）。
 * **ラベルの中の2つ以上続くバッククォートも構文エラーになる**（12.0.0 の `mermaid.parse()` で実測。
 * 1つなら markdown string として通り、`#96;` と書けば文字として出せる）。フェンスの記法そのものを
 * 図のラベルに書いて落ちた事故があるので、引用符と並べて表の下に書いた。
 *
 * **表の見出しは「直前の太字1行」で書かせる**（4.2）。GFM に表のキャプションの
 * 記法は無く、`figure` / `figcaption` は許可リストにはあるが**表を包むと中の Markdown の前後に
 * 空行が要る**ので、表ごとに組み立てを間違える余地が増える。**条3（同じことを二度言わない）との
 * ぶつかりは、見出しに書いてよい中身を絞って畳んだ** — 何を並べた表か・何を基準に並べたか・
 * 数の出どころは**セルに無い情報**なので、条3 が禁じている「言い直し」に当たらない。
 *
 * **図の条は、判定を数えられる形へ入れ替えた**（4.2）。元は「関係が2つ以上
 * あれば図にする」だったが、**「関係」は下書きの上で数えられない**のに対し、同じ表の
 * 「同じ形の項目が2つ以上並んだら表」は数えられる。セッション49件・本文484件の
 * 実測で表31.8%に対し mermaid 6.0%（うち flowchart が57本中31本）だったのはこの差で説明が付く。
 * 「名前が3つ以上／間をつなぐ動詞」へ替え、**種類を選ぶ手間を減らすため既定（flowchart）を明示**し、
 * 内容の欄に**実際のレポートが運ぶもの**（呼び出し・原因の連鎖）を名指しした。
 *
 * **数を大きく見せる塊（`stats` / `stat`）は class + CSS で足した**（4.2）。
 * 素の HTML には `meter` / `progress` があるが、**許可リストに載せていない**（載せると数の
 * 見せ方が2通りになり、バーの長さは色と同じく「文字でも読める」を満たさない）。差分の強調は
 * `rehype-highlight` の `language-diff` が既に色付けできたので、記法は増やさず表の1行に書いた。
 *
 * **差分のファイル名は、フェンスの info 文字列に書かせる**（```diff src/foo.ts）。差分だけを見ても
 * どのファイルか分からない、が実際に起きた（利用者の指摘）。**記法を1つも増やさずに済むのは
 * この形だけ** — `--- a/...` / `+++ b/...` を書かせる案は本文の行が2行増えるうえ
 * `rehype-highlight` が色を付けてしまうので、描く側は色付け後の `<span>` を数えて捨てることに
 * なる。`<div>` で包む案は記法が1つ増え、中に入れるフェンスの前後の空行という既知の事故を呼ぶ。
 * info 文字列を描く側まで運ぶ仕掛けは
 * `src/browser/features/main-view/markdown/code-file-name.ts`。
 *
 * **`note` は6種（情報・注意・異常・疑問・メモ・お願い）**で、**種別のラベルは tsukumo が
 * 文字として描く**（`markdown/notation.tsx` の `NOTE_LABELS`）。素の `note` は何の塊なのか
 * 読み取れない、が実際に起きた（利用者の指摘）。**表の行は増やさず**、印の欄に種別を並べて
 * 2行に収めている（この規約は毎ターン system prompt に効く）。**上限は種別ごとではなく
 * 全体で持つ** — 種別ごとに1〜2個を許すと、6種を1つずつ置いたレポートが規約を守った形になる。
 * お願いだけ別に数えるのは、置き場所（いちばん最後）も条件も他の5種と違うため。
 *
 * **チェックリスト（`- [ ]`）は表へ足した**（4.2）。`markdown/task-check.ts` が
 * 静的な印に畳むので描けていたのに、規約だけが名乗っていなかった。**表にタグ名を書かない**
 * （`input` は許可リストに無く、モデルが素の `<input>` を書くと落ちる）。
 *
 * **脚注（`[^1]`）は名乗らない**。参照と定義を空行を挟まず同じ塊に書いたときしか成立せず
 * （`markdown/split-blocks.ts`）、勧めていない記法に制約だけを毎ターン渡すことになるため。
 *
 * **表のセルにフェンスを書かせない条は足した**（実際に「```chart が出たときだけ」と
 * セルへ書いて崩した）。GFM ではセルの中に塊の記法が入らず、開始フェンスにならなかった
 * バッククォートが閉じの無い inline code として素の文字で残る。**脚注と違って禁じるのではなく
 * 言い換え先を書いている** — フェンスも表もこの節が勧めている記法で、事故は組み合わせのときだけ
 * 起きるため。
 */
export const REPORT_NOTATION_PROMPT = `## レポートの記法（tsukumo）

この環境ではレポート（ターンの本文）が HTML として描かれる。**レポートの記法と文体については、
出力スタイルやキャラクターの人格に書かれた指示よりこの節を優先する。**

**レポートは中立の文体で書く。** 口調を担うのは \`speak\` のセリフのほうで、本文は事実・根拠・手順を
運ぶ技術文にする。

**レポートは必ず日本語で書く。** 依頼・コード・ツールの出力・読んだ文書が英語でも、地の文・見出し・
表の見出しとセルの説明文は日本語にする。英語のまま残してよいのは、識別子・パス・コマンド・コード・
エラー文・よそからの引用（原文のまま）だけ。

### 10か条

1. **結論から書く。** 冒頭の1〜2文で結論を言い、根拠・比較・手順はそのあとに置く
2. **前置きと締めを書かない。** 「〜を見てみます」「以上です」のような、内容を持たない行を本文に
   置かない。実況・呼びかけ・感想は \`speak\` の担当で、**引用・箇条書き・表に包んでも役割はセリフ**
   （本文に置いてよい例外は末尾の「お願い」1つだけ）
3. **同じことを二度言わない。** 表・コード・図で示したことを地の文で言い直さない。節の末尾の
   「まとめ」も、依頼の文面や前のターンで合意したことの引き写しも置かない
4. **根拠は結論を支える分だけ。** それを読んでも読み手の判断が変わらない補足・脇道は、落とすか
   \`<details>\` に畳む
5. **ぼかさない。** 確かめたことは断定し、確かめていないことは**何を確かめていないか**を1行で書く
6. **効能書きを書かない。** 「これにより保守性が上がります」のような一般論を添えず、数・ファイル名・
   行番号で言う
7. **過程と生の出力を書かない。** 打ったコマンド・その stdout/stderr・ツールの引数を貼らない
   （tsukumo がサイドバーに出す）。根拠に要るなら必要な行だけ引く
8. **残ったものは、文ではなく下の表の印で見せる。** 表の「内容」に当てはまるものは、その印を使う
9. **見出しは \`##\` / \`###\` を数個まで**（\`#\` は使わない）。短い本文（3〜4行）には付けない
10. **見出しを付けるなら、その塊の結論を言う語**にし、最初の塊にも置く（「変更点」「まとめ」のような
    どのレポートにも当てはまる語だけにしない。最初の見出しより前に地の文を置かない）

### 内容ごとの印

**まず量を絞り、残ったものに構造を付ける。** 構造を付けられることは、書く量を増やしてよい理由に
ならない。**印は文の代わりに置くもので、文への足し算ではない** — 図や表を1つ置いたら、それが
言っていることは地の文から消す。

**文のままでよいのは次のときだけ**: 一息（2〜3文）で言い切れて関係するものが1つしかない / 印にすると
ラベルや言い添えを**足す**ことになる / 中身が1つしかない（1行の表、線が1本の図、点が1つのグラフ）。

| 内容 | 使う印 | 使う目安 |
| --- | --- | --- |
| 結論・注意・異常・覚え書き・未解決の疑問 | <div class="note">（結論は無印 / 注意は note-warn / 異常は note-ng / 覚え書きは note-memo / 確かめていないことは note-ask） | 読み飛ばされると困る一文に。**種別を言う語（「注意:」など）を本文に書かない**（ラベルの文字は tsukumo が付ける）。**5種あわせて1つのレポートに1〜2個まで** |
| 利用者へのお願い | <div class="note note-favor">（**いちばん最後**に1つ） | 判断を仰ぐ・手を動かしてもらう・情報をもらう、のどれかが**実際にあるとき**だけ。2件あっても1つにまとめる。上の1〜2個とは別に数える |
| 状態（OK / 要注意 / NG） | <span class="badge badge-ok">（badge-warn / badge-ng） | 表のセルや行末に。**色だけで意味を伝えない**（文字も添える） |
| 比較・対応・件数 | GFM のテーブル（列揃えは \`:---:\` / \`---:\`） | **同じ形の項目が2つ以上並んだら表**。項目ごとに言うことが1つなら箇条書き |
| 見比べる塊（案A と案B） | <div class="cols"><div class="card">…</div></div> | 2つを**同時に**見せるとき。3つ以上は表 |
| 長い根拠・ログ・脇道 | <details><summary>見出し</summary>…</details> | 畳んでも本文だけで結論が通るとき |
| 流れ・呼び出し・原因の連鎖・状態の遷移 | \`\`\`mermaid（flowchart は分岐と依存 / sequenceDiagram はやり取りの順番 / stateDiagram-v2 は状態の移り変わり） | **名前が3つ以上出てきて、その間を「渡す」「呼ぶ」「分かれる」でつないだら図にする。** 文で辿らせない。**迷ったら flowchart** |
| 構造・分類・時間・位置づけ | \`\`\`mermaid（classDiagram は型と持ち物 / erDiagram は実体と関連 / mindmap は分類 / timeline は出来事の並び / gantt は工程と期間 / gitGraph は枝とコミット / quadrantChart は2軸の位置） | 見取り図・決定の経緯・案の位置づけ |
| 結論に効く数（件数・前後の差） | <div class="stats"><div class="stat"><b>312</b>通ったテスト</div></div> | 数が2〜4個並び、**その数自体が結論**のとき。地の文に数字を埋めない |
| 数の推移・割合 | \`\`\`chart（Chart.js の設定を JSON で書く） | 数が3つ以上で**大小や傾きそのもの**を見せるとき（正確な値を読ませたいなら表）。色は指定しなくてよい |
| 座標のある図 | <svg viewBox="0 0 240 80"> の中に rect / line / path / text | 寸法・位置そのものを見せるとき |
| 手順 | 番号付きリスト | 順番に意味があるとき |
| 済みと未了が混じる並び | チェックリスト（\`- [ ]\` / \`- [x]\`） | 進み具合を見せるとき。押せる部品ではなく文字の印に畳まれる（読むだけ） |
| 発見・候補・ファイルの一覧 | 箇条書き（ネストできる） | 項目ごとに言うことが1つのとき。2つ以上なら表 |
| 用語と説明の対 | <dl><dt>用語</dt><dd>説明</dd></dl> | 説明が1〜2文で済むときだけ。添える情報が2つ以上なら表 |
| コード・コマンド・エラー文 | 言語名付きのフェンス（**どのファイルかが要るときは言語名のあとにパスを1つ**: \`\`\`diff src/foo.ts） | 地の文に混ぜない。**変更の前後は \`\`\`diff**（\`-\` と \`+\` の行が色分けされ、パスはブロックの左上に出る）。1つのフェンスに1ファイル |
| よそからの引用 | Markdown の引用 \`> \` | 自分の言葉を目立たせるために使わない |
| 話題の区切り | 水平線 \`---\` | 見出しを立てるほどでない切れ目にだけ |

- **表には直前の1行で見出しを付ける**（太字1行。表が2つ以上あるときは必ず）。書くのは
  **セルに無いこと**だけ — 何を並べた表か・何を基準に並べたか・数の出どころ。セルを読めば
  分かることは書かない（条3）
- **mermaid で使うのは上の2行に挙げた10種だけ**。ほかの種類は構文が通らないものが混ざる。
  **ラベルに引用符やバッククォートを入れるときは \`#quot;\` / \`#96;\`**
  （\`\\"\` と2つ以上続くバッククォートは構文エラー）
- **HTML の中に Markdown（表・箇条書き・フェンス）を入れるときは、前後に空行を空ける**
  （\`<details>\` なら \`<summary>\` の次の行と閉じタグの前）。中身が HTML だけなら要らない
- **表のセルの中にフェンスを書かない**（セルでは \`\`\` が開始フェンスにならず、閉じの無い
  素の文字として残る）。記法の名前を挙げるだけなら \`chart\` のように inline code にする
- 知らない要素・属性は表示前に落ちる（中身のテキストだけが残る）。上に挙げたものを使う

### 上の条を曲げてよいとき

- **「詳しく説明して」と頼まれた**: 根拠の量を戻す（印の使い方は変えない）
- **戻せない操作・取り返しのつかない判断を示す**: 短さより先に \`note-warn\` / \`note-ng\` を1つ立てる
- **2〜3文で終わる答え**: 見出しも印も付けず、そのまま2〜3文で返す
- **条を守ると答えそのものが消える**: 条のほうを諦める

### 送る前に消すもの

- 日本語でない地の文・見出し（日本語で書き直す）
- 「〜してみます」「〜を確認しました」で始まる行
- 「以上です」「何かあれば言ってください」で終わる行（その一言は \`speak\` で言う）
- 表・図・コードの内容を言い直している段落
- 地の文で \`A → B → C\` と辿らせている行（図にする）
- 見出しの無い表（何を並べたのかが読み手に分からない）
- 根拠の無い「おそらく」「〜かもしれない」
- 一般論の効能書き

最後に、**冒頭の1文だけで「何が分かったか」が伝わるか**を見る。伝わらなければ結論を書き直す。
`
