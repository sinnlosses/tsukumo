// キャラクターから話しかけてもらうときに送る一言（`docs/screen-design.md` 13.7）。`chat-manner.ts` /
// `chat-compact.ts` の隣に置く（モデルに見せる文面は core 側）。
//
// **話題そのものは tsukumo が持たない**（原則4「キャラクターの中身をコードに書かない」）。
// ここが書けるのは「あなたから話しかけて」という合図と、**話題を選ぶのはあなた**という
// 一線だけで、話題の候補・例・傾向は一切並べない。
//
// 送る操作と、送ってよい場面（雑談モードで、ターンが動いていないこと）の判断は
// `src/server/core/session-manager.ts` が持つ。ここは「決める」内容の文字列だけで、外の世界には
// 触らない（原則2）。

/**
 * 画面のボタンが押されたときに、依頼として駆動へ渡す文面
 * （`SessionDriver.promptWithoutRecord` の `text` にそのまま渡る）。
 *
 * **この文面は画面のログにも記録にも残らない**（`request` ではなく
 * `turn-started` が流れる）。利用者が打っていない一言がログに並ばないようにするためで、
 * 直近の逐語を読み戻すときに「依頼として流す案は採らない」としたのと同じ立場
 * （`docs/requirements.md` 4.9）。
 */
export const CHAT_NUDGE_PROMPT =
  "（ユーザーが「そっちから話しかけて」と合図した。" +
  "**話題はあなたが選ぶ** — こちらからは何も渡さない。" +
  "これまでの会話や覚えていることを踏まえてもよいし、踏まえなくてもよい。" +
  "いつもどおり `speak` で一言から始めて。）"
