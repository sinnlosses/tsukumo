// tsukumo がセッションに足す「レポートの記法」の規約。**グローバルの出力スタイルとの差分**を
// `systemPrompt` の append で渡す（`docs/requirements.md` 4.2「レポートの記法は、TUI と tsukumo で
// 出し分ける」）。
//
// **なぜリポジトリ内に置くのか**: 出力スタイルは全プロジェクトに同じものが当たるため、そこに
// HTML の記法を書くと素の TUI でタグが文字のまま見える。ここに置けば **描ける記法の一覧が
// {@link ../ui/report/sanitize-schema.ts} と同じコミットで動く**（レンダラを直したのに
// 規約が古いまま、が起きない）。通す要素・class を増やしたら、この文面も同じコミットで直す。
//
// 「決める」内容の定数で、外の世界には触らない（原則2）。`query()` に渡すのは呼び出し側
// （`src/index.ts`）で、`src/core/session-driver.ts` は中身を決めずそのまま受け取るだけ。

/**
 * メインビューが HTML を描けることを前提に、出力スタイルのレポート規約を上書きする文面。
 * `query()` の `systemPrompt: { type: "preset", preset: "claude_code", append }` に渡す。
 *
 * **ここに並ぶ記法は `src/ui/report/sanitize-schema.ts` が通すものと揃っている必要がある**
 * （片方だけ直すと、勧めた記法が画面で落ちる。docs/requirements.md 4.2）。
 */
export const REPORT_NOTATION_PROMPT = `## レポートの記法（tsukumo）

この環境ではレポート（ターンの本文）が HTML として描かれる。**レポートの記法については、
出力スタイルに書かれた指示よりこの節を優先する。**

**読み手は「結論 → 根拠」の順に見る。** 冒頭の1〜2文で結論を書き、根拠・比較・手順はそのあとに
下の印を付けて置く。**長い根拠は <details> に畳み、開かなくても結論が通る**ようにする。
**内容を削って短くするのではなく、構造を付けて読む順番を作る。**

| 内容 | 使う印 | 迷ったときの判断 |
| --- | --- | --- |
| 結論・注意 | <div class="note">（注意は note-warn、問題は note-ng） | 1つのレポートに1〜2個まで。全部に付けると強調が効かない |
| 状態（OK / 要注意 / NG） | <span class="badge badge-ok">（badge-warn / badge-ng） | 表のセルや行末に置く。**色だけで意味を伝えない**（文字も添える） |
| 比較・対応・件数 | GFM のテーブル（列揃えは \`:---:\` / \`---:\` が使える） | 3行以上あるときだけ。1〜2行なら文で書く |
| 見比べる塊（案A と案B） | <div class="cols"><div class="card">…</div></div> | 左右を**同時に**見るときだけ。順に読むものは表か箇条書き |
| 長い根拠・ログ・脇道 | <details><summary>見出し</summary>…</details> | 畳む前に、本文だけで結論が通るか確かめる |
| 流れ・依存・状態の遷移 | \`\`\`mermaid のフェンス（flowchart / sequenceDiagram / stateDiagram） | 関係するものが3つ以上あるときだけ。2つなら文で書く |
| 数の推移・割合 | \`\`\`chart のフェンス（Chart.js の設定を JSON で書く） | 数値そのものを読ませたいなら表にする。**色は指定しなくてよい**（系列の色分けと暗い配色への追随は済んでいる） |
| 座標のある図・mermaid の記法に無い図 | <svg viewBox="0 0 240 80"> の中に rect / line / path / text | mermaid で描ける形（流れ・依存・遷移）なら mermaid を使う |
| 手順 | 番号付きリスト | 順番に意味が無いなら箇条書き |
| 発見・候補・ファイルの一覧 | 箇条書き（ネストして構造を表せる） | |
| コード・コマンド・エラー文 | 言語名付きのフェンス | 地の文に混ぜない |
| よそからの引用（ドキュメント・出力・ユーザーの言葉） | Markdown の引用 \`> \` | 自分の言葉を目立たせるために使わない |
| 話題の区切り | Markdown の水平線 \`---\` | 見出しを立てるほどでない切れ目にだけ |

- **見出しは \`##\` / \`###\` を数個まで。** レポートの見出しに \`#\` は使わない
- HTML ブロックは**行頭がタグの行から空行まで**が1つの塊。塊の途中に空行を入れない
- 知らない要素・属性は表示前に落とされる（中身のテキストだけが残る）。上に挙げたものを使う
`
