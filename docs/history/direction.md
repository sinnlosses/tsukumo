# 指示メモのアーカイブ

`develop/direction.md` に書かれたユーザーからの指示を、タスク化した時点で**当時の記述のまま**
ここへ移す（`docs/workflow.md`「指示メモ」参照）。新しいものを上に足す。**後から書き換えない。**

## 2026-09-16 一瞬出て消えるレポートと、送信直後の吹き出し

- レポートに一瞬だけ作業内容が出て消える、あるいはレポートのようなものが少しだけ出て消えることがある。目がチラつくので出さないようにするか、中間レポートという形式で出すなら消えないように記録してほしい
- 入力画面から送信したとき、前回の最新のセリフだけ残るので、(まだ発話がありません)のように定型句でもかまわないから次のタスクに移ったことがわかるようにしてほしい

- 生成したタスク: 1つ目 → T-159、2つ目 → T-160
- タスクにしなかった項目: なし
- 登録時にユーザーへ聞いて決めたこと（1つ目の分け方）:「ツールを使う、とかこれから何をするなどは
  不要で、枠組みされたまとまった資料がたまに出てくるときがあり、それは中間レポートという形で
  表示しても良いよ」

## 2026-09-16 待っている間にサブエージェントの状況を出す

- サブエージェントに渡して待っている間もキャラに喋ってほしい
  - サブエージェントに渡しているときに待っているとヒマだから話してくれるとかは難しいのかな?
  - 2026-09-16 に実験した: サブエージェント（haiku）から `mcp__tsukumo__speak` が見えて、
    2回呼んで両方 `ok` が返った。`src/core/sdk-message.ts` は `parent_tool_use_id` が
    付いていても `speech` イベントに変えるので、経路は既にある
  - サブエージェントに実況させる場合は子狐を別キャラとして作ったうえで同じ吹き出しではなく
    別々に表示したいよね。
  - メイン側が背景で待つ場合はサブエージェントのやっていることを眺めて同じように実況できたり
    するかな?
  - 最後の定型句はやめよう、味気ないから...
  - サブエージェントが話すとメインはぼーっとしていることになるからもったいないな。サブエージェントが
    適宜状況を出力しているけど、その状況の出力をメインが理解しやすい形にする+それをメインが拾う
    方法はない?
  - 2026-09-16 に実験した: 背景で動くサブエージェントから `SendMessage({to: "main"})` を2通
    送らせ、**2通とも届いた**。2通目はメインがターンを終えて待っている状態で届き、メインが
    呼び起こされた（ポーリング不要）。`TaskOutput` はサブエージェントには非推奨で、出力ファイルは
    会話まるごとの JSONL なので読めない。押し込む口はもう1本あり、子狐がファイルに1行ずつ
    追記してメインが `Monitor` の `tail -f` で拾う形も成り立つ
  - 一旦1つ目の細かい実況（子狐が自分で speak）はタスク化するけどやらない(タイミングは考える)。
    2つ目のSendMessageはやろう。3~5通とあるけど、30秒~1分をインターバルとして出てくれると
    嬉しい。

- 生成したタスク: SendMessage で節目を押し込む → T-157、子狐が自分で喋る（別キャラ・別の吹き出し）
  → T-158（着手は保留）
- タスクにしなかった項目: **tsukumo 側から定型句を出す案**（ユーザーの言葉「最後の定型句は
  やめよう、味気ないから...」で取り下げ）
- 登録時にユーザーへ聞いて決めたこと: 間隔は30秒〜1分 → ツール5〜10回ごとに1通へ換算 /
  T-158 は「タスク化するけどやらない（タイミングは考える）」なので `loopable` を `N` にした

## 2026-09-16 レポートの見出しと、依頼の見せ方

- レポートの見出しと、依頼の見せ方を改善したい
  - ひとかたまりごとに見出しがあって、そこが要約になっているとわかりやすい
  - ときどきレポートの中で自分に依頼することがあるけど、レポートに紛れて見落とすことが
    あるから、わかりやすく位置は「見出しの最後」として、それが依頼とわかるような表示をお願い

- 生成したタスク: 1つ目 → T-154 / T-155、2つ目 → T-156
- タスクにしなかった項目: なし
- 登録時にユーザーへ聞いて決めたこと: 依頼はレポートのいちばん最後に1つにまとめる /
  吹き出しにも出す（ユーザーの言葉:「それならセリフに出してほしいけど、最後に話したいことが
  2件以上あったらどうなるかな」→ 同じターンなら speak は呼んだ数だけ積まれると回答）
- T-154 は指示から直接来たものではなく、裏を取る過程で見つけた不具合（`##` / `###` が
  sanitize の許可リストに無く、見出しとして描かれていない）

## 2026-09-16 mermaid の構文エラーに気づける仕組み

- mermaid が syntax error のときに気づける仕組みが欲しい
  - レポートに書いた mermaid がエラーで、図の代わりにエラーの吹き出しが出た
    （原因はノードのラベルの中で `\"` と書いたこと。mermaid は `#quot;` を使う）
  - こういうのはチェックできるといいね

- 生成したタスク: T-153
- タスクにしなかった項目: なし
- 登録時にユーザーへ聞いて決めたこと: 画面側（壊れていたらコードとエラー文を出す）と
  規約（`REPORT_NOTATION_PROMPT` に落とし穴を1行）の両方をやる

## 2026-09-16 セリフの頻度・タスク一覧の loopable 列・doing のステータス表示

- レポートを書いているときに、もう少し頻繁にキャラが話すようにルールづけできる?
  - こちらに話しかけるというよりは今何をしていてそこでどういう感情を持っているのかがうかがえると眺めていて楽しい
  - 頻繁の度合いがわからないのでそこは調整してほしい
- サイドバーのタスク一覧モーダルにloopableのカラムを追加してほしい(今のモーダル幅は変えなくて良いよ)
- tasks.json のステータスにdoingを追加したのでサイドバーに表示するステータスを増やしてほしい(できれば、ステータスを色分けされて枠に囲まれたラベルにできると見やすいね)

- 生成したタスク: 1つ目 → T-150 / 2つ目 → T-151 / 3つ目 → T-152
- タスクにしなかった項目: なし
- 登録時にユーザーへ聞いて決めたこと: セリフの頻度は1ターン5〜10回（ツールを走らせる前に1回、
  結果で分かったことがあればもう1回）/ `doing` の色は差し色（accent）を使い、状態の3色は増やさない

## 2026-09-16 ホットリロード・雑談モード・ターン中のキャラ切り替え

- 生成したタスク: T-146（開発中にブラウザ側を作り直して再読み込みを押す）/ T-147（雑談モードを
  入れるかとメイン画面に何を出すかを決める）/ T-148（ターン進行中はキャラの切り替えを受け付けない）
- タスクにしなかった項目: なし。**雑談モードはホットリロードの節の箇条書きに混ざっていた**が、
  別の話なので T-147 として分けた

## 2026-09-16 開発中のホットリロードが欲しい（Bun でできる範囲で）

`src/` を直すたびにプロセスを上げ直すのが手間。**Vite を足すのではなく、Bun でできるなら
それでやってほしい。**

- `docs/design.md` 11章は「HMR は持たない（欲しくなったら Vite を**開発時だけ**足す。配る経路は
  変えない）」と書いている。**「欲しくなった」の条件が満たされた**ので、この決定を見直す
- ただし手段は Vite ではなく Bun を優先して評価する
- T-145（Bun のまま進めるか Node へ寄せるか）との関係を整理してから分解すること。
  `Bun.serve` の HMR を使う道は規約「Bun固有APIに寄せない」と正面から当たるので T-145 の
  決定待ち、`node:fs.watch` + 既存の `bun build` で作り直して WebSocket で再読み込みを押す道は
  T-145 に依存しない（調査結果は 2026-09-16 のセッションのやり取りにある）
- 雑談モードを入れたい。この場合はレポートを出力するのではなくキャラと雑談するだけ。その場合メイン画面に何を表示するか、またはキャラがメイン画面で話すようにするかは議論したい

## 2026-09-16 ターンの途中でキャラを切り替えられないようにする

依頼を送ったあとにキャラクターを切り替えると、**その場で作業が消える**。`switch-character` は
起こし直しで、`session-manager.ts` の `restart()` が `live?.close()`（= `input.end()` で claude の
子プロセスを終える）を呼び、さらに `generation += 1` で途中まで出ていた結果イベントも捨てる。
中断の確認も出ない。

**ターンが進行中のあいだは切り替えさせない**（採用。2026-09-16 決定）。

- 材料はそろっている: `SessionState` に `turnInProgress: boolean` があり
  （`src/protocol/session-state.ts`）、`<Select>` は `disabled` を受け取るのに
  `src/ui/sidebar/session-info.tsx` で `disabled={false}` が直書きになっている
- **閉じ込めにはしない。** 中断ボタン（`interrupt`）が逃げ道なので、「止めてから切り替える」
  手順が残る
- 論点: 灰色にするだけだと壊れて見えるので、**切り替えられない理由を画面に出すか**。
  出すならどこに、どの文言で（サイドバーは3区画のまま・常設の要素を増やさない、という
  `docs/design.md` 13 章の制約の中で）
- モデルと許可モードの `<select>` も同じ扱いにするかは、あわせて決める

**並列で動かす案（2体を同時に走らせる）は採らない。** `createSessionManager` は
`Map<string, SessionHost>` で複数を持てるが、塞いでいるのは画面のほう（`SessionState` は
立ち絵1つ・吹き出し1本・メインビュー1つで `character` も単数）。並列化は「2体をどう見せるか」を
決める話になり、`docs/design.md` 13.1 の原則と 8 章「複数化はまだしない」に正面から当たる。
**並列が要るときは tsukumo をもう1つ起こす**（ポートが 7327〜7346 で自動にずれる。
キャラを分ければセッションの印も別なので衝突しない）。これは今すでにできる。

## 2026-09-16 Bun / Node の判断がどのタスクにも載っていない

- 生成したタスク: T-145（Bun のまま進めるか Node へ寄せるかを決める。`loopable: "N"`、
  `dependencies: ["T-122"]`）
- 置き場所は「T-046 に論点を戻す」ではなく**別タスク**に決めた。理由: T-046 は `loopable: "Y"` の
  文言修正として着手できる状態にあり、人の判断が要るこの件を混ぜると `"N"` に戻って着手が遅れる
- T-122（プロジェクト外への依存の洗い出し）を依存に置いた。`src/core/bundle.ts` の `bun` 実行時依存を
  評価するのが T-122 で、その表が判断材料になるため
- タスクにしなかった項目: `docs/research/` 各所にタスク番号が書かれている件（規約違反だが、
  この指示の範囲外。下の「気づいたこと」に記録だけ残す）

### 気づいたこと（このメモの範囲外）

`CLAUDE.md` は「コード・ドキュメントにタスク番号（`T-` + 3桁）を書かない（唯一の例外は
`docs/requirements.md`「7. 未決事項」の対応タスク列）」と定めているが、`docs/` の正典・調査メモの
**7ファイルにタスク番号が書かれている**（`architecture-proposal.md` / `app-shell.md` /
`hono-server.md` / `architecture-rethink.md` / `view-rendering.md` / `design.md` / `requirements.md`。
2026-09-16 実測）。T-145 は `docs/design.md` 1章の1件だけを消す。残りは未対応。

`docs/design.md` 1章は「`Bun.*` に寄せない規約を保ち、**箱を決めるとき（T-046）に Node へ寄せるかを
判断する**」と書き、`docs/research/app-shell.md` も「T-046 は先にこの食い違いを解く」と名指ししている。
ところが 2026-09-16 に T-046 を「方針を正典に落とす」へ更新したとき、**解くべき論点からランタイムの件が
落ちた**（いまの T-046 の論点は文言の3件だけで、「コードを変えない」とも書いてある）。**宙に浮いた
この判断を引き取るタスクを作る。**

- 食い違いの中身: `docs/design.md` は「Electron なら core を Electron の Node で動かせるので Bun は不要」、
  `docs/research/app-shell.md` の比較表7軸は「Bun のプロセスを子として起こす（あるいは
  `bun build --compile` で単一バイナリにする）」前提。**どちらを採るかで「Bun を採用できるか」の
  答えが逆になる**
- 規約「Bun固有APIに寄せない」の理由（退避先を残す）と実態のずれも一緒に扱う。`src/core/bundle.ts` が
  起動のたびに `execFile("bun", ["build", …])` を呼ぶので、**`bun` への実行時依存はすでにある**
- T-046 には戻さない（T-046 は `loopable: "Y"` の文言修正として着手できる状態にある。人の判断が要る
  この件を混ぜると着手が遅れる）
- 2026-09-16 の方針で箱は当面 Orca に決まっているので、**「いま寄せ替えない」が既定**。決めるのは
  「何が起きたら寄せ替えを再検討するか」の条件と、正典の文言の直し方

## 2026-09-16 アーキテクチャ提案（再考②）の段のタスク化漏れ

- 生成したタスク: T-142（段1 検査と再掲の掃除）/ T-143（段2 `adapter/` を切る）/
  T-144（段3 `cli.ts` の判断を `core` へ）
- T-140 の扱いは「吸収せず先に置く」に決めた（T-142 の `dependencies` に T-140 を入れ、
  T-140 側の注意書きも「まとめてやり直さない」に直した）。理由: T-140 は sonnet で小さく、
  すでに登録済みで単独で価値がある。opus の T-142 に畳むとその分だけ着手が遅れる
- タスクにしなかった項目: Bun / Node の件（メモ自身が「この指示には含めない」と明記）

`docs/research/architecture-proposal.md`（2026-09-15）が出した移行の段1〜段3が、メモの末尾で
「段1を1件、段2と段3をそれぞれ1件として起こす」と書かれているのに `develop/tasks.json` に
登録されていない。**このタスク化漏れを埋める。**

- 段1（ファイルを動かさず、検査と正典の再掲を掃除する）・段2（`adapter/` を切る）・
  段3（`cli.ts` の判断を `core` へ移す）を登録する
- **段1は T-140（`orca` を呼ぶのが `orca-host.ts` だけであることをテストで守る）と重なる。**
  提案メモ自身が「段1を先にやるならその中に含めてよい」と書いているので、
  T-140 を段1に吸収するのか、別のまま残すのかを決める
- 段2と段3は独立で順序を入れ替えられる。依存の張り方はその前提で決める
- **Bun / Node の件（T-046 から論点が落ちている話）はこの指示には含めない。** 別途扱う

## 2026-09-16 T-046（箱）の方針: Orca で進めつつ核を技術非依存に保つ

- 生成したタスク: T-046（既存タスクを更新。方針を正典に落とす）/ T-140（`orca` の限定をテストで
  守る）/ T-141（ホストを選ぶ口を作るかを決める）
- タスクにしなかった項目: 「核が orca に依存していないか」の実測そのもの（このメモを書く時点で
  済ませ、結果を各タスクの背景に写した）

- T-046（箱）の方針: **Orca で進めつつ、ドメイン（tsukumo の核）が特定の技術（orca など）に
  依存しないようにする**。他の技術に適用することもあるため。つまり「包まない」の確定ではなく
  「当面 Orca、ただし載せ替えられる形を保つことを条件にする」。受け取ったのは 2026-09-16。
  受け取り側で実測した現状（`grep -rn 'orca' src`）: 核は既に汚れていない。`src/` で `orca` が
  出るのはアダプタ（`src/core/orca-host.ts`）と配線（`src/cli.ts` の `createOrcaHost()` 1行）と
  コメントだけで、`protocol` にも `ui` にも無い。ポート（`src/core/host.ts`）の操作は
  `showView` 1つで、Orca の語彙（タブ、`--direction`）は漏れていない。**方針は既存の原則3と
  同じ向き**なので、やることは「新しく作る」ではなく「守りを固める」になる。
  埋めるべき差は4つ:
  1. **`orca` を呼ぶのはここだけ、がテストで守られていない**（コメントだけ。
     `test/architecture.test.ts` は層の辺しか見ない）。`docs/research/architecture-proposal.md`
     の段1が足す検査と同じもの
  2. **ホストの選択が `cli.ts` に直書き**（`scripts/open-views.ts` も同様に
     `createOrcaHost()` を直接呼ぶ）。別のホストを足すとき、選ぶ口が無い
  3. **`docs/architecture.md`「アダプタは当面 Orca の1つだけ。VS Code 用の実装は作らない」**は、
     2つ目を見込んでいることを理由に `codebase-design` の戒めを意図的に外している。今回の方針は
     その見込みを追認したので、文言を方針に合わせて見直す（作る/作らないの判断自体は別）
  4. `docs/requirements.md` 7章の未決事項「Electron などのスタンドアロンに移るか」を、この方針で
     書き換える。**閉じるのではなく、条件付きの保留**（当面 Orca、載せ替えられる形を保つ）に読み替える

## 2026-09-16 立ち絵の表情を増やすかと、画面の背景のカスタマイズ

- 生成したタスク: T-136（表情を増やすかと足す表情を決める）/ T-137（決めた表情を Expression 型・
  パック定義・speak ツールに通す）/ T-138（背景の置き場と受け取り方を決める）/
  T-139（決めた形で背景の差し替えを実装する）
- タスクにしなかった項目: なし

- 立ち絵ってもっとあったほうがいい?どういう表情が他に欲しいかな?
- メイン画面、キャラ画面は背景をカスタマイズできたほうが楽しいと思ったけどどうかな?

## 2026-09-15 質問の見切れと、回答を見返せない件

- 生成したタスク: T-134（質問の選択肢の説明を省略せず全部出す）/ T-135（質問と答えを記録に積み、
  メインビューで見返せるようにする）
- タスクにしなかった項目: なし

- 入力画面に表示された質問が途中で...と見切れてしまうので、そこは省略せずすべて表示してほしい(縦にスクロールすることになっても良い)
- 入力画面に表示された質問への回答が、回答後に見返せないので改善方法を考えてほしい

## 2026-09-15 作業中のコマンドをメイン画面に出さない

- 生成したタスク: T-133（ツールの行をレポートから消し、「レポートだけを出す」に戻す）
- タスクにしなかった項目: なし（`docs/requirements.md` 4.2 が予告していた差し戻しそのものだと確認した）

- 作業中のコマンドはメイン画面に出さなくていいのでルールで塞いでほしい(以下は例)
  - Agent: Implement T-113 turn window
  - Edit: /Users/sinnlos/ghq/github.com/sinnlosses/tsukumo/src/protocol/main-view.ts
  - Edit: /Users/sinnlos/ghq/github.com/sinnlosses/tsukumo/test/protocol/main-view.test.ts

## 2026-09-15 detail の不具合、前置き、補完キー、サイドバー、Fable、clear、立ち絵

- 生成したタスク: T-126（detail が割れる）、T-127（前置きを出さないか決める）、T-128（Ctrl+P / Ctrl+N）、T-129（タスク一覧の再デザイン）、T-130（Fable）、T-131（clear でセリフが残る）、T-132（立ち絵が切り替わらない）
- タスクにしなかった項目: なし（7件すべて現物で原因または再現の手がかりを確認した）

- detail が機能してない(展開を押しても意味なく、最初から展開済みの文章が出てしまっている)ので修正してほしい
- レポートの先頭に「I'll read the file first.」のような前置きが残る。ツールを呼ぶ前にモデルが出す実況で、規約の条項（2026-09-15 に足した「前置きと締めを書かない」）では抑えきれなかった。tsukumo 側で「最初のツール呼び出しより前の本文をレポートに出さない」と決めるかを検討したい（`toolVisibility` と同じ「何を出すか」の判断になる）
- Macの場合、コマンド補完をctrl+pやctrl+で上下に移動したい
- サイドバーの領域が小さく、タスク一覧の見せ方を再デザインしたい(frontend-design skill かな?)
- サイドバーで、 Fable が選べる場合はモデルに Fable を追加する
- clear コマンドを実行するとレポートは消えるがキャラのセリフはリセットされない
- アスナからtsukumoにキャラを切り替えたときに立ち絵が切り替わらない

## 2026-09-15 別プロジェクトでの起動、プロジェクト外への依存、画面からのキャラ設定

- 生成したタスク: T-121（README を現状に合わせ、`command not found` を直す）、T-122（プロジェクト外への依存の洗い出し）、T-123（画面から書き換えるための置き場と受け取り方を決める）、T-124（立ち絵と差し色を画面から差し替える）、T-125（新しいパックを画面から作る）
- タスクにしなかった項目: なし。ただし1件目は**別プロジェクトでの起動そのものは再現しなかった**（空のディレクトリで実際に起動し、4領域・立ち絵・入力欄が出ることを実測）。症状の聞き取りで `command not found` と分かったので、T-121 は README 側（PATH の確認手順と、古くなった記述7件）を直す形にした
- 3件目は聞き取りで対象を絞った（変えたいのは**立ち絵と差し色**、および**新しいキャラを画面から作る**こと。表示名・人格の文面は今回の対象外）

- READMEで書かれた方法で試したけど別のプロジェクトで tsukumo が起動できなかった。見直してみて?
- 今、プロジェクト外のファイルで tsukumo が依存しているものを洗い出してほしい。最終的に tsukumo をスタンドアロンで動かしたい
- キャラ設定を画面上からできるようにしたい

## 2026-09-14 質問画面の使い勝手と、レポートの冗長さ

- 生成したタスク: T-117（1問1答で出す）、T-118（横方向のはみ出しを止める）、T-119（レポートの冗長さを止める条項を規約に入れる）
- タスクにしなかった項目: なし（3件とも現物で再現・原因を確認した）

- 入力画面に出る質問が1問1答になってなくて、スクロールしないといけないのが使いづらい印象を持ったよ
- 入力画面に質問が出たとき、横にスクロールできたのが気になった。この場合、スクロールできないのが使い勝手良さそうだよ
- キャラにかからわず、レポートは冗長さを廃してください。[https://github.com/ayghri/i-have-adhd/blob/main/skills/i-have-adhd/SKILL.md を参考に。](https://github.com/ayghri/i-have-adhd/blob/main/skills/i-have-adhd/SKILL.md)

## 2026-09-14 履歴の数と、レポートに過程が混ざる件

- 生成したタスク: T-113（メインビューが持つやり取りを3→5に増やす）、T-114（失敗したツールの引数と出力をレポートから外す）、T-115（レポートの書き込み位置にミニ立ち絵を添えるかを決める）
- タスクにしなかった項目: なし（「レポート／セリフ／過程の分け方のルール改善」は T-114 の論点として本文に入れた。3件目は `/plan-tasks` の実行中に届いたぶんで、同じ回でタスク化した）

- やり取りの履歴を5つ持てるようにしてほしい
- Bash の出力がレポートに出てたけど、それは出さないでほしいな...レポートはレポート、セリフはセリフ、過程は今何してるかに分けるルールを改善してくれる?
- レポートを書くときに、キャラクターが書いているようにカーソルに合わせてミニキャラになった立ち絵が動くと楽しいよねきっと

## 2026-09-14 レポートのテーブルがつぶれる

- 生成したタスク: T-112（レポートの行長の上限からテーブルなど横に広がる要素を外す）
- タスクにしなかった項目: なし（`.detail-block` の `max-width: 36.36rem` が原因であることを現物で確認した）

- メイン画面にレポートを出力するときに、横幅いっぱいに使わないのでテーブルが少しつぶれてた。改善してくれる?

## 2026-09-14 切り替えと復元まわりの3件

- 生成したタスク: T-109（切り替えで会話を残せるかを決めて実装する）、T-110（前回選んだキャラを覚えて起動時に使う）、T-111（吹き出しをターンに紐づけて遡れるようにする）
- タスクにしなかった項目: なし（3件とも現物で再現を確認した）

- キャラクターを入れ替えたときにそれまでしていた会話が表示されずリセットされているので復帰できるようにしたい
- tsukumo 終了直前のキャラが tsukumo-chan だった場合は起動時にもそうであってほしい。今はデフォルトのつくもの精霊になる様子で、口調が異なっていたから違和感があった
- 過去のやり取りを遡るとキャラのセリフは遡らなかったので、レポート同様に遡るようにしてほしい

## 2026-09-13 キャラの移動と、設定の配置

- 生成したタスク: T-105（立ち絵の動きと置き場所に何を作るかを決める。`docs/requirements.md` 7章の未決事項をここで閉じる）、T-106（増えていく設定の置き場所を決める。T-101 の前段に置いた）
- タスクにしなかった項目: なし（2件とも問いかけの形だったが、どちらも決める価値のある論点だったのでタスクにした）

- キャラ画面の枠がなくなったことで、自由にキャラを移動できるようになったりする遊びができる気がする(もちろん固定がいいならそれもできる)けどどう思う?
- T-101 に関連するかもだけど、配色とかキャラ設定の変更とか各種設定の配置を再考し始めるときなのかな

## 2026-09-13 デザインの再考

- 生成したタスク: T-102（`frontend-design` のスキルで色・書体・レイアウト・原則のデザイン計画を決める。実装はしない）、T-103（決めた計画を CSS のトークンにして当てる。T-102 と T-100 待ち）
- 適用を T-100（移行の段7）待ちにした理由: 段5（T-098）の完了条件が「見た目が変わっていないこと」で、移行中に見た目を変えると移行のバグと意図した変更が区別できなくなるため。前倒しの可否は T-102 の論点に入れた

- frontend-design の skills を使ってデザインを再考しよう。

## 2026-09-13 描く層をブラウザ側へ移す（移行の段2〜段9）

- 生成したタスク: T-095（段2: protocol と core の骨組み・WebSocket・偽の駆動）、T-096（段3: サイドバー）、T-097（段4: 入力欄）、T-098（段5: キャラビュー）、T-099（段6: メインビューと unified の Markdown）、T-100（段7: 旧4層の後始末）、T-101（段8: キャラクターパックと切り替え）。段9は既存の T-078 の依存を T-100 に付け替え、本文の先頭に移行後の読み替えを足した
- 既存タスクの扱い: T-088 は着手せずに閉じた（段6で `view.ts` ごと消える）。T-063 は T-099 に統合して閉じた。T-064 / T-065 は T-101 に統合して閉じた。いずれも `passes: false` のまま `done`、`evidence` に移行先を書いた
- タスクにしなかった項目: 段1（正典の書き換え）は同日に済んでいるので起こしていない。「Node に寄せるか」はユーザーの回答「当面 Bun、箱で決める」で決着し、`docs/design.md` 1章に理由を残した
- 裏取り: 指示メモ自体がこのセッションで `docs/design.md` を書いた側が起こしたものなので、コードの現物（`src/` 全体・`sdk.d.ts`）は設計書を書く時点で読んである。T-063 / T-064 / T-065 / T-078 / T-088 の本文と依存は `develop/tasks.json` で確認した
- アーカイブ: 登録直後に `done` が10件に達したため、T-062 / T-063 / T-064 / T-065 / T-077 / T-087 / T-088 / T-090 / T-091 / T-092 を `docs/history/tasks-archive.md` へ移した

### 当時のメモ（そのまま）

ユーザーの決定「移行しようか。設計書に落とし込んでくれる?」（2026-09-13）。**設計は
`docs/design.md` が正典**で、段1（正典の書き換え: `docs/design.md` の新設、`docs/architecture.md` /
`docs/requirements.md` / `docs/coding-standards.md` / `docs/glossary.md` / `CLAUDE.md` の更新）は
同日のセッションで済ませた。**ここでタスクにするのは段2〜段9。** 段ごとの「やること・完了条件・
消えるもの」は `docs/design.md`「12. 移行の段階」の表がそのまま本文になる。以下は上乗せだけ。

- **段2〜段6は1段ずつ、依存を直列に**（段3は段2待ち、…）。次の段に入る前に前の段の
  「消えるもの」を実際に消す
- **段2（protocol と core の骨組み・ws・偽の駆動）は opus**（依存の追加・層の検査テストの
  辺の書き換え・`session-manager` の設計を含む）。**段3〜段6の各領域は、部品のテストと
  「見た目が変わらない」の CDP 実測が書けるので sonnet で、`loopable` は目視の要否で決める**
  （段3・段5は CDP の数値比較で `/loop` に載せられる見込み。段4・段6は実機で1往復するので N）
- **段6は T-063 を吸収する**（T-063 は段6の依存に付け替えるか、閉じて本文を段6に写す。
  自前レンダラに足さない）。**T-088 は着手せずに閉じる**（`passes: false` のまま `done`。
  割った先ごと段6で消える）
- **段7（後始末）は sonnet**。`src/` に `protocol` / `core` / `ui` / `cli.ts` だけが残ること、
  `docs/architecture.md`「現在の実装状況」を「移行完了」に書き換えることが完了条件
- **段8（キャラクターパック）は opus**。最初に**人格の二重適用のスパイク**（`applyFlagSettings({ outputStyle })`
  で1つになるか）を行い、結果を `evidence` と `docs/requirements.md` 7章に書く。T-064 / T-065 は
  段8に統合する（T-064 の論点は `docs/design.md` 7章で決めてあるので、残るのは実装）
- **段9（復元）は T-078 の本文をそのまま使い、依存を段7に付け替える**
- 足す npm の依存は `docs/design.md` 11章の一覧で**承認済み**。一覧に無いものを足すときは止めて聞く
- **`~/.claude/` は触らない。** 常駐している 7327 番を落とさない。偽の駆動で起こすときも
  `TSUKUMO_VIEW_PORT` を変える
- `summary` は全角40文字以内（`docs/workflow.md`）

## 2026-09-13 レポートを読む時間を減らす（T-062 の決定からの後続）

- 生成したタスク: T-093（規約の「印」の語彙と使いどころ。指示の1番）、T-094（`dl` の CSS。指示の3番。T-093 が `dl` を語彙に入れなければ閉じる逃げ道つき）
- タスクにしなかった項目: 指示の2番（テーブルの列揃え `:---:`）は**新規に作らず T-063 の本文に統合した**（同じ `renderMarkdownToHtml` を触るので、別コミットに分けると2回同じ関数のテストを直すことになる）。あわせて T-063 の移動前のパス（`src/view.ts` / `src/report-html.ts`）を4層のパスへ直し、迂回の記述を外す先を `~/.claude/output-styles/asuna.md` から `src/presentation/report-notation.ts` へ変えた（2026-09-12 の「TUI と tsukumo で出し分ける」決定に合わせた）
- 裏取り: (1) `REPORT_NOTATION_PROMPT` が37行で「使ってよい構造」の一覧止まりであること、(2) `consumeTable` が区切り行のコロンを読んでいないこと、(3) `main-turns.css` に `dl` の規則が1つも無い一方 `ALLOWED_TAGS` は `dl` / `dt` / `dd` を通すこと、を現物で確認した
- **この指示メモ自体は T-062（決めるタスク）が書き出したもの**で、ユーザーの生の指示は「レポートの出力をもっとリッチに、段組みしたり見やすくしたりできる限り人間が理解するまでの時間を減らせるよう工夫してほしい」＋「重要なのは output styles のほうなのかもしれない」

### 当時のメモ（そのまま）

決定の本文は `docs/requirements.md` 4.2「読む時間を減らすために足すのは、規約の側」。
**足すのは規約が主**で、コードは規約が要求する分だけ足す。3件を想定している。

1. **`src/presentation/report-notation.ts` の文面を「印」の語彙表に作り直す**（いちばん重要）。
   いまの文面は「使ってよい構造」の一覧止まりで、**どの内容にどれを当てるかの判断が書かれていない**。
   内容の型（結論 / 比較 / 手順 / 状態 / 根拠 / 長い補足）ごとに当てる `class` と書き方の例を
   与える。冒頭は結論、根拠は `details` に畳む、状態は `badge`、並置は `cols` + `card`、を
   例つきで。**書き手が最初からその構造で書くのは再構成ではない**（決定の線引き）
2. **GFM テーブルの列揃え `:---:` に対応する**（`renderMarkdownToHtml` の `consumeTable`）。
   区切り行のコロンを読んで `th` / `td` に左・中央・右を当てる。数値の桁が揃わないのが
   読む速度に効くため。T-063（引用・ネスト・水平線）と同じ関数を触るので、まとめてよい
3. **`dl` / `dt` / `dd` の CSS を `main-turns.css` に足す。** 許可リストには入っているのに
   見た目が無く、用語と説明の対（`.detail-block dl`）が素の字下げのまま出る。
   T-092（セッション情報のラベルと値を縦に揃える）と見た目の方針を揃える

**やらないと決めたもの**（蒸し返さないこと。理由は 4.2 の決定に書いた）: 外部 Markdown
ライブラリの同梱、`img` の許可、レンダラによる自動の段組み・目次・要約の生成、`.note` の種類追加。

## 2026-09-13 使ってみて出たキャラビューとサイドバーの直し

- 生成したタスク: T-090（セリフ0件のときの案内を吹き出しの形から外す）、T-091（最新の吹き出しを中央寄りに＋立ち絵を大きく。2件は同じ領域の取り合いなので1タスクにまとめた）、T-092（セッション情報のラベルと値を縦に揃える）
- タスクにしなかった項目: 無し
- 裏取り: (1) `buildCharacterBody` が0件のときも `<div class="balloon">` でプレースホルダを出していること、(2) `.balloon-track` が `align-self: flex-end` / `column-reverse` で最新が最下段に来ること、立ち絵が `--portrait-height: 68%` であること、(3) `.model-select-wrap` が `display: flex` で、ラベルの文字数の差（「モデル」3字 /「許可モード」5字）がそのまま `<select>` の左端のズレになること、を現物で確認した
- **過去の判断との衝突を1つ記録**: 「中央にしてほしい」は、`docs/requirements.md` 4.3 にある「2026-09-12 に一度『最新を領域の縦中央に固定し、下半分を空ける』と決めて差し戻した（高さが半分になって読みづらい）」と同じ方向。ユーザーの再指示なので実装するが、**下半分を捨てる形にはしない**ことを T-091 の論点に書いた

- 発話がないときのキャラ画面は吹き出しにせずまだ発話がありません表示してください
- キャラ画面の最新の吹き出しの位置が下過ぎます。中央にしてほしい
- キャラはもう少し大きくしていいよ
- セッション情報のコンポーネントがラベルと値で縦に揃ってないから揃えて

## 2026-09-12 4層への移動で直しきれなかった docs のパス

- 生成したタスク: T-089（`docs/requirements.md` 5箇所・`docs/glossary.md` 2箇所・`docs/workflow.md` 1箇所の計8箇所を新しいパスへ直す。`docs/architecture.md` に残る11箇所は `transcript.ts` / `state.ts` / `balloon.ts` / `draw.ts` など**もう存在しないファイルの過去の記述**なので直さない、と本文に明記した）
- 裏取り: `grep -rno 'src/[a-z/-]*\.ts' docs/*.md CLAUDE.md README.md` で件数を数え、ファイルごとの内訳（architecture 11 / requirements 5 / glossary 2 / workflow 1）と、architecture の11件がすべて撤去済みファイルへの言及であることを確認した
- タスクにしなかった項目: 無し

T-086（層をディレクトリで表す `src/` の移動）で `docs/architecture.md` の `src/*.ts` パスは
直したが、`docs/requirements.md`（4.2 のレポート記法まわり・4.1 の session-driver 言及・
4.3 の expression.ts 言及など）と `docs/glossary.md`（ホストのポート／アダプタの節）にも
移動前のパス（`src/report-html.ts` / `src/report-notation.ts` / `src/session-driver.ts` /
`src/expression.ts` / `src/host.ts` / `src/orca-host.ts` など）がそのまま残っている。
T-086 の指示は `docs/architecture.md` のパス修正だけを求めていたため対象外にしたが、
本来は同じ理由で直すべきなので、別タスクとして拾ってほしい。

## 2026-09-12 T-080 の決定から出た、層をディレクトリで表す3段階

- 生成したタスク: T-086（`src/` を4層へ `git mv` し、`test/architecture.test.ts` で許した import の辺以外を落とす。改名2件と `buildBrowserScript` のリテラルのパスを含む）、T-087（`index.ts` からユースケースを `usecase/` へ、ファイルI/Oと環境変数を `infrastructure/` へ抜き、配線だけにする）、T-088（`presentation/view.ts` の割り方を決めて割る。T-084 / T-085 の後）
- タスクにしなかった項目: 無し（3段階がそのまま3タスクになった）
- 裏取り: `src/` 18ファイルの現物と、`src/index.ts` の `buildBrowserScript` が `bundledFilePath("src", "browser", BROWSER_SCRIPT_ENTRY)` というリテラルのパスを持つこと（移動で型検査に出ない形で壊れる箇所）を確認し、T-086 の本文に書いた

## 層をディレクトリで表す（T-080 の決定を実装に落とす。3段階）

正典: `docs/architecture.md`「層をディレクトリで表し、依存の向きをテストで縛る」と
`docs/coding-standards.md`「層と依存の向き」。**決めることはもう無い。上の正典どおりに動かすだけ。**

### 段階1: 移動と検査（ロジックを動かさない）

- `git mv` で `src/` を4層に割る。`domain` = `character.ts` / `expression.ts` / `utterance.ts` /
  `question.ts` / `pending-answer.ts` / `session-event.ts` / `task-summary.ts`（`tasks.ts` から改名）、
  `usecase` = `session-view.ts`、`presentation` = `view.ts` / `report-html.ts` /
  `report-notation.ts` / `browser/`、`infrastructure` = `session-driver.ts` / `view-server.ts` /
  `host.ts` / `orca-host.ts` / `view-port.ts` / `bundled-path.ts`（`bundled-files.ts` から改名）
- `test/` も同じ構成に移す（`src/<相対パス>.ts` → `test/<相対パス>.test.ts` の対応は維持）
- `test/architecture.test.ts` を足し、`src/` の import を読んで**許した辺以外を落とす**
  （`node:fs` と正規表現で足りる。外部ツールを増やさない）
- `docs/architecture.md` の「拾う/捨てる/足す」の表と責務の表に出てくる `src/*.ts` のパスを
  新しいものに直す。**節の数は変えない**
- ロジックは1行も変えない。`bun run check` が通ることと、実機で1往復できることで受け入れる

### 段階2: `index.ts` からユースケースを抜く

- 598行のうち `createEventSink` / `createViewPublisher` / `sidebarData` /
  `workingRefreshDelayMs` / `throttle` を `usecase` へ移し、`index.ts` は配線だけにする
- ファイルI/O（`readCharacterDefinition` / `readCharacterAssets` / `readOptionalFile` /
  `readOptionalMtimeMs`）と環境変数の読み取りは `infrastructure` へ
- **ここは中身が動く。** 移す前に、移す対象の振る舞いを押さえるテストがあるかを確かめる

### 段階3: `presentation` の中を割る

- **T-084（ブラウザ側 JS を `.ts` へ）と T-085（CSS を `.css` へ）の後に着手する。**
  `view.ts` 3277行から1000行以上が外へ出てから割らないと、割る線が二度動く
- 割り方（レイアウト / 領域ごと / レポート）は着手時に決める

## 2026-09-12 T-069 の結論から出た実装3件と、個別ビューの撤去

- 生成したタスク: T-082（個別ビューのページと `/` のリンク一覧を消し、レイアウトを `/` で開く。`buildViewPage` は `test/view.test.ts` の9箇所でテストの土台に使われているので、書き換えであって削除にしない）、T-083（ブラウザ側 JS を `.ts` へ出すビルド工程を決めて**1本だけ**移して経路を通す。起動経路の設計にユーザーの判断が要るので opus）、T-084（残りのブラウザ側 JS をすべて移す）、T-085（CSS を領域ごとの `.css` に割ってビルドでまとめる）
- 既存タスクを差し替えた項目: Idiomorph の同梱と morph 化は、**T-070**（サイドバーの再描画でスクロール位置が戻る不具合）が同じものを直すので新しく作らず、T-070 の本文を「Idiomorph で差し替えを morph にする」に置き換えた（`summary` も差し替え）。同梱の材料（0.8.0 / 0BSD / min+gzip 3.7KB / 取得元 jsdelivr）は T-070 の本文に移した
- タスクにしなかった項目: 無し（4件とも現物のコードで裏を取った。`bin/tsukumo` が `import "../src/index.ts"` の2行であること、`bundledFilePath` と `/vendor/<name>` の allowlist が同梱物を配る仕組みを既に持っていることを確認し、T-083 / T-070 の背景に書いた）

- （T-069 の結論から。2026-09-12）Idiomorph 0.8.0 を `vendor/` に同梱し、`subscriptionScript` の
  `el.innerHTML = event.data` を morph に置き換える。取得元は jsdelivr（cdnjs には無い）、
  ライセンス 0BSD、min+gzip 3.7KB。`vendor/README.md` の表も足す。**T-070 はこれで直るので、
  T-070 の本文を「Idiomorph で直す」に差し替えるか、この作業に統合する**
- （T-069 の結論から。2026-09-12）ブラウザ側 JS 812行を `src/view.ts` のテンプレート文字列から
  独立した `.ts` ファイルへ出し、`bun build` でバンドルして配る。`tsc` と `oxlint` が届く状態に
  する。**`bun run start` と `bin/tsukumo`（`bun link` 済み）が壊れない形**を先に決める
  （ビルド成果物が無くても起動できるようにするか、起動時にビルドするか）
- （T-069 の結論から。2026-09-12）CSS の `STYLE` 定数（約700行）を複数の `.css` に割り、
  上のビルド工程に相乗りしてまとめる。割り方は領域の単位（レイアウト／メインビュー／キャラビュー／
  サイドバー／入力欄／レポートの見た目）を目安にする
- 個別ビューのページ（`/main` `/character` `/sidebar`）と `/` のリンク一覧を消し、**レイアウト
  ページを `/` で開く**ようにする（2026-09-12 ユーザーの指示「不要なものは消して `/` だけで
  開くようにするのが自然」）。当初案（3つを別々のタブで開いてペインに並べる）の名残で、実際の
  目視は合成データの静的 HTML を吐く方法でやっているため役目を終えている。**T-069 の後続作業
  （ブラウザ側 JS の切り出し・CSS の分割）より先にやると、移す対象が減る。**
  - 消える: `buildViewPage` / `/` のリンク一覧 / `viewPath` / `view-server.ts` のページ分岐 /
    起動ログの「個別ビュー・デバッグ用」3行
  - 残る: `VIEW_NAMES` / `viewEventPath` と `/events/*` の SSE 経路 / `subscriptionScript`
  - **注意**: `buildViewPage` は `test/view.test.ts` の9箇所で購読スクリプトやレポート描画の
    テストの土台に使われている。**`buildLayoutPage` ベースへの書き換えであって、テストの削除に
    しない**（件数が減らないことを完了条件にする）
  - `docs/architecture.md`「ビューは1枚のページにまとめる」にある「`/main` `/character`
    `/sidebar` はデバッグしやすさのため残す」という決定を覆すので、その節も直す

## 2026-09-12 入力欄のキー・質問の自由入力・待機中の表示・セッション復元・DDD 化

- 生成したタスク: T-072（入力欄を Enter で改行・Command+Enter で送信に入れ替える。補完候補が開いている間の Enter は確定だけにして、T-051 の決定を覆す）、T-073（`AskUserQuestion` の選択肢に `その他` が無いと自由入力欄がどこにも出ない件。カードごとに常に1つ出す）、T-074（`truncateRequest` が依頼の1行目しか見出しに出さない件。全行出し、タブのラベルは1行のまま）、T-075（経過時間をサイドバーの「セッション情報」から入力欄の `.dispatch-row` へ移す）、T-076（待っている間にメインビューへ何を出すかを決める。ユーザーの体感を聞いてから。実装はしない）、T-077（セッション復元の方式を決める。会話内容の複製にあたるかが最大の論点。実装はしない）、T-078（T-077 が決めた方式での実装）、T-079（段落・表のセル・箇条書きの中の HTML が `renderInline` の `escapeHtml` でタグのまま出る件。T-069 がライブラリ同梱を決めたら閉じる）、T-080（ドメイン駆動・フレームワーク非依存への作り直しを決める。原則2・原則3 と既に方向が同じなので、何に困っているかの特定から。実装はしない）、T-081（既定のモデルを Opus・effort high にする）
- 既存タスクに統合した項目: 「HTMLの描画に使えそうなライブラリある?Reactが筆頭だけど今回の要件には合わない?。検討して。」は **T-069**（2026-09-12 に登録済みの「HTML を描く技術を決める」）と同じ論点なので新しく作らず、T-069 の「解くべき論点」に**React を名指しで評価する**項を足した
- タスクにしなかった項目: 無し（10件すべて現物のコード・SDK の型定義で裏を取った）

- 入力画面は改行をEnter、送信をCommand+Enterにしてほしい
- 質問が出たときに選択肢しか選べず Type Something がないのが不便に思ったよ
- ユーザーの発言が複数行あったときにタイトルに1行しか表示されなくて不便に思ったよ
- 経過時間は入力画面の送信ボタンと同じ行に表示したほうが良さそうだね。
- 入力後、待っている間にメイン画面に表示する内容って何がいいかな?考えてもらっていい?
- tsukumo を開き直したとき、セッションを復元するように変更しよう。
- メイン画面に&lt;span class="badge badge-ok"&gt;あり&lt;/span&gt;という表示がHTMLタグそのままに出ちゃってる...
- HTMLの描画に使えそうなライブラリある?Reactが筆頭だけど今回の要件には合わない?。検討して。
- アーキテクチャとコーディング規約を見直そう。具体的にはドメイン駆動で、特定のフレームワークに依存しないような形にリファクタしよう。
- デフォルトのモデルはOpus High にして

## 2026-09-12 描画の技術・サイドバーのスクロール・吹き出しの見えづらさ

- 生成したタスク: T-069（HTML を描く技術を入れるかを、現状と理想をユーザーと突き合わせて決める。DOM 更新 / HTML の組み立て / Markdown 変換 / CSS の4層に分け、比較を `docs/research/view-rendering.md` に書く。実装はしない）、T-070（サイドバーの区画を差し替えると内側の `.sidebar-block-scroll` が新しい要素に入れ替わり `scrollTop` が 0 に戻る件。現物で裏を取った。T-069 が差分適用を入れると決めた場合は着手せず閉じる）、T-071（T-066 で `.balloon-track` を領域の上半分（`flex: 0 0 50%`）に固定したため、吹き出しが使える高さが半分になった件。見えづらさの中身はユーザーに確認済み）
- タスクにしなかった項目: 無し（3件とも現物のコードで裏を取った。サイドバーのスクロールの件はユーザーの見立てどおりだった）

- HTMLを描画するために特定の技術を検討すべきかな?一緒に現状と理想を分析して観点整理しよう
- サイドバーのタスク一覧をスクロールしている最中に何をしているかが更新されると再描画されるのかスクロール位置が上に戻る気がするけど合ってる?UXに影響するから気になって。
- キャラ画面の吹き出しが見えづらくなっちゃったよ。見直してくれる?

## 2026-09-12 セッション情報とコマンド補完の不具合

- 生成したタスク: T-067（サイドバーの「セッション情報」が見出しだけになり中身が高さ0に潰れる回帰。T-061 で入れた `.sidebar-block-scroll` / `.sidebar-block-session` の CSS が原因の見込み）、T-068（`/` 補完の候補が `init` の届く最初のターンまで0件で、ブラウザ側が空を永久にキャッシュする）
- タスクにしなかった項目: 無し（2件とも現物で再現を確認した）

- セッション情報が表示されなくなってるから修正して
- 入力画面のコマンド補完が効かなくなったから修正して

## 2026-09-12 キャラクターの切り替え

- 生成したタスク: T-064（人格（出力スタイル）と素材（`characters/` 配下）を「キャラクター1人」としてどう束ねるか、起動時だけか途中で切り替えるかを決める。人格の正典の置き場所は T-058 が決めるので依存に置いた。実装はしない）、T-065（T-064 が決めた方式で切り替えを実装し、2体目を用意した状態で目視する）
- タスクにしなかった項目: 無し

- キャラクターの切り替えを行えるようにしたい。output style と charactersディレクトリ配下の切り替えを同時に行うことが必要そう?

## 2026-09-12 最新の吹き出しを中央に

- 生成したタスク: T-066（`src/view.ts` の `.balloon-list` の配置を変え、最新の吹き出しの中心をキャラビューの領域の縦中央に固定して、過去の分をその上へ積む）
- タスクにしなかった項目: 無し

- キャラ画面の最新の吹き出しは中央に、過去の吹き出しはそれより上に配置することで今見てほしいものをハッキリさせよう。

## 2026-09-12 レポートの見やすさ

- 生成したタスク: T-062（レンダラ・通す HTML と CSS・出力スタイルの組み立て規約の3層を棚卸しし、どこに何を足すかをユーザーの感想を聞いて決める。実装はしない）、T-063（自前レンダラで描けない引用・ネストしたリスト・水平線を埋める。T-062 が「ライブラリ同梱」に決めた場合は着手せず閉じる）
- タスクにしなかった項目: 無し

- レポートの出力をもっとリッチに、段組みしたり見やすくしたりできる限り人間が理解するまでの時間を減らせるよう工夫してほしい

## 2026-09-12 サイドバーのスクロール範囲

- 生成したタスク: T-061（サイドバーの領域自体のスクロールを切り、「いま何をしているか」「タスク一覧」「セッション情報」の3区画をそれぞれ内側でスクロールさせる。T-054 で入れた `.activity-scroll` の固定 10rem を残すかも含めて決める）
- タスクにしなかった項目: 無し

- サイドバー全体はスクロール不可で、中身の何をしてるか、タスク一覧、セッション情報はスクロール可とする方針で修正お願い。

## 2026-09-12 ポートの衝突

- 生成したタスク: T-060（既定の 7327 が塞がっていたら上限まで +1 してずらし、`TSUKUMO_VIEW_PORT` を明示したときはずらさずに失敗させる）
- タスクにしなかった項目: 無し

- ポートがぶつかったら1つズラして起動できるようにしたいな。いま2つ目のプロジェクトで `tsukumo` と打つと EADDRINUSE で死ぬ。
  - 既定の 7327 が塞がっていたら 7328、7329 と順に上げる。上限は決めて、そこまで駄目なら理由を出して終わる
  - `TSUKUMO_VIEW_PORT` を明示的に渡したときはズラさない（指定したポートで待てないことに気づけなくなるので、そのまま失敗させる）

## 2026-09-12 TUI のプロジェクトでの HTML タグ

- 生成したタスク: T-058（出力スタイルの「レポートの組み立て方」が課す HTML の記法を、tsukumo 以外の素の TUI で出さないための切り替え方式を決める。出力スタイルを2つに分ける / 1ファイルで条件分岐する / tsukumo が `systemPrompt` で差分を足す、の3案を比較してユーザーが選ぶ）、T-059（T-058 が決めた方式で実装し、TUI 側とブラウザ側の両方を目視する）
- タスクにしなかった項目: 無し

- tsukumo 以外のプロジェクトで claude を使った場合に、アスナからの応答にHTMLタグが含まれてしまっていることが気になる。tsukumo がブラウザに出す場合には見やすくするために役立つけどTUIでやり取りしている別のプロジェクトではOFFに切り替えられたりできると嬉しい。

## 2026-09-12 箱の選択肢の比較

- 生成したタスク: T-057（Electron / Tauri v2 / 素のブラウザのアプリモード / WKWebView / 現状維持の Orca タブを一次情報で比較し、`docs/research/app-shell.md` に表として記録する。選ぶのは T-046）
- 既存タスクの更新: T-046（論点を「Electron か Tauri か」から「T-057 の比較表から選ぶ（2択に絞らない）」に広げ、`dependencies` に T-057 を足した。Rust の toolchain の記述を 2026-09-12 の実測に更新）
- タスクにしなかった項目: 無し

- T-046 は Electoron がベストかな? Tauri(https://v2.tauri.app/) や他の選択肢も検討する?

## 2026-09-12 サイドバーの改善

- 生成したタスク: T-054（「いま何をしているか」を固定の高さにし、並びの中でスクロールして過去のツールを見る。完了分の上限5件を50件に）、T-055（経過時間を「依頼を送ってからそのターンが終わるまで」にし、終わったらその値で止める）、T-056（タスク一覧をステータスのバッジ先頭の2列の行にし、見出しに todo / done の件数を添える）
- タスクにしなかった項目: 無し

- サイドバーの改善。
  - なにをしているかがタスク一覧を押し下げてしまって微妙な印象。高さは固定で、スクロールして過去のものを見れるイメージがいいかな。
  - 経過時間が思ったより微妙...どちらかというとユーザーが送信してから完了までの経過時間のほうが嬉しいな
  - タスク一覧が少し見づらい?サマリとステータスだよね。もう少し見やすくならないかな

## 2026-09-12 吹き出しをセリフ1件ずつに分ける

- 生成したタスク: T-053（セリフ1件につき吹き出し1つ、そのターンの分を縦に積んで最新を下に見せ、並びの中でスクロールできるようにする。前のターンの分は次の依頼で最後の1件だけ残す）
- タスクにしなかった項目: 無し（「ユーザーが入力してからのものに限定」は `src/session-view.ts` が既にそのターン最初の `speak` で前のターンの並びを捨てているので、T-053 の中で「`request` で最後の1件だけ残す」に寄せた）

- キャラ画面のセリフは1つの出力を1つの吹き出しにして区切りにすることで見やすくしたいな。複数の台詞があれば過去の台詞は上に押し上げられていって最新の台詞がよく見えるようにして、スクロールで過去の台詞が見れるように。ただし、過去っていうのは過去すべてじゃなくてユーザーが入力してからのものに限定しようか。

## 2026-09-12 スラッシュコマンド補完の改善

生成したタスク: **T-050**（候補に CSS を付け、入力欄に重ねるポップアップにし、クリックで確定できる
ようにする）／**T-051**（前方一致→部分一致・アルファベット順、Enter で実行・Tab で補完。T-050 待ち）／
**T-052**（候補に説明を添える。SDK は名前しか返さないので frontmatter から引き、足りなければ閉じる。
opus、T-050 待ち）。

**確かめた事実**: `src/view.ts` の `STYLE` に候補一覧の CSS が1つも無く（素の `<ul>` の箇条書きで、
選択中の項目も見えない）、候補は `<form>` の上に積まれるので出るたびに入力欄が押し下げられ、`<li>` に
クリックの配線が無い。絞り方は前方一致のみ、並びは SDK の `slash_commands` の順のまま、Tab と Enter は
どちらも「確定だけ」。SDK 0.3.268 の `init` は `slash_commands: string[]` で説明を持たない。

- 入力画面のスラッシュコマンドの補完の出し方がスマートじゃなくて使いづらい...改善案あるかな

## 2026-09-11（3）許可と質問の箱の置き場所

生成したタスク: **T-049**（箱を右下の入力欄の上へ移し、枠の色とタブのタイトルで気づける印を
付ける。T-043 待ち。サイドバーの作業と同じ `src/view.ts` を触るため）。

**確かめた事実**: 箱は `buildCharacterBody` の中で吹き出しの直下に出ている（T-040）。右下の
入力欄は SSE で更新されるビューではなく、進行状態は `/events/turn-status` という専用の経路で
届けている。箱も同じ形で届けるのが素直。「キャラが聞く」（Q7 の決定）は、吹き出しの `speak` が
担い、ボタンの置き場所だけを変える。

---

以下、当時の記述をそのまま。

- T-040 は良さそう。ただ、左下でキャラの下に出すのはダメだった。使いづらかったよ...
  - （確認の回答）許可と質問のボタンは**右下の入力欄の領域**に出す（答え待ちの間は入力欄の上に箱を出す。入力欄は残す）。キャラは吹き出しで「これいい？」と言うだけ
  - 使いづらかったのは「**気づかない**」（答え待ちになっていることに気づかない）と「**入力欄と離れている**」（答えるために視線とマウスが左下へ移る）

## 2026-09-11（2）どのプロジェクトでも tsukumo で起動できること

生成したタスク: **T-047**（`tsukumo` コマンドの器と、立ち絵の既定を tsukumo 自身の場所から読む
こと。T-040 待ち）、**T-048**（グローバルへの導入と、別プロジェクトでの起動の目視。T-047 待ち。
**導入はユーザーの承認が要るので `/loop` に載せない**）。

**確かめた事実**（2026-09-11）: 駆動の作業ディレクトリは既に `process.cwd()` なので、別の
ディレクトリで起こせば Claude Code はそのプロジェクトを見る（半分は満たしている）。ただし
`tsukumo` コマンドは無く（`package.json` に `bin` が無い、`which tsukumo` は空）、立ち絵の既定が
cwd 相対（`characters/tsukumo-spirit`）なので別プロジェクトではキャラが出ない。
`develop/tasks.json` も cwd 相対だが、無ければ空になるだけなのでそのまま（起動先プロジェクトの
ものがあればそれを見る）。`vendor/` は `import.meta.url` 相対で cwd に依存せず、これが手本。

**要件への反映**: 「絶対条件」なので `docs/requirements.md` 2.1 と合格シーンに入れる（T-047 の
やること）。

---

以下、当時の記述をそのまま。

- 要件としてすでにあるかもしれないけど、あるプロジェクトやディレクトリ上で tsukumo を起動して適用することができるのが絶対条件。イメージ的にはあるプロジェクトのトップディレクトリで claude を打つように tsukumo と打つとそのプロジェクトをキャラと一緒に作業できるもの。claude の拡張と言ってもいい。

## 2026-09-11 方針の全面見直し（SDK で Claude Code を動かす）

生成したタスク: **T-035**（正典の書き換え。最初の1件）、**T-036**（SDK のスパイク）、**T-037**（駆動の核）、
**T-038**（入力欄と中断）、**T-039**（吹き出しとレポートをリアルタイムに）、**T-040**（許可と質問を
キャラが聞く）、**T-041**（出力スタイルを speak ツールの規約に）、**T-042**（旧経路の撤去）、
**T-043**（サイドバー3区画）、**T-044**（表情の自動切り替え）、**T-045**（コマンド補完）、
**T-046**（Electron の判断）。

**既存タスクの扱い**: T-026 → T-041 に畳んだ。T-029 → T-043 に畳んだ。T-030 / T-033 / T-034 は
方針転換で不要（理由は `docs/history/tasks-archive.md` の各節）。**T-020 と T-028 は残した**
（T-020 は依存を T-041 に付け替え。T-028 の要約フィールドはサイドバーのタスク一覧で使う）。
`done` 9件と閉じた5件をアーカイブし、`develop/tasks.json` は 91KB → 56KB。

**タスクにしなかった項目**: 「入力履歴」「`/loop` 代替の繰り返しトグル」は Q8 で「次」と決めたので
今回は起こしていない（T-045 の注意に明記）。「Bun のまま」は決定であって作業が無い。
Q14（未コミット分の退避）はこのセッションで実施済み（stash）。

**書き手の誤りを1つ訂正した**: Q12 の「T-025 は維持」は逆で、現行の正典は「レポートの口調は
落とさない」。本文中に訂正を添えて移した（ユーザーの言葉は変えていない）。

---

以下、当時の記述をそのまま（訂正の注記を除く）。

## 2026-09-11 方針の全面見直し（grilling Q1〜Q16 の回答）

出発点のユーザーの言葉: 「今までの履歴を見れば分かる通りあまりうまくいっていない。なので改めて
tsukumo の要件を詰めたいのと、どの技術でそれが実現できるか一緒に考えてほしい。」

### 合格シーン（Q1）

- **TUI を一度も見ない**のが理想。「この orca のように統合されたアプリケーション内でキャラと
  一緒に仕事がしたい」
- v1 の合格シーン: **1コマンドで起動すると1つの画面が開く。そこで依頼を打つと、左下のキャラが
  話し、左上にレポートが出て、キャラが質問したら画面上のボタンで答えられる。Claude Code の
  TUI は一度も見ない**

### 技術の選択（Q2〜Q4）

- **Q2: Claude Code の TUI を捨て、Agent SDK（`@anthropic-ai/claude-agent-sdk`）で tsukumo が
  Claude Code を動かす。** 会話・ツール実行・許可プロンプト・質問を構造化イベントで受け取り、
  入力欄・許可ボタン・質問の選択肢を tsukumo が描く。transcript 追従・追従先の乗り換え・
  hook の状態ファイル・Orca 経由の入力送信は役目を終える。端末（PTY）は「後から同じ画面に
  足せる余地」だけ残し、最初は作らない
- **Q3: セリフはテキストの規約ではなく、tsukumo が提供する MCP ツール `speak(text, expression)`
  で受け取る。** 行頭 `アスナ: ` の切り出しは補助に格下げ。ツールの戻り値は「ok」だけにして、
  実質的な逆流はさせない
- **Q4: 箱は当面 Orca のブラウザタブ。** ページは「どの箱でも動く Web アプリ」として作り、箱の
  差し替えを1箇所に閉じる。**最終形は「使い勝手が上がるなら」Electron 等のスタンドアロン**。
  Electron の判断は縦1本が動いてから
- **Bun のまま**（SDK は Bun を公式にサポート。Electron に行くときも Bun のプロセスを子として
  起こせば済む）
- 原則「tsukumo から Claude Code へ戻る経路は無い」は消える。代わりに**「会話は tsukumo の
  プロセスの外へ出さない」**が残る（SDK もツールもローカルのプロセス内で閉じる）

### 画面の中身（Q5、Q7〜Q12、Q16）

- **Q5: メインビュー（左上）はレポートだけ。** 作業の進行（ツールの流れ）はサイドバーへ
- **Q7: 許可プロンプトはキャラが聞く。** 吹き出しに「これ実行していい？」と出て、その下に
  許可／拒否のボタン。AskUserQuestion の選択肢と同じ見た目。許可モード（毎回聞く／編集は
  自動／全部許す／プラン）を画面から切り替えられる。既定はいまの auto モード相当
- **Q8: 入力欄の v1 は (1) 複数行入力＋送信、(2) 実行中の中断、(3) `/` のコマンド補完**
  （`system/init` の `slash_commands` から）。入力履歴と「`/loop` 代替の繰り返しトグル」は次。
  画像・ファイル貼り付けはやらない
- **Q9: セッションは毎回新規（TUI と同じ）。** 再開は要らない。モデルの切り替えは `/model` を
  送るのではなく画面の選択肢から SDK のオプションで渡す
- **Q10: `speak` の引数は `text` と `expression`**（キャラ定義ファイルにある表情名から選ぶ）。
  衣装はモデル連動のまま。**表情は引数を主にして、ツール実行中は自動で「作業中」の顔**
  （いまの `src/expression.ts` の推測を流用）。呼ぶタイミング（着手の一言・ステップごと・
  完了報告・質問や許可のとき）は出力スタイル側に書く
- **Q11: サイドバーは上から (1) いま何をしているか**（実行中のツールを1行、終わったものは
  薄く数行）、**(2) `develop/tasks.json` のタスク一覧**（要約列）、**(3) セッション情報**
  （モデル・許可モード・経過時間）。コンテキスト使用量は出さない
- **Q12: レポートはリッチのまま。** 「今の表示は改善点はありつつわりと気に入っているから、
  不要なライブラリは削除していいけど簡素になるのは避けてね」。**highlight.js / mermaid /
  Chart.js / HTML 直書き（サニタイズ付き）は全部残す。** 捨てるのは PTY 路線の xterm だけ
  （stash に退避済み）。1ターン＝1枚、T-025「レポートはキャラの口調で書かない」は維持（**訂正 2026-09-11 タスク化時**: これは書き手の誤り。現行の正典 `docs/requirements.md` 4.2 は「口調は落とさない」で、T-025 は同日に撤回済み。ユーザーの決定は「今の表示を気に入っている」なので、口調もそのまま）
- **Q16: 応答の途中経過をリアルタイムに流す。** メインビューに書きかけのレポートが
  流れていき、完成した瞬間に整形し直す

### 進め方（Q13〜Q15）

- **Q13: 順番**
  1. **スパイク（半日）**: SDK で claude を起動し、出力スタイル（`asuna.md`）が効くか・`speak`
     ツールが呼ばれるか・許可と質問が `canUseTool` に届くか・auto モード相当の許可モードが
     SDK にあるかを実機で確かめる。駄目なら方針に戻る
  2. **縦1本**: 入力欄 → SDK → セリフが吹き出しに、本文がレポートに → 許可と質問にボタンで
     答える。Orca のタブ1枚で、合格シーンが1往復だけ成立する。**触れる状態になるのはここの
     終わり**
  3. **横に広げる**: サイドバー3区画、作業中の自動表情、コマンド補完、中断ボタン
  4. **Electron の判断**: 3 まで使ってみて決める
- **Q14: PTY 路線の未コミット分は捨てる**（承認済み。auto モードの安全弁で削除が止まったため
  `git stash`「PTY路線（T-032後の未コミット分。方針転換で不採用）」に退避した。戻す予定は無い）
- **Q15: 既存タスク（T-020〜T-034）は「方針転換で不要」として一括アーカイブし、`/plan-tasks` で
  切り直す。** ドキュメント（`CLAUDE.md` の「サイドカー方式が唯一の解」、`requirements.md`
  3章、`architecture.md` の設計判断）の書き換えを**新しいタスクの1件目**にする（コードより先に
  正典を直す）。**出力スタイル `~/.claude/output-styles/asuna.md` の改訂も1タスク**にし、
  グローバル設定なので実行時に承認をもらう
- **Q6: 作り直し前提で始め、使えるものだけ拾う**: キャラクター定義・表情の対応・レポートの
  HTML 化・レイアウトページ・SSE 配信

### 一次情報で確認済みの事実（2026-09-11、code.claude.com/docs の agent-sdk 各ページ）

- 構造化イベント・`canUseTool`（許可と AskUserQuestion の両方が届く）・ストリーミング入力
  モードでの追加入力と中断・スキルと `.claude/commands`・hook・MCP・Bun サポート・transcript の
  JSONL 書き出し、すべて SDK で使える
- **コマンドは `/<name>` を入力文字列に書けば送れる。** 使えるものは `system/init` の
  `slash_commands` に並ぶ。使えないのは対話端末が要るもの（`/theme` `/terminal-setup`、
  おそらく `/loop` `/rewind` `/diff` `/fork`）。`/loop` は tsukumo 側で作るほうが簡単
- headless CLI（`claude -p --input-format stream-json`）は中断ができず、
  `--permission-prompt-tool` が手元の版（v2.1.x）に無い。**採るなら SDK 一択**
- **未確認**: 出力スタイルが SDK で効くか（公式に明記なし。代替は `systemPrompt` の preset に
  `asuna.md` の中身を追記する形）。スパイクで最初に確かめる

## 2026-09-11 ターミナルを自分で持つ

生成したタスク: **T-032**（PTY の中で claude を起動して描けるかの実験）、**T-033**（端末ビューの
本実装。T-032 待ち）、**T-034**（質問の選択肢を押して PTY へキーを送る。T-033 待ち）。

**「アプリケーションにする」を2つに分解した**: ①Electron で包む ②ターミナルを自前で持つ。
**今日の問題（入力が届かない）は②だけで解ける**うえ、②なら今の HTML 資産をそのまま使える。
①は②が動いたあとでも順番が破綻しないので、**今回は②までを取りに行く**ことで合意した。

**背景の実測**（同日）: `orca terminal send` は claude の端末に `agent_prompt_blocked` を返し、
`orca keypress` は前面のペインにしか届かず、前面にしても claude の端末には入らなかった
（素のシェルには入る）。**Orca がエージェント端末への合成入力を弾いている。**

> - 根本的に見直そう。HTML1枚という形式ではなく orca みたく1つのアプリケーションにしてしまって、必要なもの（ビューワー、ターミナル）を統合するのはどうかな
> - （相談の結果）Electron で包むところまでは今はやらず、**ターミナルを自前で持つ**ところまでを取りに行く。Orca は「ページを表示する箱」のままでよい

## 2026-09-10（5）サイドバーのタスク一覧・セリフの区別・質問の表示

生成したタスク: **T-028**（tasks.json に要約フィールドを足して既存分を埋める）、
**T-029**（サイドバーをタスク一覧にし、コンテキスト使用量をやめる。T-028 待ち）、
**T-030**（キャラからの質問を画面に出す）。
**セリフとレポートの区別は新しいタスクにせず、T-025 の本文に実例として足した**。
出力スタイルには既に「地の文の中でユーザーに語りかけている部分はセリフにする」という条項が
あり（2026-09-10 追記）、**それでも破ったので条項不足ではない**と判断した。T-025 の方針転換
（レポートをキャラクターの口調で書かない）が本命なので、指摘された行をその「やってしまいがちな
例」として使う。
**質問が出ないのは仕様どおりの副作用だった**（2026-09-10 実測）: `AskUserQuestion` は
transcript に `tool_use` として残っているが、`toolVisibility` が「出すのは失敗・ファイル変更・
サブエージェント起動の3種類だけ、未知のツールは出さない」に倒しているため落ちていた。

> - サイドバーにタスク一覧を表示したい。今のタスクは task フィールドが長いから要約を載せるフィールドを追加して、要約をサイドバーに載せて。あとコンテキスト使用量は不要。
> - 「T-019 は done（コミット 8f15d00）。じゃあ T-021 の論点いくよ。」はセリフだよね。まだセリフとレポートの区別ができてないかも。改善して。
> - キャラからの質問はどこにも表示されないね...対応可能かな

## 2026-09-10（4）レポートの見せ方

生成したタスク: **なし（既存の T-021 を書き直した）**。「図や色、テーブルなど様々な HTML 技術で
レポートを改造する」は、レンダラーに記法を足す既存タスク T-021 の守備範囲そのものだったため、
新しく作らず本文を差し替えた（`docs/workflow.md`「既存タスクと重なるなら本文を更新する」）。
あわせて **T-021 の `difficulty` を sonnet → opus** に上げ（図の実現手段という判断が入るため）、
**依存の向きを入れ替えた**（旧: T-020 規約 → T-021 実装 / 新: T-021 実装 → T-020 規約）。
先に描けるようにしないと、規約が「描けないもの」を指示してしまうため。

**「テーブル」は既に満たされていた**（2026-09-10 実測）: `renderMarkdownToHtml` は GFM の
テーブルを描け、`.detail-block table` に罫線のCSSもある。T-021 に残したのは**図**と**色**、
それに未対応の記法（引用・ネストしたリスト）のほう。

> せっかく HTML にしたから、図や色、テーブルなど様々な HTML 技術を使ってレポートを改造して
> 表示しよう

## 2026-09-10（3）メインビューの長さ・セリフの量・起動コマンド

生成したタスク: **T-026**（セリフをステップごとに置く規約にする）、**T-027**（起動から
ビューを開くまでを1コマンドにする）。
「メイン画面が長すぎ。出力されるものを絞り、ステップごとに分けて表示」は**単独のタスクにせず
T-019 の本文を広げた**（同じ `buildMainBody` / `extractMainViewEntries` を触るので、分けると
同じ関数を二度書き直すことになる）。T-019 の守備範囲を「今回と過去の分離」から
「メインビューを読める長さにする」に変え、論点5（ステップの単位）・論点6（次に何を絞るか）と、
本文サイズを数で示す完了条件を足した。
**同じ指示にあった「左下と右下の規定の幅は半々で」は、T-022 が `doing` のまま開いていたので
その場で反映した**（既定を 35:65 → 50:50、コミット b3a775b）。タスクにしていない。
**「簡潔なコマンドで実行したい」は半分すでに満たされていた**: `bun run start` は引数が無いと
`~/.tsukumo/transcript-path` を読むので、長いパスを打つ必要は元から無い。T-027 に残したのは
「ビューを開くのに2コマンド目が要る」ほうだけ。

---

以下、当時の記述をそのまま。

- 左下と右下の規定の幅は半々で。
- メイン画面が長すぎ。出力されるものを絞り、ステップごとに分けて表示すること
- キャラ画面のセリフが少なすぎ。ステップごとにもっとたくさん話して
- コマンドは1つで完成するようにして。あと、簡潔なコマンドで実行したい。

## 2026-09-10（2）セリフとレポートの書き分けの方針転換

生成したタスク: **T-024**（セリフの記法を引用からプレフィックスへ変える。パーサ・テスト・
規約を同じコミットで）、**T-025**（レポートをキャラクターの口調で書かない規約にする）。
2件とも `~/.claude/output-styles/asuna.md` の同じ節を書き換えるため、T-025 は T-024 に依存させた。
あわせて **T-020 の `dependencies` に T-025 を足した**（レポートの構造化の規約は、文体が
決着してから同じ節に書くため）。**T-021 の本文から「引用はセリフの抽出に使われている記法」と
いう前提を落とした**（この方針転換で成り立たなくなるため）。
タスクにしなかった項目は無し。

---

以下、当時の記述をそのまま。

- 方針転換になるけどレポートはキャラ口調を不要にしよう(レポートとセリフどちらかわからなくなったから)
  - そのうえで、セリフに縦線を入れるのもやめよう(引用みたいに見える)。もしキミがレポートとキャラの区別をつけづらいならセリフ見たく「アスナ:(セリフ)」のようにすればいい

## 2026-09-10 メインビューの見せ方・レポートの構造・領域のリサイズ

生成したタスク: **T-019**（今回の話と過去の話の分離）、**T-020**（レポートの構造化の規約を
決めて出力スタイルへ反映）、**T-021**（決まった記法をメインビューのレンダラーで描く）、
**T-022**（4領域と入力フォームのリサイズ）。
「レポートを指してキャラがセリフを話す」は**単独のタスクにせず T-020 の論点3に畳んだ**
（規約側には 2026-09-10 追記の「地の文の中でユーザーに語りかけている部分はセリフにする」が
既にあり、条項が足りないのか守られていないだけなのかの切り分けが先のため）。

---

以下、当時の記述をそのまま。

- メイン画面に今回の話と前提以前の話が両方表示されていてわかりづらいので、今回の話を表示し、かつ過去の話を確認できる仕組みを用意したい
- 作業をして、その結果をメイン画面にレポートとして表示するわけだけど、これは単なる文章ではなく図、テーブル、フロー、箇条書きなど応答の構造を変えることで見やすくすることが求められるよ。
  - そのうえで、レポートを指してキャラが色々と左下でセリフを話すイメージ。質問したり、報告したり、応答したり。
  - 例えば「どこからいく？自分でペースを決める形で回すね。まず着手前のチェック。」とかは絶対セリフなんだよね。
- 入力フォームが狭く感じた。メイン画面、サイドバー、キャラ画面、入力フォームそれぞれの領域を広げたり縮めたりできるようにして、かつデフォルトは決めるイメージ

## 2026-09-09（4）サイドバーの候補

生成したタスク: **なし**。内容が既存の **T-012**（サイドバーに出す情報を決める）そのものだったので、
新設せず T-012 の本文に畳んだ（`docs/workflow.md` /「既存タスクと重なるなら、新しく作らず既存
タスクの本文を更新する」）。候補の一覧と取得可否の調査結果、biim のサイドバーは補足情報の
置き場であって単機能パネルにしないという注意も、そちらへ移した。

---

以下、当時の記述をそのまま。

## 2026-09-09 サイドバーの候補と、その性格

サブエージェントの状況表示をサイドバーの候補にしてもよい。

**ただし biim システム的にはサイドバーは補足情報などを表示するもので、サブエージェントの
状況だけを表示するものではないのは注意。**

### 補足（メインセッションの読み）

きっかけは、ユーザーが「サブエージェントの状況を UI で見たい」と言ったこと。Claude Code には
`/tasks` があるが、**知りたいときにコマンドを打つ必要がある＝そのたびに手が止まる**。
サイドバーに常時出ていれば打つ必要がない。これは tsukumo の目的（仕事が捗る）に直接効く。

**実現可能性は確認済み**: サブエージェントの transcript は
`~/.claude/projects/<slug>/<session-id>/subagents/agent-<id>.jsonl` に、本体と同じ JSONL 形式で
書かれている。`src/transcript.ts` が読める形。

**罠**: `docs/requirements.md` 4.1 に書いたとおり、`*.jsonl` を再帰的に拾うと
**サブエージェントの発話が本体の吹き出しに混ざる**。サイドバー用として意図的に別扱いにすること。

**Orca 側からは見えない**（実測）。`orca orchestration worker-list` は空を返す。Orca が管理するのは
`orca orchestration worker-start` で起動したワーカーだけで、Claude Code のサブエージェントは
Claude Code の管理下にあり、Orca は存在を知らない。

## 2026-09-09（3）理想の流れの掘り下げと、表示の全 HTML 化

`/grilling` の要領で Q1〜Q8 を詰めた結果、**表示をすべて HTML 化する**方針に変わった。

生成したタスク:

- 「ターミナル描画を捨てる」「HTML ビューの基盤」 → **T-013**（新規）
- 「立ち絵はキャラ専用描画領域に」「キャラビュー」 → **T-014**（新規）
- 「入力を claude に送り込みたい」（段階2） → **T-015**（新規）

書き直したタスク:

- **T-002**: 立ち絵の素材。HTML の `<img>` になったため「ドット絵か画像か」「スプライトシート」の
  論点が消え、大幅に簡素化した
- **T-009**: 「詳細を HTML にして Orca のタブで開く」→ **メインビュー（作業の進行＋レポート）**。
  Q5 で「左上では作業そのものが進む」と分かったため、レポートだけでなく `tool_use` の進行も出す
- **T-010**: 「吹き出しをセリフだけにする」→ **セリフと詳細を切り出す読む層**。表示は HTML 側に移った
- **T-011**: レイアウトの組み立て。対象がターミナルのペインから HTML ビューに変わった
- **T-012**: サイドバー。HTML ビューになった

**不要になって閉じたタスク**（どちらも本文に「やらずに閉じる」逃げ道が書いてあったのでそれに従った）:

- **T-004**（画像プロトコルで立ち絵を表示）: 立ち絵が `<img>` になったため、Sixel / iTerm inline の
  詰まりは関係なくなった
- **T-006**（statusline 併走）: サイドバーが HTML で入り、claude の TUI 自体を小さくする方針に
  なったため、statusline に出す価値が薄い

タスクにしなかった項目:

- Orca の機能調査の結果（`terminal send` / `terminal read --screen` など）は指示ではなく事実なので、
  タスク本文へ根拠として埋め込んだ
- 「入力ペインには自分の入力だけ」というユーザーの誤解は、**訂正して記録した**（TUI は会話も流れる）。
  タスクではなく前提の修正

---

以下、当時の記述をそのまま。

## 2026-09-09 理想の流れ（grilling の回答）

**Q1: 理想の光景**

1. 右下の入力から依頼をする
2. 左下のキャラが左下中央の吹き出しを通して応答する
3. 左上のメイン画面で作業する
4. 結果レポートメイン画面に。わかりやすく見やすい表示で(HTMLとか)
5. レポートをもとにキャラが左下中央の吹き出しで質問や結果報告、応答をする

**Q2: 立ち絵はどこに居てほしいか** → 立ち絵は左下のキャラ専用描画領域がいいかな

**Q3: 見るだけか、触るか** → 見るだけ

### 補足（メインセッションの読み。タスク化のときに確かめること）

**要件が1つ増えている。** これまで左上（詳細ビュー）は「完成したレポートを出す場所」として
書いてきたが（`docs/requirements.md` 4.2「詳細ビュー」）、Q1 の 3 は
**作業そのものが左上で進む**ことを言っている。biim でいうゲーム画面にあたる。
つまり詳細ビューは**作業の進行が見える場所**でもある。何を出すのか（ツールの実行、差分、
テスト結果など）は未確認。

**吹き出しの位置が具体化した。** 「左下中央」＝キャラ領域の中に立ち絵と吹き出しが同居する。
別ペインではない。

**立ち絵の置き場所はキャラ専用の描画領域**で、ブラウザビューの中ではない。したがって
**画像プロトコルが通らない問題（7. 未決事項）はクリティカルパスに残る**。
ただし「キャラ専用描画領域」がターミナルのペインなのか、2つ目の HTML ビューなのかは
**まだ確認していない**。ここで画像プロトコルの詰まりが残るかどうかが決まる。

## 2026-09-09 表示の全 HTML 化と、入力の送り込み（grilling Q4〜Q8）

- **Q4: キャラ専用描画領域は HTML ビュー**。「ターミナルにこだわるより、キャラと一緒に仕事
  できる状況が重要」
- **Q5: 左上の作業中の中身は (a) ツールの実行が流れる**（差分・テスト結果など。biim のゲーム画面）
- **Q7: これまでのターミナル描画は捨てる**（HTML に一本化）
- **Q8: 右サイドバーも HTML**
- **Q6: 入力も HTML 側から claude に送り込みたい。** ただし**段階を分ける**ことで合意:
  1. 先に HTML ビュー3つを作る。**入力は claude の TUI のまま**（ペインは小さくしておく）
  2. そのうえで入力欄を足す。claude のペインは小さいまま残す（許可プロンプトの逃げ道）

### メインセッションが調べた事実

`orca terminal send --text "..." --enter` で live terminal に入力を送れる。`--interrupt` で
中断も送れる。`orca terminal read --screen` で**claude の現在の画面を読める**ので、許可
プロンプトの検知も原理的には可能。**Orca 側の機能は揃っている。**

### 訂正した誤解

ユーザーは「入力ペインには自分の入力だけ」と考えていたが、**Claude Code の TUI は入力欄の上に
会話が流れる**（応答も表示される）。設定では変えられない。できるのはペインを数行の高さにして
実質入力欄だけを見せることで、**二重表示は減らせるが無くせない**。入力の送り込み（Q6）が
実現すれば完全に消える。

### この決定が既存タスクに与える影響（**タスクの再分解が必要**）

- **T-004（画像プロトコルで立ち絵を表示）は不要になる。** 立ち絵は HTML の `<img>` になるため、
  Sixel / iTerm inline の詰まりは関係なくなる
- **T-002（立ち絵の素材）はほぼ自明になる。** 画像ファイルを置くだけ。ドット絵かどうかの
  判断も不要
- **T-006（statusline 併走）はほぼ不要。** サイドバーが HTML で入り、claude の TUI 自体を
  小さくする方針なので、statusline に出す価値が薄い
- **`src/balloon.ts` / `src/draw.ts` は役目を終える**（折り返し・枠・全角幅・禁却処理は
  ブラウザがやる）。`src/transcript.ts` / `src/state.ts` / `src/expression.ts` は残る
- **`docs/requirements.md` 4.2 の「立ち絵はターミナルの画像プロトコルで表示する」は誤りになる**
- **未解決に置いていた「禁則処理が無い」「画像プロトコルが通っていない」は消える**
- **新しく要るもの**: HTML ビューの基盤（ローカルHTTPサーバ or ファイル生成）、
  作業の進行を transcript の `tool_use` から描く仕組み、入力の送り込み（後段）

## 2026-09-09（2）PoC を触っての感想から、目的・レイアウトの決定まで

生成したタスク:

- 「biim システムみたいな画面構成にできるといいな」＋レイアウトの決定 → **T-011**（組み立て）・**T-012**（サイドバーの中身）
- 「二重表示が厳しい」＋Q1（分離は出力スタイル側の規約で） → **T-008**（規約を入れる）・**T-010**（吹き出しをセリフだけにする）
- 「詳細が理解しづらい」＋Q2（見た目が主、HTMLで描画したい）＋詳細ペインは Orca 内のブラウザ → **T-009**
- 前提の訂正（tsukumo＝キャラと一緒に楽しく仕事をするためのターミナル環境）＋Q3（完全分離）＋Orca 前提 → **T-007**（要件そのものの書き換え）

タスクにしなかった項目:

- 「サイドバーに何を出すか」は候補が挙がっただけで未定のため、**決めること自体を T-012 のタスクにした**
- Orca の機能調査の結果（`tab create` / `terminal split` など）は指示ではなく確定した事実なので、
  タスクにせず各タスク本文へ根拠として埋め込んだ
- 既存の **T-006**（statusline 併走）は、サイドバーが入ることで存在意義が変わるため、
  新設せず本文を「まだ必要かの判断から始める」形に更新し、依存を T-012 に付け替えた

---

以下、当時の記述をそのまま。

## 2026-09-09 PoC を触っての感想

土台としては悪くない。

- biim システムみたいな画面構成にできるといいな
- 打つところと表示するところに二重に表示されるから、結局入力するときは簡素な表示のほうに
  移らないといけないのが厳しいね

### 補足（メインセッションの読み。タスク化のときに確かめること）

上の2点は引っ張る方向が逆に見えるが、biim の構成は「ゲーム画面が主役、テロップは短い」なので
噛み合う可能性がある。対応づけの案:

| biim       | tsukumo                                |
| ---------- | -------------------------------------- |
| ゲーム画面 | claude の TUI ペイン（読んで打つ場所） |
| 立ち絵     | tsukumo ペイン                         |
| テロップ   | 短い一言だけ（発話全文ではない）       |
| 情報パネル | モデル・経過時間・context 残量など     |

**確かめるべき論点**: 二重表示の何が厳しいのか。「全文が出るのは良いが2箇所にあるのが問題」なのか
「そもそも全文はいらない」なのか。ここで `docs/requirements.md` 4.2「吹き出し」の要件自体が変わる。

また、二重表示の根本的な解として、案B（statusline にマスコットを出す＝入力ペインの中に居させる、
`docs/requirements.md` 4.5 / T-006）が効く可能性がある。画面構成の検討と合わせて評価する。

## 2026-09-09 「二重表示の何が厳しいか」への回答（本質）

大まかな問題点は2つ。

1. asuna が出力するものは詳細な情報+会話のためのセリフ。実現したいのは、詳細な情報と会話の
   ためのセリフを分離して別々のペインに表示することでノイズを減らしてキャラクターとのやり取りを
   楽しみつつ仕事したいというものだよ。
2. 詳細な情報の部分が結構理解しづらいことも多いよ。ここを人間にとってわかりやすくフォーマット
   したうえで会話と分けて出せると仕事が捗る気がする。

### 補足（メインセッションの読み。タスク化のときに確かめること）

**これは `docs/requirements.md` 1章の目的そのものの書き換えになる。** 現在の要件は
「最新の assistant 発話を吹き出しに出す」だが、本当の目的は**出力を「セリフ」と「詳細」に
分離して別ペインに出し、詳細は人間に読みやすく整形すること**。吹き出しはその手段の一部。

分離の手段が最大の論点。少なくとも3案ある:

- **(a) 出力スタイル側に規約を入れて、モデルが構造的に書き分ける**（例: セリフは決まった位置に置く）。
  決定的で安上がりだが、`~/.claude/output-styles/asuna.md` の変更が要り、TUI の見え方にも影響する
- **(b) tsukumo 側がヒューリスティックで分類する**（コードブロック・表・パスは詳細、地の文はセリフ）。
  変更は tsukumo 内で閉じるが、判定が壊れやすい
- **(c) 小さいモデルに分離・整形させる**。2つ目の「読みやすく整形」にも同時に効くが、
  コストと遅延が乗り、`docs/requirements.md`「会話内容の扱い」（外部に送らない）と衝突しうる

**2つ目の「理解しづらい」の正体を先に切り分けること。** 見た目の問題（Markdown が素のまま出る）なら
レンダラを作れば済み、中身の問題（情報が多い・順序が悪い）なら要約や再構成が要る。前者なら (c) は不要。

## 2026-09-09 前提の訂正と Q1〜Q3 への回答

**前提**: tsukumo とは「キャラクターと一緒に楽しく仕事をするためのターミナル環境」。
この目的に沿うなら要件に入れてよい。詳細な情報とセリフの分離は**その手段の1つ**。

- **Q1（分離をどこでやるか）→ (a) 出力スタイル側に規約を入れて、モデルが構造的に書き分ける**
- **Q2（「理解しづらい」の正体）→ 見た目が主、で合っている。**
  実例として挙がったのは、メインセッションが書いた「目視確認の手順」の応答
  （見出し＋箇条書き＋コードブロック＋注意書き2件が一度に並ぶもの）。
  「いろんな情報があってすぐに頭に入らなかった」「見やすく HTML で描画できたりするといいけどねぇ」
- **Q3（TUI ペインに何を残すか）→ 正直不要まである。**
  入力専用・表示専用・キャラクターとのやり取り専用、と完全分離できると biim っぽくなる

### 補足（メインセッションの観察）

**ユーザーが実例として貼り返した応答は、貼られた時点で表示が途中で欠けていた**
（「衣装は当面「不明」のま の payloadにしか載らなくて」のように、行の途中が飛んでいる）。
これは「読みづらさ」が中身だけでなく**端末上の表示そのもの**にも由来している裏付けになる。

**目的の書き換えが2段階で必要**:

1. `docs/requirements.md` 1章の目的を「キャラクターと一緒に楽しく仕事をするためのターミナル環境」にする。
   現在の「発話を立ち絵と吹き出しで表示するサイドカー」は手段であって目的ではない
2. スコープ（2章）に「詳細とセリフの分離」「詳細の読みやすい整形」「ペインの役割分担」を入れる

**次に決めること（未確定）**: 詳細ペインの実体をどうするか。ターミナル内でリッチに描画するのか、
HTML を別ウィンドウ（ブラウザ）で出すのか。後者は「ターミナル環境」という前提と衝突しうるので、
ユーザーに確認すること。

## 2026-09-09 詳細ペインの実体（Orca の機能調査つき）

**HTML の方向でよい。ただし Chrome に出るのは嫌で、このシステムを使って一体感を出せる
仕組みにしたい。**

### メインセッションが調べた事実（2026-09-09、Orca v1.4.194 / com.stablyai.orca）

`orca --help` で確認した、tsukumo に効く機能:

| コマンド                | できること                                             |
| ----------------------- | ------------------------------------------------------ |
| `orca tab create --url` | **Orca 自身のブラウザタブ**を開く（Chrome に出ない）   |
| `orca terminal split`   | ペインを分割し、`--command` で起動時のコマンドも渡せる |
| `orca terminal create`  | worktree 内にターミナルを作る                          |
| `orca terminal rename`  | タブのタイトルを設定する                               |
| `orca terminal switch`  | タブを前面に出す                                       |
| `orca file open`        | Orca のエディタでファイルを開く                        |
| `orca agent-context`    | エージェント向けの機械可読なコマンドスキーマを出す     |

**これにより「Chrome に飛ばずに HTML を出す」が素直に実現できる**（詳細を HTML として
ローカルに書き出し、`orca tab create --url file://...` で Orca 内に開く）。
さらに `orca terminal split` があるので、**biim レイアウトを tsukumo 自身が組み立てられる**。

想定する構成:

```
┌──────────────┬──────────────────┐
│ claude (入力) │  Orca ブラウザタブ │
│              │   詳細をHTMLで     │
├──────────────┤   きれいに描画      │
│ tsukumo      │                  │
│ 立ち絵＋セリフ │                  │
└──────────────┴──────────────────┘
```

### タスク化のときに確かめること

- **Orca 依存をどこまで許すか。** `docs/requirements.md` は現状 macOS + VS Code 統合ターミナルを
  第一級としているが、この構成は Orca 固有の機能に強く依存する。Orca 前提にするのか、
  Orca が無いときはターミナルだけにフォールバックするのかを決める
- `orca agent-context` の中身をまだ読んでいない。実装前に読むこと
- HTML をどう生成するか（詳細のテキスト → HTML）。整形の主体は Q1 の (a) と整合させる

## 2026-09-09 Orca 依存と画面レイアウトの決定

**まずは Orca 前提でよい。**

レイアウトは biim システムをリスペクトした配置にする:

- **左上**: ブラウザ（詳細を HTML で描画）
- **左下**: 立ち絵＋セリフ
- **右下**: 入力（claude の TUI）
- **右サイドバー**: その他情報

**比率の指定**: 上段と下段で縦の仕切り位置が違う。

- **上段**: ブラウザ（広い） ＋ サイドバー（狭い）
- **下段**: 立ち絵＋セリフ（半分） ＋ 入力（半分）

サイドバーは入力の上に乗る位置関係だが、**サイドバーだけは狭い**ので、上段の仕切りは
下段より右寄りになる。

```
┌────────────────────────────────────────┬────────┐
│ ブラウザ                                │サイドバー│
│  詳細をHTMLで描画（広い）                │ （狭い） │
│                                        │        │
├─────────────────────┬──────────────────┴────────┤
│ 立ち絵＋セリフ（半分） │ 入力 claude TUI（半分）      │
└─────────────────────┴───────────────────────────┘
```

先に上下で割り、**上段と下段をそれぞれ別々に左右分割**すれば組める
（`orca terminal split` の呼び順がそのまま対応する）。

### タスク化のときに確かめること

- **「その他情報」に何を出すか**が未定。候補: 実行中のモデル、経過時間、context 残量、
  `develop/tasks.json` の進捗、直近のツール実行。**ここは biim でいうタイム表・チャートに当たる**
- Orca 前提にしたので、`docs/requirements.md`「5. 実行環境」の「VS Code 統合ターミナルを
  第一級」という記述と、`docs/architecture.md`「tmux ではなく VS Code の split terminal を
  前提にした」の設計判断を**両方書き換える**必要がある
- レイアウトを誰が組み立てるか（tsukumo が `orca terminal split` で自動構築するのか、
  ユーザーが手で配置するのか）

## 2026-09-09

出典: リポジトリ直下の `work.md`（2026-09-08 の会話を保存したもの）。`develop/direction.md` を
経由せず、リポジトリ初期化時にここへ直接アーカイブした。

生成したタスク:

- 「次の一手」1（立ち絵の素材をどうするか決める） → **T-002**
- 「次の一手」2（サイドカーのPoC: JSONL tail → 吹き出し整形 → 描画） → **T-003**
- 「次の一手」3（Sixel で立ち絵を出す実験） → **T-004**
- 「次の一手」4（hook で状態ファイルを書く） → **T-005**
- 併走させるもの（案B: statusline のマスコット） → **T-006**
- 明示されていないが上記すべての前提になる実装スタックの決定 → **T-001**

タスクにしなかった項目:

- 「調査結果」「技術制約」「採用アーキテクチャ」「環境メモ」は指示ではなく確定した事実・決定なので、
  タスクではなく `docs/requirements.md` と `docs/architecture.md` へ正典として移した
- 「タイプライター表示 ＋ VOICEVOX の口パク同期」は本人が「後回しでよい」としているため、
  タスク化せず `docs/requirements.md` 4.2 に「MVPに含めない」と記録した

---

以下、当時の記述をそのまま。

# chara-terminal — ターミナルにキャラクター立ち絵＋吹き出しを出す

作成: 2026-09-08 / 決定: **案C（サイドカー pane）で行く**

## ゴール

Claude Code との会話を、立ち絵付きの吹き出しでターミナルに表示する。

## 調査結果（2026-09-08 時点）

### 既存でやれること

- **`/buddy`** — Claude Code 本体の companion 機能。バイナリ v2.1.263 に
  `companion_intro` という内部リマインダ種別が実在するのを確認済み。
  入力欄の横に ASCII ペットが住み、10秒おきに吹き出しで喋る。18種、ユーザーID から決定論的生成。
- **claude-code-mascot-statusline** (TeXmeijin) — statusline に半角ブロックのピクセル絵マスコット。
  11個の hook イベントで表情9状態、context 消費で毛色が赤くなる。
  **キャラパックが YAML/JSON で差し替え可能**。
  https://github.com/TeXmeijin/claude-code-mascot-statusline
- **VOICEVOX / ずんだもん hooks** — Stop hook で応答を読み上げ。音声のみ、絵は出ない。

### 空いている穴

「**Claude の発話そのものを、立ち絵＋吹き出しで見せる**」ものは存在しない。

## 技術制約（設計の勘所）

Claude Code の TUI は自分で全画面を描画する。したがって:

- `claude | cowsay` のようなパイプ噛ませは**不可**
- hook の stdout はエスケープされてテキスト扱いになるため、
  そこから ANSI や画像プロトコルを流し込むのは**不可**
- → **本体の描画には割り込まない**設計にする。これが案Cを選ぶ理由。

## 採用アーキテクチャ（案C: サイドカー pane）

別ペインで常駐するプロセスが、セッションの JSONL を tail して立ち絵＋吹き出しを描く。
本体と描画が競合しないので何でもできる。

```
┌─────────────────┬──────────────────┐
│ claude (本体TUI) │ サイドカー        │
│                 │  ┌────────────┐  │
│  通常の会話      │  │  立ち絵     │  │
│                 │  └────────────┘  │
│                 │  ╭────────────╮  │
│                 │  │ 最新の発話  │  │
│                 │  ╰────────────╯  │
└─────────────────┴──────────────────┘
       │                    ▲
       │ 書く                │ tail
       ▼                    │
  ~/.claude/projects/<slug>/<session-id>.jsonl
       ＋ hook が書く状態ファイル（表情・モデル）
```

### データ源（実証済み）

最新の assistant 発話を抜くワンライナー。動作確認済み:

```bash
jq -r 'select(.type=="assistant") | .message.content[]? | select(.type=="text") | .text' \
  ~/.claude/projects/<slug>/<session-id>.jsonl | tail -1
```

- transcript は `~/.claude/projects/<cwd をスラッグ化>/<session-id>.jsonl`
- hook には `transcript_path` が渡るので、SessionStart hook で既知の場所に書いておけば
  サイドカーが追従先を知れる
- JSONL の type 分布（実測）: `assistant` / `user` / `attachment` / `mode` /
  `permission-mode` / `last-prompt` / `ai-title` ほか

### 機能

- **立ち絵**: VS Code ターミナルは `terminal.integrated.enableImages: true` +
  `gpuAcceleration` で Sixel / iTerm inline 画像が通る → ドット絵でなく本物の立ち絵が出せる
- **表情差分**: hook イベントで切替（PreToolUse=作業中 / Stop=どや顔 / エラー=あわあわ）
- **モデル別の衣装**: haiku=軽装 / sonnet=通常装備 / opus=戦闘配置。
  既存の Asuna output style の掛け声分岐と対応させる
- **タイプライター表示** ＋ VOICEVOX の口パク同期（後回しでよい）
- 副産物: ただのログ tailer なので Codex / Gemini CLI にも流用可能

### 併走させるもの（案B）

statusline 側にも小さいマスコットを出す。mascot-statusline をフォークし、
**hook が書く状態ファイルをサイドカーと共有**すれば二重実装にならない。

## 環境メモ（2026-09-08 実測）

- macOS (Darwin 25.6.0), zsh
- ターミナル: **VS Code 統合ターミナル** (`TERM_PROGRAM=vscode`, `TERM=xterm-256color`)
- あり: `node` (nodebrew), `jq`
- なし: `tmux`, `chafa`, `viu`, `timg`, `img2sixel`, `cowsay`, `deno`, `bun`
  → ペイン分割は VS Code の split terminal で代用できるので tmux は急がない
- Claude Code v2.1.263 / outputStyle: `Asuna` / model: `opus`
- 既存の hooks と statusLine は orca (`~/.orca/agent-hooks/`) が専有中。
  **設定を足すときは既存の orca エントリを壊さないこと**

## 次の一手

1. 立ち絵の素材をどうするか決める（ドット絵を描く / 画像を用意する）
2. サイドカーの PoC: JSONL を tail → 吹き出し整形 → 描画（まず文字だけ）
3. Sixel で立ち絵を出す実験（`terminal.integrated.enableImages` の有効化から）
4. hook で状態ファイルを書く（orca の設定を壊さないよう追記）
