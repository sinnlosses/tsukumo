# 進捗のアーカイブ

### 2026-09-20 レポートの記法の規約を10か条＋表の形に差し替えた（T-217）

`REPORT_NOTATION_PROMPT` を「10か条 → 内容ごとの印（表）→ 上の条を曲げてよいとき → 送る前に
消すもの」の4段に組み替え、地の文を 2431 → 1998字（−17.8%）に詰めた。記法の表は1行も減らさず、
チェックリスト（`- [ ]`）を1行足した。「本文にセリフを書かない」の正典を条2に決め、
`speech-cadence.ts` は参照の1行に落とした。

### 2026-09-20 スマホの幅でレイアウトを整えた（T-168）

狭い画面（760px 以下）を `100dvh` に固定し、上段のメインビューとサイドバーをタブで1つずつ出して下段（キャラビュー・入力欄）を常設にした。390x844 で入力欄の top が 1623→555 になり、ページ全体のスクロールが消えた（1808px→844px）。広い画面は測定値が変更前と完全一致。733 pass / 0 fail。
同日、ユーザーの指摘（「タスク一覧だけは横スクロールになっちゃう」）を受けて、「一覧を見る」で開く表を狭い画面では1タスク＝1枚のカードに組み替えた（691px の表が 324px の器から 367px はみ出し、要約の列が画面の外に出ていた）。列は落とさず、組み替えるのは CSS だけ。734 pass / 0 fail。

### 2026-09-20 埋めると決めたテストの穴を埋めた（T-224）

一覧の6件（エラー方針の分岐4・要件が明示したフォールバック2）にテストを足し、724 → 732 pass、lines 94.48% → 94.81%。`docs/coding-standards.md`「足すかどうか」に、埋めないと決めた穴を7カテゴリで書き残した（行番号は `docs/research/test-coverage.md` 側に置いた）。

### 2026-09-20 消すと決めた冗長なテストを消した（T-223）

前段の一覧どおりにテスト1件と assertion 3行を消した。725 → 724 pass、カバレッジは 94.11% funcs / 94.48% lines で変わらず（到達行は減っていない）。

### 2026-09-20 レポートの筆先にミニ立ち絵を添えると決めた（T-115）

`docs/requirements.md` 4.3 の「レポートの上に被らせない」を**ミニ立ち絵に限って**見直した。最終レポートは確定するまで出ない（4.2 の `hideUnsettledReport`）ので筆先が実在しないターンがあるが、**確定した本文も「書き上げていくように見せる」**ことで1本の仕組みにする。文字を足して変換し直す形は採らない（mermaid / Chart.js が未完成のソースで作り直される）——完成した DOM を作り、見せる範囲だけを進めて `Range` で筆先を取る。素材は `character.json` の任意の `mini` で、無ければ `portraits.default` の縮小に落ちる（4.4 に例外を作らない）。上限1.5〜2秒で出し切り、操作で即スキップ、過去のタブと `reduced-motion` では演出しない。2体見えるのは「分身」として受け入れる。`docs/design.md` 6.5 も書き分けた。実装は T-225（演出の土台）と T-226（ミニ立ち絵の追従）。

### 2026-09-20 カバレッジを見て消す候補と埋める穴を確定させた（T-222）

`docs/research/test-coverage.md` に、消す候補（テスト1件＋assertion 3行）と埋める穴6件（エラー方針の分岐4・要件が明示したフォールバック2）、埋めないと決めた境界の一覧を書いた。カバレッジは 94.11% funcs / 94.48% lines。実施は後続の2件。

### 2026-09-20 案を選ぶときの指標を規約に足した（T-220）

「コードの把握のしやすさ」を3つの物差し（開くファイルの数／呼ぶ側が外の事情を知らずに済むか／黙って効かなくならないか）にして、`CLAUDE.md` の一覧と `docs/coding-standards.md` の節に分けて書いた。工数と行数は指標にしない。`/code-review` の Standards 軸はこの2ファイルを読むので、足した時点でレビュー観点にも入る。

### 2026-09-20 復元したターンの依頼からコマンドのタグを畳んだ（T-208）

起動時の復元は transcript に残る**展開後**の姿（`<command-name>` など）を読むので、複数行になって三角つきで出ていた。`/<name> <args>` の1行に畳んで入力欄から打ったときと見え方を揃えた。メッセージ全体がタグだけのときだけ畳む（壊れた形は素通し）。

### 2026-09-20 サイドバーで選択中の枠が切れるのを直した（T-209）

`.sidebar-block-scroll` に `padding: 4px` を足した。`:focus-visible` の枠が要素の外側 4px まで出るのにスクロール領域の padding が 0 で、上端（キャラクター）と下端（許可モード）の枠が切られていた。

### 2026-09-20 タブを切り替えたときの先頭スクロールを直した（T-219）

`scrollTop = 0` は `.main-turns` が `overflow` を持たないので no-op だった。実際に転がる要素は画面幅で入れ替わる（広い画面は region、狭い画面はページ自身）ので、`scrollIntoView({ block: "start" })` にして呼ぶ側が転がる祖先を知らなくてよい形にした。CSS は触っていない。

### 2026-09-20 localStorage のキーに版を足した（T-216）

`tsukumo-layout-split:v1` と `tsukumo-appearance-color:v1` にした。旧キーからの読み替えは足していないので、**保存済みの仕切りの位置と画面の色は一度だけ既定に戻る**。

### 2026-09-20 仕切りのドラッグを直接書き込みにした（T-215）

ドラッグ中は grid の要素へ `--layout-*` を直接書き、`pointerup` のときだけ state を更新する形にした。ハンドラを最新に保つのは `useEffectEvent`（規約が既に指名していた口）で、「`split` は pointerdown の時点のもの」という回避策の注記が要らなくなった。`docs/coding-standards.md` の React 節に、`useEffectEvent` をレンダー中に呼べないことを3行足した。

### 2026-09-20 getComputedStyle の読みを描画の外へ移した（T-214）

キャラクター画面が描画のたびに呼んでいた `getComputedStyle`（色3回・衣装の差し色5回）を、マウント時の1回だけにした。色を動かしたときの「書く → setState → 描画中に読み直す」も、書いた値をそのまま表示の state へ流す形に崩した。

### 2026-09-20 隠している会話画面を hidden から <Activity> に置き換えた（T-213）

`src/browser/main.tsx` の `<div hidden>` を `<Activity mode="hidden">` にした。`hidden` 属性は描画を止めないので、キャラクター画面を開いている間も会話側が毎フレーム描き直されていた。下書き・選んでいるターン・スクロール位置が残る性質はそのまま。

### 2026-09-20 毎フレーム組み直していた箇所を memo と1回計算に直した（T-212）

`TaskTable` と `Report` を `memo` で包み、`<thead>` と2つの正規表現をモジュール定数へ上げた。`taskReadiness` は第2引数に未完了IDの `Set` を取る形にして、一覧全体で1回だけ作るようにした。`taskListTitle` の `filter` 3回は1回の走査に畳んだ。

### 2026-09-20 モバイルの orca から届くかを調べた（T-225）

モバイルの orca は Mac の runtime にペアリングした薄い遠隔クライアントで、ブラウザタブは画面配信として送られる。だからタブの中の `127.0.0.1` は Mac を指し、**tsukumo 側の変更なしで届く見込み**。推奨は orca のリモートの仕組みに乗る道で、ペアリングを `local-only` にすることが要件 2.2 との整合の条件。レポートは `docs/research/mobile-orca-reach.md`（実機では未確認）。

### 2026-09-20 姿の配りを useSyncExternalStore + セレクタにした（T-211）

`stores/session.tsx` を React の外の store にし、Context が配るのは store そのもの（参照不変）にした。部品は `useSessionSelector` で読む値だけを購読し、送るだけの部品は `useSessionDispatch` を読む。毎フレーム2回計算していたメインビューのターンは `stores/main-view-turn.ts` の WeakMap で1本化した。`pending` が動いたフレームだけ即時で、他は `startTransition`（ただし `useSyncExternalStore` の描き直しは同期レーン固定なので、効いているのは購読の絞り込みのほう）。

### 2026-09-20 責務が同居していた大きいファイルを分けた（T-196）

`shared` を4つ（`command-suggestion` / `character-definition` / `character-asset` / `expression-choice`）に割り、`mainViewEntries` は既存の `main-view.ts` へ寄せ、`adapter/server.ts` から WebSocket の境界を `session-socket.ts` に出した。`sdk-driver.ts`・`cli.ts`・`session-manager.ts` は概念が1つなので分けていない（理由は tasks.json の evidence）。

### 2026-09-20 中間レポートが畳まれてすぐ開き直すチラつきを直した（T-202）

ターンが進行中のあいだ、いちばん新しいターンの最後のステップの本文を「まとまった資料」と
判定できたときだけ出す形にし（`hideUnsettledReport`）、出した本文が消えないようにして
`superseded` の反転を止めた。`docs/requirements.md` 4.2 の「出してから消す」（2026-09-16）は
同日の決定で覆した。

### 2026-09-20 表情 serious / curious を実装に通した（T-137）

`Expression` 型・`EXPRESSIONS`・`character.json` 3種・`persona.md` 3種に `serious`（きりっ）と
`curious`（きょとん）を通し、利用者が用意した立ち絵2枚を `characters/tsukumo/` に入れた。実機で
キャラクター画面に6つ並ぶことを確認。あわせて T-136 で 4.3 に書いた「立ち絵が無い表情は選択肢に
出ない」が実装と食い違っていたので、実装（ラベルか立ち絵のどちらかがあれば出る）に合わせて直した。

### 2026-09-20 表情を6つに増やすと決めた（T-136）

`serious`（きりっ）と `curious`（きょとん）を末尾に足すと決め、識別子・日本語ラベル・出す場面を
`docs/requirements.md` 4.3 の表に書いた。`curious` は質問を出すときに必ず出すものではない
（ユーザーの判断）。コードは変えていないので、実装は T-137 で通す。

### 2026-09-20 core と adapter を src/server/ の下へ移した（T-210）

改名の段3。`src/core/` と `src/adapter/`（と対応する `test/`）を `src/server/` の下へ入れ子にし、
`architecture.test.ts` の `layerOf` を2段対応にして禁止していた辺を1本も減らさずに移した
（`server/` 直下の未分類ファイルは `throw`）。正典のドキュメントは追随させ、
**`docs/research/` は当時の記録なので書き換えない**ことにした。

### 2026-09-20 protocol を shared に、ui を browser に移した（T-195）

`git mv` で115ファイル（`src/` 74・`test/` 41）を `shared/` と `browser/` へ移し、層の名前を
「どの実行環境で動くか」に揃えた（`core` / `adapter` は段3＝T-210 のまま）。
`test/architecture.test.ts` の辺は9本とも残し、README のツリーと `docs/architecture.md` の
置き場の表（実行場所の列）も実態に追随させた。

### 2026-09-20 入力欄に @ のファイル補完を足した（T-188）

`GET /repository-file?t=<起動トークン>` を1本生やし、`adapter/repository-file.ts` が
`git ls-files -z` を起こす唯一の境界になった（失敗時は空）。絞り込みはブラウザ側で、一覧は
`useQuery`（`staleTime` 30秒、`@` を打っている間だけ `enabled`）。候補は判別可能な合併型に
畳んで `/` のコマンド補完と同時に出ないことを型で保証した。

### 2026-09-20 色の連続変更を自前の debounce でまとめて書き込むようにした（T-204）

`src/ui/lib/debounce.ts` の `useDebouncedCallback` を足し、差し色の送信と画面の色の
`localStorage` 書き込みを 200ms まとめた。見た目は `onChange` のまま即時で、引きずったまま
画面を閉じたときは待機中の値を flush する（落とさない）。

### 2026-09-20 TanStack Query を入れて立ち絵の SVG 取得を useQuery にした（T-203）

`components/portrait.tsx` の `useSvgMarkup` を `useQuery`（`queryKey: [url]`、`staleTime`/`gcTime`
は `Infinity`）に置き換え、`useEffect` を消した。URL が `?v=<パック>@<版>` を含むので、素材を
差し替えると別のキャッシュ行になる。サーバの `no-store` は変えていない。

### 2026-09-20 このリポジトリに置く hooks の候補を提案した（T-201）

`develop/direction.md` に候補7件（上位3件は `PreToolUse` / `Bash` / `deny` で、素の `bun test`・
作業ツリーを戻す git・`git add -A` を止める）と、hooks に向かない5件を理由つきで置いた。
設定ファイルは1つも作っていない（`.claude/` も `~/.claude/settings.json` も無傷）。採否はユーザー待ち。

### 2026-09-20 vendor の3ファイルを npm 依存に移し node_modules から配った（T-200）

`vendor/` を削除し、mermaid / chart.js / highlight.js を `package.json` の依存にした。
`src/adapter/vendor-asset.ts` が `node_modules` の実ファイルを読み、`/vendor/<名前>` の経路と
「記法が出たときだけ読む」性質は据え置き（3.3MB の mermaid を束ねに入れない）。版は据え置きで、
mermaid とテーマ CSS は同梱物とバイト一致。表示時の外部通信ゼロも目視で確認した。

### 2026-09-20 使い捨ての scratchpad/sdk-spike を消した（T-199）

2026-09-11 の SDK スパイク（役目を終えていた）を `scratchpad/` ごと削除した。本体からの
参照は元々0件で、必要になれば `104e27a` から取り出せる。

### 2026-09-20 レポートの記法の規約を短くする案と対応表を作った（T-198）

`docs/research/report-notation-shortening.md` に条項26項目の対応表（残す20 / 削る3 / 畳む3）と
短縮版の草案を置いた。4372文字 → 3589文字（−約17%）で、記法は1つも増減していない
（タグ9個も class 8個も許可リストと対応表に全部含まれることを検算済み）。採否はユーザー待ち。

### 2026-09-20 react-best-practices を src/ui/ に当てて改善点を洗い出した（T-197）

17件が当たった（`rerender-` 7 / `js-` 5 / `client-` 2 / `rendering-` 2 / `advanced-` 1）。
根拠はサーバが 100ms ごとにフレームを押すことと、`session.tsx` の context value が毎レンダー
新オブジェクトで、`useSession()` を読む14部品が全部再描画されること。採否はユーザー待ちで
`develop/direction.md` のドラフトに置いた。

### 2026-09-20 src/ のディレクトリ構成を組み替える案を比べ、推奨を1つ出した（T-194）

`docs/research/architecture-placement.md` に候補 A〜D を並べ、**A を第1段階にして C**
（`shared` / `server/{core,adapter}` / `browser/{screen,region,…}`）を推した。依存の辺は
実測で設計どおり（`core → adapter` 0本）なので、直すのは構造ではなく名前の読めさ。
採否はユーザー待ちで、`develop/direction.md` のドラフトに置いた。

### 2026-09-20 レポートで消えている表現3件を直し、無装飾の要素に手当てした（T-192）

チェックリストは `markdown/task-check.ts` がサニタイズ前に `<input>` を静的な印の `<span>` へ
畳む（許可リストに操作できる要素を入れない方針を保つ）。`sup`/`sub`/`tfoot` を通し、`mark` は
配色から浮くので外した。脚注の英語ラベルは `remarkRehypeOptions` で「脚注」に替えた。
`details` / `hr` / `figure` / `figcaption` / `small` / `kbd` / `samp` に装飾を当てた。

### 2026-09-20 src/ui/styles の CSS を CSS Modules へ移した（T-191）

領域別の CSS を `features/<領域>/*.module.css` へ移し、`styles/` に残るのは `theme.css` 1枚。
`buildUiScript` / `buildStyleSheet` は `buildUiBundle` 1本に統合し（class 名のハッシュが JS と
CSS の両方に焼かれるため）、一時ディレクトリへ `--outdir` して読んで消す形にした。
`narrow-screen.css` は各モジュールの `@media` へ分解。`ui-rebuild.ts` は `page` 更新へ倒した。

### 2026-09-20 レポートの意味のクラス名を React 部品へ解決する変換層を置いた（T-190）

`markdown/notation.tsx` が `div` / `span` を受け、記法の5系統だけを `report-` 付きの class に
置き換える（知らない class 名と `style` 属性は素通し）。「お願い」のラベルは CSS の `::before`
から部品が描く文字へ移した。

### 2026-09-20 oxlint の react プラグインを有効にし、既存7件を直した（T-189）

`.oxlintrc.json` に `react` を足し（`react-in-jsx-scope` だけ off）、指摘7件を lint の抑制ではなく
構造で解いた。`layout.tsx` の ref 書き込みは `onCommit(percent)` への契約変更で、
`turn-selection.tsx` の effect はレンダー中に前回値と見比べる形で消した。

### 2026-09-20 サイドバーの「続きから」の印を仕様ごと撤去した（T-187）

`session-restored` イベントと `SessionState.restored` を、表示・畳み込み・イベント型まで落とした
（続きから起こし直すこと自体と `replayRestoredSession` は残した）。

### 2026-09-20 「見た目」の引き出しを消し、右下を比率リセットに戻した（T-185）

`<Appearance>` と `appearance.css` を削除し、`<Layout>` が「領域の比率を既定に戻す」ボタンを直接
描くようにした（`renderAppearance` prop が消え、1列表示では仕切りと一緒に隠れる）。

### 2026-09-20 キャラクター画面を別画面として作り、作成・編集・色の口を移した（T-184）

T-183 で決めた形（`docs/design.md` 13.6）のとおり、`#character` / `#character/new` の2画面を `stores/screen.tsx` の
`useScreen`（`useSyncExternalStore` で hash を読む）と `main.tsx` の `<Root>` で出し分け、会話の画面は `hidden` で隠して
下書き・選んでいるターン・スクロール位置を保つようにした。`<Portrait>` は `components/` へ、色と編集と作成は
`features/character-screen/` へ移した（「見た目」の引き出しの撤去は T-185）。

### 2026-09-20 入力欄の質問でラベルと説明が重なる件を直した（T-205）

複数選択の選択肢だけで、ラベルの文字が行の箱からはみ出して右隣の説明に重なっていた（`.question-choice-checkbox-row` が縮むのに中のラベルが `flex: 0 0 auto` で縮まない）。あわせて `<label>` 版だけ `content-box` で箱が横に溢れていたのも直した。再現用の場面 `question-long` を台本とカタログに足してある。

### 2026-09-20 TanStack の各ライブラリの採否を決めた（T-193）

公開されている24件をこのリポジトリの実物に当てて採否を決め、採用は TanStack Query 1件に絞った（`portrait.tsx` の `useSvgMarkup` と、T-188 が足す GET が当て先）。採用分と T-188 の本文更新案は `develop/direction.md` の `## エージェントのドラフト` に置いてあり、承認待ち。

### 2026-09-18 「立ち絵の位置を固定する」を仕様ごと撤去した（T-182）

`src/ui/lib/portrait-fixed.ts` と「見た目」の引き出しのチェックボックスを消し、`usePortraitMotion` を
`resolvePortraitMotion` の呼び出しだけにした。`<Portrait>` の `motion` も `undefined` を取らない形にし、
動きを止める道は `prefers-reduced-motion: reduce` だけになった（`docs/requirements.md` 4.3 /
`docs/design.md` 6.5・13.6 も更新）。

### 2026-09-17 /model の切り替えをサイドバーへ即時反映した（T-186）

`assistant` の `local_command_run` から `model-changed` を出し、`MODEL_ALIASES` に完全一致する
ときだけ `state.model` を先回りで更新するようにした。**このフィールドは SDK 0.3.274 で入ったもので
0.3.268 には無く**、実装だけでは効かなかったので `package.json` の下限を上げている
（`docs/design.md` 4.1 に理由を書いた）。

### 2026-09-17 サイドバーのモデルに Fable を足した（T-130）

`setModel("fable")` が通ることを SDK で実測し（対照の無効な名前は reject）、`MODEL_ALIASES` と
サイドバーの `MODEL_LABELS` に `fable` を足した。衣装は `opus` と同じ戦闘配置（`OUTFITS` は増やさない）。

### 2026-09-17 キャラクター画面の中身とデザインを決めた（T-183）

会話の画面と入れ替わるキャラクター画面（`#character`）と作る画面（`#character/new`）を `location.hash` で
切り替える形にし、入る口はサイドバーのキャラクター行の「整える」、中身は立ち絵の並びが主役と決めて
`docs/design.md` 13.6 に書いた（採らなかった案つき）。T-184 / T-185 の本文をその形に合わせて書き直した。

### 2026-09-17 thinking のラベルを「ふむ」にし、表情の選び分けを persona に書いた（T-181）

`characters/tsukumo` の `thinking` を「本気」→「ふむ」、`tsukumo-spirit` と `local` を
「作業中」→「思案」にし、各 `persona.md` に「調べる前・迷うときは thinking」の1行を足した
（ラベルだけでは出番が増えないため）。

### 2026-09-17 working を thinking に改名し、必須の立ち絵を default だけにした（T-180）

`Expression` の `working` を `thinking` へ改め、`REQUIRED_EXPRESSIONS` を `default` 1つに絞った（自動の上書きを撤去した T-179 で、必須に据えた根拠が失効したため）。作成画面は立ち絵1枚で最後まで進むようになり、`thinking` は画面から消せる表情になった。日本語ラベル（「作業中」）はそのままで、変更は T-181 の担当。

### 2026-09-17 ツール実行中の表情の自動上書きをやめた（T-179）

`resolveExpression` とクールダウン（4000ms）・遅延（1000ms）の定数、`lastToolFinishedAt`、`currentExpression`、`workingSpeech`（3パック）と作業中の吹き出しを撤去し、**表情の源を `speak` の1つ**にした。撤去の理由は `docs/requirements.md` 4.3 に残してある（同じ案がまた出るため）。`working` は `speak` で選べる表情として残る。

### 2026-09-17 ランタイムは Bun のままと決めた（T-145）

いま Node へ寄せ替えず、再検討は**箱が変わったときだけ**にした。規約「Bun固有APIに寄せない」は T-162 の決着どおり保ち、`src/adapter/bundle.ts` が `bun` コマンドに実行時依存している（退避先が完全でない）ことを規約の理由の側に明記した。`docs/design.md` 1章からタスク番号が消え、`docs/requirements.md` 7章に条件付きの保留が1行増えた。

### 2026-09-17 画面の状態を台本のカタログにして一括で撮れるようにした（T-178）

台本の場面に名前を付け、`TSUKUMO_FAKE_SCENE` で名指しすると起こした直後にその状態が出るようにした。`scripts/capture-catalog.ts` が5状態×2窓を一括で撮り、索引 HTML を書き出す。依頼を手で送らずに質問の箱を撮れるので、その場限りの Playwright スクリプトを書き直さずに済む。

### 2026-09-17 答え待ちの間は入力欄の領域を質問の箱に全部渡すようにした（T-177）

選択肢が行の途中で切れて「答える」ボタンと重なって見えていた。質問のときだけ下のテキスト入力欄と「送る」行を `display: none` で畳み（下書きは残す）、`.question-card` は 126/236 → 241/241 で中身が収まった。許可要求は自由入力欄を持たないので畳まず 64% のまま。

### 2026-09-17 レポートに KPI タイル（stats / stat）を足した（T-176）

視覚優先化の2軸目。候補3つのうち数を大きく見せる塊だけを class + CSS で足し、差分は `language-diff` が既に色付けできたので規約の1行に、ファイルツリーは ```text で代用できたので落とした。メーターは数の見せ方が2通りになるので採らず、`meter`/`progress` が落ちることをテストで固定した。

### 2026-09-17 レポートの規約を視覚寄りに緩め、mermaid を10種に増やした（T-175）

表の3列目を「文へ倒す下限」から「その印が向く内容」へ反転させ、文で通す条件は表の前に1箇所だけ置いた。mermaid は実機で描けることを確かめた10種に増やし、あわせて1レポート内の図を1つずつ描くようにした（同時に走らせると中身の無い SVG になる実測）。

### 2026-09-17 組み立て直しの失敗理由をターミナルに出すようにした（T-174）

`bundleWithBun()` を成功／失敗の直和にして `bun build` の stderr を拾い、定型文の1行に続けてエラー文を出す（起動時の失敗も同じ扱い）。長いときは先頭20行・2000文字で切り、切ったことを1行で示す。失敗後に再試行しないのは現状維持で、理由をコメントに残した。

### 2026-09-17 ツール実行中は吹き出しにも作業中の一言を出すようにした（T-173）

立ち絵が `working` なのに吹き出しが直前のセリフのままで食い違う件を、吹き出し側で解いた。`speeches` には積まず描くときに重ねるので記録は汚れない。判定は表情とまったく同じ値から引く。文言は `character.json` の `workingSpeech` に置き、無ければ `expressions.working` のラベルに落ちる。

### 2026-09-17 入力欄のプレースホルダをキャラクターへの依頼の言い方にした（T-172）

「claude への依頼を書く」を `character.name` から組み立てる形にし、名前が届く前は主語を落として「依頼を書く（…）」にした（`claude` へは戻さない）。操作の案内はそのまま残してある。

### 2026-09-17 メインビューの上限が画面に出るものだけを数えるようにした（T-166）

実物のやり取り720件で測ると 22件（3.1%）が上限 40 に当たり、うち16件は画面から何も消えていないのに「n 件は省略した」を出していた（2026-09-16 のツール非表示化に上限が追随していなかった）。値ではなく数える対象を直し（レポートと質問だけ）、落ちるターンは 22 → 0件になった。

### 2026-09-17 Bun固有APIに寄せない規約は緩めないと決めた（T-162）

`src/adapter/` 12ファイルで `node:` により書けない不都合は0件、遠回りも `bundle.ts` と `server.ts` の2件で実害なし。`src/` の `node:` import 25件のうち23件が `adapter/` に集中しているので、「境界だけ緩める」は実質の撤回になる。**T-145（Bun か Node か）が同じ節を触るので、そちらを先にする。**

### 2026-09-17 mermaid が構文エラーのときコードとエラー文を出すようにした（T-153）

`suppressErrorRendering` を立てて mermaid 自身のエラー図を止め、`run()` の失敗を1本の `catch` で受けて元のコードとパースエラー文を出す形にした（読み込み失敗も同じ経路）。あわせてレポートの規約に「ラベルの引用符は `#quot;`」を1文足した。

### 2026-09-17 タスクの status を枠付きの色分けラベルにした（T-152）

`doing` が「想定外の値」と同じ注意色に落ちていたのを、差し色（`--accent`）の枠付きバッジにした。見出しは todo を常に、doing / done は0件でないときだけ添える形にし、モーダルの表の status 列は素のテキストのままにした（同じ行に色付きラベルが2つ並ぶのを避けた）。

### 2026-09-17 ターン進行中はキャラクターを切り替えられないようにした（T-148）

切り替えは起こし直しなので、送信後に切り替えると作業が黙って消えていた。キャラクターの `<select>` を進行中だけ `disabled` にし、理由を `title` で見せる（常設の枠は増やさない）。画面を経ない依頼に備えてサーバ側でも弾く。モデルと許可モードは会話が消えないので塞がない。

### 2026-09-17 cli.ts の判断を core へ移した（T-144）

アーキテクチャ提案の段3。起こす一続きの順序を `core/session-launch.ts` に、初期パックの順位を `core/character-selection.ts` に移し、`cli.ts` は 431 → 379 行になった（目標の 150 行には届かず、残りは配線とパックの持ち回りに張り付いた見た目の書き込み）。`restart` の「画面を初期状態に戻す」判断は `session-manager` に残し、起動時と起こし直しが同じ1本を通る形にした。

### 2026-09-17 画面から新しいキャラクターパックを作れるようにした（T-125）

「見た目」の引き出しに、名前・必須の2枚の立ち絵・差し色を受け取って `~/.tsukumo/characters/<name>/` にパックを作る口を足した。同名は弾き、名前は半角英数と `. _ -` だけ（`.` 始まり不可）に絞ってディレクトリ名がパスにならないようにしてある。作っても切り替えず、増えるのは `<select>` の選択肢だけ。

### 2026-09-16 src/ui/ の箱どうしの import をテストで守るようにした（T-171）

`docs/design.md` 2章の表を `ALLOWED_UI_BOX_IMPORTS` として `test/architecture.test.ts` に写した。
`ui/` 直下に未知のディレクトリが増えたら `throw` するので、箱を足したときの直し忘れで検査が
素通りしない。わざと違反させて2通りとも落ちることを確認済み。

### 2026-09-16 src/ui/ を bullet-proof-react の形に組み替えた（T-170）

`git mv` で74件（`src/ui/` 50 + `test/ui/` 24）を移し、`features/` `components/` `lib/` `stores/`
`styles/` の形になった。中身は `App` → `SessionProvider` の改名と `<Layout>` が
`renderAppearance` を props で受け取る形の2点だけ変え、**見た目は1pxも変えていない**
（移動前の HEAD を一時 worktree に出して撮り比べた）。

### 2026-09-16 src/ui/ を bullet-proof-react の形にする設計を決めた（T-169）

箱は `main.tsx`（直下）/ `features/` / `components/` / `lib/` / `stores/` / `styles/` の5つ。
52ファイルの移動の対応表と、採らなかった12要素の理由を `docs/design.md` 2章に書いた。
単数形の規約は「ファイルは単数形、ディレクトリも単数形だが `src/ui/` の置き場所だけ例外」に変わった。
**ファイルはまだ動いていない**（組み替えは T-170）。

### 2026-09-16 答えた質問をメインビューに残すようにした（T-135）

答えが確定した時点で `question-answered` を流し、`SessionRecord` の `question` に積む。答えは
質問ごとの並びで運ぶ形にして、SDK 向けの「、」連結は `core/pending-answer.ts` へ移した
（`PROTOCOL_VERSION` は 2 へ）。復元の経路までは広げていない。

### 2026-09-16 冒頭の塊にも見出しを置く条項を足した（T-164）

`REPORT_NOTATION_PROMPT` の見出しの条項が3つになった。「短い本文には付けない」は残したまま、
付けると決めたレポートでは**最初の見出しより前に地の文を置かない**を足した。

### 2026-09-16 畳んだ中間レポートの開閉が入れ替わるのを直した（T-165）

`<Step>` の `key` が配列の添字だったので、`limitTurnEntries` が古いステップを落とすと
React が別のステップの DOM を使い回していた。`MainViewStep.id`（作成順の通し番号）に替えた。

### 2026-09-16 character-pack.ts を core と adapter に割らないと決めた（T-163）

fs に触らない3関数（`characterChangedEvent` / `buildSystemPromptAppend` /
`toCharacterPackChoices`）も `adapter/character-pack.ts` に置いたまま。層は「外の世界に触るか」で
決め、ファイルの中身の純度で割り直さない、という決定を `docs/architecture.md` に足した。

### 2026-09-16 立ち絵が working と往復するのを止めた（T-167）

ツールが終わっても 4000ms は `working` を保つクールダウンを入れた。クールダウン中に始まった
ツールは遅延を待たず即座に `working` にするので、覆う長さは隙間ぶんだけで済む。

### 2026-09-16 待機中のメインビューの件を着手せずに閉じた（T-076）

「依頼を送って待っている間にメイン画面へ何を出すか」を決めるタスクだったが、レポートを
リアルタイムに流す形と中間レポート（T-127・T-161）で待機中の画面はすでに埋まっている。
棚卸しと候補出しはしていないので `passes` は `false` のまま閉じた。

### 2026-09-16 質問の選択肢の説明を折り返して全文出すようにした（T-134）

`.question-choice-description` の省略記号を外して折り返す形にした。選択肢そのものは広がらず、
はみ出るぶんは `.question-card` の中でスクロールする。

### 2026-09-16 追い越された中間レポートを畳むようにした（T-161）

同じターンで後ろにレポートが出た中間レポートを `<details>` にし、`<summary>` に
「中間レポート: <先頭行>」を出す。いちばん下の1件は開いたまま残る。

### 2026-09-16 src/adapter/ を切り、core を純粋な判断に絞った（T-143・移行の段2）

境界の 12 ファイルを `src/adapter/` へ移し、`core` から `node:` の import が 0 件になった。
`test/architecture.test.ts` が `core → adapter` を落とすので、**純粋さがテストで守られる**。
T-162 / T-163 のブロックが外れた。

### 2026-09-16 利用者への「お願い」を本文に置けるようにした（T-156）

レポートのいちばん最後に `note-favor` の塊を1つだけ置ける形にし、「語りかけはセリフ」の原則に
例外を1つ開けた。ラベルは CSS の `::before` が出すので、書き忘れても文字が残る。

### 2026-09-16 見出しを要約として書く条件を規約に足した（T-155）

`REPORT_NOTATION_PROMPT` に「短い本文には見出しを付けない」と「見出しはその塊の結論を言う語に
する」を足した。T-154 で見出しが実際に描かれるようになったことの上に乗る。

### 2026-09-16 送信した時点で吹き出しが切り替わるようにした（T-160）

`request` で `speeches` を空にし、既存のプレースホルダ「（まだ発話がありません）」の経路に
乗せた。表情も既定へ戻す。2026-09-12 の「最後の1件だけ残す」を置き換えたもの。

### 2026-09-16 中間レポートを残し、実況だけを落とすようにした（T-159）

`keepOnlyInterimReports`（旧 `dropNarration`）が本文の中身を見るようになり、構造の印を持つ
まとまった本文は「中間レポート」として残る。一瞬出て消えるのは実況だけになった。

### 2026-09-16 レポートの見出しが描かれるようになった（T-154）

`h2` / `h3` がサニタイズの許可リストに無く、タグが落ちて素のテキストになっていた。許可リストに
足したうえで `h4` / `h5` として描き、利用者の依頼の見出し（`h2.turn-request`）と段が被らない
ようにした。

### 2026-09-16 委譲中も吹き出しが止まらないようにした（T-157）

`src/core/speech-cadence.ts` に「委譲は背景で行い、子狐からの合図を受けてメインが `speak` する」
を書いた。**このタスク自身を背景で委譲して仕組みを実地で確かめた**（合図が3通届いた）。
文面が効くのは次にプロセスを起こしてから。

### 2026-09-16 セリフの間合いを core の規約にした（T-150）

`speak` を呼ぶ頻度（1ターン5〜10回）・契機・セリフに入れる中身を `src/core/speech-cadence.ts`
に置き、3つの `persona.md` に重複していた「呼ぶタイミング」を消した。パックが決めるのは
口調だけになった。**効くのは次にプロセスを起こしてから。**

### 2026-09-16 タスク一覧の表に loopable の列を足した（T-151）

`src/protocol/task-summary.ts` が `loopable` を読み捨てていたので `TaskSummaryItem` に足し、
モーダルの表（`src/ui/sidebar/task-board.tsx`）に難易度の次の列として出した。モーダルの幅は
変えていない（1024px のまま）。

### 2026-09-16 ツールの行をレポートから消し、「レポートだけを出す」に戻した（T-133）

`toolVisibility` と `<ToolRun>` と `.step-tools` をまとめて撤去した（判断そのものが要らなく
なったので器を残さない）。進行・失敗を見る場所はサイドバーの「いま何をしているか」に一本化。
`MainViewStep.actions` のツールは残す（`dropNarration` が判定の材料に使う）。

### 2026-09-16 コマンド補完の上下移動に Ctrl+P / Ctrl+N を足した（T-128）

候補が出ている間だけ矢印キーと同じ扱いにして `preventDefault` する（macOS では既定で
キャレットが動くため）。Meta 併用は無視、候補が出ていないときは何もしない。

### 2026-09-16 立ち絵と差し色を画面から差し替えられるようにした（T-124）

「見た目」の引き出しに立ち絵（表情ごと）と差し色（衣装4つ）の口を足した。書き込み先は
`~/.tsukumo/characters/<name>/` で、画像は data URL を WebSocket のコマンドで受け取る（前段の決定どおり）。
`default` と `working` を消す操作は protocol・core・ui の3層で弾く。常設のボタンは3つのまま。

### 2026-09-16 README を現状に合わせ、command not found の対処を足した（T-121）

`which tsukumo` での確認と `~/.bun/bin` を PATH に足す手順を Quick Start に追加。環境変数名
（`TSUKUMO_CHARACTER`）・3層の構成・`bun test --isolate`・セッションの復元など、現状と食い違って
いた7項目を直した。空のディレクトリで手順どおり起動できることを実際に確かめてある。

### 2026-09-16 ツール呼び出しが続く本文をレポートから落とすようにした（T-127）

「まず読むね」のような実況は、あとにツールが続いたという**事実**で落とす（`main-view.ts` の
`dropNarration`）。質問はツールに数えないので、質問の直前に書いたレポートは残る。規約の条項
（`report-notation.ts`）は素の TUI のために残した。

### 2026-09-16 タスク一覧を表のモーダルで見渡せるようにした（T-149）

T-129 の「畳んで開く」への修正依頼。区画は全件をスクロールで並べるだけに戻し、見出しの
「一覧を見る」から `<dialog>` の表（ID / status / 難易度 / 依存 / 着手 / 要約）を開く。
1400x900 で一度に見えるのは 27件中 19件。着手可否は依存と status から計算する
（`status.py` と同じ規則を `src/protocol/task-summary.ts` に写した）。

### 2026-09-16 サイドバーのタスク一覧を畳んで開く形にした（T-129）

見出しを押すと区画の高さの取り合いが入れ替わる形にし、畳んだ既定では `done` を除いた先頭3件と
「ほか N 件」だけを出す。1400x900 で見えるタスクは 2件 → 畳んで3件・開いて3件+内側スクロールになった。
**中身なりで止まる側を縮まない（`flex-shrink: 0`）＋上限で止める形にした**のが要で、縮む余地を
残すと中身の高い区画が相手を高さ 0 まで潰す。

### 2026-09-16 開発中に src/ui/ の保存で画面が入れ替わるようにした（T-146）

`node:fs` の `watch` で `src/ui/` を見張り、既存の `bun build` で組み立て直して、新設の
`refresh` フレームで開いているタブへ押す。**CSS だけの変更はページを読み込み直さない**ので、
入力欄の書きかけもターンの選択も残る。**Vite を足していない**（`package.json` の差分は空）。

**見張るのは `TSUKUMO_WATCH_UI=1` のときだけ。** `tsukumo` は `bun link` で普段使いと開発が
同じ経路なので、常時入れると仕事中の保存でページが読み込み直されうる。

**救えるのは `src/ui/` だけ**（直近30コミットの実測で全体の52%）。`src/core/` と
`src/protocol/` は上げ直しが要る——protocol は畳み込みがサーバ側でも回っていて、片側だけ
新しくすると食い違った状態が動くため、あえて見張らない。

### 2026-09-16 プロジェクト外への依存を洗い出して1枚の表にした（T-122）

`docs/research/external-dependency.md` を新規作成。19系統を3表（本体が要る／外にあるのが自然／開発時だけ）に分け、外せない7・外せる9・要検討3で評価した。下調べの6系統のうち3件は実測で訂正している（同梱バイナリを起こしている点、出力スタイルは読むのでなく上書きする点、`bun` が自分自身を子に渡す点）。これで Bun / Node の判断（T-145）の材料が揃った。

### 2026-09-16 箱の方針（当面 Orca、載せ替えられる形を保つ）を正典に落とした（T-046）

`docs/requirements.md` 7章の「Electron などのスタンドアロンに移るか」を条件付きの保留に書き換え（再検討の条件＝Orca のタブでは埋められない使い勝手の不足が実際に出たとき）、`docs/architecture.md`「ホスト依存の操作は1つのポートにまとめる」節を「2つ目のアダプタを書くまでポートは `showView` 1つに保つ」に改めた。コードは変えていない。

### 2026-09-16 層の検査を広げ、旧パスと経路名の再掲を掃除した（T-142）

`docs/research/architecture-proposal.md` の移行の段1。`src/protocol/session-socket.ts` を作って
`"/ws"` とトークンのクエリ名を1箇所に集め、`core/server.ts` と `ui/socket.ts` の再掲を消した。
`test/architecture.test.ts` の `it` は 4→8（SDK・`node:child_process`・`process.env`・経路名の限定）。
消えた `src/presentation` を指すコメント15件も掃除した。

`ws` の限定・`core` の `node:` 全面禁止・`Layer` への `adapter` 追加は、**段2（T-143）で
書き換わる前提**なので足していない。既存の「`protocol` は `document` に触らない」はテスト名だけで
実装が無かったので、この機会に実装を入れた。

### 2026-09-16 orca を呼ぶのが1ファイルだけであることをテストで固定した（T-140）

`docs/architecture.md` 原則3 の「`orca` コマンドを呼ぶのは `src/core/orca-host.ts` だけ」は
コメントにしかなく、委譲先が破っても気づけなかった。`test/architecture.test.ts` に検査を1つ足し、
`src/` の中で `orca-host.ts` 以外にコマンド名の文字列リテラルが現れたら落ちるようにした。

わざと `src/core/host.ts` に `"orca"` を書くと落ち、戻すと通ることを確認済み。`src/` のコードは
変えていない（`orca-host.ts` の冒頭コメントに「この境界はテストが落とす」の1文だけ足した）。
移行の段1（T-142）は、この隣に SDK・`process.env`・経路名の限定を並べる形になる。

### 2026-09-16 details が機能しないのを直した（T-126）

レポートを塊に割る `splitReportBlocks` が **HTML ブロックの中の空行でも割っていた**ため、
`<details>` の中身が `<details>` の外の段落になって常に見えていた。閉じタグが必須の囲み要素
だけを深さで数え、閉じるまで割らないようにした（閉じタグを省ける `p` / `li` / `td` などは
数えない——待ち続けると以降ずっと割れなくなる）。

実機で `<details>` が**畳まれた状態で出て、クリックで開閉する**ことと、表・段落が中に
収まることを目視した。`<div class=cols>` の入れ子も割れない。

**規約の側も直した。** 「塊の途中に空行を入れない」は CommonMark の規則と衝突していた
（HTML の中で Markdown を解釈させるには空行が要る）ので、逆に「空行を空ける」に改めた。

### 2026-09-16 パックを切り替えても立ち絵が変わらないのを直した（T-132）

立ち絵の URL に `?pack=<name>` を足した（`characterAssetPath` 1箇所で組み立てる）。アスナと
tsukumo がどちらも `default.png` なので URL が一致し、`<img src>` が書き換わらず再取得が
起きていなかった。配る側はこの値を見ない（`server.ts` が `?` 以降を落とす）ので、allowlist は
触っていない。

実機（Chrome・台本の駆動）で local → tsukumo → local → つくもの精霊 → tsukumo と行き来し、
`<img src>` と naturalSize（246x203 ↔ 830x1254）が毎回入れ替わること、3パックの絵柄が
実際に差し替わることを画面写真で目視した。精霊の SVG インライン経路も維持されている。

この目視で退行を1件見つけて直した: `classifyPortraitFile` が `?pack=` 付きの URL で拡張子を
見失い、立ち絵が丸ごと消えていた（`fileExtension` が `?` 以降を落とすようにした）。

### 2026-09-15 /clear のあとに前の会話が残るのを直した（T-131）

**T-131 完了。** `/clear` のとき SDK が **`conversation_reset`** を流すことを実測で確かめ
（`new_conversation_id` 付き、直後に**新しい `session_id`** の `system/init`）、これを
`conversation-cleared` イベントとして拾う形にした。**依頼の文面が `/clear` かどうかは見ない**ので、
`/clear extra` や別名でも漏れない。

**`/compact` では `conversation_reset` が流れない**（同じ実測で `system/status` と**同じ
`session_id`** の `init` だけだった）ので、要約では何もリセットされない。論点だった「同じ扱いに
しないことを明示するか」は、**イベントが別なので自然に分かれる**という形で決着した。

**消すのは吹き出しとメインビューが読む値だけ**（`speeches` / `speechExpression` /
`speechCalledInTurn` / `records` / `partialUtterance`）。キャラクター定義・セッション情報・
答え待ちの列は残す（`pending` の正典は core の待ち行列なので、状態側で空にすると実際の待ちと
食い違う）。**吹き出しは空にしてよい**と決まった — 空でもキャラクターは消えず、起動直後と同じ
「（まだ発話がありません）」が出て立ち絵は残るため。普通のターンの `speeches.slice(-1)`
（`docs/requirements.md` 4.2 の 2026-09-12 決定）はそのままで、空にするのは `/clear` だけ。

**実機で確かめた**（127.0.0.1:7399、続きから起こしたセッション）: `/clear` のあと吹き出しが
4件 → 1件のプレースホルダ、ターンのタブが 5 → 0、立ち絵とセッション情報は残った。
**サイドバーの「いま何をしているか」は消していない**（決めた範囲の外）。

### 2026-09-15 画面から作るパックの置き場と受け取り方を決めた（T-123）

**T-123 完了（決めるところまで。実装は後続タスク）。** 書き込み先は
`~/.tsukumo/characters/<name>/` の1箇所、探索は**同梱 → ホーム → 起動先 `characters/local`**
の3箇所で同名は後ろが勝つ。画像は **data URL を既存の WebSocket のコマンドに載せて**受け取り、
上限は 1枚 2 MiB / パック 8 枚 / `maxPayload` 4 MiB。`docs/design.md` に 7.1 を新設し、
`docs/requirements.md` 4.4 の置き場所の表に1行足した。

**起動先の `characters/local` の1つ固定は残した。** ホームを間に挟むだけなので、`.gitignore` も
`characters/README.md` の説明も変えずに済み、「同名は起動先が勝つ」という既存の規則も保てる。

**却下**: `<cwd>/characters/<name>/`（`.gitignore` を広げる必要があり、リポジトリ内で起動すると
同梱パックと取り違える）、同梱側（`bun link` で他プロジェクトから作ったパックが git の差分になる）、
`multipart/form-data` の POST（node:http にパーサーが無く外部依存が要る）、生バイトの POST
（トークンと `Origin` の照合をもう一組書くことになる）。

### 2026-09-15 持つやり取りを3→5に増やした（T-113）

**T-113 完了。** `MAX_MAIN_VIEW_TURNS` を 3 → 5 に戻した（**もとは5で、2026-09-10 に
ユーザーの指定で3へ下げた値**）。定数1つが正典なので、タブのラベルは `${index}つ前` が
`4つ前` まで自動で伸びる。

**論点だった「5枚で折り返すか」は折り返さないと分かった**（実測: タブ帯の高さ 39px、
1400px と 760px の両方で1行）。レポートの先頭も押し下がっていない（依頼の見出しの上端 68px、
領域の上端 16px）。

**テストのハードコードを2箇所直した。** 絞りのテストは8件流していたのに期待値が 3 固定で、
`turn-speech.test.ts` にも「直近3ターン」前提の `[2,3,4]` が残っていた。どちらも
`MAX_MAIN_VIEW_TURNS` から組み立てる形にしたので、次に増減しても落ちない。

### 2026-09-15 レポートの規約に冗長さを止める条項を入れた（T-119）

**「内容を削って短くするのではなく、構造を付けて読む順番を作る」（2026-09-13 決定）を撤回した。**
`REPORT_NOTATION_PROMPT` を「**まず量を絞り、残ったものに構造を付ける**」に書き換え、冗長さを
止める条項を6つ足した（前置きと締め / 二度言わない / 根拠は結論を支える分だけ / 既知のことを
書き直さない / ぼかさない / 効能書き）。**量の上限は数で縛らない**（ユーザーの選択。行数で縛ると
長い調査の報告まで同じ長さに押し込められるため）。参考資料（`i-have-adhd` の SKILL.md）から
**採らなかった4条項**とその理由も `docs/requirements.md` 4.2 に書いた（5項目上限 / 現在位置の
再述 / 最初の行を行動に / 推測表現の全面禁止）。節数は 26 → 26 で不変。
**効くのは書く前のモデルだけ**で、本文が書かれたあとに tsukumo が削る経路は無い（2.2 の線引きは
保つ）。**定数なので、規約の変更が効くのは次にプロセスを起こしたときから。**
**実物で確認**（ポート 7406 に `TSUKUMO_NEW_SESSION=1` で新しいプロセスを起こし、本物の駆動で
2ターン。常駐の 7327 は未使用）: 本文は「1〜2文の結論＋表1つ」まで縮み、見出し 0 個・締め・
効能書きは出なかった。ユーザーの判断は「試してみるから一旦OK」。
`bun run check` は 452 pass / 0 fail（文面だけの変更なので件数は変わらない）。

### 2026-09-15 領域の横スクロールを閉じた（T-118）

**`.layout-region` に `overflow-x: hidden` を足し、4領域とも横に転がらないようにした**
（ユーザーの選択。`overflow-y: auto` だけを書くと、片方の軸が `visible` でなくなったときに
もう片方の `visible` も `auto` に計算されるので、書いていない `overflow-x` が `auto` になっていた）。
**タスク本文が根拠にしていた症状2つは、着手時点で T-117 が先に消していた**（箱の中身の 7px の
はみ出しと、右端が切れる自由入力の「送る」ボタン。前者は選択肢を1行にして `min-width: 0` を
入れたこと、後者はボタンごと撤去したことによる）。残っていたのは `overflow-x` が `auto` に
計算される件だけで、直したのは CSS 1ファイル。
**実機の目視**（偽の駆動・ポート 7405。常駐の 7327 は未使用）: 質問が出ている状態で4領域とも
`overflowX === "hidden"` で `scrollLeft = 999` を入れても転がらない。1400 / 760 / 400px の
いずれもページ全体は横スクロールしない。**T-112 の `.table-scroll` は働いたまま**で、表に10列
足すと容器の中だけが転がり（1002/950）、領域は 1005/1005 のまま。1400px と 400px の目視で
引用・表・badge・カード・mermaid の図が切られずに出ることも確認した。
`bun run check` は 452 pass / 0 fail（テストは増えていない。絵は目視で守る方針）。

### 2026-09-15 質問を1問1答にした（T-117）

**`AskUserQuestion` の質問を1問ずつ出す形にした。** `QuestionAsk` が全問を縦に積んでいたのをやめ、
`index` の state で `questions[index]` を1枚だけ描く。単一選択は選んだ瞬間に次の質問へ進み、
最後の1問を答えた時点で**全問ぶんの `labels` を1回だけ dispatch する**（`labels[i]` は
`questions[i]` への答え1つ、という契約は変えていない）。進捗（`2問中1問目`）を出し、その行に
「戻る」を同居させた（独立した行にすると、その高さのぶん選択肢が見えなくなる）。
**ユーザーが挙げた2件の不具合も直した**: 自由入力に打っても直前に押した選択肢が光ったままだった件
（単一選択では「選択肢」と「自由入力」を排他にした）と、自由入力の「送る」が押しても何も
起きなかった件（`needsSubmitButton` のとき即 return していた。ボタンごと撤去し Enter で進む形に
統一）。**高さの逃げ道は「説明を畳む」＋「箱の中だけスクロール」**（ユーザーの選択）。選択肢を
ラベル＋説明の1行にし（`/` 補完の `.dispatch-suggestion-item` と同じ形）、`.pending-answer` を
`max-height: 64%`、`.question-card` だけ `overflow-y: auto` にした。**`.dispatch-pending-glow` の
`inset: -1px` が領域の外へ 1px はみ出していた**ので内向きの影に変えた（T-118 が見ている
`overflow-x` そのものは触っていない）。
**実機の目視**（`TSUKUMO_DRIVER=fake`・ポート 7403・1400x900・既定の比率。常駐の 7327 は未使用）:
`.layout-dispatch` は scrollH 335 = clientH 335 / scrollW 669 = clientW 669（着手前は 464 > 335）。
質問2件でも `.question-card` は1枚、「戻る」で答えを保ったまま1問目へ戻る。
`bun run check` は 452 pass / 0 fail（443→452）。

### 2026-09-15 失敗したツールの引数と出力をサイドバーへ寄せた（T-114）

**失敗したツールだけが引数と出力をレポートに出していたのをやめた。** `toolVisibility()` から
`result.isError` の分岐を落とし、失敗も他のツールと同じ分類（ファイルを変えた操作 /
サブエージェントの起動 / 見せない）にした。`Bash` が非0で終わってもコマンドと stdout/stderr が
レポートに流れ込まない。**中身を読む経路はサイドバーへ移した**: `ToolActivity` に
`failureOutput` を足し、「いま何をしているか」の行に「失敗」の文字（色だけに頼らない）と
`<details>` を付けて、開くと出力→引数の順に読める。ユーザーの選択は **(1) 失敗はサイドバーへ、
(2) 成功側の2種はレポートに残す、(3) 規約にも足す**。`src/core/report-notation.ts` に
「過程と生の出力はレポートに書かない」を1行足し、モデルが自分で貼る経路も塞いだ。
**実機の目視**（`TSUKUMO_DRIVER=fake`・ポート 7399・常駐の 7327 は触っていない）で、レポート側の
`pre.tool-input` / `pre.tool-result` / `.tool-block-failed` が 0 件、サイドバーの
`details.activity-failure` が開いて中身が読めること（ブロックは `scrollH` 268 > `clientH` 103 で
スクロールして届く）を確認した。`bun run check` は 443 pass / 0 fail（436→443）。
**委譲した opus のサブエージェントはセッション上限（429）で途中終了した**ので、残り（テストの
更新・CSS・ドキュメント・規約・台本・目視）はメインセッションで仕上げた。

### 2026-09-15 起動時のキャラクターが既定に戻る報告を調べた（実装は正常・T-110）

**T-110 の不具合報告は実装の不具合ではなかった（2026-09-15）。** 「起動したらつくもの精霊になる」
という指摘の原因は、**旧方針の hook（2026-09-12 に撤去）が使っていた `~/.tsukumo/state.json` が
残っていたこと**。中身が当時の形（`{"event": …}`）なので `readRememberedCharacter` が `undefined` を
返し、仕様どおり既定の `tsukumo-spirit` へ落ちていた。`TSUKUMO_CHARACTER` は設定されておらず、
**T-116 とは無関係**。画面で切り替えた時点で正しい形に上書きされたので自己解消済みで、
`cli.ts` と同じ経路を呼んで初期パックが `tsukumo` になることを確認した（サーバは起こしていない）。
旧方針の残骸（`targets/` 4件・`transcript-path`・`state.json.tmp.*`）を消し、置き場の再利用という
罠を `docs/architecture.md`「既知の制約・注意点」に1件足した。コードは変更していない。

`develop/progress.md` の「完了したこと」から、過去セッション分の小節をここへ移す
（基準と手順は `docs/workflow.md`「肥大化したときのアーカイブ」が正典）。

`### YYYY-MM-DD 〜` の形の小節を、**当時の記述のまま**移す。後から書き換えない。
「次にやること」「未解決」「注意」は移さない。

セッション開始時に読む必要はない。**通読しない。**

### 2026-09-14 セリフをターンに紐づけて遡れるようにした（T-111）

**T-111 完了。タブを切り替えると吹き出しと表情も一緒に遡る。** セリフを `SessionRecord` に
`{ kind: "speech", text, expression }` として積み、`request` を境目にターンごとへ分ける純粋関数
（`src/protocol/turn-speech.ts` の `turnSpeeches`）から引く。**選んでいるターンは
`src/ui/turn-selection.tsx` の Context** に上げた（`ui/` の直下は共有部分なので、メインビューと
キャラビューの両方から読めて領域どうしの import は増えない）。

**ユーザーの要望は「表情と動きも戻す」だった。表情は戻した**（そのターンの最後のセリフの表情。
ツール実行中の「作業中」の上書きは今回のターンだけに効かせる）。**動きは T-120 に分けた** —
4つの動きのうち3つは「いま起きていること」に紐づく時間相対のアニメーションで、過去のターンで
再生するには `docs/requirements.md` 4.3 の「**動くのは利用者の注意が空いているときだけ**」の
見直しが要る。技術的には可能（そのターンの終わり方は記録から引ける）なので、やらない判断では
なく別の1コミットにした。

**ターンの通し番号が噛み合うかが最大の落とし穴だった。** `mainViewEntries` は `speech` の記録を
落とす（セリフをレポートに出さないため）ので、**「最初の依頼より前のまとまり」が
`groupIntoTurns` 側にできるのはセリフ以外の記録が先にあるときだけ**になる。`turnSpeeches` は
同じ条件で数え始めることで番号を揃えていて、そこをテストで固定してある。

**委譲の実装に1つ漏れがあり、直した。** 行頭マーカーの補助（`withMarkerFallback`。規約が
守られず本文にセリフが紛れたときの受け皿）で拾ったセリフは記録に積まれておらず、**その経路の
セリフだけ過去のターンで消えていた**（今のターンは `speeches` から出るので気づきにくい）。

**実機で確認した**（偽の駆動、`HOME` を一時ディレクトリに差し替え、ポート 7399）。タブを
今回→1つ前→2つ前と切り替えると吹き出しが **1→3→1 件**に変わり、表情のラベルも
**どや顔→どや顔→あわあわ**と切り替わった。**最新の吹き出しの下端は3タブすべて 760px で一定**
（`--balloon-bottom-gap` の規則が過去のターンでも保たれている）。目視の材料として
`test/fixture/fake-session.json` の3場面目にセリフを2件足してある（架空の文面）。

### 2026-09-14 前回のキャラクターを覚えて起動時に使う（T-110）

**T-110 完了。切り替えたキャラクターで次も立ち上がる。** 保存先は `~/.tsukumo/state.json` で、
書くのは**パックの名前だけ**（`src/core/remembered-character.ts`。ホームのファイルに触るのは
このモジュールだけで、`homedir()` は既定値を組み立てる関数の中でしか呼ばない）。

**`localStorage` 案はユーザーが却下した。** 選択はブラウザで起きるが使うのはサーバの起動時なので、
`localStorage` に持つと「つくもの精霊で立ち上がってから、タブが繋がった時点で起こし直す」形に
なり、**一瞬だけ別のキャラクターが見える**。ファイルなら起動の時点で読めるので最初からそのキャラで
立ち上がる。

**覚えるのは画面から選んだときだけにした**（委譲の実装は起動時にも書いていたので直した）。
起動時にも書くと、`TSUKUMO_CHARACTER` で**その回だけ**指定したパックが次の起動の初期値として
残ってしまう。優先順位は **`TSUKUMO_CHARACTER` > 覚えた値 > 同梱の既定**。

**`docs/design.md` 13.6 は「第3の扱い」にした。** 「セッション限り」から「ずっと」へ移すのでは
なく、**選ぶ操作自体はセッション限り（起こし直す）だが、次の起動の初期値としては覚える**。
置き場所（サイドバー「セッション情報」）は変えていない。

**`TSUKUMO_CHARACTER` を調べたら用途が1つしか残っていなかった。** 値は**パスとして**解かれる
（`resolveBundledDir` が `resolve(cwd, ...)`。`docs/design.md` 5章の表が言う「パックの名前」では
**指せない**）。切り替えの一覧は同梱の `characters/` と起動先の `characters/local/` を常に返すので、
アスナを選ぶのに環境変数は要らない。**T-110 の後に残るのは「`characters/` の外のパックを初期値に
する」1点だけ**で、撤去の可否は T-116 に起こした。

**`~/.tsukumo/` には旧実装の置き土産が残っている**（`state.json` は hook が書いていた
`{"event":"PreToolUse",...}`、ほかに `state.json.tmp.74230` / `targets/` / `transcript-path`）。
**いまのコードはどれも読まない**ので、`state.json` は最初の切り替えで上書きされる
（形が違うので `readRememberedCharacter` は `undefined` を返し、既定に落ちる）。片付けるかは
ユーザーの判断待ち。

### 2026-09-14 レポートの行長の上限を撤去した（T-112）

**T-112 完了。表がつぶれなくなった。** 原因は `.detail-block { max-width: 36.36rem }` が
**子孫すべて**に効くことで、`width: 100%` の表も 581.7px に閉じ込められていた。**ユーザーは
「表・コード・図だけ上限から外し、本文は 42 全角のまま保つ」案ではなく「上限そのものをやめる」
方を選んだ**（1400px の窓で本文の行長が約 67 全角に戻ることを示したうえでの選択）。そのため
`docs/design.md` 13.1 原則4「読む面は行長を測る」と 13.3「行長は 42 全角で切る」は**例外を
足すのではなく撤回**し、理由と日付を残した。

**器だけでは足りず、セルの下限も要った。** 表を `div.table-scroll`（`overflow-x: auto`）で
包む実装だけでは、`width: 100%` の表は列の数だけ縮んで器に収まってしまい、**横スクロールが
一度も働かない**（実測: 12列で最小セル 41px、器の `scrollWidth == clientWidth`）。
`min-width: 4em` を th / td に足して「どのセルも最低4全角は1行で出す」を決め、入りきらない
ぶんだけ器が転がる形にした。**`em` はセル自身の字の大きさ基準で全角の字はちょうど 1em** なので、
px の実測値を持たずに「4全角」と書ける（撤去した 36.36rem のような環境依存の数が増えない）。

器を `src/ui/report/markdown.tsx` の `components` に置いたので、**Markdown の表とレポートが
直接書いた `<table>` の両方に効く**（`rehype-raw` が同じ hast の木に入れるため）。
`sanitize-schema.ts` は触っていない（器は React 側で作られ、サニタイズを通らない）。

### 2026-09-14 キャラクターごとにセッションを持つ（T-109）

**T-109 完了。切り替えても会話が消えなくなった。**

**スパイクで分かったことが、そのまま設計の土台になった。** `resume` したセッションでは
`systemPrompt` の append が**差し替わらない**（新しい人格の絶対命令すら効かず、前の口調が残る）。
最初はこれを「会話を継ぐ案が潰れた」と読んだが、**ユーザーが「キャラクターごとにセッションを
持つ」形を提案してくれて、欠点が利点に反転した** — そのパックのセッションを `resume` すれば、
**そのパックの人格と会話がまとめて戻る**。

実装は思ったより小さかった。`selectSessionToResume(sessions, tag)` が**印を引数で受け取る**形に
なっていたので、印を `tsukumo:<パック名>` にして、起動時も切り替え時も「これから起こすパックの
印」で探すだけ。画面の履歴は移行の段9の `replayRestoredSession` がそのまま使えた。
**会話を tsukumo 側に保存していない**（4.8 の決定は変えていない。claude の transcript を読むだけ）。

ユーザーのもう1つの案「会話の内容を保存してロード」は採らなかった。4.8 が
「復元のために会話を保存しない。キャッシュ・スナップショットは作らない」と決めており、
これは「会話内容の扱い」の「別の場所に複製しない」を満たすための決定だから。
**保存しなくても同じ結果が出る**ので、規約を曲げる必要が無かった。

実機で両方向を確認した。A で1ターン → B へ切替で画面がリセットされ B の口調になる →
**A へ戻ると前の会話が画面に戻り、claude も覚えていて A の口調** → **B へ戻っても B の会話と
口調がそのまま**。`docs/design.md` 7章の「会話は続かない」は、**なぜ変えたのか**（段9で材料が
増え、スパイクで `resume` が人格を保つと分かったこと）を添えて書き直した。

**申し送り**: `startDriver` の戻りが `Promise<SessionDriver>` になった（切り替え先の探索が
非同期のため）。`persona.md` を編集しても既にあるセッションには効かない（`resume` が最初の
append を保つため、新しく起こしたセッションから効く）。ターンを1つも終えずに切り替えると、
そのパックのセッションは次回見つからず新規になる（印はターン終了の3秒後に付く）。

### 2026-09-14 移行の段8（キャラクターパックと切り替え）— これで移行は全段完了

**T-101 完了。描く層をブラウザ側へ移す移行は、段1から段9まですべて終わった。**

**最大の論点だった人格の二重適用は、第一候補の方式で解けた**（スパイクで実測）。
`applyFlagSettings({ outputStyle: "default" })` を呼ぶと `init` の `output_style` が `Asuna` から
`default` になり、**グローバルの出力スタイルが消えてパック側の `persona.md` だけが残る**
（そのままだと応答が両方の人格を名乗った）。**セッション限りの層にしか触らず設定ファイルを
書き換えない**ので、`settingSources` を外す案（orca の hooks まで外れる）は不要になった。
結果は `docs/requirements.md` 4.4 に書き、7章の未決事項から該当行を外した（残り2件）。

`expressions`（表情名 → ラベル）・`speechMarker`・`persona.md` を定義ファイル側へ移し、
コード上の `expressionLabel` と `DEFAULT_SPEECH_MARKER` を消した（**原則4「キャラクターの中身を
コードに書かない」がこれで完全に効いた**）。`characters/tsukumo-spirit/persona.md` は
`~/.claude/output-styles/asuna.md`（159行）から人格と `speak` の使い方だけを写した125行で、
**レポートの記法は写していない**（`src/core/report-notation.ts` が正典なので二重に持たない）。

**実機で確かめた**（2体目は `characters/local/` に置いて確認後に削除。コミットしていない）:
`<select>` で切り替えると立ち絵・ラベル・`--accent` が変わり、吹き出し・立ち絵・メインビューが
一度消え、**次のターンの口調が完全に入れ替わった**（「ご主人様」と感嘆符2個 → 「あなた様」と
感嘆符0）。切り替え前の口調は混ざっていない。`~/.claude/settings.json` と `asuna.md` の
`shasum` は前後で一致している。

**ユーザーの指摘で3点直した**（同日）。

1. **選択肢にアスナが無かった。** 同梱パックの `name` は「つくもの精霊」なのに `persona.md` が
   アスナだった。段8より前は立ち絵と人格が別の軸だったので、ずれが見えていなかった
2. **公開リポジトリに既存作品のキャラクターの人格記述をコミットしていた**（私の見落とし）。
   2.2 が素材の同梱を外している理由は「権利の扱いをこのリポジトリに持ち込まないため」で、
   画像でなくても同じ問題になる。→ **同梱は自作の2体**（つくもの精霊・ひだまり）にし、
   アスナは `characters/local/` へ移した。**未 push だったのでユーザーの承認を得て HEAD を
   amend し、履歴からも消した**（全履歴の grep で0件）。2.2 に「人格の記述もここに数える」と明記
3. **レポートまでキャラの口調になっていた。** → **レポートは中立の技術文**と決め、
   キャラクターの口調は `speak` のセリフが担う形にした。書く場所は `src/core/report-notation.ts` で、
   `persona.md` の側には書かない（どのパックでも本文の読みやすさが変わらないようにするため）。
   `docs/requirements.md` 4.2 にも残した

**気づいた制約**: `characters/local/` は**それ自体が1つのパックのディレクトリ**（`local/character.json`）
なので、**利用者が持てるローカルのパックは1つだけ**になる。いまはアスナがその1つを使っている。
`docs/requirements.md` 4.4 の表の書き方どおりではあるが、2体目以降を置きたくなったら足りなくなる。

**あわせて直したもの**: コード・テストのコメントに積み上がっていたタスク番号16件を規約どおり
外した。`CLAUDE.md` の「よく使うコマンド」の素の `bun test` を `bun run test` に直した
（素のほうは `mock.module` が漏れて19件落ちる。実際に子セッションがこの罠に踏み込んだ）。

### 2026-09-13 立ち絵が動くようになった（T-107）

**T-107 完了。** 決めておいた4つの動き（呼吸 / 待っている間の移動 / 完了の反応 / 失敗でびくっ）を
実装した。判定は `src/protocol/portrait-motion.ts` の `resolvePortraitMotion` に純粋関数として
置き、`"reading" | "waiting" | "success" | "failure"` を返す。**動くのは利用者の注意が空いている
ときだけ**という原則がそのまま優先順位になっている（失敗直後 > 完了直後 > 進行中なら待機 > 読書）。
材料として `SessionState` に `lastToolFailureAt` を足した。

**立ち絵は1枚の矩形として扱う**という決定どおり、`transform` と `@keyframes` だけで作ってあり、
素材の中身には触っていない。だから SVG でも PNG でも同じだけ動く。`<canvas>` もアニメーションの
ライブラリも足していない。

実測（偽の駆動・CDP）: 呼吸は `y` が 0 → −3.2px を周期 3.2 秒で往復、待っている間は `x` が
最大 8px まで滑らかに移動、完了直後は跳ねて着地し自動で読書へ戻る、失敗は 3 度傾いて −4.8px 引く。
**領域からのはみ出しは歩行中も含めて最大 0px。** `prefers-reduced-motion: reduce` で止まり、
`tsukumo-portrait-fixed` を `true` にすると `data-motion` 自体が付かない。
**速さと大きさはユーザーが Orca のタブで見て「このままで OK」と了解した。**

調整つまみ9つは `.character-region` の1箇所に集約してある（後から詰めたくなったらここだけ触る）。
サブエージェントが実測中にバグを1件見つけて直している（失敗と完了の窓が重なると「完了」の反応が
固まったまま戻らなかった。窓が無くなるまでタイマーを立て直す形に変更）。

### 2026-09-13 移行の段9（セッションの復元）

**T-078 完了。これで移行の段1〜段9のうち、残るのは段8（キャラクターパック）だけになった。**
`docs/requirements.md` 4.8 の決定どおり、**tsukumo 側に会話を保存せず** claude 自身の transcript を
読み直す形で復元する。鍵は `cwd` ＋ tsukumo が付けた印。

`src/core/session-restore.ts` に純粋関数を2つ切り出した（復元するセッションを選ぶ／transcript の
メッセージ列を `SessionEvent[]` に変える）。SDK を呼ぶのは `session-driver.ts` のままで、
**SDK の import は1ファイル**という決定を崩していない。「続きから」の表示は
`session-restored` → `SessionState.restored` → サイドバーの印。逃げ道は `TSUKUMO_NEW_SESSION=1`。

**実機で確かめた**（スクラッチの作業ディレクトリ。このリポジトリのセッション一覧は汚していない）:
1往復して落とし、起こし直すと**タブが2枚に戻り、吹き出しに前のセリフが戻り**、サイドバーに
「セッション 続きから」が出た。渡しておいた合言葉を聞き直すと答えられた（`resume` が効いている）。
`TSUKUMO_NEW_SESSION=1` では履歴が出ないことも確認。

**設計とのズレが2件あり、どちらも正典に書き戻してある。**

1. **印を付ける契機を変えた。** 4.8 は「IDが届いたら付ける」だったが、**そこで付けた印は消える**
   （本体もターンの終わりにセッションの要約を書いていて、その書き込みに印が含まれないため）。
   スパイクの実測に基づき、**ターンが終わるたびに3秒おいて付け直す**形にした。副作用として、
   ターンを1つも終えずに落とすと次回は新規から始まる（4.8 の「失敗時は新規」の範囲内）
2. **画面からの `new-session` は入れていない。** セッションを起こし直す仕組みは段8が作るので、
   そちらと一緒に入れるのが筋と判断した

あわせて `docs/requirements.md` 2.2 の「セッションの再開はしない」（2026-09-11）を、
**4.8 で覆ったと明記したうえで**「セッションを選ばせる画面は作らない」に差し替えた。
受け入れ時に、`state-warn` を「続きから」にも使っていることを `docs/design.md` 13.2 の表へ
書き足した（実装だけが知っている使い方にしないため）。

### 2026-09-13 「見た目」の引き出しを作る（T-108）

**T-108 完了。** `docs/design.md` 13.6 の決定どおり、ずっと残る設定（地・領域・字の色／立ち絵の
固定／領域の比率のリセット）を `<dialog>` の引き出しにまとめ、**`.layout-reset` のボタンを開く口と
置き換えた**（常設のボタンは「送信」「見た目」の2つで着手前と同数＝差し引きゼロ）。

色の検証は `src/ui/appearance/appearance-color.ts` の `changeAppearanceColor` **1箇所に封じ込めて**
ある。**JS 側に16進の既定を持たず** `getComputedStyle` で `:root` から読むので、「`theme.css` 以外に
16進を書かない」規約が保たれる。コントラストの下限は WCAG AA の 4.5:1。立ち絵の固定の鍵は
`tsukumo-portrait-fixed`（JSON の真偽値、既定 `false` ＝可動）で、これは立ち絵の動きの実装が読む。

**受け入れ時に不具合を2件見つけて直した。どちらも数値の完了条件では出ない類のもの。**

1. `narrow-screen.css` が旧 `.layout-reset` の規則を**名前だけ変えて引き継いで**いて、760px 未満で
   開く口ごと隠れ、**狭い窓から設定に一切たどり着けなくなっていた**。旧ボタンは比率のリセットしか
   持たなかったので隠してよかったが、引き出しは色と立ち絵の固定も持つ
2. コントラストで弾いたときに `ground` と `ink` の上書きを**両方消していた**。地を決めたあとに字で
   読めない色を試すと、**地の設定まで失われる**。「受け取らない」は「それまで受け取った値を捨てる」
   ではないので、拒否は新しい1色を受け取らないだけにした。**修正前のコードで落ちる回帰テスト**を
   足してある（既存のテストは `ground: undefined` から始まっていて、この差を見分けられなかった）

### 2026-09-13 増えていく設定の置き場所を決める（T-106）

**T-106 完了。コードは1行も変えていない**（実装は T-108）。画面から変えられるものを数えたら
**8つ**あり、**「セッション情報 vs 設定」ではなく寿命で割れる**ことが分かったのが決め手だった。

- **今回だけのもの**（モデル・許可モード・キャラクターの切り替え）— セッションを起こし直すと
  消える。いま何が動いているかを見る場所＝サイドバーに属する
- **ずっとのもの**（色3つ・立ち絵の固定・領域の比率）— 一度決めたら変えない。
  **読んでいる間に見えている必要がない**

そこで原則を**「今回のことはサイドバーに、ずっとのことは普段しまっておく」**に置き、
**寿命は置き場所そのもので表す**（「このセッションだけ」のような添え書きは出さない。ユーザーの選択）。

**引き出しの開く口は、いま「既定の比率に戻す」が常設されている画面の右下と置き換える。**
比率のリセットは引き出しの中へ移るので、**常設の要素は差し引きゼロ**になり、13.1 原則2
（枠を持たないのはキャラクターだけ）を壊さない。サイドバーは3区画のまま。

`docs/design.md` に 13.6「設定の置き場所」を足し、**13.5 で保留していた配色の受け渡しの経路も
ここで解いた**（引き出しから変えて `localStorage`）。6.1 の部品の木に `<Appearance>` を足した。
節数 49 → 50。実装は **T-108** として登録し、**T-107 の依存を T-106 から T-108 に付け替えた**
（立ち絵の固定の切り替えが引き出しの中に入るため）。

### 2026-09-13 立ち絵の動きに何を作るかを決める（T-105）

**T-105 完了。コードは1行も変えていない**（実装は T-107）。`docs/requirements.md` 7章の
未決事項「立ち絵の動きに何を作るか（遷移・まばたき・登場）」を閉じ、表から行を外した。

**決定の芯は「立ち絵を1枚の矩形として扱う」。** きっかけはユーザーの問い
「キャラは png でしか用意できないこともあるかも? 形式含めて考えたい」で、調べると
**既定のパックの SVG も `id="body"` しか持たず、目に名前が無い**ことが分かった。
`docs/requirements.md` 2.2 が「素材は利用者が自分で用意する」と決めている以上、素材がどう
作られているかは当てにできない。そこで tsukumo が動かすのは**位置・大きさ・傾き・上下・
不透明度**だけと割り切り、**その見返りに SVG でも PNG でも GIF でも同じだけ動く**形にした
（パック作者に何も要求しない）。

**もう1つの芯は「キャラの自由は、利用者の注意が空いているときに限って与える」。**
読む画面なので動くものは目を引く＝害だが、「誰かが居る」には動きが要る。この矛盾は場面で
解ける。判定の材料は `SessionState` に既にある（`turnInProgress` / `runningTools` /
`turnFinishedAt`）。

作るのは4つ（ユーザーが選択）: **呼吸**（常時のごく小さい上下）/ **待っている間の移動**
（ターン進行中に領域の中をゆっくり歩く）/ **完了の反応**（小さく跳ねる）/ **失敗でびくっ**。
「退屈のふるまい」は落とした（長いアイドル＝席を外している時間で誰も見ていない）。
動ける範囲は**キャラビューの中だけ**、利用者は**固定を選べる**。

**副産物が2つ。** (1) まばたきと表情のクロスフェードは**作らないという決定**になった
（素材の構造に依存するため）。(2) 制御できない GIF / APNG の穴が穴でなくなった
（もともと素材と同期しない設計なので、焼かれた動きが回っていても矛盾しない）。

### 2026-09-13 デザイン計画を CSS のトークンにして当てる（T-103）

**T-103 完了。** `docs/design.md` 13章のトークンを `src/ui/style/theme.css` に集約し、CSS 8ファイル
1,065 行の直書き（17色 114 箇所・`font-size` 7種類・`font-family` 2種類）を参照に置き換えた。
**`theme.css` 以外の16進と `font-family` はどちらも 0 件**になった。

見た目の変更も当てた。**キャラビューだけが枠も地も失い、`ground` の上に直接立つ**（大胆さの
1箇所）。`font-size` は4段、本文の行間 1.75、**レポートの行長は 42 全角で切る**
（`.detail-block` 581.75px ＝ 全角42文字の実測 581.72px で一致）。既定の `accent` は
`#f2b0a0`（立ち絵の頬と耳の色）で、`character.json` から差せる経路も作った
（`protocol/character.ts` → `character-changed` → `ui/app.tsx` が `--accent` に流す）。

実測は4つの幅（1400 / 900 / 600 / 400）すべてで横のはみ出し 0px、フォールバック値と
`split.ts` の `DEFAULT_SPLIT` が 3/3 一致、`:focus-visible` の輪が操作できる要素すべてに当たる。
Orca のタブ（7421）でユーザーの了解を得た。

**受け入れ時に3件直した。** (1) `a` が本文と同じ色で下線も無く、**リンクだと分からなくなっていた**
（色を取り上げるかわりに下線で区別する形にした。色だけに頼らないので、どのパックの `accent` が
来ても効く）。(2) `.sidebar-block h2` の `font-size` がトークン化のときに抜け落ち、**ブラウザ既定の
`h2`（親の 1.5em）に落ちて**サイドバーがメインビューより強く見える階層の逆転が起きていた。
(3) コードブロックだけ汎用の `monospace` に落ちていた（インラインのコードは `--font-mono`）。

当ててみて確定した解釈（`:hover` は色を出してよい側・見出しやツール名の文字は `ink` で色を持つのは
隣の罫・リンクは下線）は、**実装だけが知っている決定にしないよう `docs/design.md` 13.1 に書き戻した**。

### 2026-09-13 画面のデザイン計画を決める（T-102）

**T-102 完了。コードは1行も変えていない**（適用は別タスク）。`docs/design.md` に
「## 13. 画面のデザイン」を足した（節数 43 → 49）。

**ユーザーの指示で計画の芯が変わった。** 当初は「いまのほぼ黒＋青（スキルが名指しする既定値に
そのまま当てはまる）をどう替えるか」を聞いたが、返ってきたのは**「配色は一緒に仕事をする
キャラクターや使う人がカスタマイズできるのが理想」**。そこで、配色を1つに決める作業ではなく
**色がどこに住んでよいかの規則**を決める作業に切り替えた。芯になる原則は
**「色はキャラクターの持ち物、静けさは画面の持ち物」**で、読む面（レポート・サイドバーの文字・
入力欄）はキャラクターの色を受け取らない。

決めたこと:

- **差せるつまみは4つだけ。** `ground` / `surface` / `ink` を使う人、`accent` をパック
  （`character.json`）が差す。`ink-quiet` と `rule` は導出、状態の3色（ok / warn / ng）は固定
  （意味が動くと色が情報を運べなくなる）
- **既定の `accent` は立ち絵の頬と耳から採った `#f2b0a0`。** 移行前の `#8ab4ff` は VS Code や
  GitHub Dark が持つ青で、この題材から出てきた色ではないので捨てた。地も中性の黒をやめ、
  紫に寄せた暖色の黒にして淡いピンクの立ち絵と喧嘩しないようにした
- **大胆さは1箇所、キャラビューに置く。** 立ち絵と吹き出しだけが枠も地も持たず `ground` の上に
  直接立つ（付喪神は器物に宿るのであって、ウィジェットの中に座っているのではない）
- **書体は `system-ui` と等幅の2本のまま。** 外部フォントは足さない（ネットワークへ出るのは
  `127.0.0.1` に閉じる方針に触れ、見返りが薄い）。仕事は寸法にさせる。`font-size` を7種類から
  4段へ畳み、本文の行間 1.75、**行長を 42 全角で切る**（いまは 1400px で約 67 全角あり長い）
- **等幅は区別に使う。** 機械が付けた名前（ID・ツール名・キー・モデル名）は等幅、人が書いた
  言葉（レポート・セリフ）は本文書体

適用のタイミングは**据え置き**（前倒しの必要が無くなった。段7が終わっているので、この計画が
閉じた時点で適用のタスクが着手可になる）。

### 2026-09-13 日本語の強調が約物で壊れる回帰を直す（T-104）

**T-104 完了。** 移行の段6（T-099）で Markdown を unified に替えた副作用で、**日本語の強調の
中身を約物（`「」（）。`）で始める・終えると記法のまま画面に出る**ようになっていた。CommonMark の
delimiter run の規則によるもので remark としては仕様どおり（旧レンダラは素朴な正規表現
`/\*\*([^*]+)\*\*/g` だったので、規則に関係なく通っていた）。

**`remark-cjk-friendly`（MIT / ^2.3.1）を入れて直した**（ユーザーの承認を得て追加。
`docs/design.md` 11章の一覧にも同じコミットで追記した）。`remark-gfm` の後ろに置く。
回帰テストは実測した9ケースをそのまま入れてある。実機（7409）でレポートを1つ出し、本文の
`**「…」**` 3箇所がすべて太字になり生のアスタリスクが0であることを確かめた。

**取り消し線 `~~「…」~~` は穴のまま閉じた。** `remark-cjk-friendly` は GFM の `~~` には効かず
（別の判定を通るため）、埋めるには `remark-cjk-friendly-gfm-strikethrough` をもう1つ足すことになる。
**レポートの規約が取り消し線を一度も勧めていない**ので実害が無いと判断し、ユーザーの了解を得て
依存を増やさない側に倒した。挙動を固定するテストと理由のコメントは入れてある。

### 2026-09-13 レポートの語彙に `dl` を入れる（T-093 / T-094）

**T-093 完了、T-094 を同じコミットで実施。** 着手前に現物を照合したところ、**T-099 の書き直しで
T-093 の完了条件の大半はすでに満たされていた**（内容の型 → 構造の対応表12行・「迷ったときの判断」
列つき、冒頭は結論、`details` に畳む、`badge`、`cols` + `card`）。残っていたのは「サニタイザは
通すが CSS も規約も無い要素」をどうするかだけで、**ユーザーの判断で `dl` だけ入れた**
（`kbd` / `mark` / `del` / `ins` / `figure` / `small` / `section` は入れない。規約は毎ターン効くので、
増やすほどコストになるため）。

足したのは規約の1行と CSS の2つだけ（許可リストは `dl` / `dt` / `dd` を元から通していた）。
見た目は**2列に組んで説明の左端を揃える形**にした（ユーザーが見本から選択）。狭い画面
（760px 未満）は1列へ戻す。語彙の突き合わせは class 9/9・要素 21/21 が両方に存在することを
`grep` で確認した。

**目視**（`http://127.0.0.1:7408/`、本物の駆動）: 新しい規約を当てたセッションでモデルが自発的に
`dl` を使い、1400px で説明の左端が 120px に4対とも揃い、600px では1列に戻って横スクロール0。
ユーザーの了解を得た。

**同じ目視で、段6（T-099）の回帰を1件見つけて T-104 に起こした。** 日本語の強調
（`**太字**` / `*斜体*` / `~~打ち消し~~`）は、**中身を約物（`「」（）。`）で始める・終えると
記法のまま画面に出る**。CommonMark の delimiter run の規則によるもので、remark としては正しい
（旧レンダラは素朴な正規表現だったので通っていた）。このリポジトリは `**「…」**` を多用するので
影響が小さくない。直し方（依存を足す / 自前で書く / 規約で避ける）は T-104 で決める。

### 2026-09-13 移行の段7（旧4層を消し、src を protocol / core / ui / cli.ts にする）

**T-100 完了。これで描く層をブラウザ側へ移す移行（段1〜段7）が終わった。** 旧層に残っていた
`infrastructure/view-port.ts` を `core/port-resolution.ts` に、`src/index.ts` を `src/cli.ts` に
移し（`bin/tsukumo` と `package.json` の `start` も追従。`bun link` 済みのグローバルの
`tsukumo` はシンボリックリンクなのでそのまま効いた）、`src/infrastructure` と
`test/infrastructure` を消した。`test/architecture.test.ts` は併存の規則を落として3辺＋配線に
なった。残る段8（キャラクターパックの切り替え）と段9（セッションの復元）は、移行ではなく
独立した機能追加として別タスクにしてある。

**枠組みの説明を実態に合わせて書き直した**（ディレクトリ名を消すだけでは足りない部分）。
`CLAUDE.md` 原則2 の「受け取る／決める／描く」はクリーンアーキテクチャの語彙で実態を
説明していなかったので、**「両側で共有する契約（`protocol`）／サーバ（`core`）／
クライアント（`ui`）」**に改めた。`docs/design.md` 2章には、この形が
**「共有コントラクト＋クライアント/サーバ分割」**であることと、`protocol` が `node:` にも
`document` にも触らないのは思想ではなく**両方の実行環境で動くという物理的な制約**である
ことを足した。`core/host.ts` / `orca-host.ts` のポートとアダプタは、2つ目のホスト
（Electron / Tauri）を実際に見込んでいるので残してある。

`bun run check`: 322 pass / 0 fail / 42 files。5つの docs すべてで節数は不変
（requirements 26 / architecture 10 / coding-standards 21 / design 43 / glossary 42）。
`grep -rn "移行前\|移行中" docs/architecture.md CLAUDE.md docs/coding-standards.md` は0件。

**実機**: グローバルの `tsukumo` を別プロジェクト（`day-snap`）で `TSUKUMO_VIEW_PORT` を変えて
起こし、偽の駆動・本物の駆動とも Orca のタブが開いて1往復できた。作業ディレクトリ依存も
正しく働いている（`day-snap` には `develop/tasks.json` が無いので、サイドバーのタスク一覧が
「不明」になる）。

**段6の回帰を1件見つけて直した。** `toolVisibility` が「見せない」と決めたツール（読み取り・
検索・コマンドの出力）**しか無いステップ**が、枠だけの空のカードとしてメインビューに残っていた
（実機で発見）。移行前の `stepHtml` は「絞ったあとの数」で空を判定していたのに、React 版の
`Step` が絞る前の `actions.length` で判定していたのが原因。同じ判定に直し、回帰テストを2件
足した（修正前の `turn.tsx` では落ちることを確認済み）。

### 2026-09-13 移行の段6（メインビューを React にし、Markdown を unified に置き換え）

**T-099 完了、T-063 を吸収して閉じた。** メインビュー（やり取りのタブ・レポート・ツールの行・
質問の記録）を `src/ui/main-view/`（`MainView` / `TurnTabs` / `Turn` / `Report` / `ToolRun` /
`QuestionRecord`）にし、Markdown の変換を自前の行ベースパーサから `src/ui/report/`
（`markdown.tsx` = react-markdown + remark-gfm + rehype-raw + rehype-sanitize + rehype-highlight、
`sanitize-schema.ts`、`MermaidBlock` / `ChartBlock`、`split-blocks.ts`）へ置き換えた。
**T-063（引用・ネスト・水平線・列揃え）が閉じた**（unified がそのまま描く。列揃えは GFM の
`align` を rehype-sanitize の schema で通す）。`src/protocol/main-view.ts` に `groupIntoTurns` /
`limitTurnEntries` / `toolVisibility` を移し、`src/ui/layout/` に `Layout` / `LayoutResizer` を
移して `src/ui/main.tsx` を `<div id="app">` への単一 root（`<Layout main=... sidebar=...
character=... dispatch=...>` の props スロットで4領域を合成）にまとめた。

旧の `src/presentation/` を丸ごと削除（`view.ts` / `report-html.ts` / `report-notation.ts` /
`browser/` 一式）。`report-notation.ts` は `src/core/report-notation.ts` に移し、「描けない」の
迂回指示を外した。`src/infrastructure/view-server.ts` の静的配信を `src/core/server.ts` に合流
（design.md 5章の想定どおり http+ws を1ファイルに）。`browser-bundle.ts` → `core/bundle.ts`
（browser.js のビルドは削除）、`bundled-path.ts` → `core/bundled-path.ts`（core が使うため）。
`src/usecase/`（`event-sink.ts` / `throttle.ts` / `view-publish.ts`）は消費先が消えたため削除し、
ディレクトリごと空になった。`vendor/idiomorph.min.js` と `highlight.min.js` を削除
（色付けは rehype-highlight が hast の時点で済ませ、領域差し替えは React の再描画に変わったため）。

**設計との整合のため2点、既存コードに手を入れた。** (1) `test/architecture.test.ts` の
「ui/ の領域どうしの import」から `report` を外し共有部分にした（`main-view/` の `<Report>` が
`ui/report/` の `<Markdown>` を直接使う必要があり、design.md 6.1 の部品の木と両立させるため。
T-096 が `report` を領域として名指ししていたが main-view との依存を見落としていた）。
(2) `protocol/session-state.ts` の `mainViewEntries` が `tool` 系レコードを除外していたのを
復活させた（`toolUseId` 等の内部情報は落とす）。`docs/requirements.md`「メインビューはレポート
だけ」（2026-09-11）は文言のままだと `ToolRun` と矛盾するが、design.md 6.1 の部品の木と
タスクの完了条件（toolVisibility の3種のテスト）を優先し、requirements.md 4.2 に
「サイドバーは進行、メインビューはその依頼の記録」と役割の違いを追記した。

`bun run check`: 399 pass / 43 files → 320 pass / 42 files（3層併存の旧テストが大量に消え、
`protocol/main-view.test.ts` / `ui/report/markdown.test.tsx` / `ui/main-view/{main-view,report}.test.tsx`
などを追加）。**`bun test --isolate` に変更**（`package.json`。`mock.module` がプロセス全体に
効き、`report.test.tsx` のスタブ化した `Markdown` が他ファイルへ漏れたため）。

偽の駆動（7401番）+ Playwright: 台本に4つ目のターン（引用・2段ネスト・水平線・列揃え・
note/badge/cols/mermaid/chart の見本）を足し、blockquote・入れ子の `ul`・`hr`・
`style="text-align: center/right"`・`.note.note-warn`・`.badge.badge-ok`・`.cols > .card`・
`pre.mermaid` 内の `svg`・`.chart-block` 内の `canvas` がすべて実際に描かれることを確認。

**実機（本物の claude、7403番）でも3往復して確かめた。** レポートは 0.7 秒ごとの実測で
69 → 584 文字・塊 0 → 6 と 18 秒かけて流れ、**ターンが終わった瞬間に 584 → 573 文字へ整形し直された**
（`docs/requirements.md` 4.2「リアルタイムに流す」が実機で成立している）。同じ往復で引用・2段ネスト・
水平線・列揃え（中央4セル・右4セル）・note・badge・cols+card・mermaid の `svg`・chart の `canvas` が
描かれ、`img` と `script` は0件。2ターン目でタブが「今回」「1つ前」の2つ出て先頭へ戻り（`scrollTop` 0）、
失敗した `Bash` がツールの行として赤枠で出た。ブラウザのコンソールに出るエラーは `/favicon.ico` の
404 だけで、**これは移行前（HEAD）でも同じ**（段6が持ち込んだものではない）。

**受け入れ時に4点直した。** 推移的な依存 `hast-util-sanitize` からの型 import を `rehype-sanitize` の
`Options` に変更（足してよい依存は design.md 11章の一覧だけ）。`--isolate` の前提が古くなっていた
コメント2箇所（`test/dom-environment.ts` / `test/ui/main-view/report.test.tsx`）を実態に合わせ、
`core/report-notation.ts` の経緯のコメントを「規約と schema を揃える」という制約の記述に置き換えた。

**ユーザーの追認が要る点**: 上の (2) は 2026-09-11 の決定（「メインビューはレポートだけ」）の
読み替えにあたる。タスクの完了条件と design.md 6.1 がツールの行を求めているので進めたが、
決定そのものを戻したいなら `mainViewEntries` のフィルタと requirements.md 4.2 の該当箇所を戻せばよい。

### 2026-09-13 移行の段5（キャラビューを React の部品に）

**T-098 完了。** キャラビュー（立ち絵と吹き出し）を `src/ui/character-view/` の4部品
（`CharacterView` / `Portrait` / `BalloonTrack` / `Balloon`）にし、素材は HTML に埋めずに
`/character/<file>` から取る形にした。`core/character-pack.ts` が `character.json` を読んで
起動時に `character-changed` を1回流し、`SessionState.character` には **URL だけ**が乗る
（allowlist は `portraits` に書かれたファイル名のみ）。SVG は部品が `fetch` して
`dangerouslySetInnerHTML` でインラインにするので、差し色の `--outfit-accent` が効く。
表情の「作業中」への遅延切り替えは、サーバ（`event-sink.ts`）のタイマーから部品の
`useEffect` へ移した。旧の `/events/character`・`buildCharacterBody` / `portraitMarkup`・
`infrastructure/character-asset.ts` は削除。**残るはメインビューだけ（段6）。**
`bun run check` は 399 pass / 0 fail（393→399）。目視は偽の駆動 + CDP（1400x900）で
最新の吹き出し bottom 759.0、立ち絵の高さ 242.55px が段4と一致することを確認した。

**`/character/<file>` の置き場所は設計書と1点ずれている。** `docs/design.md` 5章は
`core/server.ts` に置くとしているが、HTTP の配信自体がまだ旧 `infrastructure/view-server.ts`
にあり、旧4層から `core` を import できない。allowlist とファイル読み取りだけを core に置き、
配信側には関数を注入する形にした（**段7で HTTP 配信ごと `core` へ移すときに解消する**）。

### 2026-09-13 移行の段4（入力欄を React の部品に）

**T-097 完了。** 入力欄（送信・中断・経過時間・`/` 補完・答え待ちの箱）を `src/ui/dispatch/` の
5つの部品にし、旧の `POST /api/prompt` / `/api/interrupt` / `/api/answer`、`GET /api/commands`、
`/events/turn-status` / `/events/pending-answer`、`browser/dispatch.ts` / `command-suggestions.ts` /
`pending-answer.ts`、`dispatchRegionHtml` 一式を消した。**残る2領域（メイン・キャラ）は旧のまま。**
経過時間は `SessionState.turnStartedAt` / `turnFinishedAt` から部品が計算する（1秒の刻みは部品の
ローカルなタイマーで、秒数は状態に持たない）。`protocol/tool-summary.ts` は段3 の申し送りどおり
`ui/component/tool-summary.ts` へ移した。`bun run check` は 393 pass / 0 fail（432→393。旧の HTML
文字列テストが消えた差し引き）。

**委譲した実装を2点差し戻した。** (1) `Answer.labels` を「質問が1件のときだけ意味が変わる」形
（選んだラベルをそのまま並べ、`core` 側で「、」結合）にしていたので、**契約どおり
`labels[i]` は `questions[i]` への答え1つ**に統一した（複数選択は部品の側で「、」につなぐ）。
件数でフィールドの意味が変わるのは型に表れない暗黙の契約になる。**タスク本文の完了条件(8)
「labels に2つ入る」はこの理由で満たしていない**（`evidence` に記録）。(2) `formatElapsed` が
テストのためだけに `export` されていたので外し、部品経由の検証に置き換えた。

**実機で1往復した**（SDK の駆動、`TSUKUMO_VIEW_PORT=7502`、Playwright で操作）: 送信 →「中断」に
変わる → 完了で「所要」、⌘Enter でも送れる、中断が効く、**許可モードを「毎回聞く」にして許可と
拒否の両方**、答え待ち中はタブのタイトルが「● 」で始まり答えると戻る、枠の強調（`.dispatch-pending-glow`）
が出て消える、`/` 補完10件と Tab 確定（送信しない）。偽の駆動（7503）で **multiSelect に2つ答え
`labels: ["A案、C案"]` が届く**のを確認。**7327 の常駐は元から止まっており、触っていない。**

### 2026-09-13 移行の段3（サイドバーを React の部品に）

**T-096 完了。** サイドバーの3区画（いま何をしているか・タスク一覧・セッション情報）を React の
部品にし、旧の `/events/sidebar`・`buildSidebarBody`・`POST /api/model` / `/api/permission-mode`・
`browser/session-info.ts` を消した。**他の3領域は旧のまま動いている。** タスク一覧は
`core/task-summary.ts` が mtime を見て `tasks-changed` イベントを起こす形に変わり、
`SessionState.tasks` に入る。**`ui/` の作法3点も同じ段で入れた**（`ui/component/` を切る・領域
どうしの import を禁じる辺・barrel file を作らない規約）。`bun run check` は 432 pass / 0 fail
（445→432。旧の HTML 文字列テストを消して部品のテスト30件を足した差し引き）。**見た目は据え置き**
（4つの幅で実測、段2 と同値）。

**`bunfig.toml` が増えた**（`test/dom-environment.ts` を preload して `@testing-library/react` が
要る DOM のグローバルだけを借りる。`fetch` / `WebSocket` / タイマーは差し替えないので、実際の
HTTP と WebSocket を使う既存のテストが巻き添えにならない）。

**`protocol/tool-summary.ts` は段4 で `ui/` へ移す**（旧の答え待ちの箱と新しいサイドバーの両方が
読むため一時的に `protocol` へ置いた。申し送りは T-097 の本文）。

### 2026-09-13 Playwright（playwright-core）の導入

**ユーザーの承認を得て `playwright-core` を devDependency に足した**（タスクIDなし）。
`docs/design.md` 10章が「偽の駆動で起こした tsukumo に Playwright」と名指ししていたのに
11章の依存の一覧から漏れていたので、そこも埋めた。**ブラウザは落とさない**
（`playwright` の側は postinstall で約130MB を取りに行く。`playwright-core` は driver だけ 13MB で、
`chromium.launch({ channel: "chrome" })` として手元の Google Chrome 153.0.8010.36 を動かす）。
**テストランナーは足していない**（`@playwright/test` ではなくライブラリだけ。`bun test` と競合しない）。
道具は `scripts/capture-view.ts` 1本で、URL を開いて画像に撮り、`--measure <selector>` で位置と
大きさを数値で出す。**`bun run check` には入れない**（生きたサーバが要って遅い）。
**偽の駆動（7398）で動作確認済み**: 1400x900 で撮れ、`.layout-main` 1006.8x505.44 /
`.layout-sidebar` 335.61x505.44 / `.layout-character` と `.layout-dispatch` が各 671.2x336.97 /
`.portrait` 高さ 242.55px（T-091 の記録 242.5px と一致）/ 横のはみ出し 0px。画像には4領域・立ち絵・
吹き出し・タスク一覧・入力欄が出ていた。`bun run check` は 445 pass / 0 fail。

### 2026-09-13 移行の段2（protocol と core の骨組み・WebSocket・偽の駆動）

**T-095 完了。** `src/protocol/`（両側で共有する語彙・畳み込み・zod）と `src/core/`（SDK 駆動・
セッション管理・WebSocket・偽の駆動・環境変数）を切り、`/ws?t=<起動トークン>` を1本通した。
**旧の SSE 5本と POST 6本はそのまま動いている**（見た目は変わらない）。着手前にユーザーと決めた
4点: **zod は境界だけ**（`ClientCommand` は全部、`ServerFrame` は封筒だけ。`SessionEvent` /
`SessionState` は TS の型のまま）、**起動トークンを入れる**、**`showView` はタブを貼り直す**、
**バッチ 100ms ＋ `ui.js` の空の入口まで**。`domain/` は消えて `protocol/` に吸収され、
`session-driver` / `host` / `orca-host` は `core/` へ移った（`REPORT_NOTATION_PROMPT` は
`systemPromptAppend` として `index.ts` から注入する形にした）。**`orca tab goto` は存在しない**
ことが実測で分かり（正しくは `orca goto --url --page`）、`docs/requirements.md` 5章の表を直した。
`bun run check` は 445 pass / 0 fail（415→445）。**実機で確認済み**（偽の駆動でタブの貼り直し、
SDK の駆動で1往復。詳細は `evidence`）。

### 2026-09-13 描く層をブラウザ側へ移す決定と、正典の書き換え（移行の段1）

**ユーザーの依頼「最適なアーキテクチャを再考して提案して。規約にとらわれず、言語やエコシステムから
見直してもいい」に対して、`src/` 全体と正典を読んで `docs/research/architecture-rethink.md`（経緯・
症状・採らなかった案・判断の基準）を書き、ユーザーの訂正2件（立ち絵は動くが**話さない**。端末ペインは
用途が無いので**余地も残さない**）を反映したうえで、「移行しようか」の決定を受けて
`docs/design.md`（設計書。正典）を新設した。** 中身は `protocol` / `core` / `ui` の3層、WebSocket 1本の
プロトコル（`hello` / `events` / `error` と `ClientCommand`）、両側で共有する reducer、React の部品、
unified の Markdown、キャラクターパック（`persona.md`）、偽の駆動、起動トークン、段2〜段9 の完了条件。
言語（TypeScript）とランタイム（当面 Bun）は変えない。

**正典を移行後の形に合わせた**: `docs/architecture.md`（現在の実装状況に移行中の注記、設計判断に
「描く層をブラウザ側へ移す」を追加、SSE / フレームワーク不採用 / 4層の3節に「役目を終える」注記）、
`docs/requirements.md`（2.2 の PTY と音声、4.2 の Markdown ライブラリを覆した注記と配り方、4.3、4.4、
5章の依存の承認、7章の未決事項3件。`^#{2,3}` の節の数は 26 のまま）、`docs/coding-standards.md`
（「層と依存の向き」を3層に書き換え、旧4層との併存の規則、会話内容にブラウザ側のメモリの1文）、
`docs/glossary.md`（「通信（移行後）」の節に5語）、`CLAUDE.md`（現在の状態・原則2・関連リンク）。
**`develop/direction.md` に段2〜段9 をタスク化する指示を書いた**（次のセッションが `/plan-tasks` で
起こす。T-088 は閉じる、T-063 は段6に吸収、T-064 / T-065 は段8に統合、T-078 は段9）。
コードは1行も変えていない。`bun run check` は通る（件数は下の「次にやること」の直前に記録）。

### 2026-09-13 キャラビューの立ち絵と吹き出しの位置

**T-091 完了。** 立ち絵を大きくし（`--portrait-height` 68%→78%）、最新の吹き出しの位置を
**セリフの件数によらず固定**した。`.balloon-track` を下端揃えのまま `--balloon-bottom-gap`
（7rem）ぶん浮かせ、`max-height` も同じだけ差し引く形。**一度 `align-self: center` で
「箱ごと中央寄せ」にしたが差し戻した**（箱の高さがセリフ件数で伸びるので、箱の中心を固定すると
下端＝最新が下へズレていく。2026-09-12 の「下半分を空ける」案の差し戻しとは別の失敗）。
**尻尾は下辺から左下へ出す形をやめ、左辺から真横（左）へ**（ユーザーの指示）。
`.balloon-track` は `overflow-y: auto` を持つと横方向もクリップされるので、
`padding-left: var(--balloon-tail)` で尻尾の場所を内側に確保している。立ち絵と吹き出しは
最後に揃えて 1.5rem ずつ下げた（`--portrait-drop` 0.75rem→2.25rem）。
**数値は CDP で実測**（1400x900、セリフは合成）: 最新の rect が 1/3/8 件とも top 680.45 /
bottom 735.0 で不動、並びに使える高さは変更前後で同じ、立ち絵は 211.5→242.5px。
**実機の目視も実施**（Orca のタブ、127.0.0.1:7327）でユーザーの了解を得た。
`bun run check` は 415 pass / 0 fail。

**T-062 完了（決めるタスク。実装はしない）。** レポートを読む時間を減らすために**どの層に足すかを
決めた**。3層を実物で棚卸しした結果、**語彙はすでに足りていて、足りないのは「どう描くか」の指示**
だと分かった（ユーザーの言葉「重要なのは output styles のほうなのかもしれない」）。決定は
`docs/requirements.md` 4.2「読む時間を減らすために足すのは、規約の側」に記録し、**見せ方と再構成の
線引き**（本文の文字列を変えないものが見せ方／レンダラが目次・要約・段組みを起こすのは再構成で
やらない／書き手が最初からその構造で書くのは執筆）も一緒に書いた。後続3件の案は
`develop/direction.md` へ（規約の印の語彙・列揃え `:---:`・`dl` の CSS）。
着手前に `develop/progress.md` の `### 2026-09-12` の1小節（290行）を
`docs/history/progress-archive.md` へ移した（`tasks.json` 側はトリガー未該当）。

**T-090 は着手せずに閉じた**（ユーザーの判断「やらなくていいや」。2026-09-13）。`passes` は
`false` のまま `done` にしてある（未達で終了の形）。セリフ0件のプレースホルダは
`<div class="balloon">` のままなので、気が変わったら新しいタスクとして起こす。

### 2026-09-10 メインビュー（T-009）

**T-009 完了。** 左上に**ツールの実行（名前・引数・結果）と詳細のレポート**が時系列で積まれる
ようになった。実装は sonnet に委譲し、受け入れはメインで実施。168 pass / 0 fail。

- **「作業中／完了後」の状態は明示的に分けず、時系列で積む形にした。** assistant の1メッセージ内で
  `text` と `tool_use` が混ざる順序に保証が無く、transcript から状態を確実に判定できないため。
  結果として末尾が結果待ちのツールなら「作業中」、末尾が詳細なら「完了後」に自然に対応する
- **Markdown → HTML は自前の簡易レンダラー**（見出し・コードブロック・箇条書き・表・太字・
  インラインコード・リンク）。非対応の記法は段落として素のまま出るので壊れない
- 切り詰めは上限8000文字＋件数表示。直近40件のみ表示

**私の指示の誤りが1つ正された**: ツールの結果は `toolUseResult` ではなく
**`user` 行の `message.content[]` の `tool_result`**（`tool_use_id` で対応付ける）。
実測で確かめられて `docs/requirements.md` に反映できる材料になった。

**受け入れで1度差し戻した（本物の穴）**: Markdown のリンクがスキームを検査せず
`javascript:alert(1)` を `href` に入れていた。**このビューは localhost で会話を持っているので、
そこでスクリプトが走るのは避けたい。** allowlist（`http:` / `https:` / `mailto:` / 相対）に直し、
弾いたものはリンクにせず平文で残す形にした（情報を失わないため）。テストで固定済み。

**私のテストが1件誤検知だった**（`onerror=` を「生で出ている」と判定したが、実際は
`<code>&lt;img src=x onerror=alert(1)&gt;</code>` と正しくエスケープされていた）。
疑わしいときは周辺を実際に見ること。

### 2026-09-10 メインビューの絞り込み（T-016）

**T-016 完了。** 「ファイルを変えた操作・サブエージェントの起動・失敗したツール」だけに絞った。
実装は sonnet に委譲し、受け入れはメインで実施。201 pass / 0 fail。

**効果は実測で確認**: ツール実行ブロック 306→24、本文 1.35MB→242KB。配信されるメインビュー
（直近40件の窓）は 61KB→13KB。成功した `Bash` のコマンドは1つも出ない。

**解釈が入った点（ユーザーに伝達済み）**: **失敗したツールは引数ごと出す。** 決定表は
「エラーの内容」としか書いていなかったが、何をして失敗したかが分からないと実用性を欠くため。
**ユーザーの「打ったBashコマンドはさすがに出さなくてよかった」は成功したコマンドについての
言及**なので、失敗時にコマンドが見えるのは妥当と判断した。ただし `Bash` の引数に秘密が入る
露出は、失敗時には残る。

### 2026-09-10 チカチカの解消（T-017）

**T-017 完了。** 本文が前回と同じなら `innerHTML` を差し替えないガードを入れた。207 pass / 0 fail。

**原因は2つあった**（サブエージェントが切り分けた）:

1. ポーリングは transcript の mtime/size の変化だけを見て**3ビューをまとめて publish** する。
   ツール実行が続く間 transcript は高頻度で変わるが、**キャラビューの見た目は変わらない**ことが
   多く、同じ内容の差し替えが繰り返されて立ち絵のフェードが再生され続けていた
2. **購読開始直後の1回目の push が、ページに埋め込み済みの本文と同じもの**を送っていた。
   ページを開いた瞬間にもう一度描き直していた（②はこちらが挙げていなかった原因）

あわせてスクロール位置を保つようにした（いちばん下にいたときだけ追従）。

**残る粗**: セリフだけ変わったときも本文全体としては変わるので立ち絵は作り直される。
厳密に「表情が変わったときだけ」にするには立ち絵と吹き出しを別々に差し替える必要がある。
**1応答に1回なのでむしろ自然**と判断して許容した。気になればタスクに切る。

### 2026-09-10 送り先の一覧と見た目（T-018）

**T-018 完了。** 212 pass / 0 fail。

**`preview` を読まずに済む方法が見つかった。** `orca terminal list --json` の各要素に
**`agentIdentity`** があり、claude が動いているターミナルにだけ `"claude"` が入る（実測で
5件中2件が該当し、完全に一致）。これは Orca 自身が付けた**分類ラベル**で、ターミナルの出力
そのものではない。**`Pane` 型に `preview` を持たせないことで、構造的に漏れる経路が無い。**

**絞り込まず、並び順で寄せる。** 判定が外れても選べるように、全件を返して claude らしいものを
上に置き、印を付けるだけにした。テストで「`likelyClaude` が false でも選べる」ことを固定済み。

見た目は他の3領域と同じ語彙（暗い地・`#3a4256` の枠線・角丸）に揃えた。

### 2026-09-10 4領域のリサイズ（T-022）

**T-022 完了。** 3本の仕切り（上段の縦・下段の縦・上下の横）を
ドラッグで動かせるようにし、比率を `localStorage` に保存・復元、既定に戻すボタンも付けた。
実装は sonnet に委譲し、受け入れはメインで実施。221 pass / 0 fail。

- **4列共有の `grid-template-columns` をやめた。** 上下段で縦の仕切り位置が違うので、
  1つの列定義では3本を独立に動かせない。上下段をそれぞれ別の grid にし、行の高さと各行の
  列幅を CSS カスタムプロパティで持たせて、ドラッグはその変数を書き換えるだけにした
  （実際のサイズ計算は grid に任せる）
- **既定を 7:3・1:1 から 60:40・75:25・50:50 に変えた。** 「入力フォームが狭い」が動機なので
  下段の高さを広げ、**下段の左右はユーザーの指定で半々**にした。可動域は15〜85%にクランプ
- `textarea` の `resize: none` は維持（領域側のドラッグとネイティブのハンドルが競合するため）
- 壊れた `localStorage` 値でのフォールバックは自動テスト2件（不正 JSON / 型・範囲違い）で固定

**目視確認済み**（Orca 内のブラウザタブ `http://127.0.0.1:7327/layout`）。3本の仕切り・
リロードでの復元・「既定に戻す」ボタン・**`position: fixed` で右下に置いたそのボタンが
入力欄の「送る」ボタンと重なっていないこと**をユーザーが確認し、問題なしと判断した。

### 2026-09-10 起動コマンドを1つに（T-027）

**T-027 完了。** `bun run start` だけで、サイドカーの起動からレイアウトページのタブが
Orca 内に開くところまで進むようになった。実装は sonnet に委譲し、受け入れはメインで実施。
223 pass / 0 fail。

- **足したのは `announce` の後の1呼び出しだけ。** タブを開く手段は `src/host.ts` の
  `Host.showView` として既にポートにあり、`main` も `createOrcaHost()` を持っていた
- **重複タブの回避は既に解かれていた。** `src/orca-host.ts` の `showView` が
  「同じ URL のタブがあれば reload、無ければ create」に倒しているので、`src/index.ts` 側に
  判定を重ねて持たせていない
- `TSUKUMO_OPEN_VIEW=0` で自動オープンを止められる。**テストの既定はこちら**
  （空きポートで何十回も起動するので、既定のままだと実機の Orca にタブが積み上がる）
- `orca` が使えなくても起動は続く。PATH の先頭に必ず失敗する `orca` を置くテストで固定した
- **実機で確認済み**: 引数なしの起動で `orca tab list --json` に `/layout` のタブが
  `loadError: null` で出た

**メイン側で1つ拾った**: `docs/architecture.md`「手で確かめること」の手順2が
`bun run scripts/open-views.ts <URL>` を要求したままだった。**ここは全タスクの目視確認が
参照する場所**なので直した。

### 2026-09-10 セリフの記法をプレフィックスに変える（T-024）

**T-024 完了。** セリフの記法を引用（`> `）から行頭マーカー
（既定 `アスナ: `）に変えた。論点はメインでユーザーと決着させ、実装は opus に委譲。
226 pass / 0 fail。

- **マーカーは設定値。** 既定は `アスナ: `、`TSUKUMO_SPEECH_MARKER` で差し替えられる。
  **原則4（キャラクターの中身をコードに書かない）との折り合いをこの形で付けた**
- **決め手になった発見**: 出力スタイルの人格（アスナ）と、キャラクター定義の `name`
  （`characters/tsukumo-spirit` は「つくもの精霊」）は**別物で一致していない**。
  定義から名前を取る案はこれで消えた
- **旧記法は一度に捨てた。** `> ` はセリフでなくなり、レポート内の普通の引用として使える
  （メインビューのレンダラーが引用を描けるようにするのは後続タスクの担当）
- `main` で1回読んで3経路（キャラビュー・メインビュー・ポーリング）へ渡す形。
  `isQuote` → `isSpeech` などで「quote」の語彙をソースから消した
- `~/.claude/output-styles/asuna.md` はユーザーの承認を得て反映。
  `asuna.md.bak.20260910-220540` にバックアップあり

**目視確認済み**（2026-09-10、Claude Code とサイドカーを起動し直した新セッション）。
キャラビューの吹き出しに直前の応答のセリフ2行（75文字）だけが出て、マーカー `アスナ: ` は
残っていない。同じ2行はメインビューに不在で、`アスナ: ` で始まる行も無い。

**確認しようとして踏んだ罠**: 動いていたサイドカーは**起動時に決めた前セッションの transcript を
追い続けていた**ので、吹き出しは古いままだった（`~/.tsukumo/transcript-path` は新しい
セッションを指していたのに、である）。**T-023 の症状がそのまま目視確認の妨げになる。**
確かめるときは transcript を引数で明示してサイドカーを起動し直す。

### 2026-09-10 追従先がセッション再起動に追いつくようにした（T-023）

**T-023 完了。** サイドカーが**同じディレクトリで始まった新しいセッションへ自動で乗り換える**
ようになった。239 pass / 0 fail。

- **hook が `cwd` も書くようにした**（`{"transcriptPath":…,"cwd":…}`）。追従先ファイルは
  全セッション共通の1つなので、**cwd を見ないと別リポジトリで claude を起動した瞬間に
  ビューを奪われる**。読む側（`src/transcript-target.ts`）が「同じ cwd かその配下」だけを
  採ることで弾く
- **旧形式（パスだけの1行）は捨てた。** cwd が無いものは `missing` に倒れるので、hook を
  直した直後の1回だけ、次の SessionStart まで引数なし起動ができない窓がある
- **引数で渡したときは乗り換えない**（特定のセッションを見張る逃げ道。ユーザーの決定）
- **乗り換えたら直前のセリフを捨てる。** 「規約に従っていない発話では直前のセリフを出し続ける」
  のは同じセッションの中での話。判定は `createCharacterViewPublisher` が持つ1箇所に閉じたまま
- 新しい追従先がまだ読めないうちは乗り換えない（読めている今の相手を手放さない）

**実機で確認済み**（引数なしで `bun run start`）: ①同じ cwd の新しい transcript を hook が
書くと吹き出しが切り替わり、前のセリフが消える ②別 cwd では切り替わらない
③`tsukumo: 追従先を切り替えた: <path>` が stderr に残る。**確認には本物の会話を使わず、
手で書いた架空の transcript を使った。**

### 2026-09-10 メインビューをやり取りごとに区切った（T-019）

**T-019 完了。** 利用者の依頼を境目に**やり取り**へ分け、**今回だけを開いて上から読める**形に
した。過去は**今回・1つ前・2つ前**の3つをタブで選ぶ。257 pass / 0 fail。

- **読む層は境界を積むだけ、まとめるのは決める層。** `extractMainViewEntries` は依頼を
  `kind: "request"` として時系列に積み、`buildMainBody` がやり取り→ステップへまとめる
- **依頼の判定**（2026-09-10 実測どおり）: `type: "user"` かつ `message.content` が文字列。
  ツールの結果（配列）と `isMeta` の行は境界にしない
- **ステップ＝レポート1件＋それに続くツールの実行**（ユーザーの決定）。レポートより前の
  ツールは、レポートを持たないステップになる
- **タブの選択はブラウザ側だけが持つ。** push で本文が丸ごと差し替わっても選択が戻らないよう、
  `MutationObserver` で差し替えを検知して付け直す。**新しいやり取りが始まったら先頭へ戻す**が、
  過去のタブを見ている間は動かさない。判定は**やり取りの数ではなく今回の通し番号**で行う
  （上限に達すると数が増えないまま中身だけが進むため）
- **長さは減っていない**: 同じ transcript で本文全体 7,333→12,003 文字（過去ぶんを持つため）。
  今回のパネル単体は 7,913 文字。**決定④「今回は全文」を採ったので、狙いが「絞る」から
  「区切って読める」に移った**。転送量が気になるなら、過去のパネルを必要になってから
  取りに行く形が次の手

### 2026-09-10 レポートを HTML で組めるようにした（T-021）

**T-021 完了。** レポートに**HTML を直接書ける**ようにし、**図・グラフ・コードの色**の
ライブラリを同梱した。282 pass / 0 fail。

- **外部ライブラリ禁止の規約はユーザーの判断で緩めた。** ただし**CDN からは読まない**。
  会話が載ったページで外部スクリプトが動き、表示のたびに外部へ通信が飛ぶため。
  `vendor/` に同梱して `127.0.0.1` から配る形にして、**機能はそのまま・表示時の外部通信ゼロ**に
  した（`docs/architecture.md`「外部ライブラリは CDN から読まず、同梱して自分で配る」）
- **同梱の実サイズ**: highlight.js 118KB（常に読む）／Chart.js 196KB・**mermaid 3.2MB**
  （**その記法が実際に出たときだけ読む**）
- **無害化は許可リスト方式**（`src/report-html.ts`）。`script` / `style` / `iframe` は中身ごと、
  `on*` と `javascript:` は属性ごと落とす。`style` 属性は通すが `url(` / `@import` を含むものは
  落とす。**リンクのスキーム判定もこのモジュールへ集約**した（正典が2箇所にならないように）
- **1度差し戻した**: ステップを2列グリッドで横に並べたが、**ステップは「ひとかたまり」を
  示すだけで並べる単位ではない**というユーザーの指摘で、**縦1本・番号なし**に直した。
  段組みはステップの中（本文の下にツールを行内のチップ）と、レポート本文の中（書き手が
  HTML で組む）で作る

### 2026-09-10 レポートの文体は変えないことにした（T-025）

**T-025 完了（変更なしで閉じた）。** 「レポートはキャラクターの口調を不要にする」という
方針転換を**ユーザーが撤回した**。

- ユーザーの言葉:「やっぱり口調はそのままにしよう。今はずいぶん見やすくなったし」
- **覆す動機は表示側で解けていた。** 同じ日に入った T-019（やり取りごとの区切り）と
  T-021（ステップのカード・HTML レポート）で、セリフとレポートが画面上で読み分けられるように
  なったため、文体を触る必要が消えた
- `~/.claude/output-styles/asuna.md` は**未変更**（承認を求める場面が発生しなかった）
- **撤回の経緯を `docs/requirements.md` 4.2 に残した。** これが無いと、次のセッションが
  同じ提案を持ってくる

### 2026-09-09 AI向けドキュメントの構築

helm-yadokari の `.claude/` / `docs/` / `CLAUDE.md` / `develop/` の構成を、このリポジトリ向けに
移植した。

- `.claude/skills/` に14スキルを配置。`codebase-design` / `domain-modeling` / `diagnosing-bugs` /
  `grilling` / `research` / `resolving-merge-conflicts` / `tdd` は汎用なのでそのまま、
  `next-task` / `plan-tasks` / `implement` / `code-review` は tsukumo 向けに書き換えた
  （`pnpm check` 固定だった箇所を「チェックコマンド」に一般化し、issue/spec の出典を
  `docs/requirements.md` と `develop/tasks.json` に差し替えた）
- `docs/requirements.md` / `docs/architecture.md` / `docs/glossary.md` を `work.md` の
  調査結果・技術制約・採用アーキテクチャから新規に起こした
- `docs/coding-standards.md` は実装スタック未決のため**言語非依存の原則**として書き、
  末尾に「スタック決定後に埋めること」のチェックリストを置いた
- `docs/workflow.md` は helm-yadokari のものをほぼそのまま移植し、`pnpm check` への参照と
  難易度の例だけを差し替えた
- `work.md` の内容は当時のまま `docs/history/direction.md` へアーカイブした

### 2026-09-09 実装スタックの決定（T-001）

**T-001 完了。** Bun + TypeScript に決定し、雛形とチェックコマンドを整備した。
`bun run check` は `tsc --noEmit` → `oxlint` → `oxfmt --check` → `bun test` の順で走り、
2 pass / 0 fail で通っている。`docs/coding-standards.md` の「スタック決定後に埋めること」は
全項目を消化して節ごと削除し、`Bun固有APIに寄せない`（`node:` の標準APIに寄せる）と
`整形の対象外`（上流由来の `.claude/` は oxfmt にかけない）の2節を足した。

**判断の記録**: 画像デコード・量子化のライブラリは要らないと分かった（iTerm2 inline images は
PNG を base64 するだけで、リサイズも端末側がやる）。そのうえで**当面は外部コマンドにも依存
しない**方針にした（使われない分岐をアダプタに先置きしないため）。
`docs/architecture.md`「画像処理に外部コマンドを使わない（当面）」に残した。

**副産物の実測**: 画像プロトコルのプローブを VS Code 統合ターミナルと Orca で実行したが、
iTerm inline も Sixel も**どちらの端末でも表示されなかった**。T-004 の背景と
`docs/requirements.md`「7. 未決事項」に反映済み。

### 2026-09-09 サイドカーの PoC（T-003）

**T-003 完了。** 「読む」`src/transcript.ts` /「決める」`src/balloon.ts` /「描く」`src/draw.ts` に
分け、`src/index.ts` が配線とポーリング（500ms間隔、mtime/size の変化を見る）を持つ形にした。
実装は sonnet のサブエージェントに委譲し、受け入れはメインで実施。`bun run check` は
19 pass / 0 fail。

**受け入れで足したもの**: `thinking` を吹き出しに出さないことがコードでは効いていたが
テストで固定されていなかったので、2件足した（実測で assistant 行の `content[]` の
半分近くが `thinking` だったため、事故ると内部の思考が画面に出る）。

**実測で分かったこと**（`docs/requirements.md` 4.1 に反映済み）:

- 行の `type` は 2026-09-08 の記録から**5種類増えていた**（`bridge-session` / `atis-latch` /
  `system` / `file-history-snapshot` / `file-history-delta`）。「未知の type で落ちない」は
  空論ではなく実際に必要
- assistant 行の `content[]` は `tool_use` 84 / `thinking` 58 / `text` 30。**出してよいのは
  `text` だけ**
- 同じディレクトリに `<session-id>/subagents/*.jsonl` ができる。`*.jsonl` を再帰的に拾うと
  サブエージェントの発話が混ざる

**検証**: 本物の transcript（907行）に対して起動し、SIGTERM まで生存・枠付きで最新発話を描画・
追記の反映は 153ms。会話の中身は出さず、構造とテキスト一致だけで確かめた。目視確認（Orca、
幅163）でも、最新発話への追従とペイン幅への追従をユーザーが確認して完了。

**目視確認で1件見つけて直した**: ペインを狭めると枠の右辺が消える。原因は**リサイズ非対応**で、
描画が transcript の変化でしか起きないため箱が前の幅のまま残っていた。`process.stdout` の
`resize` を拾って再描画するようにした。切り分けでは先に文字幅を疑ったが、非ASCII文字611種を
DSR で実測して食い違い0・制御文字0・各行の桁数も端末幅ぴったりと確認でき、いずれもシロだった。
この実測手順は `scripts/probe-balloon-rows.ts` として残し、`docs/architecture.md`
「手で確かめること」から参照している。

**吹き出しの角**を角丸（`╭╮╰╯`）にした。検討時のスケッチでは立ち絵を角ばった枠、吹き出しを
角丸で描き分けており、実装が両方とも角ばっていたため、立ち絵が入ると区別がつかなくなる。

### 2026-09-09 hook で状態ファイル（T-005）

**T-005 完了。** `hooks/state.sh` が `~/.tsukumo/state.json` にイベント種別とモデル名を書き、
`src/state.ts`（読む）/ `src/expression.ts`（決める）が表情・衣装に変換する。立ち絵がまだ無いので
表示はテキスト1行の暫定。実装は sonnet に委譲し、受け入れはメインで実施。57 pass / 0 fail。

**受け入れで直したもの**: hook が書くイベント名を allowlist で絞った。`hook_event_name` の抽出が
`sed` の貪欲一致で、`UserPromptSubmit` の payload（ユーザーの入力そのものが載る）に
`"hook_event_name":"..."` が含まれると**会話の断片が状態ファイルへ漏れる**経路になっていた。

**`~/.claude/settings.json` にユーザーの承認を得て5イベント分を追記した。** バックアップを取り、
追記の前後で orca の12エントリと statusLine をエントリ単位のハッシュで比較し、完全一致を確認済み。
設定は動的に読み直されるため、登録したセッションの途中から hook が効いた。

**分かったこと**: `model` は `SessionStart` の payload にしか載らない。以降のイベントは状態ファイルの
前回値を引き継ぐ設計にした（`docs/requirements.md` 4.3 に記録）。

### 2026-09-09 要件そのものの書き換え（T-007）

**T-007 完了。** `docs/requirements.md` 1章の目的を「キャラクターと一緒に楽しく仕事をするための
ターミナル環境」に書き換え、立ち絵・吹き出し・分離・詳細ビュー・レイアウトを**すべて手段**と
位置づけ直した。4.7（画面レイアウト）を新設。`docs/architecture.md` の VS Code 前提の設計判断は
**当時の判断を残したまま** Orca への乗り換えと理由を併記した。実装は opus に委譲し、受け入れは
メインで実施。57 pass / 0 fail。

**判断のうち解釈が入った点**（記録に無いので、次に読む人が疑ったときのために）: 「ターミナル環境」と
「詳細を HTML でブラウザに出す」の字面の衝突は、**境界を「Orca のウィンドウの中で完結し、外部
ブラウザへ飛ばないこと」に引く**ことで解いた。根拠はユーザーの「Chrome に出るのは嫌で、この
システムを使って一体感を出せる仕組みに」という原文。

**受け入れで直したもの**: `CLAUDE.md`「現在の状態」が雛形段階のままだったので実態に更新。
`README.md` にサブエージェントの文脈が漏れた記述（「このセッションでは行わない」）があったので
書き直し。`package.json` の `description` を新しい目的に。`docs/requirements.md` が 26KB になり
自分で書いた 20KB の閾値を超えたので、読み方を索引＋`sed` 運用に切り替えた。

**`orca` への実行時依存**は、ユーザーが「すべて orca 依存でも問題ない。ただし後で VS Code でも
やりたくなったときに対応できるよう必要な処理を抽象化できるといい」と述べたため、**全面依存を
受け入れたうえでホスト依存の操作をポートにまとめる**方針にした
（`docs/architecture.md`「ホスト依存の操作は1つのポートにまとめる」）。

**これは `.claude/skills/codebase-design` の「アダプタが1つならそれは仮説上のシーム」に意図的に
反している。** ユーザーが2つ目のホストを実際に見込んでいるため。ただし戒めの趣旨は守り、
**インターフェースに Orca の語彙を漏らさない**ことと**ポートの範囲を広げない**ことを条件にした。
VS Code 用のアダプタは作らない。

### 2026-09-09 要件の掘り下げと方針転換（T-008 と再分解）

**T-008 完了。** セリフは引用（`> `）で書く規約を出力スタイルに入れた。不可視マーカーは
3案とも TUI に表示されると実測で分かったため、「隠す」から「見えていて意味が通る形」へ方針を
変えた。文体を分ける提案はユーザーが却下（「アスナがまとめてくれたレポートみたいで好印象。
ただ、見せ方の問題だね」）。

**そのあと要件を掘り下げた結果、表示をすべて HTML 化する方針に変わった。**
決め手はユーザーの「ターミナルにこだわるより、キャラと一緒に仕事できる状況が重要」。

- **T-004・T-006 を不要として閉じた**（どちらも本文に「やらずに閉じる」逃げ道があった）
- **T-002・T-009・T-010・T-011・T-012 を HTML 前提に書き直した**
- **T-013・T-014・T-015 を新設した**
- `src/balloon.ts` / `src/draw.ts` は役目を終える（T-013 で削除）。**今日作ったばかりだが、
  折り返し・全角幅・禁則処理はブラウザがやる**。`src/transcript.ts` / `src/state.ts` /
  `src/expression.ts` は表示方法に依存しないので残る

### 2026-09-09 HTML ビューの基盤（T-013）

**T-013 完了。** ターミナル描画（`src/balloon.ts` / `src/draw.ts`）を削除し、`127.0.0.1` にだけ
バインドする HTTP サーバから3つのビュー（メイン／キャラ／サイドバー）を配る形にした。
実装は opus に委譲し、受け入れはメインで実施。62 pass / 0 fail。

- **ホスト依存の操作のポート**を `src/host.ts` に定義（`openPane` / `showView`）。
  アダプタは `src/orca-host.ts` の1つだけ。**インターフェースに Orca の語彙は無い**。
  配置は `horizontal`/`vertical`（ツールごとに意味が逆になる語）ではなく `beside`/`below`
- **更新は SSE で押す**。ポーリングの遅延と無駄な再読み込みを避け、メインビューの
  スクロール位置を飛ばさないため
- ビューのポートは既定 7327（`TSUKUMO_VIEW_PORT` で変更可）

**受け入れで自分で確かめたこと**: `127.0.0.1` → 200 / LAN IP → 接続不可 /
`lsof` が `TCP 127.0.0.1:7327 (LISTEN)`。書き込み系APIは `src/` `scripts/` に0件で、
**会話がディスクに落ちる経路が無い**。最初 `0.0.0.0` 宛で 200 が返って驚いたが、
**macOS では `0.0.0.0` 宛の接続がループバック扱いになるためで、テスト側の不備**だった。

**副産物の削除**: `scripts/probe-terminal-width.ts` と `scripts/probe-balloon-rows.ts` も消えた。
前者は立ち絵が `<img>` になって消費者が無くなり、後者は `balloon.ts` を import していたため。

**残る割り切り**: ループバックに配る以上、**同じマシンの他のプロセスからは無認証で読める**。
単一利用者の開発機を前提とした割り切りとして `docs/architecture.md`「既知の制約」に明記した。

### 2026-09-09 セリフと詳細の切り出し（T-010）

**T-010 完了。** `src/transcript.ts` に `splitUtterance()` を追加し、1つの発話を
セリフ（引用行）と詳細（それ以外）に分けられるようにした。実装は sonnet に委譲し、
受け入れはメインで実施。69 pass / 0 fail。

- **コードブロック内の `> ` は拾わない**（``` の開閉を追跡する）。テストで固定
- **引用が複数箇所にあるときは出現順に全部連結する**（後の引用が前を上書きしない）。
  セリフは役割で書き分けられるので、冒頭の掛け声と締めの報告はどちらも残すべきという判断
- **セリフが無い発話では `speech` に `undefined` を返すだけ**。「直前のセリフを出し続ける」
  記憶は表示側の責務にして、切り出す側に状態を持たせない
- 行頭に空白があるインデント付き引用は拾わない（規約が「`> ` で始まる行」と literal に
  書かれていることに合わせた）

`src/index.ts` は未変更なので、**ビューの見た目はまだ変わっていない**（セリフだけを出すのは
キャラビュー側の担当）。

### 2026-09-09 立ち絵の素材（T-002）

**T-002 完了。** `characters/tsukumo-spirit/` に**自作のマスコット**（付喪神モチーフの精霊）を
SVG 4表情ぶん置いた。`character.json` が表情→ファイル、衣装→差し色の対応を持つ。

**リポジトリが公開されているため、権利のある画像をコミットしない置き方にした。** 利用者の
素材は `characters/local/`（`.gitignore` 済み。`git check-ignore` で無視されることを確認）。
ブルーアーカイブのキャラクターを描いた絵は**作っていない**（公開リポジトリでは権利の問題が出るため）。

**表情は「あるものだけ」で動く。** 見つからない表情は `default` に落ちるので、1枚から始めて
増やせる。衣装は画像を増やさず**差し色**で表す（4表情×3衣装＝12枚を要求しないため）。

**実装に効く発見**: **差し色が効くのはインラインで埋め込んだ SVG だけ。** `<img>` で読み込んだ
画像は独立した文書として扱われ、ページ側の CSS 変数が届かない。**キャラビュー（T-014）は
`<img>` ではなくインライン `<svg>` で埋め込む必要がある。** ラスタ画像を使う場合は衣装も
ファイルを分けることになる。`docs/requirements.md` 4.4 と `characters/README.md` に記録済み。

### 2026-09-09 サイドバー（T-012）

**T-012 完了。** 出すものは**ユーザーと合意して3つに決めた**: コンテキスト使用量 /
サブエージェントの状況 / タスクの進捗。**判断はメインで済ませ、実装だけ sonnet に委譲した**
（`docs/workflow.md`「方針決めはメインで済ませてから、決まった作業だけを委譲する」）。113 pass / 0 fail。

**決める前に候補が本当に取れるかを全部実測し、取れないものは1つも無かった。**
「context 残量が不明」は解決（`message.usage` の `input_tokens + cache_read_input_tokens +
cache_creation_input_tokens`。実測 630,127）。ただし**残量%を出すにはモデルの窓の大きさを
設定で持つ必要がある**（transcript には無い）。

**出さないと決めたもの**: 経過時間（席を外した時間も入り誤解を招く）、実行中のモデル / effort
（衣装と重複）。

**受け入れで1度差し戻した。** 最初の実装はサブエージェントの区画が「Bash ×5」でどれが何か
分からず「状況」になっていなかった。`meta.json` の `description` と `model` を出すよう直させた。
**副産物としてモデル名が見えるので、`difficulty` どおりに委譲できているかが一目で分かる。**

**「走っている／終わった」のラベルは付けていない**（ファイルから確実に判定できないため）。

### 2026-09-09 キャラビュー（T-014）

**T-014 完了。** 立ち絵（インライン SVG）と吹き出し（セリフのみ）が同じ領域に出るようになった。
実装は sonnet に委譲し、受け入れはメインで実施。144 pass / 0 fail。

**受け入れで鎖がつながることを確認した**: 状態ファイル（`PreToolUse` / `claude-opus-5`）→
表情「作業中」の SVG が選ばれ、差し色 `#ffb3a7`（戦闘配置）が入る。同時に transcript →
`splitUtterance` → **吹き出しにはセリフだけ**（詳細は混ざらない）。

- **SVG はインラインで埋め込む**（`<img>` だと `--outfit-accent` が届かない）。
  実装は拡張子だけでなく**中身が `<svg` / `<?xml` で始まるかも見て**いる
- 利用者の素材は `TSUKUMO_CHARACTER_DIR` でディレクトリを指す。既定は `characters/tsukumo-spirit`
- ラスタ画像は data URI で埋め込む（配信経路を増やさないため）。大きい画像だと本文が膨らむ
- `src/expression.ts` の `describeStatus`（立ち絵ができるまでの暫定テキスト表示）は削除され、
  alt テキスト用の `expressionLabel` に置き換わった

**端末での目視確認はまだ**（狭い領域での立ち絵と吹き出しの収まりを見てもらう必要がある）。

### 2026-09-11 質問の表示と、ターミナルを自分で持つ方針（T-030 / T-031 / T-032〜）

**T-031 完了。** 追従先ファイルを**セッションの cwd ごと**に分けた（`~/.tsukumo/targets/`）。
共通の1ファイルだと、別のリポジトリで claude を起動しただけで奪われ、**元のディレクトリで
サイドカーが起動すらできなくなる**（ユーザーが実機で踏んだ）。303 pass / 0 fail。

**T-030（質問の表示）は、表示は完成・回答は持ち越し。** 答え待ちの質問がある間、入力欄の領域が
質問に差し替わるところまでは動いている。**押して答える経路は、実測3回で否定された**:

1. `orca terminal send` → claude の端末には `agent_prompt_blocked` で届かない
2. `orca keypress`（`--worktree` 指定）→ **前面のペインにしか届かない**。何も起きない
3. `terminal switch` で前面にしてから `keypress` → **素のシェルには入るが、claude の端末には
   入らない**（質問中ですらない状態でも入らない）

**つまり Orca はエージェント端末への合成入力を弾いている。** こちらから覆せない。

**方針転換: ターミナルを自分で持つ**（ユーザーの提案「orca みたく1つのアプリケーションに
してしまって、必要なもの（ビューワー、ターミナル）を統合するのはどうかな」）。相談の結果、
**Electron で包むのは後回し**にして、**PTY を自前で持つところまで**を取りに行く。

**下調べ済み**（`scratchpad/pty-spike` で実測）:

- **Bun では `node-pty` が使えない**（読み込みと spawn はできるが `onData` が発火しない）
- **Node v22.13.1 では完全に動く**（TTY・`stty size` 40×120・`term.write` の注入まで成功）
- `bun install` は `spawn-helper` の実行権限を落とす（`chmod +x` が要る。踏むと
  `posix_spawnp failed`）

**副産物**: `orca` の失敗理由を**形で許可**して出せるようにした（小文字とアンダースコアだけの
短い1語＝エラーコードの形だけを通す）。これが無ければ `agent_prompt_blocked` は
「終了コード 1」のままで、原因に辿り着けなかった。

**T-032（実験）完了。方針転換は成立する。** `scratchpad/pty-spike` に Node + node-pty + ws +
xterm.js の最小構成を書き、Orca のタブで `claude` を動かした。**TUI は崩れずに描け、サイズも
伝わり、キー注入（文字・日本語・Enter・Esc・矢印）が全部届いた。** IME での直接入力だけ未確認。

決まったこと:

- **Node は端末ホストだけを別プロセス**にする（Bun のテスト資産307件をそのまま保てる）
- 端末の描画は **`@xterm/xterm` を `vendor/` へ同梱**（同梱の仕組みは T-021 で作ってある）
- **WebSocket は端末ホストが自分のポートで持つ**。**端末のバイトをサイドカーに通さない**
  （会話内容が流れる経路を増やさないため）

踏んだ罠: `bun install` は `node-pty` の `spawn-helper` の実行権限を落とす（`chmod +x` が要る。
踏むと `posix_spawnp failed`）。HTML 属性に `\r` と書いても2文字の文字列にしかならない。

### 2026-09-11（2）方針の全面見直し（grilling Q1〜Q16）

ユーザーの言葉: 「今までの履歴を見れば分かる通りあまりうまくいっていない。なので改めて tsukumo の
要件を詰めたいのと、どの技術でそれが実現できるか一緒に考えてほしい」。`/grilling` で 16 問を
詰めた。決定の全文は `docs/history/direction.md` 2026-09-11。要点:

- **合格シーン**: 1コマンドで1画面。依頼を打つとキャラが話し、レポートが出て、質問や許可に
  画面のボタンで答える。**Claude Code の TUI は一度も見ない**
- **TUI を捨て、Agent SDK で tsukumo が Claude Code を動かす**（transcript 追従・hook・追従先の
  乗り換え・Orca 経由の送信は役目を終える）。**セリフは MCP ツール `speak(text, expression)`**
- 箱は当面 Orca のタブ。最終形は使い勝手が上がるならスタンドアロン。**Bun のまま**
- メインビューはレポートだけ（リッチのまま。highlight.js / mermaid / Chart.js は残す）。
  進行はサイドバーへ。本文はリアルタイムに流す
- 順番: スパイク → 縦1本 → 横に広げる → Electron の判断

**一次情報で確認した事実**: SDK は構造化イベント・`canUseTool`（許可と AskUserQuestion）・
ストリーミング入力と中断・スキル・`.claude/commands`・hook・MCP・Bun を公式に支える。
**コマンドは `/<name>` を入力文字列に書けば送れ**、一覧は `system/init` の `slash_commands` に
並ぶ。使えないのは対話端末が要るもの（`/theme`、おそらく `/loop` `/rewind`）。**未確認は
「出力スタイルが SDK で効くか」だけ**（T-036 で確かめる）。headless CLI は中断できず
`--permission-prompt-tool` が手元の版に無いので採らない。

**片付け**: PTY 路線の未コミット分は `git stash`「PTY路線（T-032後の未コミット分。方針転換で
不採用）」に退避（削除は auto モードの安全弁に止められたため）。`done` 9件と閉じた5件
（T-026 / T-029 / T-030 / T-033 / T-034）をアーカイブし、T-035〜T-046 の12件を登録した。

**T-035 完了。** 正典5ファイルを新方針に書き換えた（opus に委譲）。副産物として
`docs/architecture.md` の「通読しない … まだ」の `sed` の例に、別の節（外部ライブラリの同梱）の
本文が流し込まれていた事故を直し、その節を「設計判断」に復旧した。用語集に「箱」を足した。

**T-028 完了。** `develop/tasks.json` の全タスクに `summary`（全角40文字以内、必須）を足した。
サイドバーのタスク一覧（T-043）はこれを読む。

**T-036 完了。スパイクは4点とも成立。** `scratchpad/sdk-spike/spike.ts`（`bun spike.ts init|speak|perm|ask|interrupt`）で実測:

- **出力スタイルは SDK でもそのまま効く**（`init` の `output_style` が `"Asuna"`、応答も規約どおり）
- **speak ツール**（`createSdkMcpServer` でプロセス内定義）は指示すれば呼ばれる。モデルは先に
  `ToolSearch` で `mcp__tsukumo__speak` を引いてから呼んだ（MCP ツールは遅延ロード）
- **`canUseTool`** に Bash（書き込み）と `AskUserQuestion` が届く。質問は `updatedInput.answers`
  で答えると会話が続く。`ls` のような読み取り専用は Claude Code 側が自動で通す（`default` でも）
- **`permissionMode: "auto"` がある。** `dontAsk` もある
- 補足: `slash_commands` は60件で **`loop` も入っている**（T-045 の補完候補に出せる。TUI 専用か
  どうかは実際に送ってみないと分からない）。`terminal_slash_commands` に端末専用のものが別途
  並ぶ。`interrupt()` で中断できて、そのターンの `result` は `error_during_execution`。
  **`init` はプロンプトを送るたびに届く**（新セッションの合図にしない）。`Query` に `setModel` /
  `setPermissionMode` がある

踏んだ罠: 中断の実験で、追加入力のターンにも中断をかけ直すループを自分で作ってしまい、SDK の
子プロセスが1つ孤児になった（`kill` で回収）。**`query()` を止めるときは子プロセスまで
落ちたか `pgrep -f claude-agent-sdk` で確かめる。** 1ターンあたりの費用は $0.3〜0.5
（この環境の CLAUDE.md と hooks を全部読むため）。

**T-037 完了。SDK 駆動の核が入った。** `src/session-driver.ts`（SDK を import する唯一の場所）・
`session-event.ts`（SDK メッセージ → 内部イベントの純粋変換）・`session-view.ts`（イベントの畳み込み）・
`pending-answer.ts`（`canUseTool` の答え待ち）を追加し、`src/index.ts` の起動を transcript 追従から
駆動へ差し替えた（旧モジュールは残っているが呼ばれない）。`POST /api/prompt` を最小で足した。
**目視で確認済み**: Orca のタブで依頼を送ると、吹き出しに `speak` の完了報告（表情 proud）、
メインビューに本文が出た。**本文に書かれたセリフはメインビューに出る**（モデルが締めの一言を
本文に書いた）。これは出力スタイルが旧規約のままだからで、T-041 で「本文にセリフを書かない」
まで規約に入れる。`test/index.test.ts` の CLI 起動 E2E 24件は撤去（起動すると本物の claude が
立ち上がるため）。テスト全体が 22 秒 → 0.3 秒に。

**T-038 完了（目視済み）。** 入力欄の送り先を `/api/prompt` に付け替え、`/api/interrupt` と
進行状態の SSE（`/events/turn-status`）を足した。送信中はボタンが「中断」になる。送り先の一覧は
UI から外した（経路はサーバに残置、撤去は旧経路の撤去タスクで）。ユーザーが Orca のタブで目視済み（右下が入力欄＋送信だけ、送ると「中断」に変わる）。

**T-039 完了（目視済み）。** 吹き出しは同ターン内の直近3件の `speak` を並べ、speak が無い
ターンだけ行頭マーカーの切り出しを補助に使う。メインビューはターンの本文だけ（ツール系は
サイドバーへ回す前提で除外）、直近20ターンの窓。書きかけの本文はそのまま流れ、レンダラは未閉の
コードブロック・表・HTML タグでも例外を出さない（テストで担保）。ユーザーが Orca のタブで目視済み（本文が段々書き足され、吹き出しに着手と完了の2行）。**申し送り**: 行頭マーカー `アスナ: ` の既定値が `src/transcript.ts` の定数に
なった（環境変数 `TSUKUMO_SPEECH_MARKER` の読み取りは駆動への差し替えで消えた）。原則4に照らすと
キャラクター定義側へ移すのが筋で、出力スタイルの改訂か旧経路の撤去のときに扱う。

**T-040 完了（目視済み。ただし置き場所は不採用）。縦1本がつながった。** 許可と質問の答え待ちを吹き出しの直下の箱に
出し、ボタンで `/api/answer` → 駆動の `answer` を呼ぶ。許可モードはキャラビューの `select` で
`/api/permission-mode` → `setPermissionMode`（`bypassPermissions` は警告色）。旧 `question` ビューは
撤去した。**目視で確かめること**: Bash を使う依頼で吹き出しの下に許可／拒否が出る、許可で会話が
続く、質問の選択肢を押すと会話が続く。`Host.pressKey` と `transcript.ts` の旧 `PendingQuestion` は
呼ばれないまま残っている（旧経路の撤去タスクで消す）。**ユーザーの目視: 動きは良いが、左下の
キャラの下に出すのは使いづらかった**（気づかない・入力欄と離れている）。右下の入力欄の上へ移す
（T-049）。

**T-047 完了。** `bin/tsukumo` と `package.json` の `bin` を足し、同梱物（`vendor/`・既定の立ち絵）の
置き場所を `src/bundled-files.ts` に集約して cwd から独立させた。空のディレクトリから
`bun bin/tsukumo` で起こして立ち絵が出ることを確認。グローバルへの導入（`bun link`）は T-048 で
承認を得てから。

**T-043 完了（目視は未実施）。** サイドバーを3区画に書き直した（いま何をしているか／`develop/tasks.json`
のタスク一覧／セッション情報。モデルと許可モードの `select` はここ）。`/api/model` を追加。ツール入力の
要約は `summarizeToolInput` に1箇所化。**目視で確かめること**: 3区画が並ぶ、実行中は普通の色で完了は
薄い色、モデルを変えると次のターンから変わる。

**T-049 完了（目視は未実施）。** 答え待ちの箱を右下の入力欄の上へ移した（`/events/pending-answer`）。
答え待ち中は入力欄の枠が注意色になり、タブのタイトルに「● 」が付く。キャラビューは立ち絵と
吹き出しだけに戻った。**目視で確かめること**: 許可待ちで入力欄の上に許可／拒否が出て枠が黄色く
なる、押すと消えて戻る、左下には出ない。

**T-044 完了（目視は未実施）。** 表情は直近の `speak` の引数が主で、ツールが1秒以上走っていると
き だけ自動で `working`（`src/expression.ts` が正典。時刻は配線層から渡す）。`state.ts` への依存は
消えた。`default` と `working` は定義ファイル側の必須の名前（`characters/README.md`）。

**T-045 完了（目視は未実施）。** 入力欄で `/` を打つとコマンドの候補が出る（`GET /api/commands`。
`init` の `slash_commands` から端末専用を除いたもの、57件）。**目視で確かめること**: `/ne` で絞れる、
Tab か Enter で埋まって送信されない、候補が閉じているときの Enter は送信のまま。
サブエージェントが利用上限（1:40 リセット）で1回止まり、差分を残したまま再開して仕上げた。

### 2026-09-12 出力スタイルの改訂と、`/loop` の残り

**T-087 完了。** `src/index.ts` を **635 → 231行**にして、配線だけの場所にした。`usecase/` へ
`throttle.ts` / `event-sink.ts` / `view-publish.ts`、`infrastructure/` へ `character-asset.ts` /
`task-summary.ts` / `auto-open-view.ts` / `browser-bundle.ts` を新設。**層の規則を満たすために
依存を注入する形にした**（`event-sink` は `now()` を引数で受け取り `PendingAsk` と
`onSessionEnded` を返す、`view-publish` は `buildCharacterBody` などの描画関数を注入される）。
テストは 382 → 415 pass / 0 fail（新規33件。移す前に書いた。従来の `test/index.test.ts` は
CLI の起動失敗しか見ていなかった）。実機（7434 / 7435番）で1往復し、メインビューへの押し出しと
`turn-status` の `null → turnStartedAt → turnFinishedAt` を確認。**経過時間はサイドバーではなく
入力欄で `turn-status` から描かれる**作りだった（タスク本文の記述と違ったので記録）。

**T-089 完了。** 4層への移動で残っていた `docs/` の旧パス8箇所（`docs/requirements.md` 5 /
`docs/glossary.md` 2 / `docs/workflow.md` 1）を新しいパスへ直し、`test -f` で実在を確認した。
節の数は3ファイルとも不変。**`docs/architecture.md` の11箇所は直していない**（`transcript.ts` /
`state.ts` / `balloon.ts` / `draw.ts` など、撤去済みファイルについての過去の記述だから）。

**T-085 完了。** CSS の巨大な `STYLE` 定数を `src/presentation/style/` の7ファイル＋入口
`main.css` に割り、**`/assets/style.css` の `<link>` で配る形**にした（インラインの `<style>` は
撤去）。組み立ては `browser.js` と同じ工程（起動時に `bun build` を子プロセスで起こし、
メモリに持つ。失敗したら起動を止める）。**`src/presentation/view.ts` は 2320 → 1604行**
（716行減）で、**T-084 と合わせると 3279 → 1604行、半分以下になった**。CSS を検査していた
テストは出どころを `.css` に付け替えて中身を減らさず維持し、経路のテストを足した
（380 → 382 pass / 0 fail）。見た目は機械で確認（Chrome headless ＋ CDP。grid の3トラック、
立ち絵の接地が 1px 差、サイドバー3区画の内側スクロール、送信ボタンの `::after` が `⌘⏎`）。

**T-086 完了。** `src/` の18ファイルを `domain` / `usecase` / `presentation` /
`infrastructure` の4層へ `git mv` で移し、**`test/architecture.test.ts` で依存の向きを縛った**
（`node:fs` と正規表現だけ。外部ツールは増やしていない）。**型検査に出ない2箇所を手で直した**:
`index.ts` の `bundledFilePath("src", "browser", …)` と、`bundled-path.ts` の
`new URL("..")` → `new URL("../..")`（1階層深くなったため。基準はリポジトリのルートのまま）。
検査の実効性はメインセッションでも再確認した（`domain` → `presentation` の辺を足すと違反の行を
出して落ち、復元で緑に戻る）。改名2件（`tasks.ts` → `domain/task-summary.ts`、
`bundled-files.ts` → `infrastructure/bundled-path.ts`）も反映済み。`bun run check` は
380 pass / 0 fail、`bun build` は 30950 バイト、`docs/architecture.md` の節の数は 27 のまま。
**`docs/requirements.md` と `docs/glossary.md` に残る旧パスは対象外**にして
`develop/direction.md` にメモを残した（次のセッションで `/plan-tasks`）。

**T-084 完了。** `src/view.ts` に残っていたブラウザ側 JS 8関数をすべて `src/browser/` の `.ts` へ
移した（新規7ファイル。`viewScript` は T-082 で単体ページが消えて未使用だったので削除）。
**`src/view.ts` は 3279 → 2319行（960行減）**で、ページのインライン `<script>` は0になった
（残るのは `/assets/browser.js` と vendor 2本）。**サーバ側から渡す値は `data-` 属性に統一**
（T-083 の `data-event-path` に合わせた。経路8種とラベル2種。初期 HTML に出ている文言は要素から
読むだけにして属性を増やさない）。テストは 379 pass のまま0 fail で、**vm で文字列を実行していた
土台を、移した関数を直接呼ぶ形に書き換えた**。わざと型エラーを入れると `TS2322` で落ちることも
確認済み。実機（7433番、Orca のタブ）で6項目すべて動くことをユーザーが確認した。

**T-081 完了。** セッションを起こすときの既定を **Opus・effort `high`** にした
（`src/session-driver.ts` の `DEFAULT_MODEL` / `DEFAULT_EFFORT`）。`query()` の `options` は
クロージャを含まない部分を `buildQuerySeedOptions` に切り出し、**本物の claude を起こさずに
既定値を検査できる形**にした（テスト2件追加）。画面側の `MODEL_FALLBACK` も `opus` に揃えた。
**実測で分かったこと**: `model: "opus"` を渡すと `init.model` は `claude-opus-5`。一方
**effort は外から観測できない**（`init` に `effort` キーが無く、`setModel` はモデル名しか
取らない）ので、切り替えで保たれるかは不明。**分からないことを前提に effort を画面の要素に
しない**と `docs/requirements.md` 4.1 に書いた。`bun run check` は 379 pass / 0 fail。

**T-080 完了（決めるだけ。実装は `develop/direction.md` の3段階）。** アーキテクチャを
ドメイン駆動へ作り直すかを決めた。**結論は「層をディレクトリで表し、依存の向きをテストで縛る」**
（ユーザー選択）。ユーザーの困りごとは**「読みにくい・探しにくい」と「差し替えが怖い」**の2つで、
許容する規模は「ディレクトリ構成から変える」。層は `domain` / `usecase` / `presentation` /
`infrastructure` の4つ（すべて**単数形**。`adapter` ではなく `infrastructure`、`view` ではなく
`presentation` はユーザーの指定）。**実測で分かった大事なこと**: `src/` 18ファイル・7322行に
import の循環は0件で、**いまの依存関係は許した辺をすべて満たしている**。だから段階1は `git mv` と
import の書き換えだけで、ロジックは1行も動かない。差し替えた原則は**原則2・原則3・原則5**の3つで、
`CLAUDE.md` と `docs/architecture.md` と `docs/coding-standards.md`（「層と依存の向き」を新設）を
同時に直した。**`view.ts` 3277行の分割は T-084 / T-085 の後**（1000行以上が外へ出てから割らないと
線が二度動く）。

**T-059 完了。** T-058 が決めた方式（tsukumo が `systemPrompt` の append で差分を足す）を実装した。
`src/report-notation.ts` に `REPORT_NOTATION_PROMPT` を置き、`startSession` の `query()` へ
`systemPrompt: { type: "preset", preset: "claude_code", append }` で渡す。**規約の正典がリポジトリ内に
来たので、描ける記法の一覧が `src/report-html.ts` と同じコミットで動く**。テスト
（`test/report-notation.test.ts` 3件）は**規約が名乗る要素をサニタイザが通すか**と、**名乗る class に
CSS があるか**を両側へ突き合わせる（文面だけが先に進むのを防ぐ）。グローバルの
`~/.claude/output-styles/asuna.md` は「レポートの組み立て方」だけを TUI 向けに差し替え（HTML と
mermaid/chart を削除、引用 `> `・水平線 `---` を解禁、ネスト1段の制限を解除）。バックアップは
`~/.claude/asuna.md.bak-20260912`、節の数は前後とも 11、`settings.json` は無傷。
**目視は両側で実施**: 素の TUI（day-snap）で HTML タグ・mermaid/chart が0件、tsukumo（7431番）の
メインビューで `.note` / `.badge` / `.cols` が枠・ラベル・横並びとして描かれることをユーザーが確認。
`bun run check` は 377 pass / 0 fail。

**`develop/tasks.json` の todo 13件に `loopable` を入れた。** 各タスク本文の「注意」が既に書いていた
判断を固定しただけで、決め直してはいない。**`Y` は T-085 の1件のみ**で、残り12件は方針決め・
グローバル設定の書き換え・実機確認のいずれかに当たる。`/loop /next-task` で進むのは T-085 だけ。

**T-058 完了（決めるだけ。実装は T-059）。** 素の TUI にレポートの HTML タグが漏れる件の方式を
決めた。**tsukumo が `systemPrompt` の append で HTML・mermaid・chart の規約を足し**、グローバルの
`~/.claude/output-styles/asuna.md` は TUI 向け（HTML 無し・引用/ネスト/水平線は解禁）に保つ
（ユーザーの選択）。**実測で2つ確かめた**: append と出力スタイルは同時に効く（`init` の
`output_style` は `"Asuna"` のまま人格と append の両方が出る）、`applyFlagSettings({outputStyle})`
もセッション限りで効き設定ファイルは不変（SDK v0.3.268）。**スタイルを2ファイルに分ける案は、
共通部分113行が重複する**（tsukumo 専用は141行中28行）ので採らなかった。書き換えの承認範囲は
`asuna.md` 1ファイルのみで、`settings.json`（orca の hooks / statusLine）には触らない。
`bun run check` は 374 pass / 0 fail。

**T-083 完了。** ブラウザ側 JS を `.ts` へ出すビルド工程を作り、1本目（SSE の購読）を移した。
**起動のたびに `bun build` で組み立て、ディスクに置かない**（ユーザーの選択。`execFile` の標準出力を
メモリに持ち、`/assets/browser.js` で配る）。置き場所は `src/browser/` で、**入口 `main.ts` だけが
副作用を持ち**、仕組みの側は関数を公開するのでテストから import できる。領域は `data-event-path` で
購読先を示す。**型を分ける必要は無かった**（`@types/bun` がブラウザの型を持つ）。**検査が届くことは
わざと壊して確認**（型エラーで `tsc` が3件、未使用変数で `oxlint` が検出）。`bun run check` は
374 pass / 0 fail。CDP で外に出した購読が実際に効くことも確認。**claude を起こす確認は未実施。**

**T-079 完了。** 段落の途中・表のセル・箇条書きの項目に書いた HTML が生タグで出ていた件を直した。
`renderPlainInline` が `escapeHtml` ではなく **`sanitizeReportHtml` を通す**形にして、行頭からの
HTML ブロックと同じ許可リストを共有する（サニタイズは1箇所のまま）。コードスパンは
`splitOnCodeSpans` で先に切り出し、中身を HTML として解釈しない。`bun run check` は 372 pass / 0 fail。
**安全側は攻撃的に実測**（`<script>` / `<img onerror>` / `<iframe>` は落ちる、`onclick` と `style` は
剥がれる、`javascript:` の href だけ落ちて `<a>` になる、属性値の中の `**` は太字化されない）。

**T-075 完了。** 経過時間をサイドバーから**入力欄の送信ボタンと同じ行**へ移した。経路は
`TURN_STATUS_EVENT_PATH` に相乗りし、ペイロードを真偽値から `{turnStartedAt, turnFinishedAt}` の
JSON に変えて、「進行中か」はブラウザ側で導出する（冗長な状態を持たない）。書式（`N秒` /
`M分SS秒`）とラベルの出し分けは T-055 のまま。サイドバー向けの `publish` がこの2値を運ばなく
なったので `PublishState` ラッパー型も廃した。`bun run check` は 367 pass / 0 fail。CDP で
進行中「経過 5秒」＋「中断」→ 終了後「所要 3秒」＋「送信」→ その後は増えない、を確認。

**T-074 完了。** 依頼の見出しに**全行を既定で見せる**ようにした。1行なら `<h2>`、複数行なら
`<details open>`（`<summary>` に1行目、中に2行目以降で重複なし）。全文は 2000 文字で切り、
`max-height: 40vh` で画面を埋めない。**サブエージェントは既定で畳んだ `<details>` にしていたので
受け入れ時に直した**（クリックが1手増えるだけで「1行しか出ない」という指摘が残るため）。あわせて
「タブのラベルが依頼の文面を切り詰めている」という**事実と違うコメント**も直した（`turnTabHtml` が
出すのは「今回」「1つ前」だけで、`truncateRequest` は誰も使っていなかったので削除）。
`bun run check` は 364 pass / 0 fail。CDP で既定 `open: true` と2行目以降が見えることを確認。

**T-073 完了。** 質問に**自由入力欄を常に1つ出す**ようにした（モデルが選択肢に `その他` を入れて
きたときは二重に出さない）。`questionCardHtml` が末尾に足す形で、中身は `freeTextOptionHtml` に
切り出して `questionOptionHtml` の `その他` 分岐と共有する。**multiSelect は既存の配線のまま**で、
選んだラベルと自由入力は「、」で結合されて1つの label になる。`bun run check` は 362 pass / 0 fail。
確認は合成データの機械検証（`question-other-input` の数と、CDP で「答える」を押したときに届く値）。

**T-082 完了。** 個別ビューのページ（`/main` `/character` `/sidebar`）と `/` のリンク一覧を消し、
**レイアウトページを `/` で配る**ようにした（`LAYOUT_PATH` が `"/"`）。消えたのは `viewPath` /
`buildViewPage` / `buildIndexPage` / `VIEW_TITLE` / `ViewServer.urlOf` / 起動ログの「個別ビュー・
デバッグ用」3行。**SSE の経路（`/events/*`）は無傷。** テストは削除ではなく `buildLayoutPage` ベースへの
**書き換え**で、`it()` の数は 164→164 / 36→36 のまま（359 pass / 0 fail）。経路は使い捨てサーバ
（7403）で実測: `/` が 200 で3領域を含み、`/layout` を含む旧経路は 404、`/events/*` は
`text/event-stream` で 200。**実機の目視は未実施で、開きっぱなしの `/layout` のタブは開き直しが要る。**

**T-070 完了。** Idiomorph 0.8.0（0BSD、10587バイト）を `vendor/` に同梱し、`subscriptionScript` の
`el.innerHTML = event.data` を `Idiomorph.morph(el, event.data, { morphStyle: "innerHTML" })` に
替えた。**外したのは差し替え前後の scrollTop の保存・復元だけ**で、「いちばん下から24px以内なら
追従」（morph では再現されない別の意図）と「本文が同じなら差し替えない」ガードは残っている。
中身は素の IIFE なのでグローバルで読み、highlight.js と同じ「常に読む」扱い。**目視は合成データの
使い捨てサーバ（7401）を立てて Chrome DevTools Protocol で機械的に確認**: タスク一覧を
scrollTop=150 にして SSE の更新を3回またいでも 150 のまま、同時に実行中の行は更新されていた
（差し替えは起きている）。`bun run check` は 359 pass / 0 fail。**実機の目視は未実施。**

**T-069 完了。** 描画の技術を決めた。**フレームワークは入れない**（React / Preact / Lit /
Next.js / htmx / Turbo / Datastar）。ユーザーに確かめた不足は (a) DOM の状態が飛ぶ (b) ブラウザ側
JS 812行が文字列の中で検査が効かない (c) CSS が約700行の1定数 の3つで、**Markdown の記法と
レポートの表現力は不足に挙がらなかった**。ビルド工程の追加は許容を得た。層ごとに
**層1=Idiomorph 0.8.0 を同梱して `innerHTML` を morph に / 層2=ブラウザ側 JS を `.ts` へ出して
`bun build` / 層3=自前の `renderMarkdownToHtml` を保つ / 層4=CSS を `.css` に割って同じ工程で
まとめる**と決めた。決め手は「**この一族が解く問題（遷移・フォーム・部分更新の push）のうち
tsukumo が持っているのは push だけ**で、残るのは押された HTML を DOM に当てるところ＝一族が
どれも内部でやっている morph そのもの」。比較は `docs/research/view-rendering.md`、決定は
`docs/architecture.md`（節数 25→26）。実装3件は `develop/direction.md` へ。

**T-072 完了。** 入力欄を **Enter で改行・Command+Enter で送信**に入れ替えた（ユーザーの指示で
T-051 の「Enter で確定して送信」を覆した）。`/` 補完の候補が開いている間の Enter は Tab と同じ
**確定だけ**にし、送信は Command+Enter に一本化。送信ボタンには `⌘⏎` を添えるが、**ラベルの
`textContent` とは分けて `data-shortcut` 属性に持たせ**（初期 HTML にも入れる）、CSS の
`.dispatch-send[data-shortcut]::after` で描く。こうしないと「送信／中断」の切り替えを
`textContent` の一致で見分けられなくなる。中断のときは `applyButtonLabel` が属性ごと外す。
`bun run check` は 357 pass / 0 fail（353 から +4）。**目視は合成データの静的ページで実施**
（Chrome headless 1440x900。「送信 ⌘⏎」と新しい placeholder を確認）。**実機の目視は未実施**
（Enter で改行・Command+Enter で送信・進行中に記号が消える、の3点）。

**T-041 完了。** 出力スタイル（`~/.claude/output-styles/asuna.md`）を「セリフは `speak` で言い、本文に
セリフを書かない」規約に差し替えた（承認済み、バックアップあり）。ユーザーの指摘（「質問は
AskUserQuestion で出して…経路は生きてるね」のようなセリフがメインビューに出る）は、モデルが旧規約の
まま本文にセリフを書いていたのが原因。保険として、`speak` ありのターンでも本文の行頭マーカー行は
吹き出しへ回すようにした（T-039 の決定を1つ覆した）。実機で着手と完了の `speak` が2回、本文の漏れ 0。
**注意: 出力スタイルはセッション起動時にしか読まれない。** tsukumo を起こし直せば新しい規約で動く。

**T-020 完了。** 出力スタイルに「レポートの組み立て方」（内容の種類→使う構造の表、冒頭は結論、
1ターンのレポートは最後に1つ）を承認を得て追記し、決定を `docs/requirements.md` 4.2 に記録した。
レンダラ側の宿題は Markdown の引用 `> `（規約が許すが描けない。規約側で `<blockquote>` に寄せた）。
**出力スタイルはセッション起動時にしか読まれない**ので、tsukumo を起こし直すと効く。

**T-050 完了。** `/` 補完の候補一覧をポップアップにした（`src/view.ts`）。候補は textarea と同じ包みに入れ、
**textarea の下端に底を合わせて中に重ねる**（サブエージェントは form の上に `bottom: 100%` で出したが、
それは領域の `overflow-y: auto` に切られて見えないので差し替えた）。mousedown で確定、選択は
`scrollIntoView` で追従。目視は未実施（ユーザーが後で行う）。

**T-051 完了。** `/` 補完を前方一致→部分一致（各アルファベット順、合計10件）にし、候補が開いている間の
Enter を「確定して送信」、Tab を「確定だけ」にした（`docs/requirements.md` 4.2 に決定を追記）。
目視は未実施（ユーザーが後で行う。`/co` + Enter でターンが始まるか）。

**T-052 完了。** `/` 補完の候補に説明を添えた。frontmatter から引けるのは 11/57 で閉じる基準だったが、
**SDK の `supportedCommands()` と `commands_changed` が組み込み分も含めて名前と説明を返す**ので、
そちらを出どころにした（tsukumo 側に説明の表もファイル読み取りも持たない。取れなければ名前だけに戻る）。
**組み込みコマンドにも説明が出るかは型定義からの推定**なので、目視（`/cl` で `/clear` に説明が付くか）が要る。

**T-053 完了。** 吹き出しをセリフ1件ずつに分け、今のターンの分を `.balloon-list` に縦へ積んで最新を下に
見せる（DOM は新しい順＋`column-reverse` で、差し替え直後も最新が見える）。`request` で前のターンの
最後の1件だけ残す。並びの高さは領域から `height: 100%` で継いで `max-height: 100%`（サブエージェントの
`50vh` は下段の既定 40% を超えるので差し替えた）。目視は未実施（ユーザーが後で行う）。

**T-054 完了。** サイドバーの「いま何をしているか」を固定の高さ（10rem）の `.activity-scroll` で包み、
並びの中でスクロールするようにした（空のときも同じ高さ）。完了分の上限は 5→50 件。目視は未実施。

**T-055 完了。** サイドバーの経過時間を「依頼を送ってからそのターンが終わるまで」にした。終了時刻は
`src/index.ts` の配線が `turnFinishedAt` として持ち、ブラウザ側がそれを終点に固定してラベルを
「所要」に変える（書式は `N秒` / `M分SS秒`）。目視は未実施。

**T-056 完了。** サイドバーのタスク一覧を、status のバッジを先頭に置いた2列の行にし、見出しに
todo / done の件数を添えた。未使用だった `countTaskStatuses`（`src/tasks.ts`）は消した。目視は未実施。
**T-061 完了。** サイドバーの領域自体のスクロールを切り（`.layout-sidebar` を `overflow-y: hidden` の
flex 縦積みに）、3つの区画をそれぞれ内側でスクロールさせるようにした（`.sidebar-block-scroll`）。
高さは「いま何をしているか」「タスク一覧」が残りの2等分、「セッション情報」は中身なり。T-054 で入れた
`.activity-scroll` の固定 10rem は廃止（意図は flex の割り当てで保てる）。**目視はユーザー確認待ち**で、
claude を起こさず静的な HTML だけを配る使い捨てサーバ（7399）を Orca のタブに開いてある。

**T-048 完了。** 承認を得て `bun link` で `tsukumo` をグローバルに入れた（`~/.bun/bin/tsukumo`。
消すときはリポジトリ直下で `bun unlink`）。別プロジェクト（`day-snap`）から起こして、立ち絵が出て
そのプロジェクトの内容で答えることを curl で確認した。**Orca のタブでの目視は未実施。**

**T-042 完了。** 旧経路（transcript 追従・状態ファイル・hook・Orca 経由の送信）をコードごと撤去した。
`~/.claude/settings.json` の tsukumo の hook エントリ5件も**承認を得て**外した（バックアップあり。
orca の12件と statusLine は無傷）。`src/transcript.ts` の残り（`splitUtterance`）は `src/utterance.ts`
へ、`MainViewEntry` は `src/session-view.ts` へ移した。テストは 436→339 件（消したモジュールの分）。

**T-066 完了。** 最新の吹き出しをキャラビューの縦中央に据えた。`.balloon-list` を
`.balloon-anchor`（縦2段の箱）/ `.balloon-track`（上半分・`column-reverse`・内側スクロール）/
`.balloon-spacer`（下半分・空けたまま）に分け、**並びの下端＝最新の吹き出しの下端が領域の中心に
接する**形にした（中心を合わせるには高さを測る必要があり CSS だけでは決まらないので下端を基準に
した）。セリフが増えても最新の位置は動かない。決定は `docs/requirements.md` 4.2 と
`docs/glossary.md`「吹き出し」に記録。**目視は合成データのページで実施**（1512x900 / 1512x1400）。

**T-060 完了。** 既定ポート 7327 が塞がっていたら +1 して20個先（7346）まで順に試すようにした。
概念を `src/view-port.ts` に切り出し、`resolveViewPort` が「既定か明示か」を返す形にして、
**明示のときはずらさずそのまま失敗**させる。リトライは配線側（`src/index.ts`）に置き、
`src/view-server.ts` は触っていない。実機で 7327 が塞がった状態から 7328 へずれることを確認。
決定は `docs/requirements.md` 4.6 に記録。

**T-068 完了。** `/` 補完が起動直後に候補0件のままになる不具合を直した。**原因は想定と逆**で、
`init`（`session-info`。`slashCommands` の出どころ）は**依頼を送るたびに届く仕組みで開始直後には来ない**
（実測で約59.5秒後、最初の ping の直後）。一方 `supportedCommands()` 由来の `command-descriptions` は
起動から約1秒で60件届いていた。そこで `commandSuggestions` を「`slashCommands` が空なら
`commandDescriptions` を名前の出どころにする」形にし、ブラウザ側は0件をキャッシュせず次の `/` で
取り直すようにした。決定は `docs/requirements.md` 4.2 に記録。**この間だけ端末専用コマンドの除外が
効かない**（`init` 到達で除外込みに戻る）。

**T-067 完了。** サイドバーの「セッション情報」が見出しだけになる回帰（T-061 由来）を直した。原因は
`.sidebar-block-scroll` の `flex-basis: 0`。親（`.sidebar-block-session`）の高さが auto のとき、
内容サイズの見積もりが 0 になって中身が潰れていた。`flex: 1 1 auto` に変え、狭い画面（760px 以下）で
同じ理由で潰れる「いま何をしているか」「タスク一覧」も `flex: 0 1 auto` に揃えた。**目視は合成データの
ページで実施**（Chrome headless 1512x900。会話の中身は写していない）。

**T-057 完了。** 箱の選択肢（Orca のタブのまま／Electron／Tauri v2／素のブラウザのアプリモード／WKWebView／
その他）を一次情報で比べて `docs/research/app-shell.md` に記録した（結論は書かない。T-046 が読んで決める）。
一次情報で埋まらなかった10欄は「不明」と理由付き。手元は `cargo` / `rustc` / `rustup` 未導入、Xcode あり。
**これで `/loop` に載せられるタスクは無くなった**（残るのは感想が要る T-046 だけ）。
done 10件（T-020 / T-041 / T-050〜T-057）を `docs/history/tasks-archive.md` へ移した。

**T-071 完了。** キャラビューを作り直した。ユーザーの指示でスコープを「吹き出しの高さ」から画面全体の
再設計に広げ、合成データで3案を見比べて**案A（立ち絵を主役に、最新のセリフだけ強調）**を採った。
`.balloon-anchor` / `.balloon-spacer` を廃し、立ち絵は領域の高さいっぱい、並びは下端揃えで高さ全開。
最新（DOM 先頭）だけ大きく青枠＋尻尾、過去は小さく薄く。**目視は合成データのページで実施**
（1512x900 / 700x1000。セリフ5件が全部入る・立ち絵 110px→170px）。**実機の目視は未実施**
（tsukumo を起こし直すと効く）。

**吹き出しの位置関係を直した（指示への直接対応。タスク番号なし）。** 立ち絵は左下・最新の吹き出しは
右上の**対角**に置き、尻尾は吹き出しの左下の角から左下（立ち絵の側）へ向けて出す（2026-09-12
ユーザーの指示）。`.balloon-track` を `column-reverse` から `column` に変え（DOM は新しい順のまま
なので最新が一番上に来て、`scrollTop = 0` が最新を指す性質も保たれる）、`align-self: flex-start` /
`align-items: flex-end` で右上へ寄せた。領域の下端から浮かせる `--balloon-lift` は役目を終えたので
撤去。尻尾は `border-top` + `border-right` の直角三角形に作り直し、塗りの `::after` を右上へ 1px
ずらしたうえで一回り小さくして、左の辺と斜辺にだけ枠の色を残した。`docs/requirements.md` 4.2 と
`docs/glossary.md`「吹き出し」も差し替え（節数 25→25 / 36→36）。**目視は合成データのページで実施**
（Chrome headless 1512x900 と 700x1000。セリフの中身は合成）: 立ち絵が左下・最新が右上・尻尾が
左下向きで、尻尾の塗りと枠が出ていること、セリフ1件だけのときも尻尾が切れないこと、狭い画面の
縦積みでも破綻しないことを確認。`bun run check` は 353 pass / 0 fail。

**吹き出しの積み方と立ち絵の位置を直した（指示への直接対応。タスク番号なし）。** 上の「対角」の形は
ユーザーの再指摘（「最初の『発話がない』が右上すぎる」「過去の発言は上に押し上げる」「キャラはもう少し
下に配置」）で作り直した。`.balloon-track` を `column`・`align-self: flex-start`・
`align-items: flex-end` から `column-reverse`・`align-self: flex-end`・`align-items: flex-start` に
変え、**最新が一番下（立ち絵のすぐ隣）、過去は上へ押し上がる**形にした。アニメーションも
`balloon-push-down` → `balloon-push-up`（下から上へ）。立ち絵は `--portrait-drop: 0.75rem` ぶん
下へずらし、はみ出した足元は `.character-layout` の `overflow: hidden` で切る（領域自体に
スクロールバーを出さないため）。**尻尾の作り（`border-top` + `border-right` の直角三角形、`::after` を
ずらして口を開ける）はそのまま**で、最新が下端に来たぶん立ち絵との距離が縮んだ。
**目視は合成データのページで実施**（Chrome headless 1440x900。セリフは合成）: セリフ0件の
プレースホルダが立ち絵の隣・下端に出て尻尾が左下から出ること、6件では最新が下端で強調され過去5件が
上へ積まれて最古が並びの外へ流れること、立ち絵が枠の下端に接することを確認。
`bun run check` は 353 pass / 0 fail。**実機の目視は未実施**（tsukumo を起こし直すと効く）。

## 廃止した「次にやること」節（2026-09-16 に退避、当時の記述のまま）

**移行（`docs/design.md` 12章）は段1から段9まで完了した**（2026-09-14）。残っているのは
移行ではない26件。**`loopable: "Y"` が15件、`"N"` が11件**（2026-09-15 に判定の基準を
見直した。`docs/workflow.md`「`loopable` の判定」）。`"Y"` のうち依存が解けているのは
**T-046 / T-121 / T-122 / T-124 / T-128 / T-133 / T-134 / T-135 / T-140 の9件**で、これは
`/loop /next-task` でそのまま進められる。**残る一覧は `/list-tasks` が出す**（正典は
`develop/tasks.json` で、下の表は次の更新で落とす）。`"N"` は人にしか決められないものが残っているタスク。

| タスク                                                   | difficulty | 依存 | 着手 |
| -------------------------------------------------------- | ---------- | ---- | ---- |
| T-046 箱の方針（当面 Orca）を正典に落とす                | opus       | 済み | 可   |
| T-076 待っている間にメイン画面へ出すものを決める         | opus       | —    | 可   |
| T-115 レポートの書き込み位置にミニ立ち絵を添えるか決める | opus       | —    | 可   |
| T-116 TSUKUMO_CHARACTER を撤去するか決める               | sonnet     | —    | 可   |
| T-120 過去のターンで立ち絵の動きも再生するか決める       | opus       | 済み | 可   |
| T-121 README を現状に合わせ command not found を直す     | sonnet     | —    | 可   |
| T-122 プロジェクト外への依存を洗い出す                   | opus       | —    | 可   |
| T-124 立ち絵と差し色を画面から差し替える                 | opus       | 済み | 可   |
| T-125 新しいキャラクターパックを画面から作る             | opus       | 124  | 待ち |
| T-126 details が割れるのを直す（塊の分割）               | opus       | —    | 可   |
| T-127 前置きをレポートに出さないか決める                 | opus       | —    | 可   |
| T-128 補完の上下移動に Ctrl+P / Ctrl+N を足す            | sonnet     | —    | 可   |
| T-129 サイドバーのタスク一覧を作り直す                   | opus       | —    | 可   |
| T-130 Fable が通るか実測し、通れば選択肢に足す           | sonnet     | —    | 可   |
| T-133 ツールの行をレポートから消す                       | sonnet     | —    | 可   |
| T-134 質問の選択肢の説明を省略せず全部出す               | sonnet     | —    | 可   |
| T-135 質問と答えを記録に積み、見返せるようにする         | opus       | —    | 可   |
| T-136 表情を増やすかと足す表情を決める                   | opus       | —    | 可   |
| T-137 決めた表情を型・パック定義・speak ツールに通す     | sonnet     | 136  | 待ち |
| T-138 背景の置き場と受け取り方を決める                   | opus       | —    | 可   |
| T-139 決めた形で背景の差し替えを実装する                 | opus       | 138  | 待ち |
| T-140 `orca` の限定をテストで守る                        | sonnet     | —    | 可   |
| T-141 ホストを選ぶ口を作るかを決める                     | opus       | 046  | 待ち |

**T-046 / T-140 / T-141 は 2026-09-16 の方針から起こした**（「Orca で進めつつ、ドメインが特定の
技術に依存しないようにする」）。**T-046 は新規ではなく既存タスクの更新**で、元の問い（Orca の
タブのままか包むか）に第3の答えが返ったため、やることを「方針を正典に落とす」に変えた。
これで完了条件がユーザーの感想ではなくなったので、**`loopable` を `"N"` から `"Y"` にした**。
`orca` が `src/` のどこに出るかは実測済みで（アダプタ `src/core/orca-host.ts` と `src/cli.ts` の
配線とコメントだけ。`protocol` にも `ui` にも無い）、**方針は既存の原則3と同じ向き**だったので、
残るのは守りを固める2件だけになった。T-140 は「`orca` を呼ぶのはここだけ」がコメントにしか
無い件（`docs/research/architecture-proposal.md` の段1が足す検査と同じもの。段1を先にやるなら
その中に含めてよい）。T-141 は `cli.ts` が `createOrcaHost()` を直書きしていて選ぶ口が無い件で、
**作らない結論も十分ありうる**ため決めるタスクにした（実装が1つのうちに選択の仕組みを足すのは
「抽象を先に足す」に当たる）。

**T-134 / T-135 は同日の指示から起こした**（質問が `…` で見切れる件と、答えを見返せない件）。
**T-136〜T-139 は 2026-09-16 の指示から起こした**（表情を増やすか、画面の背景を
カスタマイズできるか）。どちらも**決めるタスクを前に置き、実装をその依存に置いた**（T-123 →
T-124 / T-125 と同じ形）。決めるほうは好みが要るので `loopable` は `"N"`。
T-134 は `.question-choice-description` の `white-space: nowrap` + `text-overflow: ellipsis` を
外す話で、**`docs/requirements.md` 4.2 の「省略記号で切る」という同日の決定の見直し**にあたる。
T-135 は**見返す先の部品（`<QuestionRecord>`）はあるのに記録を積む経路が無い**のが原因
（`SessionRecord` に `question` の枝が無く、`MainViewEntry` の `question` は production では
一度も作られない）。

**T-126〜T-132 は 2026-09-15 の指示から起こした。** 原因まで辿れているものが多い。

**T-133 は同日の指示から起こした**（作業中のコマンドを出さない）。`docs/requirements.md` 4.2 が
**「さらに戻して『レポートだけを出す』にするなら、直すのは `toolVisibility` の判定とこの段落」**と
予告していた差し戻しの最後の一歩で、T-114 で失敗したツールを寄せたあとに残っていた2種類
（ファイルを変えた操作・サブエージェントの起動）を落とす。**`MainViewStep.actions` に
ツールを積むのは残す**（T-127 が「そのステップにツール呼び出しがあったか」を材料に使う）。

- **T-126（`details` が機能しない）の原因は `splitReportBlocks`。** 空行で塊に割るので
  `<details>` の中で割れ、中身が `details` の外に出て常に見えていた。**フェンスには同じ対策が
  すでにある**（`inFence`）のに HTML ブロックには無い。規約の「塊の途中に空行を入れない」は、
  Markdown を HTML の中で解釈させるには空行が要るという CommonMark の規則と両立しないので、
  **規約ではなく実装で直す**
- **T-132（立ち絵が切り替わらない）の原因は URL の一致。** `characterAssetPath` はパックの
  名前を含まないので、`local`（アスナ）と `tsukumo` がどちらも `/character/default.png` になる。
  `<img src>` の文字列が変わらないため再取得が走らない。**つくもの精霊は `.svg` でインライン
  埋め込みの経路に入る**ので、そことの行き来では起きない
- **T-127（前置き）は T-119 の規約では抑えきれなかった分。** ツールの呼び出しという事実で
  判定する形（`toolVisibility` と同じ層）にするかを決める
- **T-129 は T-114 の影響も受けている。** 失敗したツールの出力がサイドバーへ入って
  「いま何をしているか」が重くなった一方、上2区画は残りの高さの2等分のままで、タスク一覧に
  使える高さは変わっていない
