# ドキュメントの棚卸し 2026-09-20

すべて対応済み（15件）。**編集は0件** — 15件すべてが「そのままが正しい」との判断。

## 検査ごとの件数（実行前 → 実行後）

| 検査                           | 実行前 | 実行後 |
| ------------------------------ | -----: | -----: |
| 1 索引の取りこぼし             |      0 |      0 |
| 2 索引のリンク切れ             |      0 |      0 |
| 3 先回りの空索引               |      0 |      0 |
| 4 相対リンク切れ               |      0 |      0 |
| 5 タスク運用節の形             |      0 |      0 |
| 6 実在しないパスの言及         |     11 |     11 |
| 7 置き場の逸脱                 |      1 |      1 |
| 8 正典の二重化                 |      3 |      3 |
| 9 索引の無い大きなドキュメント |      0 |      0 |
| 10 スキル参照の不一致          |      0 |      0 |

確定群は0件。前回（2026-09-16）の検査6は15件だったので、その後の作業で4件が解消している。

## 判断した結果、そのままが正しかったもの（15件）

### 検査6（11件）

- **`docs/architecture.md` の6件**（68/406/490/501/503/617行）: すべて経緯の記録。
  該当する節は冒頭に但し書きを持つか、記述自体が過去形・日付付きになっている。
  - 68行 —「**捨てる**」の表。「2026-09-12 に撤去済み」と本文に明記
  - 406行 — `#### 追従先は自前でスラッグ化せず、hookが書いたパスを読む`。節の冒頭に
    「この判断は 2026-09-11 の方針転換で役目を終えた。経緯として残す（以下は当時の記述）」
  - 490/501/503行 — `#### 表示はターミナル描画をやめて、すべて HTML にした`。
    「当初の判断（2026-09-08）」「捨てたもの」「残したもの」という当時の決定の記録
  - 617行 — `#### 描画にフレームワークを入れず、morph とビルド1段で足りないところを埋める`。
    節の冒頭に「この判断は 2026-09-13 の移行で役目を終える」。本文も「実際に採った形
    （2026-09-12 …）」と時点付き
- **`docs/research/app-shell.md` の1件**（`src/bun/index.ts`）: 誤検知。Electrobun の
  main プロセスの置き場所という**外部プロジェクトの説明**で、このリポジトリのパスではない
- **`docs/research/architecture-proposal.md` の1件**（`src/ui/app.tsx`）: 提案書の現状分析の表。
  調査時点（段7の前）の姿の記録
- **`docs/research/view-rendering.md` の3件**（`src/view.ts` / `src/index.ts` /
  `src/report-html.ts`）: 調査メモ。`src/view.ts` は 3206 行という実測値つきで、
  当時の姿を残すもの

### 検査7（1件）

`docs/architecture.md` — 前回と同じ誤検知。引っかかったのは他所（`docs/research/` の提案書）
への参照と、このファイル自身の役割の説明。提案の本文は混ざっていない。

### 検査8（3件）

3件とも前回と同じ判定で、いずれも二重化ではない。

| 重複した見出し・値 | 二重化でない理由 |
| --- | --- |
| 「7. 未決事項」 | `docs/requirements.md` は要件の未決事項（対応タスク列を持つ正典）、`docs/research/architecture-proposal.md` は提案が置いた仮定。扱う対象が違う |
| 「会話内容の扱い」 | `README.md` は利用者向けの説明、`docs/coding-standards.md` は開発規約の正典。読み手が違う |
| `bun run check` | 契約として読まれるのは「## タスク運用」節の `- 検証コマンド:` の1行だけ。他は「よく使うコマンド」の一覧・完了報告の IMPORTANT・CI/CD の説明という別の文脈 |

## 気づいたが、このスキルでは書かないこと

- **`docs/README.md` が無い。** `docs/` 直下に13ファイルあるが索引が無く、検査1は
  「索引が無い」ことを指摘しない（遅延作成の原則）。索引を書くのは
  `architecture-proposal` / `domain-modeling` / `research` の仕事で、このスキルは
  新しいドキュメントを出さない
- **タスク番号（`T-` + 3桁）の言及が `docs/` に13箇所ある**（`architecture.md` 4件・
  `requirements.md` 4件・`design.md` 5件）。`CLAUDE.md` の規約は
  「唯一の例外は `docs/requirements.md`「7. 未決事項」の対応タスク列」としているが、
  該当の4件はいずれも7章の外（本文中の経緯）。経緯の記録として残すか規約に寄せるかは
  中身の判断なので触っていない
- 前回挙げた `docs/workflow.md` の `summary` の目安（全角40文字以内）と `status.py` の
  `long_summary`（80桁で警告）のズレは、今回も未処理

## コマンド

- `bun run format`: 実行済み（差分なし）
- `bun run check`: **708 pass / 0 fail**
- 検査の再実行: 件数は変わらず（確定群0 / 候補群15）。新しい指摘は増えていない
