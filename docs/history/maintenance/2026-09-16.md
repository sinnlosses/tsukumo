# ドキュメントの棚卸し 2026-09-16

すべて対応済み（24件）。初回の実行。

## 検査ごとの件数（実行前 → 実行後）

| 検査                           | 実行前 | 実行後 |
| ------------------------------ | -----: | -----: |
| 1 索引の取りこぼし             |      0 |      0 |
| 2 索引のリンク切れ             |      0 |      0 |
| 3 先回りの空索引               |      0 |      0 |
| 4 相対リンク切れ               |      0 |      0 |
| 5 タスク運用節の形             |      0 |      0 |
| 6 実在しないパスの言及         |     20 |     15 |
| 7 置き場の逸脱                 |      1 |      1 |
| 8 正典の二重化                 |      3 |      3 |
| 9 索引の無い大きなドキュメント |      0 |      0 |
| 10 スキル参照の不一致          |      0 |      0 |

確定群は最初から0件。残った19件はすべて「そのままが正しい」と判断したもの。

## 直したもの（5件・いずれも今の姿の説明として書かれていた）

| ファイル                   | 直した記述                                                | 直した先                                                                            |
| -------------------------- | --------------------------------------------------------- | ----------------------------------------------------------------------------------- |
| `docs/workflow.md`         | プロジェクト固有の値は `develop/workflow.json` が持つ     | `CLAUDE.md` の「## タスク運用」節（T-005 で `workflow.json` は廃止）                |
| `docs/workflow.md`         | `src/presentation/view.ts` の `taskItemHtml`              | `src/ui/sidebar/task-list.tsx` の `TaskItem`（移行の段3で移った）                   |
| `docs/workflow.md`         | `docs/history/tasks-archive.md` / `progress-archive.md`   | `docs/history/tasks.md` / `progress.md`（T-005 でファイル名を固定）                 |
| `docs/requirements.md` 4.2 | `src/presentation/report-html.ts` の `sanitizeReportHtml` | `src/ui/report/sanitize-schema.ts` の `REPORT_SANITIZE_SCHEMA`（移行の段6で移った） |

`docs/workflow.md` の3件は **T-005（`docs/history` のファイル名を固定し、`workflow.json` を廃止する）の追随漏れ**。
`docs/requirements.md` の1件は移行の段6の追随漏れで、同じ段落の直前の行は既に新しいパス
（`src/ui/report/split-blocks.ts`）を指していた。

## 判断した結果、そのままが正しかったもの（19件）

- **検査6 / `docs/architecture.md` の6件**（`src/transcript.ts`・`src/transcript-target.ts`・
  `src/state.ts`・`src/balloon.ts`・`src/draw.ts`・`src/index.ts`）: すべて
  `## 設計判断（なぜ今の形なのか）`（188行〜）か、`## 現在の実装状況` の「**捨てる**」の表
  （64行。「2026-09-12 に撤去済み」と明記）の中にある**昔の話**。直すと経緯が失われる
- **検査6 / `docs/design.md` の1件**（`vendor/idiomorph.min.js`）: 「12. 移行の段階」の表の
  段6の行で、**消した対象**として挙がっている。経緯の記録
- **検査6 / `docs/research/` の8件**: 調査メモは調べた時点の姿を残すもの。
  `architecture-proposal.md` の4件（`src/core/session-launch.ts` など）は**提案が作ろうとしている
  ファイル**で、まだ無いのが正しい
- **検査7 / `docs/architecture.md`**: 誤検知。引っかかったのは「候補の比較は
  `docs/research/app-shell.md`」のような**他所への参照**と、このファイル自身の役割の説明
  （「なぜその案を採らなかったのかだけを書く」）。提案の本文は混ざっていない。提案書は
  `docs/research/architecture-proposal.md` にあり、architecture.md はそれを参照しているだけ
- **検査8 /「7. 未決事項」**: `docs/requirements.md` は要件の未決事項（対応タスク列を持つ正典）、
  `docs/research/architecture-proposal.md` は提案が置いた**仮定**。扱う対象が違う
- **検査8 /「会話内容の扱い」**: `README.md` は利用者向けの説明（丁寧体・「外へ出ません」）、
  `docs/coding-standards.md` は開発規約の正典。読み手が違うので二重化ではない
- **検査8 / `bun run check` が CLAUDE.md の複数箇所**: 契約として読まれるのは
  「## タスク運用」節の `- 検証コマンド:` の1行だけで、他は「よく使うコマンド」の一覧・
  完了報告の IMPORTANT・CI/CD の説明という別の文脈。正典は1つ

## 気づいたが、このスキルでは書かないこと

`docs/workflow.md` の `summary` の目安が「全角40文字以内」のままで、`status.py` の
`long_summary` は80桁で警告する。どちらに寄せるかは中身の判断なので触っていない。

## コマンド

- `bun run format`: 実行済み（差分なし）
- `bun run check`: **562 pass / 0 fail**。`docs/requirements.md` の節の数は 26 で不変
