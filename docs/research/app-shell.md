# 箱（ウィンドウ）の選択肢調査

**この文書は調査メモであって正典ではない。** `docs/` の他ファイルにある「節の索引」はここには作らない。

**このタスクでは選ばない。** T-046（Orca のタブのままか、アプリに包むか）が、この表を読んで実際に使った感想と合わせて決める。ここに書くのは一次情報（公式ドキュメント・ベンダーの文書・手元の実測）に基づく事実の比較だけで、結論（どれにするか）は書かない。

箱を替えるとは、`src/host.ts` の `showView`（URL を箱の中に開く）の実装を差し替えることで、Web アプリ側（`http://127.0.0.1:<port>/layout` を配る HTTP サーバ）を作り直すことではない。7つの軸は `develop/tasks.json`（このタスクの依頼文）で固定されたもの。

## 着手時に実測したツールチェーン（2026-09-12）

| コマンド                     | 結果                                                                                                                   |
| ---------------------------- | ---------------------------------------------------------------------------------------------------------------------- |
| `cargo` / `rustc` / `rustup` | 3つとも未導入（`command -v` がいずれも何も返さない）                                                                   |
| `node`                       | 導入済み。`v22.13.1`（`/Users/sinnlos/.nodebrew/current/bin/node`）                                                    |
| `npm`                        | 導入済み。`11.1.0`                                                                                                     |
| Xcode                        | 導入済み。`xcode-select -p` → `/Applications/Xcode.app/Contents/Developer`                                             |
| macOS                        | `sw_vers` → ProductVersion `26.6.2`（BuildVersion `25G83`）                                                            |
| `orca`                       | 導入済み。`orca status --json` → `appVersion "1.4.200"`（`docs/requirements.md` 記載の `v1.4.194` から更新されている） |

**実測値は時間が経つと変わる**ので、T-046 で決めるときはその場で確認し直す。

**再実測（2026-09-16）**: `cargo` / `rustc` は依然として未導入。`node` `v22.13.1`・`npm` `11.1.0`・macOS
`26.6.2` は変わらず。`orca` は `1.4.203` に上がっている（上表の `1.4.200` から）。加えて
`/Applications/Google Chrome.app` の存在を確認した（候補4が前提にするもの）。

## 候補ごとの比較

### 1. Orca のタブのまま（現状維持のベースライン）

| 軸                                        | 内容                                                                                                                                                                                                           | 出典                                                                                                             |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| 追加で要るツールチェーン                  | 無し。`orca` コマンドは既に導入済み                                                                                                                                                                            | 手元の実測（`orca status --json`、2026-09-12）                                                                   |
| Bun プロセスを子として起こせるか・同梱    | 該当しない。tsukumo の Bun プロセスが `orca` を子プロセスとして呼ぶ側（`execFile("orca", ...)`）であり、Bun を Orca の子にする組み方自体が無い                                                                 | `src/orca-host.ts`（`runOrca` 関数、手元のコード）                                                               |
| `.../layout` を開くだけで済むか           | 済む。`showView` は `orca tab list --json` で既存タブを探し、無ければ `orca tab create --url <url>` を呼ぶだけ                                                                                                 | `src/orca-host.ts` L60-72                                                                                        |
| 既定で外部と通信するか                    | する。**テレメトリは既定でオン**（匿名の利用イベントを PostHog Cloud に送信。ファイルパス・プロンプト・端末出力は送らないと明記）。**自動更新も既定で有効**（`installMode: "interactive"`, `automatic: true`） | https://www.onorca.dev/docs/telemetry ／ 手元の実測（`orca status --json` の `remoteUpdateSupport`、2026-09-12） |
| ウィンドウまわりでできること              | 独立したウィンドウではなく、Orca 本体1つのウィンドウの中の1タブ。幅・比率・常に手前を指定するフラグは `orca --help` / `orca agent-context --json` の全コマンドに存在しない                                     | `docs/requirements.md` 5章（2026-09-09 実測の記録）／ 今回の `orca --help` 実測（2026-09-12、同じ結果）          |
| 導入から「ウィンドウが1枚出る」までの手数 | 0（既に動いている）。`bun run start` を叩くだけ                                                                                                                                                                | `CLAUDE.md`「よく使うコマンド」                                                                                  |
| 撤退のしやすさ                            | 該当しない（これが現状そのものなので、撤退の概念が無い）                                                                                                                                                       | ―                                                                                                                |

### 2. Electron

| 軸                                        | 内容                                                                                                                                                                                                                                                                                                                                    | 出典                                                                                                                                                                                                                        |
| ----------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 追加で要るツールチェーン                  | Node.js（最新の LTS 推奨）。手元に既に `node`/`npm` があるので新規導入は無さそう。**macOS でのコード署名・公証に Xcode Command Line Tools が要るかは、このページには記述が無い（不明）**                                                                                                                                                | https://www.electronjs.org/docs/latest/tutorial/tutorial-prerequisites （Xcode の要不要は記述なしのため未確認）                                                                                                             |
| Bun プロセスを子として起こせるか・同梱    | 起こせる。メインプロセスは Node.js 環境そのもので Node の全 API を使える（`child_process` も可。Electron は代わりに `UtilityProcess` API を推奨）。同梱は Electron 本体にバンドラが無く、`electron-builder` の `extraResources` で実行ファイルを `Contents/Resources` に同梱し `process.resourcesPath` から参照する                     | https://www.electronjs.org/docs/latest/tutorial/process-model ／ https://www.electron.build/docs/configuration/ （`electron-builder` はベンダーの公式ドキュメント。Electron Foundation 本体のドキュメントではない点に注意） |
| `.../layout` を開くだけで済むか           | 済む。`new BrowserWindow(...).loadURL('http://127.0.0.1:<port>/layout')` で開ける（`win.loadURL('https://github.com')` が公式の用例）                                                                                                                                                                                                   | https://www.electronjs.org/docs/latest/api/browser-window                                                                                                                                                                   |
| 既定で外部と通信するか                    | **テレメトリ機能自体の記述が公式ドキュメントに見当たらなかった（不明。専用ページを検索したが見つからなかった）**。自動更新（`autoUpdater`）は `setFeedURL` を呼んだ上で `checkForUpdates()` を明示的に実行しない限り通信しない（既定オフ）                                                                                              | https://www.electronjs.org/docs/latest/api/auto-updater                                                                                                                                                                     |
| ウィンドウまわりでできること              | `setAlwaysOnTop(flag, level)` あり。サイズの記憶は API レベルには無く `getBounds()`/`setBounds()` で自前実装が要る。キーボードは `globalShortcut` / `Menu` モジュール。`Notification` モジュールは OS ネイティブ通知（macOS は `UNNotification`）だが**メインプロセス専用**で、レンダラー側は Web 標準の Notifications API を使えと明記 | https://www.electronjs.org/docs/latest/api/browser-window ／ https://www.electronjs.org/docs/latest/api/notification                                                                                                        |
| 導入から「ウィンドウが1枚出る」までの手数 | `git clone https://github.com/electron/electron-quick-start` → `npm install` → `npm start` の3手順（Electron 公式組織のサンプルリポジトリ）                                                                                                                                                                                             | https://github.com/electron/electron-quick-start （README）                                                                                                                                                                 |
| 撤退のしやすさ                            | `loadURL` で既存 URL を開くだけなので Web アプリ側に変更は残らない設計にできる。撤退時は Electron 側のラッパー（`main.js` や `package.json` の `electron-builder` 設定）を消すだけで済むはず（`showView` の実装だけを差し替える設計と整合）                                                                                             | 上記 `loadURL` の仕様から論理的に導ける（追加のドキュメントはこの点を明記していない）                                                                                                                                       |

### 3. Tauri v2

| 軸                                        | 内容                                                                                                                                                                                                                                                                                   | 出典                                                                                                                             |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- |
| 追加で要るツールチェーン                  | **Rust（`rustup` でインストール）が必須**。macOS では Xcode（デスクトップのみなら Command Line Tools でも可）が要る。フロントエンドに JS フレームワークを使うなら Node.js（LTS）も要る。手元は `cargo`/`rustc`/`rustup` が3つとも未導入（上表）                                        | https://v2.tauri.app/start/prerequisites/                                                                                        |
| Bun プロセスを子として起こせるか・同梱    | 起こせる。`tauri.conf.json` の `bundle.externalBin` にサイドカーとして登録し、`$TARGET_TRIPLE` 付きの名前でバイナリを用意する。`@tauri-apps/plugin-shell` の `Command.sidecar(...)` で実行し、`capabilities` で実行権限を許可する必要がある                                            | https://v2.tauri.app/develop/sidecar/                                                                                            |
| `.../layout` を開くだけで済むか           | 既定のカスタムプロトコルの代わりに `localhost` プラグインを使えば、`http://localhost:{port}` をそのまま `WebviewUrl` として開ける。ただし公式ドキュメントが「considerable security risks（かなりのセキュリティリスク）」があると明記しており、既定のプロトコル実装の使用を推奨している | https://v2.tauri.app/plugin/localhost/                                                                                           |
| 既定で外部と通信するか                    | updater プラグインは既定で組み込まれておらず、`tauri add updater` で明示的に追加し、`check()` を呼んだ時だけ通信する（既定オフ）。**Tauri CLI・コア自体がテレメトリを送るかどうかは、philosophy ページに記述が無く、専用ページも見つからなかった（不明）**                             | https://v2.tauri.app/plugin/updater/ ／ https://v2.tauri.app/about/philosophy/ （テレメトリの明記なし）                          |
| ウィンドウまわりでできること              | `WebviewWindow.setAlwaysOnTop(boolean)` あり。ウィンドウの位置・サイズの記憶は `window-state` プラグイン（`@tauri-apps/plugin-window-state`）が担う（閉じるときに保存、次回起動時に復元）                                                                                              | https://v2.tauri.app/reference/javascript/api/namespacewebviewwindow/ ／ https://v2.tauri.app/reference/javascript/window-state/ |
| 導入から「ウィンドウが1枚出る」までの手数 | `npm create tauri-app@latest` → プロンプトに回答 → `cd <project>` → `npm install` → `npm run tauri dev` で新しいウィンドウが開く                                                                                                                                                       | https://v2.tauri.app/start/create-project/                                                                                       |
| 撤退のしやすさ                            | `localhost` プラグイン経由で既存 URL をそのまま開けるので Web アプリ側の変更は最小限で済むはず。撤退時は `src-tauri/` ディレクトリと `tauri.conf.json` ごと削除すればよい設計（`showView` 差し替えの前提と整合）                                                                       | 上記 `localhost` プラグインの仕様から論理的に導ける（明記したドキュメントは見つからなかった）                                    |

### 4. 素のブラウザをアプリとして使う（Chrome `--app` / Safari「Dock に追加」）

| 軸                                        | 内容                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | 出典                                                                                                                                                  |
| ----------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- |
| 追加で要るツールチェーン                  | 無し。手元に Chrome（`153.0.8010.36`）・Safari とも既に導入済み                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 手元の実測（`/Applications` 配下、`Google Chrome --version`、2026-09-12）                                                                             |
| Bun プロセスを子として起こせるか・同梱    | 該当しない。ブラウザは tsukumo の Bun プロセスとは別の既存アプリを起動するだけで、同梱（バイナリを1つにまとめる）という概念が無い                                                                                                                                                                                                                                                                                                                                                                                                           | 手元の実測（下記の起動テストの構成）                                                                                                                  |
| `.../layout` を開くだけで済むか           | Chrome は `--app=<url>` を付けて起動するだけ。実際に `open -na "Google Chrome" --args --app=http://127.0.0.1:8765/ --user-data-dir=<tmp>` でプロセスがエラー無く起動することを確認した。**ただし Chromium 公式は「全スイッチの一覧は無い」と明記しており、`--app` 自体の挙動を説明する一次情報のページは見つけられなかった（不明。タブバー無しウィンドウになるかは今回サンドボックス環境で画面キャプチャ権限が無く視覚確認できていない）**。Safari は GUI操作（File > Add to Dock）で追加する運用で、コマンドラインからの起動という形は無い | 手元の実測（2026-09-12、後述） ／ https://www.chromium.org/developers/how-tos/run-chromium-with-flags/ （「There is no list of all switches」と明記） |
| 既定で外部と通信するか                    | **する。Chrome は既定で使用統計とクラッシュレポートを Google に送信する設定がオン**（設定でオフにできる）。Safari 固有の既定値を明記した一次情報は見つけられなかった（macOS 全体の「分析情報を共有」設定は見つかったが、Safari だけを対象にした記述ではないため不明）                                                                                                                                                                                                                                                                       | https://support.google.com/chrome/answer/96817 ／ Safari 側は不明（macOS 全体の設定: https://support.apple.com/guide/mac-help/mh27990/mac）           |
| ウィンドウまわりでできること              | Safari の Web App は「独立したウィンドウ」として動き、通常のアプリと同様に通知を受け取れる（macOS Sonoma 14 以降で対応）。常に手前・キーボードショートカットの扱いについての記述はこのページには無い（不明）                                                                                                                                                                                                                                                                                                                                | https://support.apple.com/guide/safari/add-to-dock-ibrw9e991864/mac                                                                                   |
| 導入から「ウィンドウが1枚出る」までの手数 | Chrome: 既に入っているので `--app=<url>` を付けて起動する1手。Safari: File > Add to Dock → 名前入力 → Add の3手                                                                                                                                                                                                                                                                                                                                                                                                                             | 手元の実測（Chrome）／ https://support.apple.com/guide/safari/add-to-dock-ibrw9e991864/mac （Safari）                                                 |
| 撤退のしやすさ                            | 非常に容易。フラグを外す・Dock から削除するだけで、Web アプリ側には何も残らない                                                                                                                                                                                                                                                                                                                                                                                                                                                             | 上記の起動手順から論理的に導ける                                                                                                                      |

**Chrome `--app` の手元の実測メモ（2026-09-12）**: `python3 -m http.server` でローカルにテストページを立て、`open -na "Google Chrome" --args --app=http://127.0.0.1:8765/ --user-data-dir=/tmp/chrome-app-profile --no-first-run --no-default-browser-check` を実行したところ `ps aux` に `Google Chrome --app=http://127.0.0.1:8765/ ...` のプロセスがエラー無く現れ、レンダラー・GPU の子プロセスも起動した。ただしこのサンドボックス環境には `screencapture` も `System Events` 経由のウィンドウ問い合わせも権限が無く（`could not create image from display` / AppleEvent タイムアウト）、タブバーやアドレスバーが本当に無いかは視覚的に確認できなかった。作業後、テスト用の Chrome プロセスと HTTP サーバ、一時プロファイルは削除済み。

### 5. WKWebView を薄く包む

| 軸                                        | 内容                                                                                                                                                                                                                                                                                                                             | 出典                                                                                                                                                                        |
| ----------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 追加で要るツールチェーン                  | Xcode（Swift での開発が前提）。手元には既に導入済み                                                                                                                                                                                                                                                                              | 手元の実測（`xcode-select -p` → `/Applications/Xcode.app/Contents/Developer`、上表）                                                                                        |
| Bun プロセスを子として起こせるか・同梱    | **不明**。WKWebView / AppKit の公式ドキュメントで、Bun のような外部バイナリを Xcode プロジェクトのビルド成果物に同梱する方法（Electron の `extraResources` や Tauri の `externalBin` に相当するもの）に触れたページを見つけられなかった                                                                                          | ―（未確認）                                                                                                                                                                 |
| `.../layout` を開くだけで済むか           | 済まない。WKWebView 自体はウィンドウを持たず、`NSViewController`（macOS）/ `UIViewController`（iOS）に埋め込んだ上で `webView.load(URLRequest(url: url))` を呼ぶ必要がある。つまり Xcode プロジェクト一式（ターゲット・Info.plist・エンタイトルメント）を新規に作る必要がある                                                    | https://developer.apple.com/documentation/webkit/wkwebview                                                                                                                  |
| 既定で外部と通信するか                    | **不明**。App Sandbox 環境でローカルホストへの接続にも `com.apple.security.network.client` エンタイトルメントが要るらしいという情報はあるが、そのエンタイトルメントの公式ページ（Apple Developer Documentation）が JavaScript レンダリングのページでこのセッションの取得ツールでは本文を読み取れず、一次情報で裏取りできなかった | 参照のみ: https://developer.apple.com/documentation/bundleresources/entitlements/com.apple.security.network.client （本文未取得）                                           |
| ウィンドウまわりでできること              | WKWebView 自体には無く、AppKit の `NSWindow` 側の責務。`setFrameAutosaveName(_:)` でウィンドウの位置・サイズを自動的に defaults に保存・復元できる。`NSWindow.Level.floating` を `window.level` に設定すると常に手前にできる                                                                                                     | https://developer.apple.com/documentation/appkit/nswindow/setframeautosavename(_:) ／ https://developer.apple.com/documentation/appkit/nswindow/level-swift.struct/floating |
| 導入から「ウィンドウが1枚出る」までの手数 | **不明**。Xcode でプロジェクトを新規作成する手順の一次情報（Apple 公式のステップバイステップ）を、このセッションの取得ツールでは本文まで確認できなかった                                                                                                                                                                         | ―（未確認）                                                                                                                                                                 |
| 撤退のしやすさ                            | Web アプリ側は `webView.load(URLRequest(url:))` で既存 URL を開くだけなので変更は残らないはず。撤退時は Xcode プロジェクト一式を削除すればよい                                                                                                                                                                                   | 上記 `load(_:)` の仕様から論理的に導ける                                                                                                                                    |

### 6. Electrobun（2026-09-16 追記）

**この候補は 2026-09-12 の調査で漏れていた**（当時の足切りは Wails / Neutralino / Photino の3つで、
Electrobun には触れていない）。v1 が 2026-02-06 に出ており、調査時点で既に存在した。

| 軸                                        | 内容                                                                                                                                                                                                                                                                                                                                                                         | 出典                                                                                                                                                   |
| ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 追加で要るツールチェーン                  | **`hutch` という新しい外部コマンド**（「Electrobun's build and workspace CLI」）を `curl -fsSL https://hutch.blackboard.sh/hutch/install.sh \| sh` で入れ、`hutch electrobun init <name>` で作る。`bunx electrobun init` / `npx electrobun init` も動くと明記されている。Bun の明示的な導入要件は Quick Start に記載なし                                                     | https://framework.blackboard.sh/electrobun/guides/quick-start/                                                                                         |
| Bun プロセスを子として起こせるか・同梱    | **不明**。「Bundling and distribution」に外部バイナリ（サイドカー）の同梱方法の記載が無い（Electron の `extraResources` や Tauri の `externalBin` に相当するものを見つけられなかった）。main プロセスは `src/bun/index.ts` で、ランタイムは Cottontail / Bun / Zig / Rust / Go / Odin から選べると書かれているが、**外部バイナリの同梱は別の話なので、この軸は未確認のまま** | https://framework.blackboard.sh/electrobun/guides/bundling-and-distribution/ ／ https://framework.blackboard.sh/electrobun/guides/native-main-process/ |
| `.../layout` を開くだけで済むか           | 済む。`new BrowserWindow({ title, url, frame: { width, height } })` の `url` に文字列を渡す（既定は `null`、`views://` で同梱コンテンツも指せる）。ただし公式が「信頼できないコンテンツに特権 RPC ハンドラを繋がないこと。リモートページには `sandbox: true` を使うこと」と警告している                                                                                      | https://framework.blackboard.sh/electrobun/apis/browser-window/                                                                                        |
| 既定で外部と通信するか                    | **自動更新は既定で無効**。`release.generatePatch` が true かつ `release.baseUrl` がある場合に生成され、アプリ側も Updater API を呼ぶ必要がある。**テレメトリの有無はドキュメントに記載なし**（不明）                                                                                                                                                                         | https://framework.blackboard.sh/electrobun/guides/bundling-and-distribution/                                                                           |
| ウィンドウまわりでできること              | `frame` で `x` / `y` / `width` / `height`（既定 800×600、x/y 省略で中央）。常に手前は `setAlwaysOnTop(boolean)` / `isAlwaysOnTop()` がある。**サイズ記憶に相当する標準 API はドキュメントに無い**                                                                                                                                                                            | https://framework.blackboard.sh/electrobun/apis/browser-window/                                                                                        |
| 導入から「ウィンドウが1枚出る」までの手数 | `hutch` を入れる → `hutch electrobun init <name>` → `hutch run dev`（「builds the app, launches it, and rebuilds when you edit source files」）の3手。**手元では未実行**（`hutch` を導入していない）                                                                                                                                                                         | https://framework.blackboard.sh/electrobun/guides/quick-start/                                                                                         |
| 撤退のしやすさ                            | Web アプリ側は `url` を渡すだけなので変更は残らないはず。撤退時はプロジェクトと `hutch` を消す。**`hutch` がホームディレクトリのどこに何を置くかは未確認**                                                                                                                                                                                                                   | 上記 Quick Start の手順から論理的に導ける（`hutch` の設置場所は未確認）                                                                                |

**補足（サイズと WebView）**: 既定で OS 内蔵の WebView を使い（macOS は WKWebView）、`bundleCEF` で
Chromium を同梱する選択もできる。hello world が「about a megabyte」と説明されている。ライセンスは MIT、
公式対応は macOS 14+ / Windows 11+ / Ubuntu 24.04+。GitHub の README では **ベータ**と位置づけられている。
出典: https://blackboard.sh/electrobun/ ／ https://github.com/blackboardsh/electrobun

**確度についての注記**: 調査中にドキュメントのドメインが `blackboard.sh/electrobun/docs/` →
`framework.blackboard.sh/electrobun/` → 一部ページが `electrobun.dev` へ、と 301 / 302 で行き来していた。
**一次情報の置き場所自体がまだ動いている**ので、T-046 で使うときは URL を取り直す。

### その他（足切りしたもの）

Wails（[wails.io](https://wails.io/docs/introduction/)）・Neutralino.js（[neutralino.js.org](https://neutralino.js.org/)）・Photino（[tryphotino.io](https://www.tryphotino.io/)）は、いずれも macOS で動きメンテナンスも継続しているため「動かない／止まっている」による足切りではないが、**Wails は Go、Photino は .NET(C#) という、tsukumo のスタック（Bun + TypeScript）にも Tauri（Rust）にも無い新しい言語ランタイムを追加で持ち込む**ため、Neutralino.js は OS 内蔵の WebView をそのまま使い軽量な一方で **ドキュメント上でも Electron/Tauri ほどウィンドウ操作 API が整理されていない**ため、3つとも比較表には入れず名前だけ残す。

## T-046 で決めるときに効く違い

- **追加ツールチェーンの重さが候補で大きく違う。** Orca のまま・Electron・素のブラウザは手元の `node`/`npm` でおおむね足りるが、Tauri は `cargo`/`rustc`/`rustup` の新規導入が要り、WKWebView は Xcode プロジェクト一式を新規に作る必要がある（`CLAUDE.md`「`orca` 以外の外部コマンド依存を増やすときはユーザーの承認を得る」に関わる）。
- **既定の外部通信の有無に差がある。** 現状の Orca はテレメトリと自動更新の両方が既定でオン。Chrome も既定で使用統計を送る。一方 Electron 本体・Tauri の updater/localhost プラグインは、明示的にコードを書かない限り既定では通信しない（Electron・Tauri のテレメトリ自体の有無は一次情報で確認できず不明）。
- **「URL を開くだけ」で済むかに段階がある。** Electron の `loadURL`・Tauri の `localhost` プラグイン・Chrome の `--app` はほぼそのまま開けるが、Tauri の `localhost` プラグインは公式が「かなりのセキュリティリスク」と明記している。WKWebView だけは Xcode プロジェクトを新規に組む必要があり、他の候補と手数の桁が違う。
- **ウィンドウ操作の作り込み量が違う。** Tauri と Electron はサイズ記憶・常に手前がプラグイン／API 呼び出し1つで揃うが、WKWebView は AppKit の `NSWindow` を自分で操作する必要がある。Orca のタブのままは、そもそも独立したウィンドウにならない。
- **撤退のしやすさの土台は共通。** どの候補も `showView` の実装だけを差し替える設計にできそうだが、後始末の重さ（Rust のツールチェーンや Xcode プロジェクト、エンタイトルメントの片付け）は候補によって違う。

## 箱の仕事の大きさは、tsukumo 側の形で変わる（2026-09-16 追記）

上の比較は「tsukumo の Bun プロセスが今の形のまま動く」ことを前提にしている。**その前提は
tsukumo 側の選択で動く**ので、T-046 で読むときに効く点を書いておく。結論は書かない。

- **「Bun プロセスを子として起こせるか・同梱」の軸が、そもそも成立しない組み方がある。** 現状の
  `src/core/bundle.ts` は起動のたびに `execFile("bun", ["build", …])` を呼ぶので、実行時に `bun` と
  `src/ui/**` の両方が要る（T-122 の依存3と依存5）。`bun build --compile` で単一の実行ファイルに
  すればこの2つは消え、箱が抱えるのは**バイナリ1つ**になる。埋め込みは `with { type: "file" }` と
  `Bun.embeddedFiles` で行う。出典: https://bun.com/docs/bundler/executables
- **その形にすると、箱に残る仕事は「窓を1枚開くこと」だけになる。** 比較表の7軸のうち「Bun プロセスを
  子として起こせるか・同梱」の重みが下がり、「ウィンドウまわりでできること」と「追加で要る
  ツールチェーン」の重みが上がる。Chromium を同梱する候補（Electron、Electrobun の `bundleCEF`）は、
  そのぶんの容量に見合う機能があるかで見ることになる。
- **ただし `--compile` を採るかは `docs/coding-standards.md`「Bun固有APIに寄せない」の扱いと連動する。**
  同規約は「退避先（Node へ移せること）を残すため」と理由を書いているが、上記のとおり
  `bundle.ts` 経由で `bun` への実行時依存は既にある。**規約の現状と実態のずれは
  `docs/research/architecture-proposal.md` 側の論点**で、この文書では扱わない。
- **`docs/design.md` 1章と T-046 の依頼文で、Electron の想定が食い違っている**（前者は「core を
  Electron の Node で動かせるので Bun は不要」、後者は「Bun のプロセスを子として起こす」）。
  **この比較表は後者の前提で書かれている**（7軸の2つ目がそれを前提にしている）。どちらを採るかで
  「Bun を採用できるか」の答えが変わるので、T-046 は先にこの食い違いを解く。
