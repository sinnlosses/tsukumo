# 振り返りの記録

最後に振り返ったコミット: `59c00be`

<!-- 上の1行だけが機械の読む値（~/.claude/skills/retrospect/scripts/scan.py）。形を変えない。
     振り返りの出力そのものは develop/direction.md の ## エージェントのドラフト に積む。
     ここに残すのは「どこまで見たか」と「何を出したか」の索引だけ。 -->

## 振り返りの履歴

### 2026-09-23 `c37d714`..`59c00be`（29コミット / 手を動かしたのは T-370, T-371, T-372, T-373, T-378, T-379, T-380, T-381。残りはタスクの登録と着手の印）

- 並行の作業ツリーを前提に「## タスク運用」節の記述を見直す（merge commit は出ないという記述、ポートは分かれないという例外）→ `develop/direction.md` の `## エージェントのドラフト`
- `docs/design.md` 5章に残る型の写しを4章と同じ形にする（経路の表はすでにドリフト）→ 同上
- 振り返ったが出さなかったもの: 同じファイルを3回以上直した例が3タスクにあった（T-370 の chat-log.test.ts×7、T-371 の use-screen-nav.ts×3、T-372 の design.md×5）が、いずれも対象が複数ある task で1箇所ずつ直した回数であり、手順の乱れではない

### 2026-09-23 `8f585d6`..`c37d714`（130コミット / /loop /next-task で回した18件を中心に。T-298, T-299, T-312, T-313, T-314, T-325, T-326, T-330, T-331, T-336, T-337, T-338, T-339, T-340, T-341, T-360, T-361, T-364 ほか33タスクは件名と差分の大きさだけ）

- 変更前と撮り比べる手順が4通りに割れ、共有の stash まで使われた → `develop/direction.md` の `## エージェントのドラフト`
- `shared` の型を変えたときに `docs/design.md` の表が追随しない（T-313・T-314 の2連続）→ 同上
- タスク本文の行番号・件数が着手時にずれている（T-313・T-340・T-360）→ 同上
- 振り返ったが出さなかったもの: T-339 の記録で python が落ちたのに後続のコミットが走った件（書き方の事故で、以後はスクラッチのスクリプトに寄せて再発していない）。T-332 が判断待ちに戻った件（本文の候補を調べて初めて分かる性質で、切り方の問題ではない）

### 2026-09-22 `af1a0f5`..`8f585d6`（23コミット / T-303, T-304, T-305, T-319, T-320, T-321, T-323, T-324）

- `progress.md` のアーカイブのトリガーが切った直後に再点火し、極小のコミットを量産している
  （23コミット中6件、うち3件は `+4/-4`）→ `develop/direction.md` の `## エージェントのドラフト`
- `docs/architecture.md` の `capture-catalog.ts` の一覧の出し方が実物と違う（オプション無しは
  全件撮影で、一覧は `--help`）→ 同上
- 「コメントに日付を書かない」に機械の検査が無く、層で割った3タスクから `scripts/` が漏れた
  → 同上
- 振り返ったが出さなかったもの: `src/server/core/token-usage.ts` が T-319 → T-320 → T-321 と
  3タスク連続で育った件（記録→集計→画面の順に積む自然な進み方で、分け方を変えても同じ形になる）

### 2026-09-22 `b70ab50`..`af1a0f5`（3コミット / T-302）

- 起こし直さないと変わらないもの（`speak` の enum・パックの読み込み）を確かめる作法が
  `docs/workflow.md` に無い → `develop/direction.md` の `## エージェントのドラフト`
- 「触らない」と書いた資源を、完了条件の確認手順が触らざるをえなかった → 同上
- 語彙を1つ増やすと、型検査が挙げない「件数に依存したテスト」が落ちる → 同上
- 機械的な追随（表情キーの列挙）を人が手で書いている → 同上
  （**ユーザーが振り返りをレビューして指摘した分**。初回は拾えていなかった）
- 振り返ったが出さなかったもの: `characters/README.md` の合計枚数が本文の「やること」に
  無かった件（`MAX_IMAGE_FILES_PER_PACK` から必然に導けて、その場で正しく処理された）
