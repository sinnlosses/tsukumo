# 現在の状態

最終更新: 2026-09-09（**リポジトリの立ち上げと実装スタックの決定**。2026-09-08 の検討メモ
（`work.md`）を `docs/` の正典と初期タスク6件に起こし、**T-001 を完了**した）

スタックは **Bun + TypeScript**、チェックコマンドは **`bun run check`**。
**transcript の追従・吹き出し・hook 連動の表情切り替えまでが動き、実端末で目視確認も済んでいる**
（立ち絵はまだ無い）。**`develop/direction.md` は空**（2026-09-10 に2回に分けて計6件の
タスクへ起こした）。

## 完了したこと（このセッション）

### 2026-09-10 メインビュー（T-009）

**T-009 完了。** 左上に**ツールの実行（名前・引数・結果）と詳細のレポート**が時系列で積まれる
ようになった。実装は sonnet に委譲し、受け入れはメインで実施。168 pass / 0 fail。

- **「作業中／完了後」の状態は明示的に分けず、時系列で積む形にした。** assistant の1メッセージ内で
  `text` と `tool_use` が混ざる順序に保証が無く、transcript から状態を確実に判定できないため。
  結果として末尾が結果待ちのツールなら「作業中」、末尾が詳細なら「完了後」に自然に対応する
- **Markdown → HTML は自前の簡易レンダラー**（見出し・コードブロック・箇条書き・表・太字・
  インラインコード・リンク）。非対応の記法は段落として素のまま出るので壊れない
- 切り詰めは上限8000文字＋件数表示。直近40件のみ表示

**私の指示の誤りが1つ正された**: ツールの結果は `toolUseResult` ではなく
**`user` 行の `message.content[]` の `tool_result`**（`tool_use_id` で対応付ける）。
実測で確かめられて `docs/requirements.md` に反映できる材料になった。

**受け入れで1度差し戻した（本物の穴）**: Markdown のリンクがスキームを検査せず
`javascript:alert(1)` を `href` に入れていた。**このビューは localhost で会話を持っているので、
そこでスクリプトが走るのは避けたい。** allowlist（`http:` / `https:` / `mailto:` / 相対）に直し、
弾いたものはリンクにせず平文で残す形にした（情報を失わないため）。テストで固定済み。

**私のテストが1件誤検知だった**（`onerror=` を「生で出ている」と判定したが、実際は
`<code>&lt;img src=x onerror=alert(1)&gt;</code>` と正しくエスケープされていた）。
疑わしいときは周辺を実際に見ること。

### 2026-09-10 メインビューの絞り込み（T-016）

**T-016 完了。** 「ファイルを変えた操作・サブエージェントの起動・失敗したツール」だけに絞った。
実装は sonnet に委譲し、受け入れはメインで実施。201 pass / 0 fail。

**効果は実測で確認**: ツール実行ブロック 306→24、本文 1.35MB→242KB。配信されるメインビュー
（直近40件の窓）は 61KB→13KB。成功した `Bash` のコマンドは1つも出ない。

**解釈が入った点（ユーザーに伝達済み）**: **失敗したツールは引数ごと出す。** 決定表は
「エラーの内容」としか書いていなかったが、何をして失敗したかが分からないと実用性を欠くため。
**ユーザーの「打ったBashコマンドはさすがに出さなくてよかった」は成功したコマンドについての
言及**なので、失敗時にコマンドが見えるのは妥当と判断した。ただし `Bash` の引数に秘密が入る
露出は、失敗時には残る。

### 2026-09-10 チカチカの解消（T-017）

**T-017 完了。** 本文が前回と同じなら `innerHTML` を差し替えないガードを入れた。207 pass / 0 fail。

**原因は2つあった**（サブエージェントが切り分けた）:

1. ポーリングは transcript の mtime/size の変化だけを見て**3ビューをまとめて publish** する。
   ツール実行が続く間 transcript は高頻度で変わるが、**キャラビューの見た目は変わらない**ことが
   多く、同じ内容の差し替えが繰り返されて立ち絵のフェードが再生され続けていた
2. **購読開始直後の1回目の push が、ページに埋め込み済みの本文と同じもの**を送っていた。
   ページを開いた瞬間にもう一度描き直していた（②はこちらが挙げていなかった原因）

あわせてスクロール位置を保つようにした（いちばん下にいたときだけ追従）。

**残る粗**: セリフだけ変わったときも本文全体としては変わるので立ち絵は作り直される。
厳密に「表情が変わったときだけ」にするには立ち絵と吹き出しを別々に差し替える必要がある。
**1応答に1回なのでむしろ自然**と判断して許容した。気になればタスクに切る。

### 2026-09-10 送り先の一覧と見た目（T-018）

**T-018 完了。** 212 pass / 0 fail。

**`preview` を読まずに済む方法が見つかった。** `orca terminal list --json` の各要素に
**`agentIdentity`** があり、claude が動いているターミナルにだけ `"claude"` が入る（実測で
5件中2件が該当し、完全に一致）。これは Orca 自身が付けた**分類ラベル**で、ターミナルの出力
そのものではない。**`Pane` 型に `preview` を持たせないことで、構造的に漏れる経路が無い。**

**絞り込まず、並び順で寄せる。** 判定が外れても選べるように、全件を返して claude らしいものを
上に置き、印を付けるだけにした。テストで「`likelyClaude` が false でも選べる」ことを固定済み。

見た目は他の3領域と同じ語彙（暗い地・`#3a4256` の枠線・角丸）に揃えた。

### 2026-09-10 4領域のリサイズ（T-022、目視待ち）

**実装は入ったが `status` は `doing`。** 3本の仕切り（上段の縦・下段の縦・上下の横）を
ドラッグで動かせるようにし、比率を `localStorage` に保存・復元、既定に戻すボタンも付けた。
実装は sonnet に委譲し、受け入れはメインで実施。221 pass / 0 fail。

- **4列共有の `grid-template-columns` をやめた。** 上下段で縦の仕切り位置が違うので、
  1つの列定義では3本を独立に動かせない。上下段をそれぞれ別の grid にし、行の高さと各行の
  列幅を CSS カスタムプロパティで持たせて、ドラッグはその変数を書き換えるだけにした
  （実際のサイズ計算は grid に任せる）
- **既定を 7:3・1:1 から 60:40・75:25・50:50 に変えた。** 「入力フォームが狭い」が動機なので
  下段の高さを広げ、**下段の左右はユーザーの指定で半々**にした。可動域は15〜85%にクランプ
- `textarea` の `resize: none` は維持（領域側のドラッグとネイティブのハンドルが競合するため）
- 壊れた `localStorage` 値でのフォールバックは自動テスト2件（不正 JSON / 型・範囲違い）で固定

**残っているのは目視確認だけ。** ブラウザに出た絵は自動テストで守らない方針なので
（`docs/coding-standards.md`「描画は自動テストで守らない」）、実際に仕切りが動くか・
リロードで復元されるか・760px 未満で畳んだときに崩れないかは人が見る必要がある。
**`.layout-reset` は `position: fixed` の右下**なので、入力欄の「送る」ボタンや状態表示と
重なっていないかも合わせて見る。

### 2026-09-10 起動コマンドを1つに（T-027）

**T-027 完了。** `bun run start` だけで、サイドカーの起動からレイアウトページのタブが
Orca 内に開くところまで進むようになった。実装は sonnet に委譲し、受け入れはメインで実施。
223 pass / 0 fail。

- **足したのは `announce` の後の1呼び出しだけ。** タブを開く手段は `src/host.ts` の
  `Host.showView` として既にポートにあり、`main` も `createOrcaHost()` を持っていた
- **重複タブの回避は既に解かれていた。** `src/orca-host.ts` の `showView` が
  「同じ URL のタブがあれば reload、無ければ create」に倒しているので、`src/index.ts` 側に
  判定を重ねて持たせていない
- `TSUKUMO_OPEN_VIEW=0` で自動オープンを止められる。**テストの既定はこちら**
  （空きポートで何十回も起動するので、既定のままだと実機の Orca にタブが積み上がる）
- `orca` が使えなくても起動は続く。PATH の先頭に必ず失敗する `orca` を置くテストで固定した
- **実機で確認済み**: 引数なしの起動で `orca tab list --json` に `/layout` のタブが
  `loadError: null` で出た

**メイン側で1つ拾った**: `docs/architecture.md`「手で確かめること」の手順2が
`bun run scripts/open-views.ts <URL>` を要求したままだった。**ここは全タスクの目視確認が
参照する場所**なので直した。

## 次にやること

**2026-09-10 に指示メモを4件のタスクにし、実使用で見つかった1件（T-023）を足した。
さらに同日、セリフとレポートの書き分けの方針転換を2件（T-024 / T-025）足した。**
**T-022 は実装済み・目視待ち（`doing`）。** 2026-09-10 の3回目の指示メモから **T-026 / T-027**
を足し、**T-019 の守備範囲を「メインビューを読める長さにする」に広げた**。
**T-027 は完了**（`bun run start` 一発でタブまで開く）。着手可能は **T-019 / T-023 / T-024** の
3件で、**3件とも「ユーザーと決めてから着手」なので `/loop` の自動進行では進まない。**

- **T-019（opus）**: メインビューを**読める長さにする**。今回の話中心＋過去を辿れるように＋
  ステップごとの区切り＋レポート側の絞り込み。**論点1・2・5・6をユーザーと決めてから着手する**
  （委譲・`/loop` 不可）
- **T-020（opus、T-025 待ち）**: レポートの構造化の規約を決めて
  `~/.claude/output-styles/asuna.md` へ反映する。**グローバル設定の書き換えなので承認が要る**
  （委譲・`/loop` 不可）
- **T-021（sonnet、T-020 待ち）**: T-020 が洗い出した記法を `renderMarkdownToHtml` に足す
- **T-022（sonnet、`doing`）**: 実装は入った。**目視確認だけが残っている**（上の小節参照）
- **T-023（opus）**: サイドカーの追従先がセッション再起動に追いつくようにする。
  **論点2（グローバルに1つの `transcript-path` をどうするか）をユーザーと決めてから着手する**
  （委譲・`/loop` 不可）
- **T-024（opus）**: セリフの記法を引用（`> `）からプレフィックスへ変える。パーサ・テスト・
  規約を**同じコミットで**入れる（出力スタイルはセッション起動時にしか読まれないため、
  ずらすと吹き出しが止まる窓ができる）。**グローバル設定の書き換えなので承認が要る**
  （委譲・`/loop` 不可）
- **T-025（opus、T-024 待ち）**: レポートをキャラクターの口調で書かない規約にする。
  **2026-09-09 の「文体はどちらもキャラクターのまま」を覆す方針転換**
  （委譲・`/loop` 不可）
- **T-026（opus、T-025 待ち）**: セリフをステップごとに置く規約にする。**吹き出しは発話
  （＝ステップ）ごとに差し替わる作りが既にある**ので、直すのは規約の側（委譲・`/loop` 不可）

積み残しの候補（タスク化していない）:

- **立ち絵と吹き出しを別々に差し替える**（セリフだけ変わったときに立ち絵のフェードが
  再生されるのを止める）。1応答に1回なので今は許容している
- **ビューに認証を足す**（同じマシンの他のプロセスから無認証で読める割り切りを埋める）
- **中断（`--interrupt`）を入力欄から送れるようにする**（T-015 のスコープ外にした）
- **メインビューの差分更新**（今は本文全体が変わると40件ぶん作り直される）。**T-019 と
  同じ場所を触る**ので、やるなら T-019 のあとに切る

## 未解決

- **キャラビューの中の配置は T-011 へ持ち越し。** 単体では「立ち絵が上、直下に吹き出し」で
  問題ないとユーザーが確認済み。ただし元の構想は「左下中央の吹き出し」で、**メインビューと
  サイドバーが揃わないと収まりの良し悪しは判断できない**ため、4領域が並んでから調整する
- **表情が状態に追従して切り替わることの目視確認は未完。** 実装とテストは通っているが、
  ユーザーが実際に切り替わりを見たという確認は取れていない

**方針変更で消えた未解決**（記録として残す）:

- ~~どの端末でも画像プロトコルが通っていない~~ → 立ち絵が HTML の `<img>` になったため無関係に
- ~~吹き出しに禁則処理が無い~~ → 折り返しをブラウザがやるため消えた
- ~~発話が空文字のときプレースホルダーが出ない~~ → ターミナル描画ごと消える

## 注意

次のセッションで踏み外しやすい点:

- **サイドカーは起動時に決めた transcript を追い続ける**（T-023 で扱う）。**Claude Code を
  再起動したらサイドカーも起動し直す。** `~/.tsukumo/transcript-path` は全セッション共通の
  1ファイルで、別リポジトリのセッションに奪われるので、引数で明示するのが確実:
  `bun run start "$(ls -t ~/.claude/projects/<プロジェクト>/*.jsonl | head -1)"`
- **出力スタイル（`~/.claude/output-styles/asuna.md`）はセッション起動時にしか読まれない。**
  `/clear` では読み直されない（2026-09-10 実測。「セリフと詳細の書き分け」の節を持たない
  古い版が読み込まれたままだった）

- **2026-09-09 に `done` 11件、2026-09-10 にさらに 7件（T-009 / T-011 / T-014〜T-018）を
  アーカイブした。** `develop/tasks.json` に無いタスクID（T-001〜T-018）は**完了済み**と
  みなす。中身は
  [`docs/history/tasks-archive.md`](../docs/history/tasks-archive.md) に `## T-XXX` の節で残っている

- **`~/.claude/settings.json` の hooks と statusLine は orca（`~/.orca/agent-hooks/`）が
  専有している。** 設定を足すときは既存エントリを壊さず追記すること。上書きすると orca が
  黙って動かなくなる
- **`tmux` は未導入で、要件にもしない。** ペイン分割は VS Code の split terminal で代用する。
  同様に `chafa` / `viu` / `timg` / `img2sixel` / `deno` / `cargo` も未導入（2026-09-09 実測）。
  **`bun` は導入済み**（2026-09-08 の記録では未導入だった）
- **transcript はユーザーの生の会話ログ。** 複製しない・外部に送らない・全文をログに出さない
  （`docs/coding-standards.md`「会話内容の扱い」）
- **CLI を起動するテストは、負荷が高いとフレークする。** `spawnSync` の `timeout` で
  SIGTERM を送って落とす作りで、通常は全体7秒で終わるが、整形やバックグラウンドの
  ジョブと同時に走らせたとき1件が457秒かかったことがある（2026-09-09）。実装側の問題では
  ないが、**`bun run check` は他の重い処理と同時に走らせない**
- **`oxfmt` は Markdown も整形する。** `.claude/` は `.prettierignore` で除外してあるが、
  `docs/` は対象。表の桁が全角幅で揃うので歓迎してよい変化だが、**整形前提で書く**こと
  （手で桁を合わせても上書きされる）
- **環境の実測値は1日で変わった。** 2026-09-08 に「未導入」だった `bun` が入り、
  ターミナルも `vscode` から `Orca` に変わっていた。`docs/requirements.md`
  「5. 実行環境・非機能要件」の値も**前提にする前にその場で確認する**
