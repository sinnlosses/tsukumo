# 現在の状態

最終更新: 2026-09-10（**T-024 を完了にし、T-023（追従先の乗り換え）を実装した**。リポジトリの
立ち上げと実装スタックの決定は 2026-09-09）

スタックは **Bun + TypeScript**、チェックコマンドは **`bun run check`**。
**transcript の追従・吹き出し・hook 連動の表情切り替えまでが動き、実端末で目視確認も済んでいる**
（立ち絵はまだ無い）。**`develop/direction.md` は空**（2026-09-10 に2回に分けて計6件の
タスクへ起こした）。

## 完了したこと（このセッション）

### 2026-09-11 質問の表示と、ターミナルを自分で持つ方針（T-030 / T-031 / T-032〜）

**T-031 完了。** 追従先ファイルを**セッションの cwd ごと**に分けた（`~/.tsukumo/targets/`）。
共通の1ファイルだと、別のリポジトリで claude を起動しただけで奪われ、**元のディレクトリで
サイドカーが起動すらできなくなる**（ユーザーが実機で踏んだ）。303 pass / 0 fail。

**T-030（質問の表示）は、表示は完成・回答は持ち越し。** 答え待ちの質問がある間、入力欄の領域が
質問に差し替わるところまでは動いている。**押して答える経路は、実測3回で否定された**:

1. `orca terminal send` → claude の端末には `agent_prompt_blocked` で届かない
2. `orca keypress`（`--worktree` 指定）→ **前面のペインにしか届かない**。何も起きない
3. `terminal switch` で前面にしてから `keypress` → **素のシェルには入るが、claude の端末には
   入らない**（質問中ですらない状態でも入らない）

**つまり Orca はエージェント端末への合成入力を弾いている。** こちらから覆せない。

**方針転換: ターミナルを自分で持つ**（ユーザーの提案「orca みたく1つのアプリケーションに
してしまって、必要なもの（ビューワー、ターミナル）を統合するのはどうかな」）。相談の結果、
**Electron で包むのは後回し**にして、**PTY を自前で持つところまで**を取りに行く。

**下調べ済み**（`scratchpad/pty-spike` で実測）:

- **Bun では `node-pty` が使えない**（読み込みと spawn はできるが `onData` が発火しない）
- **Node v22.13.1 では完全に動く**（TTY・`stty size` 40×120・`term.write` の注入まで成功）
- `bun install` は `spawn-helper` の実行権限を落とす（`chmod +x` が要る。踏むと
  `posix_spawnp failed`）

**副産物**: `orca` の失敗理由を**形で許可**して出せるようにした（小文字とアンダースコアだけの
短い1語＝エラーコードの形だけを通す）。これが無ければ `agent_prompt_blocked` は
「終了コード 1」のままで、原因に辿り着けなかった。

**T-032（実験）完了。方針転換は成立する。** `scratchpad/pty-spike` に Node + node-pty + ws +
xterm.js の最小構成を書き、Orca のタブで `claude` を動かした。**TUI は崩れずに描け、サイズも
伝わり、キー注入（文字・日本語・Enter・Esc・矢印）が全部届いた。** IME での直接入力だけ未確認。

決まったこと:

- **Node は端末ホストだけを別プロセス**にする（Bun のテスト資産307件をそのまま保てる）
- 端末の描画は **`@xterm/xterm` を `vendor/` へ同梱**（同梱の仕組みは T-021 で作ってある）
- **WebSocket は端末ホストが自分のポートで持つ**。**端末のバイトをサイドカーに通さない**
  （会話内容が流れる経路を増やさないため）

踏んだ罠: `bun install` は `node-pty` の `spawn-helper` の実行権限を落とす（`chmod +x` が要る。
踏むと `posix_spawnp failed`）。HTML 属性に `\r` と書いても2文字の文字列にしかならない。

## 次にやること

**2026-09-11 に方針転換した。** `orca` 経由では claude の端末に入力を入れられないことが実測で
確定したので、**ターミナルを自分で持つ**方向へ舵を切った（T-032 → T-033 → T-034）。
**Electron で包むのは後回し**で、PTY を持つところまでが今回の射程。

**着手可能**: T-020 / T-026 / T-028 / T-030 / **T-033**（いずれもユーザーと決めてから着手なので
`/loop` の自動進行では進まない）。**方針転換の本線は T-033（端末ビューの本実装）。**

- **T-033（opus）**: 端末ビューの本実装。画面のどこに置くか・`orca` 経由の送信を
  残すかを決める
- **T-034（sonnet、T-033 待ち）**: 質問の選択肢を押して PTY へキーを送る。**今日の出発点**
- **T-030（opus、`doing`）**: 質問の表示は完成。**押して答える部分は T-034 へ持ち越し**
- **T-028（opus）**: `develop/tasks.json` に要約フィールドを足す。**T-029（サイドバーの一覧）**の前段
- **T-020（opus）** / **T-026（opus）**: レポートの構造化の規約 / セリフをステップごとに置く規約

積み残しの候補（タスク化していない）:

- **立ち絵と吹き出しを別々に差し替える**（セリフだけ変わったときのフェード再生を止める）
- **ビューに認証を足す**（同じマシンの他のプロセスから無認証で読める割り切りを埋める）
- **メインビューの差分更新**（今は本文全体が変わると作り直される）

## 未解決

- **キャラビューの中の配置は T-011 へ持ち越し。** 単体では「立ち絵が上、直下に吹き出し」で
  問題ないとユーザーが確認済み。ただし元の構想は「左下中央の吹き出し」で、**メインビューと
  サイドバーが揃わないと収まりの良し悪しは判断できない**ため、4領域が並んでから調整する
- **表情が状態に追従して切り替わることの目視確認は未完。** 実装とテストは通っているが、
  ユーザーが実際に切り替わりを見たという確認は取れていない

**方針変更で消えた未解決**（記録として残す）:

- ~~どの端末でも画像プロトコルが通っていない~~ → 立ち絵が HTML の `<img>` になったため無関係に
- ~~吹き出しに禁則処理が無い~~ → 折り返しをブラウザがやるため消えた
- ~~発話が空文字のときプレースホルダーが出ない~~ → ターミナル描画ごと消える

## 注意

次のセッションで踏み外しやすい点:

- **サイドカーは引数なしで起動したときだけ、同じディレクトリの新しいセッションへ乗り換える**
  （T-023）。Claude Code を再起動してもサイドカーは追いつくので、起動し直さなくてよい。
  **引数でパスを渡して起動したときは乗り換えない**ので、そちらは従来どおり起動し直しが要る
- **`~/.tsukumo/transcript-path` の形式が変わった**（パスだけの1行 → `transcriptPath` と `cwd`
  を持つ JSON）。**hook を直した直後は旧形式のまま残っている**ので、次の SessionStart まで
  引数なしの起動ができない。急ぐときは transcript を引数で渡す
- **出力スタイル（`~/.claude/output-styles/asuna.md`）はセッション起動時にしか読まれない。**
  `/clear` では読み直されない（2026-09-10 実測。「セリフと詳細の書き分け」の節を持たない
  古い版が読み込まれたままだった）

- **2026-09-09 に `done` 11件、2026-09-10 にさらに 7件（T-009 / T-011 / T-014〜T-018）を
  アーカイブした。** `develop/tasks.json` に無いタスクID（T-001〜T-018）は**完了済み**と
  みなす。中身は
  [`docs/history/tasks-archive.md`](../docs/history/tasks-archive.md) に `## T-XXX` の節で残っている

- **`~/.claude/settings.json` の hooks と statusLine は orca（`~/.orca/agent-hooks/`）が
  専有している。** 設定を足すときは既存エントリを壊さず追記すること。上書きすると orca が
  黙って動かなくなる
- **`tmux` は未導入で、要件にもしない。** ペイン分割は VS Code の split terminal で代用する。
  同様に `chafa` / `viu` / `timg` / `img2sixel` / `deno` / `cargo` も未導入（2026-09-09 実測）。
  **`bun` は導入済み**（2026-09-08 の記録では未導入だった）
- **transcript はユーザーの生の会話ログ。** 複製しない・外部に送らない・全文をログに出さない
  （`docs/coding-standards.md`「会話内容の扱い」）
- **CLI を起動するテストは、負荷が高いとフレークする。** `spawnSync` の `timeout` で
  SIGTERM を送って落とす作りで、通常は全体7秒で終わるが、整形やバックグラウンドの
  ジョブと同時に走らせたとき1件が457秒かかったことがある（2026-09-09）。実装側の問題では
  ないが、**`bun run check` は他の重い処理と同時に走らせない**
- **`oxfmt` は Markdown も整形する。** `.claude/` は `.prettierignore` で除外してあるが、
  `docs/` は対象。表の桁が全角幅で揃うので歓迎してよい変化だが、**整形前提で書く**こと
  （手で桁を合わせても上書きされる）
- **環境の実測値は1日で変わった。** 2026-09-08 に「未導入」だった `bun` が入り、
  ターミナルも `vscode` から `Orca` に変わっていた。`docs/requirements.md`
  「5. 実行環境・非機能要件」の値も**前提にする前にその場で確認する**
