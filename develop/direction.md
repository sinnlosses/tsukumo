# 未対応の指示メモ（ここに書くと次のセッションが `/plan-tasks` でタスク化する。正典: `docs/workflow.md`）

- 質問の `multiSelect` で、選択肢のボタンが「1つ選ぶと他が全部外れる」単一選択的な挙動になって
  いる（T-073 の作業中にサブエージェントが気づいた。`pendingAnswerScript` のトグル。複数選べる
  はずの質問で複数選べない）。**現物で再現を確かめてからタスクにする**
