# 未対応の指示メモ（ここに書くと次のセッションが `/plan-tasks` でタスク化する。正典: `task-workflow` の `WORKFLOW.md`）

## ユーザーから

## エージェントのドラフト

- （2026-09-23、T-467 で `docs/display.md` を削ったときに見つけた、今と食い違う参照。T-467 の範囲外なので直していない）
  - `docs/display.md`「そのほかの決まりごと」の「レポートの構造は出力スタイル側の規約で決める（正典は同ファイル「レポートの組み立て方」）」が古い。tsukumo 向けの記法は 2026-09-12 以降 `src/server/core/report-notation.ts` にあり、`asuna.md` の「レポートの組み立て方」は TUI 向けに差し替わっている（置き場所の書き換えは決定に触れうる）
  - `src/browser/features/main-view/turn.tsx` 4行目のコメント「進行はサイドバーが持つ」が古い（いまは帯の「いまの作業」が持つ）
  - `src/shared/session-state.ts` などが引く 4.2「記録の時刻」「次の speak が来た時点でそのターンのものだけになる」は、T-466 で移した時点で `display.md` に無い（どこかの時点で消えた見出し・句を指している）
