# 未対応の指示メモ（ここに書くと次のセッションが `/plan-tasks` でタスク化する。正典: `task-workflow` の `WORKFLOW.md`）

## ユーザーから

## エージェントのドラフト

- **設計を書くタスクの後ろに並べる実装タスクは、登録の時点では目的と完了条件の芯だけにし、本文は設計の着地後に揃える前提にする**（振り返り: T-561, T-562）
  - 根拠: T-561〜T-568 を1コミット（+1041行）で一度に登録し、T-561 が設計を書いたあとに T-562〜T-568 の7件を +36/-27 で書き直した（a34a98a5）。T-561 の `## 結果` にも「後続の本文は未反映。食い違いは反映が要る」とある。T-569〜T-575 も、出し方をデザインする T-573 と、それに従って描く T-574 を同時に登録していて、同じ形になっている
  - 出し先: `plan-tasks` の SKILL.md 手順3「方針決めが要るものは前段のタスクに切り出して」の後ろに、「前段が設計を書くなら、後段の `## 背景` には設計の結論を先取りして書かず、前段の完了条件に後段の本文の追随を入れる」を足す
- **`bun test` は `TZ` を UTC にして走らせるので、日付の境目を見るテストは実行環境のタイムゾーンで組むと書く**（振り返り: T-562）
  - 根拠: T-562 で `main-history.test.ts` のコミット日時を `+09:00` 固定で組んだら日の境目がずれ、`Temporal.Now.timeZoneId()` を基準に組み直した（`## 結果` の3行目）。`docs/coding-standards.md` と `docs/architecture.md` には UTC の記述が0件で、後続の T-563（灯りの暦を日ごとに数える）も同じ罠に当たる
  - 出し先: `docs/coding-standards.md`「テスト」節か「`Date` を使わない」節に1項目
