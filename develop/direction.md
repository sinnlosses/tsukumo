# 未対応の指示メモ（ここに書くと次のセッションが `/plan-tasks` でタスク化する。正典: `task-workflow` の `WORKFLOW.md`）

## ユーザーから

## エージェントのドラフト

- （2026-09-23、`design.md` 13章を `docs/screen-design.md` へ移したタスクから）`docs/requirements.md`
  の4章「機能要件」も同じ扱いにしたい。いま 1979 行のうち4章が約1690行で、中でも 4.9「雑談モード」
  （約650行）と 4.2「表示」（約450行）が大きい。**4章をまるごと出すと別のファイルが1690行になって
  問題が移るだけ**なので、分ける単位は 4.x の節（まず 4.9、次に 4.2）にする。やり方は今回と同じ:
  節の番号（`4.9`・`4.2` と、その下の番号）はそのままで概念の名前のファイルへ逐字で移し、移した先の冒頭に節の索引、
  `requirements.md` には番号の読み替えを1行残し、`requirements.md 4.9` と明示した参照（`src` /
  `test` / `docs` で `requirements\.md.{0,3}4\.(2|9)` が約220件）はスクリプトで一括置換、
  `docs/history/` は据え置く。完了条件の目安は `wc -l docs/requirements.md` が 1000 行以下
