# 未対応の指示メモ（ここに書くと次のセッションが `/plan-tasks` でタスク化する。正典: `task-workflow` の `WORKFLOW.md`）

## ユーザーから

## エージェントのドラフト

- **`develop/progress.md` のマージドライバが、`main` 側で足された同じ日付の小節を「## 完了したこと」の下へ落とさないようにする**（振り返り: T-402, T-464, T-435 ほか）
  - 根拠: 範囲の208コミットのうち6件（`5a61b2d1` `8b9dfab6` `823e3509` `123c2798` `9cf12e51` `97d9bf6a`）が、`git merge main` のあとに小節の並びを手で直すだけのコミットだった。原因は `scripts/progress-done-section.ts` の `mergeDoneSections` にある。見出しを「ours の順 → theirs にだけある見出し」の順に集め、`orderByDateDescending` で日付だけを見て並べ直すので、同じ日付のあいだでは `main`（theirs）が先に足した新しい小節が、枝（ours）の小節の下に落ちる。日付が同じなので `status.py` は ERROR を出さず、並びは黙って崩れる（いまは auto-memory の覚え書きで気づいている状態）
  - 出し先: タスク。同じ日付のあいだでは、base に無い小節（どちらかの側が新しく足したもの）を base にある小節より上に置く。両側が新しく足したものどうしの順も決める。`test/scripts/merge-progress.test.ts` に、同じ日付の小節を両側が足したときの並びのテストを足す
- **`docs/` の節を削る・移すタスクのために、コードとドキュメントから引かれている「docs/<ファイル>.md <番号>「<句>」」がその先に実在するかを数える道具を作る**（振り返り: T-465, T-467, T-470, T-468）
  - 根拠: T-465 と T-467 の evidence は、どちらも「src/test/docs から「…」で引かれている句」を手で洗い出して数えていた（T-467 は9件残存。そのうち2件は着手前から `display.md` に無かった）。T-470 は、そこで見つかった迷子の参照3件を直すために、タスクを1件使っている。同じ種類のタスク T-468（`requirements.md` の残りを削る）がいま `doing` で、同じ洗い出しをもう一度手でやることになる。一方、ファイル名の置き換えそのもの（T-445 で311件、T-464 で114件、T-466 で101件）はスクリプトで一括にできていて、漏れていない。漏れているのは句の実在のほう
  - 出し先: タスク。`scripts/` に、`src/`・`test/`・`docs/`（`docs/history/` を除く）・`CLAUDE.md` から「`docs/X.md` + 番号 +「句」」の形の参照を拾い、句が `docs/X.md` の中に文字として存在しないものを並べる道具を置く。`bun run check` に入れるかどうか（句の言い換えで落ちる頻度）はタスクの論点にする
