# 未対応の指示メモ（ここに書くと次のセッションが `/plan-tasks` でタスク化する。正典: `task-workflow` の `WORKFLOW.md`）

## ユーザーから

## エージェントのドラフト

## 積み残し

- サイドバーのコンテキストの行を、行ぜんぶが押せる札（数字＋棒＋自動圧縮までの残り）に作り直す（difficulty: sonnet / loopable: Y / 元の指示: docs/history/direction.md 2026-09-24「サイドバーのコンテキスト表示」）
  - 目的: `src/browser/features/sidebar/context-usage-row.tsx` の文字リンク1行を、見本 `/Users/sinnlos/Downloads/サイドバーのコンテキスト表示-html/SB-Context.dc.html` の「案1」の札にする。1段目「コンテキスト」・大きめの割合（`28%`）・`使っている量 / 窓の大きさ`・右端の `›`、2段目に高さ 8px の棒、3段目に「自動圧縮まで あと N」（`useContextUsage` の `untilCompactTokens`。取得側は触らずに済むはず）。ホバーとフォーカスで背景と枠が浮き右端が「詳しく ›」、70% 以上で割合と棒を `--state-warn` 系にして3段目を「そろそろ区切りどき。自動圧縮まで あと N」にする。`aria-label` は「コンテキスト N% 使用。トークン消費の画面で詳しく見る」。押す先は `#token-usage` のまま
  - 決まっていること: 文字列にリンクを張る方法ではなく行ぜんぶを押せる札にする。**見本の各図の下の説明文（「「28%」を大きく…」など）は実装のための説明で、画面には出さない**。色は見本の16進を写さず `theme.css` のトークンから導く。数の書き方は `formatCount` のまま
  - 完了条件: 取得中・取れないときも札の高さが変わらない。`docs/screen-design.md` の「#### 使用量の行」を書き直す。`bun run check` が通り、`bun run build` のあと実画面で通常・ホバー・70% 以上の3つを目視して結果に書く。見本が消えていたら、ここに書いた形だけで進めてよい
