# 未対応の指示メモ（ここに書くと次のセッションが `/plan-tasks` でタスク化する。正典: `task-workflow` の `WORKFLOW.md`）

## ユーザーから

## エージェントのドラフト

- **値の入った見本（`docs/history/mockup/*.html`）が届いたら、同じ画面を指す `todo` タスクの本文にもそのパスを足す**（振り返り: T-389, T-384）
  - 根拠: T-389 の本文（11:21 登録）は絵の `sidebar-2026-09-23.png` しか指しておらず、13:35 に値の入った `sidebar-tasks-2026-09-23.html` が入ったあとの 14:30 に着手した。1回目（`12e327f`）は絵から寸法を見積もって顔を 1.1rem（17.6px）にし、35分後の2回目（`18fcebf`）で「寸法は見本から取り直した」として顔 28px・ラベル・`<select>` の高さ・区画の切り方を作り直した（12ファイル +134/-122）。同じ見本を指すよう本文を書き換えた T-384 は、骨格の値が1回で通った
  - 出し先: `docs/workflow.md`「タスクを書くとき・受け入れるとき」に1行（見本を `docs/history/mockup/` に足すときは、同じ画面の `todo` タスクを `grep` して本文に HTML のパスと値の表を足す。着手時に本文が絵だけを指していたら、同じ画面の HTML が無いかを見る）
- **`done` のあとの追補は、`progress.md` に1節足して記録を実物に合わせる**（振り返り: T-384）
  - 根拠: T-384 の追補（`a013b73`、帯を 60px→52px・歯車・ドロップダウン）は `docs/design.md` とコードだけを直し、`develop/progress.md` の T-384 の節と `docs/history/tasks.md` の evidence は「60px」のまま残った（`theme.css` は `--screen-nav-height: 52px`）。追補はアーカイブ（`78d6a77`）の21分後で、evidence の置き場はすでに書き換えない履歴だった。`bun run check` を通した記録もどこにも無い。対照的に T-389 の2回目（`18fcebf`）は `progress.md` と `evidence` を書き直している
  - 出し先: `docs/workflow.md` に「`done` のあとに同じタスクの続きを頼まれたとき」の節（`progress.md` に日付つきの追補の節を足し、検証コマンドの結果を書く。範囲が広いなら新しいタスクにする）
