# 未対応の指示メモ（ここに書くと次のセッションが `/plan-tasks` でタスク化する。正典: `task-workflow` の `WORKFLOW.md`）

## ユーザーから

- **雑談の記憶を、索引で局所的に引く仕組みに作り直す**（2026-09-25）
  - 設計は `docs/research/chat-memory-index.md`（ユーザー判断は同文書の冒頭）。`/compact` による要約をやめ、背景の使い捨て `query()` が窓から溢れた会話をエピソード索引とあらすじに畳む
  - 容量は1つの表に集め、値を書き換えるだけで上げられるように作る
  - 雑談は常に `resume` し、新しいセッションにするのは利用者（自動で切り替えない・画面の復元もしない）

## エージェントのドラフト

- **`test-audit` のキャンペーンで消したテストの跡に「どこへ移したか」のコメントを残さない、と書く**（振り返り: T-620）
  - 根拠: T-620 で `session-socket.test.ts` の2件を消した跡に、keeper の在りかを説明する6行のコメントが残り、受け入れでメインが外した（コメントは今の制約だけを書く規約。keeper は報告と `## 結果` に置けば足りる）
  - 出し先: `~/.claude/skills/test-audit/CAMPAIGN.md` の切り替えの段（C/D を編集する段）に1行
- **日記を書く段（`DiaryStage`）が戻らないことを、畳み込みで守るようにする**（振り返り: T-622）
  - 根拠: T-622 の監査で、`src/shared/session-state.ts` の `withDiaryStage` が `writing` 中ならどの段にも上書きすると分かった。コメントと `docs/design.md` は「段は戻らない」としている。いまは戻す向きの `diary-stage` イベントが来ないので害は無い
  - 出し先: `withDiaryStage` で段の順を比べて、後ろの段への移動だけを受け付けるタスク（テストを1件添える）
- **委譲先のサブエージェントに、別のエージェント（fork を含む）を立てさせない、と条件なしで書く**（振り返り: T-624）
  - 根拠: T-624 の本文には「別のエージェントを立てられない**なら**自分で読む」と条件付きで書いてあった。サブエージェントは fork を3つ立てて読ませたが、3つとも「読むだけ」の指示を破って作業ツリーを書き換え、1つは `git checkout --` で先に入っていた編集を消した（エラー5件）。さらに3つとも同じ誤った「重複」の判定を返し、変異で確かめると誤りだった
  - 出し先: `~/.claude/skills/test-audit/CAMPAIGN.md` の段3・段6と、`/next-task` の手順5で委譲するときの指示に「サブエージェントの中では別のエージェントを立てず、レーンを順に自分で読む」を1行。残りのキャンペーンのタスク（T-625〜T-627）の `## 注意` も同じ条件付きの書き方になっている
