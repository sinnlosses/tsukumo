# 未対応の指示メモ（ここに書くと次のセッションが `/plan-tasks` でタスク化する。正典: `task-workflow` の `WORKFLOW.md`）

## ユーザーから

## エージェントのドラフト

### ナビゲーションの改善（2026-09-21 の雑談から）

ビューの中で「どこを見ているか・どこへ戻るか」を扱う口は3つある
（画面の切り替え = `stores/screen.tsx` の hash、やり取りのタブ = `features/main-view/turn-tabs.tsx`、
狭い画面の領域タブ = `features/layout/layout.tsx`）。弱いのは2つめ。
**「ラベルを中身にする」と「見ているやり取りを hash に乗せる」の2つは 2026-09-21 に承認を得て
タスク化した**（`docs/history/direction.md`）。残りは次の1つ。

- **キーボードで前後のやり取りへ移る**: いまはタブを押すしかない。入力欄から手を離さずに
  前のやり取りを見られるようにしたい。割り当てる鍵は未決。
