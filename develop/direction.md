# 未対応の指示メモ（ここに書くと次のセッションが `/plan-tasks` でタスク化する。正典: `task-workflow` の `WORKFLOW.md`）

## ユーザーから

## エージェントのドラフト

- **`task done` とタスクの登録で書いたファイルを、送る前に整形コマンドへ通す**（振り返り: T-527, T-553）
  - 根拠: T-527 の `task ship` が付け替え後の `format:check` で落ちた。原因は2つで、`task done --result-file` が書き込んだ `## 結果` の表と、先に `main` へ入った T-553 の登録コミットが `develop/direction.md` に残した空行2行（`115a3843`）。どちらも手で `bun run format` をかけ、amend して送り直した
  - 出し先: `task-workflow` の `scripts/task.py`（`done` と `new` が書いたファイルに、CLAUDE.md の整形コマンドをかける）か、`next-task` の手順7と `plan-tasks` のコミットの直前に「整形コマンドを打つ」を足す
- **正典を縮めるタスクには、消した規則それぞれの行き先を表で出させる**（振り返り: T-527, T-528）
  - 根拠: T-528 で CLAUDE.md を 413行 → 347行に縮めたとき、「新しい作業ツリーの立ち上げは人がやる（`bun install` と `bun run build`。自動化はしない）」が、どこにも移されずに消えていた（受け入れのときに見つけて戻した）。完了条件で項目ごとの行き先の表を求めた T-527（89件）では、消えたものは見つかっていない
  - 出し先: `docs/workflow.md`「タスクを書くとき・受け入れるとき」に、「CLAUDE.md や docs の節を削る・縮めるタスクは、消した規則ごとに行き先（移した節・捨てた理由）を完了条件に書かせる」を足す
- **`task` を、変数に入れずに打てるコマンドにする**（振り返り: T-537, T-533）
  - 根拠: `T="python3 …/task.py"; $T …` が zsh で単語に分かれず「no such file」で空振りした件が2回目（1回目は T-537 で、`task done` だけ落ちて `git commit` が走った。2回目は T-533 の `task status` で、害は無かった）。1回目のときに「2回目が起きたら仕組みで防ぐ」と決めていた
  - 出し先: `task` を PATH 上のコマンドにするタスク（例: `bun link` と同じく `~/.bun/bin/` などへ置くラッパー）。グローバルなツールの導入にあたるので、形を決める前に承認を取る
