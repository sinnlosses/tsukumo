[
  {
    "id": "T-046",
    "summary": "Orca のタブのままか、アプリに包むかを使ってから決める",
    "task": "Orca のタブのまま行くか、Electron 等で1つのアプリに包むかを決める。\n\n## 背景\n\nユーザー決定 Q4: 箱は当面 Orca のブラウザタブ。**最終形は「使い勝手が上がるなら」スタンドアロンのアプリ**（「この orca のように統合されたアプリケーション内でキャラと一緒に仕事がしたい」）。判断は縦1本と横の広がり（T-042〜T-045）まで動いて、実際に使ってからにする（Q13 の 4）。\n\nいまの構成では、ページは「どの箱でも動く Web アプリ」で、Orca への依存は `showView`（`orca tab create --url`）だけ（T-042 後）。Electron で包む場合は、Bun のプロセス（駆動＋ビューサーバ）を子として起こし、`BrowserWindow` で `http://127.0.0.1:<port>/layout` を開くだけで済む見込み。Tauri なら Rust の toolchain が要る（2026-09-12 に再確認して `cargo` / `rustc` / `rustup` はいずれも未導入。`node` v22.13.1 と Xcode はある）。\n\n**選択肢の比較そのものは T-057 が先に作る**（2026-09-12 のユーザーの問いかけ「Electoron がベストかな? Tauri や他の選択肢も検討する?」）。このタスクは、その表（`docs/research/app-shell.md`）とユーザーの使った感想を突き合わせて**決める**側に専念する。\n\n## 解くべき論点\n\n- **Orca のタブで困ったことが実際にあったか**（ユーザーが使った感想が正典。推測で決めない）: タブが他のタブに埋もれる、起動に `orca` が要る、ウィンドウの大きさ、キーボードショートカットの衝突、通知\n- 包むなら何で包むか。**候補と軸は T-057 の `docs/research/app-shell.md` から取り、Electron / Tauri の2択に絞らない**（素のブラウザのアプリモード・WKWebView・現状維持も候補）。決め手は「外部依存を増やす承認」（`CLAUDE.md`）と、ユーザーの手元の toolchain、そして1で挙げた困りごとが実際に解けるか\n- 包んだあとも Orca のタブで開ける経路を残すか（残すと `showView` の抽象がそのまま活きる）\n\n## やること\n\n1. ユーザーに使った感想を聞き、困りごとを列挙する\n2. 困りごとが無ければ「Orca のタブのまま」で閉じ、`docs/requirements.md` 7章の未決事項から外す\n3. 包むなら、T-057 の比較表から1つ選び、選んだ理由と実装の手順を `develop/direction.md` に書いてタスク化する（このタスクでは実装しない）\n\n## 完了条件\n\n- 判断とその根拠（ユーザーの感想の要約）が `evidence` と `docs/requirements.md` 7章に書かれていること\n- 包むと決めた場合、`docs/research/app-shell.md` のどの軸が決め手になったかが `evidence` に書かれていること\n- `bun run check` が通ること\n\n## 注意\n\n- **ユーザーへの確認が要る。** サブエージェントに委譲せず、`/loop` に載せない\n- 外部依存（Electron / Tauri / cargo）を増やすときはユーザーの承認を得る",
    "status": "todo",
    "difficulty": "opus",
    "passes": false,
    "evidence": "",
    "dependencies": ["T-042", "T-043", "T-044", "T-045", "T-057"]
  },
  {
    "id": "T-058",
    "summary": "TUI のプロジェクトで HTML を出さない切り替え方を決める",
    "task": "`tsukumo` 以外のプロジェクト（素の TUI）でレポートに HTML タグが出ないようにする方式を決める。\n\n## 背景\n\n出力スタイルは `~/.claude/output-styles/asuna.md` の1ファイルで、`~/.claude/settings.json` の\n`outputStyle: \"Asuna\"`（2026-09-12 実測）により**すべてのプロジェクトに同じものが適用される**。\nこのファイルの「### レポートの組み立て方」（2026-09-12 時点で 104〜130行）は、レポートに\n`<div class=\"note\">` / `<span class=\"badge\">` / `<div class=\"cols\">` / `<details>` /\n`<blockquote>` / `<hr>` を使うよう指示している。これらは tsukumo のメインビュー\n（`src/report-html.ts` が Markdown を HTML に変える）で描くための記法なので、**tsukumo 以外の\nプロジェクトで素の TUI を使うと、タグがそのまま文字として見えてしまう**（ユーザーの報告）。\n\n同じ節は逆向きの制約も課している: 「Markdown の引用 `> `・ネストしたリスト・画像・水平線 `---` は\n描けない」。これは tsukumo のレンダラの都合であって、TUI では成り立たない制約。\n\n**分岐の前例は既にある。** 同ファイルの「### `speak` の呼び方」は、`mcp__tsukumo__speak` が\nツール一覧にあるかどうかで `speak` と行頭 `アスナ: ` を切り替えている。環境を見分ける手がかりは\nモデル側に既に渡っている。\n\nSDK 側の手がかり（`node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts` を 2026-09-12 に確認、\nv0.3.268）: `Query.applyFlagSettings({ outputStyle })` がセッション限りの層に出力スタイルを\n指定できる（設定ファイルには書かない）。`Query.updateSettings('localSettings', ...)` の許可キーも\n`outputStyle` だけ。`init` メッセージに `output_style` と `available_output_styles` が来る。\n`src/session-driver.ts` の `startSession` はいま `outputStyle` を一切指定していない。\n\n## 解くべき論点\n\n- **どの方式で切り替えるか。** 少なくとも次の3つを比べる:\n  - (a) **出力スタイルを2つに分ける**: `asuna.md`（TUI 向け・HTML なし）と tsukumo 向けの別ファイルを\n    置き、tsukumo が起動時に `applyFlagSettings({ outputStyle: ... })` で明示指定する。確実に\n    切り替わるが、**人格の記述が2ファイルに重複**して片方だけ直す事故が起きる\n  - (b) **1ファイルのまま規約を条件分岐にする**: 「`mcp__tsukumo__speak` が無い環境では HTML ブロックと\n    描けない記法の制約を使わない」と書く。既存の `speak` の分岐と同じ手口で、実装は要らない。ただし\n    **守られたかどうかがこちら側から分からない**（`docs/requirements.md` 4.2「なぜテキストの規約を\n    やめたか」と同じ弱点を抱える）\n  - (c) **差分を tsukumo 側が足す**: グローバルの `asuna.md` は TUI 向けに保ち、HTML の記法は tsukumo が\n    `systemPrompt` の append（`src/session-driver.ts` の `query()` オプション）で足す。TUI には\n    物理的に届かず、正典がリポジトリ内に来る。**`systemPrompt` と出力スタイルが重ねて効くかは未確認**\n- **重複をどう防ぐか**（(a) を採る場合）。人格の記述を両方に持つのか、片方を「もう片方＋差分」と書くのか\n- **TUI 側で何を許すか**: HTML を消すだけか、mermaid / chart のフェンスも消すか、引用 `> ` や\n  ネストしたリストの禁止も解くか\n- **ユーザーが手で戻せる状態を残すか**（`/output-style` で選び直せるか）\n\n## やること\n\n1. 上の3方式を、(i) TUI にタグが漏れない確実さ (ii) 記述の重複 (iii) tsukumo 側の実装量\n   (iv) 手で戻せるか、の4軸で比べて表にする\n2. (c) を候補に残すなら、**`systemPrompt` の append と出力スタイルが同時に効くかを実際に確かめる**\n   （`TSUKUMO_VIEW_PORT` を変えて起こし、`init` の `output_style` と応答の口調を見る）。効かないと\n   分かったらそこで候補から外し、理由を `evidence` に書く\n3. ユーザーに方式を選んでもらい、**グローバル設定（`~/.claude/output-styles/` と\n   `~/.claude/settings.json`）を触る範囲について承認を得る**\n4. 決めた方式・根拠・実装手順を `evidence` と `docs/requirements.md` 4.2 に書く。**実装はしない**\n\n## 完了条件\n\n- 3方式の比較表と、選んだ方式・選んだ理由が `evidence` に書かれていること\n- `docs/requirements.md` 4.2 に決定が追記され、`grep -c '^#\\{2,3\\} ' docs/requirements.md` が\n  編集の前後で変わらないこと（前後の値を `evidence` に書く）\n- (c) を検討したなら、`systemPrompt` の append と出力スタイルが同時に効くかの実測結果が\n  `evidence` にあること\n- `bun run check` が通ること\n\n## 注意\n\n- **ユーザーの選択と承認が要る。サブエージェントに委譲せず、`/loop` に載せない**\n- **このタスクでは `~/.claude/` を書き換えない**（決めるだけ）\n- 後続タスクで `~/.claude/settings.json` を触るときは、**orca が専有する hooks（12件）と\n  statusLine を壊さない**。`outputStyle` 以外のキーに触れない\n- 動作確認で tsukumo を起こすときは `TSUKUMO_VIEW_PORT` を変え、**常駐している 7327 番のプロセスを\n  落とさない**。止めるのは自分が起こした pid だけ",
    "status": "todo",
    "difficulty": "opus",
    "passes": false,
    "evidence": "",
    "dependencies": []
  },
  {
    "id": "T-059",
    "summary": "決めた方式で TUI 向けの HTML なし出力を実装する",
    "task": "T-058 が選んだ方式で、tsukumo 以外のプロジェクト（素の TUI）のレポートに HTML タグが出ないようにする。\n\n## 背景\n\n`~/.claude/output-styles/asuna.md` の「### レポートの組み立て方」（2026-09-12 時点で 104〜130行）が\n`<div class=\"note\">` / `<span class=\"badge\">` / `<details>` などを使うよう指示しているため、tsukumo の\nメインビュー以外（素の TUI）ではタグが文字として見える。**方式と実装手順は T-058 の `evidence` と\n`docs/requirements.md` 4.2 が正典**で、このタスクでは決め直さない。\n\ntsukumo 側で触る可能性があるのは `src/session-driver.ts` の `query()` オプション\n（`systemPrompt` の append、または起動後の `applyFlagSettings({ outputStyle })`）だけ。\n**`src/report-html.ts` とブラウザ側の CSS は変えない**（ブラウザでの描き方は今までどおり）。\n\n## やること\n\n1. T-058 が決めた方式で実装する\n2. `~/.claude/` を触るなら、**先にバックアップを取り、ユーザーの承認を得る**\n   （`asuna.md.bak-<日付>` の形式。既存のバックアップと同じディレクトリ）\n3. `bun run check` を通す\n4. TUI 側と tsukumo 側の両方を目視する（完了条件のとおり）\n5. 実装してみて T-058 の想定どおりに動かなかったら、**押し切らずに**理由を `evidence` に書いて止め、\n   `develop/direction.md` に差し戻す\n\n## 完了条件\n\n- **TUI 側の目視**: tsukumo 以外のプロジェクト（例:\n  `/Users/sinnlos/ghq/github.com/sinnlosses/day-snap`）で素の `claude` を起こし、表・箇条書き・\n  注意の強調を含む答えになる依頼を1つ送る。応答に `<div` / `<span` / `<details` / `<blockquote` /\n  `<hr` が**1つも出ない**こと。どのディレクトリで何が見えたかを `evidence` に書く\n  （**依頼と応答の中身は写さない**）\n- **tsukumo 側の目視**: `tsukumo` を起こして Orca のタブで同じ種類の依頼を送り、メインビューに\n  **注意の枠（`.note`）とバッジ（`.badge`）が今までどおり描かれる**こと（タグが素の文字として\n  出ていないこと）\n- `bun run check` が通ること\n- `~/.claude/output-styles/asuna.md` を変更したなら、`grep -c '^#\\{2,3\\} ' ~/.claude/output-styles/asuna.md`\n  が編集の前後で減っていないこと（前後の値を `evidence` に書く）\n- `~/.claude/settings.json` を変更したなら、`hooks` のキー12件と `statusLine` が編集の前後で\n  変わらないこと（前後の値を `evidence` に書く）\n\n## 注意\n\n- **`~/.claude/output-styles/asuna.md` と `~/.claude/settings.json` はユーザーのグローバル設定。\n  書き換えには必ず承認を得る。** サブエージェントに委譲せず、`/loop` に載せない\n- **出力スタイルはセッション起動時にしか読まれない**（`/clear` では読み直されない。2026-09-10 実測）。\n  目視の前に claude / tsukumo を起こし直す\n- 起こしたプロセスは終わったら止める。**常駐の 7327 番を落とさない**\n  （`pgrep -f claude-agent-sdk` は常駐の分も拾うので、空になることを完了条件にしない）\n- `src/report-html.ts` とブラウザ側の CSS は変えない",
    "status": "todo",
    "difficulty": "sonnet",
    "passes": false,
    "evidence": "",
    "dependencies": ["T-058"]
  },
  {
    "id": "T-062",
    "summary": "レポートを読む時間を減らすために何を足すかを決める",
    "task": "レポートを読み終えるまでの時間を短くするために、どの層に何を足すかを決める。\n\n## 背景\n\nユーザーの指示: 「レポートの出力をもっとリッチに、段組みしたり見やすくしたりできる限り人間が\n理解するまでの時間を減らせるよう工夫してほしい」。**何が読みにくいかはユーザーの主観が正典**なので、\n推測で足さずに聞いてから決める。\n\nいまの到達点（2026-09-12 に現物を読んで確認。3層に分かれている）:\n\n- **(i) レンダラ**: `src/view.ts` の `renderMarkdownToHtml`（2548行目付近）が自前の Markdown→HTML。\n  **外部の Markdown ライブラリに依存しない**方針（同関数の doc コメント）。対応するのは\n  見出し / フェンス付きコード / 箇条書き（**ネストは1段に平らにする**）/ GFM テーブル /\n  インライン（`**太字**`・`` `code` ``・`[text](url)`）/ HTML ブロック。\n  **描けないもの: 引用 `> `・ネストしたリスト・画像・水平線 `---`**（段落テキストに落ちる）。\n  テーブルの列揃え（`:---:`）も見ていない。```` ```mermaid ```` は `<pre class=\"mermaid\">`、\n  ```` ```chart ```` は `<canvas data-chart>` になり（2686〜2694行目付近）、mermaid（3.2MB）と\n  Chart.js（196KB）はその記法が出たときだけ読み込む（144行目付近）\n- **(ii) 通す HTML と見た目**: レポートが直接書いた HTML は `src/report-html.ts` の\n  `sanitizeReportHtml` の許可リストを通る（`div` / `span` / `table` / `details` / `summary` /\n  `dl` / `figure` / `svg` 系など、15行目〜）。見た目の語彙は `src/view.ts` の CSS にある\n  `.detail-block` 配下の `.cols`（`repeat(auto-fit, minmax(14rem, 1fr))` の段組み）/ `.card` /\n  `.badge`（ok / warn / ng）/ `.note`（warn / ng）/ `blockquote` / `table`\n  （1934〜1969行目付近と 2079〜2090行目付近）\n- **(iii) 組み立ての規約**: `~/.claude/output-styles/asuna.md` の「### レポートの組み立て方」\n  （104〜130行）が「内容の種類 → 使う構造」の対応表を持つ。**(i) が描けないぶんを\n  `<blockquote>` / `<hr>` への迂回で埋めている**（126行目）\n\n**外してはいけない制約**: `src/view.ts` の `buildMainBody`（1049〜1050行目付近）に\n「**中身を要約・再構成しない**（読みづらさの主因は見た目であって内容ではないため。\n`docs/requirements.md` 2.2）」と書いてある。ここは 2.2 のスコープ外と直結しているので、\n見せ方で解く。\n\n## 解くべき論点\n\n- **どの層に効かせるか。** (i) レンダラの記法を増やす / (ii) CSS と通す HTML を増やす /\n  (iii) 出力スタイルの組み立て規約を直す、の3つは効き方が違う。(iii) だけで済むなら\n  コードは1行も要らない\n- **「中身を要約・再構成しない」との線引き。** 段組みにする・目次を付ける・先頭に結論を固定するは\n  見せ方か再構成か。**どこまでを見せ方と呼ぶかをここで決めて書き残す**（後で同じ議論が再燃するため）\n- **外部の Markdown ライブラリを同梱するか。** いまは自前・実行時依存なし。同梱するなら\n  `docs/architecture.md`「外部ライブラリは CDN から読まず、同梱して自分で配る」に従い、\n  サニタイズ（`sanitizeReportHtml`）を通す経路を保つ必要がある。容量も増える\n  （mermaid の 3.2MB という前例がある）\n- **段組みを誰が決めるか。** モデルが `<div class=\"cols\">` で明示するのか、レンダラが\n  内容から自動で段に割るのか。自動にすると (i) の制約に触れる\n- **追加候補の棚卸しと取捨。** 目次、`<details>` の既定の開閉、コードの行番号、\n  テーブルの列揃え（`:---:`）、`.note` の種類を増やす、レポート先頭の要約を固定表示、\n  見出しへのアンカー、長いレポートでのスクロール位置の保持。**足さないものは理由を書く**\n  （`CLAUDE.md`「機能を足そうとする前に `docs/requirements.md` 1と 2.2 を見る」）\n\n## やること\n\n1. いま描けるもの・描けないものを**実物で棚卸しして表にする**（上の (i)(ii)(iii) の3層ごと）。\n   確かめ方は、各記法を含むレポートを1つ書いて Orca のタブで見る\n2. ユーザーに**どのレポートのどこが読みにくかったか**を聞く。主観が正典なので、ここを飛ばさない\n3. 3層のどこに何を足すかを決める。**足すものと足さないものを、理由付きで両方書く**\n4. 決めた実装は**このタスクではやらない**。後続タスクの案を `develop/direction.md` に書き出す\n   （T-046 と同じ進め方）\n5. 決定を `docs/requirements.md` 4.2 に記録する\n6. **棚卸しの結果「いまの語彙で足りていて、足りないのは規約の書き方だけ」と分かったら**、\n   出力スタイル（(iii)）の改訂1件だけに落として閉じてよい。その場合はコードを触らず、\n   理由を `evidence` に書く\n\n## 完了条件\n\n- **棚卸しの表**（3層それぞれの「描ける / 描けない」）が `evidence` に書かれていること\n- **ユーザーから聞いた読みにくさ**の要約が `evidence` にあること（**レポートの中身は写さない**）\n- 足すと決めたものと、**足さないと決めたものとその理由**が両方 `evidence` にあること\n- 後続タスクの案が `develop/direction.md` に書かれていること。または 6 の逃げ道で閉じた場合は\n  その理由が `evidence` にあること\n- `docs/requirements.md` 4.2 に決定が追記され、`grep -c '^#\\{2,3\\} ' docs/requirements.md` が\n  編集の前後で変わらないこと（前後の値を `evidence` に）\n- `bun run check` が通ること\n\n## 注意\n\n- **ユーザーの感想が要る。サブエージェントに委譲せず、`/loop` に載せない**\n- **このタスクでは実装しない**（決めて書き出すところまで）\n- `~/.claude/output-styles/asuna.md` はユーザーのグローバル設定。**書き換えには承認を得て、\n  先にバックアップを取る**。`~/.claude/settings.json` を触る場合は、orca が専有する hooks（12件）と\n  statusLine を壊さない\n- 棚卸しで tsukumo を起こすときは `TSUKUMO_VIEW_PORT` を変え、**常駐している 7327 番のプロセスを\n  落とさない**。止めるのは自分が起こした pid だけ\n- **`bun run start` / `tsukumo` は本物の claude を子プロセスで起こす**（API の利用が発生する）",
    "status": "todo",
    "difficulty": "opus",
    "passes": false,
    "evidence": "",
    "dependencies": []
  },
  {
    "id": "T-063",
    "summary": "レポートで描けない引用・ネスト・水平線を埋める",
    "task": "レンダラが描けない Markdown 記法（引用・ネストしたリスト・水平線）を埋める。\n\n## 背景\n\n`src/view.ts` の `renderMarkdownToHtml`（2548行目付近）は、**引用 `> `・ネストしたリスト・\n画像・水平線 `---` をブロックとして認識せず、段落テキストとして出す**（同関数の doc コメントに\n明記されている既知の制約）。箇条書きのネストも `consumeList` が1段に平らにする。\n\nそのぶんを出力スタイル側が迂回で埋めている。`~/.claude/output-styles/asuna.md`\n「### レポートの組み立て方」（126行目）は「引用と区切りが要るときは `<blockquote>` / `<hr>` を\nHTML で書く」「**ネストは1段まで**」と、**レンダラの都合を書き手の規約として押し付けている**\n状態にある。`develop/progress.md` にも T-020 の残した宿題として記録がある。\n\nCSS 側には受け皿が既にある（`.detail-block blockquote` は 1963行目付近で定義済み）。\n`sanitizeReportHtml`（`src/report-html.ts`）も `blockquote` / `hr` / `ul` / `ol` / `li` を通す。\n\n**このタスクの進め方は T-062 の決定に従う。** T-062 が「外部の Markdown ライブラリを同梱する」と\n決めた場合、自前で記法を足す作業は無駄になるため、着手前に T-062 の `evidence` と\n`docs/requirements.md` 4.2 を読む。\n\n## やること\n\n1. **T-062 の決定を読む。** 「自前のレンダラのままで足す」と決まっていればこのタスクを進める。\n   ライブラリ同梱に決まっていた場合や、これらの記法を足さないと決まっていた場合は、\n   **実装せずに理由を `evidence` に書いて閉じる**\n2. `renderMarkdownToHtml` に引用（`> `）・水平線（`---` / `***`）のブロック認識を足す\n3. `consumeList` のネストを2段まで（インデント2スペースまたはタブ1つを1段とみなす）扱えるようにする\n4. 出力スタイル（`~/.claude/output-styles/asuna.md`）の「### レポートの組み立て方」から、\n   **描けないことを前提にした迂回の記述を外す**（承認を得てから。バックアップを取る）\n5. `docs/requirements.md` 4.2 の対応記法の記述を実態に合わせる\n6. `bun run check` を通す\n\n## 完了条件\n\n- **テスト**: `test/view.test.ts` に次の3つを足し、`bun test` が通ること\n  （増えた件数を `evidence` に書く）\n  1. `> 引用` の行が `<blockquote>` を含む HTML になる\n  2. 2段のネストした箇条書きが `<ul>` の入れ子になる（平らにならない）\n  3. `---` だけの行が `<hr>` になる\n- **描画が壊れていないこと**: 既存の `test/view.test.ts` の件数が減っていないこと\n  （テーブル・コードブロック・HTML ブロックの判定を壊していない。特に `---` を水平線として\n  扱うようにすると**GFM テーブルの区切り行と衝突する**ので、テーブルのテストが通ることを\n  `evidence` で示す）\n- **目視**: Orca のタブで、引用・2段のネスト・水平線を含むレポートを1つ出し、\n  それぞれが引用の縦線・入れ子の箇条書き・横線として描かれていること。どの URL で何が見えたかを\n  `evidence` に書く（**レポートの中身は写さない**）\n- `~/.claude/output-styles/asuna.md` を編集したなら、\n  `grep -c '^#\\{2,3\\} ' ~/.claude/output-styles/asuna.md` が編集の前後で減っていないこと\n- `bun run check` が通ること\n\n## 注意\n\n- **`---` は GFM テーブルの区切り行と見分けが要る。** `isTableStart` の判定より後ろで\n  水平線を判定するか、区切り行の形（`|` を含む）を先に除く\n- **目視が要るので `/loop` に載せない。** 実装だけをサブエージェントに委譲してよい\n- `~/.claude/output-styles/asuna.md` はユーザーのグローバル設定。**書き換えには承認を得て、\n  先にバックアップを取る**（`asuna.md.bak-<日付>`）。`~/.claude/settings.json` は触らない\n  （触る必要が出たら、orca が専有する hooks（12件）と statusLine を壊さない）\n- **外部の Markdown ライブラリを持ち込まない**（それを採るかは T-062 が決める。\n  このタスクは自前で足す前提）\n- 目視で tsukumo を起こすときは `TSUKUMO_VIEW_PORT` を変え、**常駐している 7327 番を落とさない**",
    "status": "todo",
    "difficulty": "sonnet",
    "passes": false,
    "evidence": "",
    "dependencies": ["T-062", "T-069"]
  },
  {
    "id": "T-064",
    "summary": "キャラクター切り替えの単位と切り替え時機を決める",
    "task": "キャラクターを切り替えられるようにするための方式を決める（何を1人の単位にするか・いつ切り替わるか・操作をどこに置くか）。実装はしない。\n\n## 背景\n\nユーザーの指示: 「キャラクターの切り替えを行えるようにしたい。output style と characters ディレクトリ配下の切り替えを同時に行うことが必要そう?」\n\nいまの構成（2026-09-12 に現物を読んで確認）:\n\n- **素材と定義**: `characters/<名前>/character.json`（`portraits` / `outfitAccents`）を `src/character.ts` の `parseCharacterDefinition` が読む。どのディレクトリを読むかは `src/index.ts` の `resolveBundledDir(process.env.TSUKUMO_CHARACTER_DIR, cwd, [\"characters\", \"tsukumo-spirit\"])` が**起動時に1回だけ**決め、`createViewPublisher` の引数として固定される（154〜162行目付近）。いま置いてあるのは `characters/tsukumo-spirit/` の1体だけ\n- **人格**: `~/.claude/output-styles/asuna.md` の1ファイル。`~/.claude/settings.json` の `outputStyle: \"Asuna\"` で全プロジェクトに効く（2026-09-12 実測。`~/.claude/output-styles/` には `asuna.md` と `.bak` 5件しか無く、**選べる自前のスタイルは1つ**）。`src/session-driver.ts` の `query()` は `outputStyle` を一切指定していない\n- **表情名の出どころ**: `availableExpressions(readCharacterDefinition(characterDir))` が `startSession` の `expressions` に渡り、`speak` の `expression` の zod enum になる（`src/session-driver.ts` の `speakServer`）。**セッションを起こす時点で固定される**\n- **人格と素材の名前は一致していない**: 立ち絵は「つくもの精霊」、人格は「Asuna」。2つは独立して選ばれている（既知として `docs/history/progress-archive.md` に記録あり）\n\nSDK 側でできること（`node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts` v0.3.268 を 2026-09-12 に確認）:\n\n- `Query.applyFlagSettings({ outputStyle })` — セッション限りの層に出力スタイルを指定する（設定ファイルには書かない）\n- `Query.updateSettings('localSettings', { outputStyle })` — 許可キーは `outputStyle` だけ。**プロジェクトの local 設定ファイルに書く**\n- `Query.reloadOutputStyles()` — 出力スタイルのディレクトリを読み直し、選べる名前を返す\n- `initializationResult()` と `init` メッセージの `output_style` / `available_output_styles` — 今のスタイルと選択肢が取れる\n- `Query.setMcpServers(...)` — MCP サーバを入れ替えられる（`speak` の表情 enum を作り直す経路になる）\n- **いずれも「本当に効くか」は未実測**。「出力スタイルはセッション起動時にしか読まれない」という実測（2026-09-10）があるので、途中で差し替えて口調が変わるかは確かめるまで分からない\n\n## 解くべき論点\n\n- **「キャラクター1人」を何で表すか。** 人格（出力スタイル）と素材（`characters/<名前>/`）を1つの単位に束ねるのか、別々に選べるままにするのか。束ねるなら `character.json` に人格への参照（出力スタイル名、または同じディレクトリの人格ファイル）を持たせるのか\n- **人格の正典をどこに置くか。これは T-058 が決める話と同じ場所を触る。** T-058 が (c)「tsukumo が `systemPrompt` で足す」を選ぶなら人格の正典はリポジトリ側（キャラクターディレクトリ）に来るので、切り替えはファイルの読み替えで済む。(a)「出力スタイルを複数置く」なら `~/.claude/output-styles/` に人格ぶんのファイルが並び、切り替えは `applyFlagSettings({ outputStyle })` になる。**T-058 の決定に乗り、ここで別の方式を作らない**\n- **いつ切り替わるか**: (i) 起動時だけ（環境変数・引数で選ぶ。コードはほぼ要らない）/ (ii) セッションの途中（画面から選ぶ。出力スタイルが途中で効くか、`speak` の表情 enum を作り直す必要があるかの未実測を解く必要がある）/ (iii) 画面から選んだらセッションを起こし直す（**会話は捨てる**。`docs/requirements.md` 2.2 でセッションの再開は対象外なので、続きにはできない）\n- **操作をどこに置くか**（画面から切り替える場合）: サイドバーのセッション情報にある許可モード・モデルの `<select>` の隣か。**`<select>` 1つで人格と素材がまとめて変わる**のが指示の意図に合う\n- **切り替えたときに画面の何を捨てるか**: 吹き出し（前のキャラのセリフ）・立ち絵・メインビューのレポート\n- **素材が1体しか無い環境で何を出すか**（いまの状態）。選択肢が1つのときに `<select>` を出すのか\n- **`~/.claude/` を触るか。** 触るならユーザーの承認が要る（`CLAUDE.md`）。触らずに済む方式があるならそちらを優先する\n\n## やること\n\n1. T-058 の決定（`evidence` と `docs/requirements.md` 4.2）を読み、人格の正典の置き場所を確認する\n2. 切り替えの時機 (i)(ii)(iii) を「指示の意図（人格と素材が同時に変わる）を満たすか」「未実測の前提がいくつ残るか」「実装量」「切り替えで会話が消えるか」の4軸で比べて表にする\n3. (ii) を候補に残すなら、**出力スタイルをセッションの途中で差し替えて実際に口調が変わるかを確かめる**（`TSUKUMO_VIEW_PORT` を変えて起こし、`applyFlagSettings({ outputStyle })` の前後で応答を見る。**会話の中身は記録しない**）。変わらないと分かったら (ii) を落とし、理由を `evidence` に書く\n4. ユーザーに方式を選んでもらう。**2体目の素材と人格をどう用意するつもりかもここで聞く**（`characters/local/` は `.gitignore` 済みで、素材の同梱は `docs/requirements.md` 2.2 で対象外）\n5. 決めた方式・根拠・実装手順を `evidence` と `docs/requirements.md` 4.4 に書く。**実装はしない**\n6. **聞いた結果「2体目を用意する予定が無い」と分かったら、切り替えの仕組みは作らずに閉じてよい**（理由を `evidence` に書く）。その場合は T-065 も同じ理由で閉じる\n\n## 完了条件\n\n- 切り替えの時機の比較表と、選んだ方式・選んだ理由が `evidence` に書かれていること\n- 「キャラクター1人」を何で表すか（人格と素材の束ね方）の決定が `evidence` にあること\n- (ii) を検討したなら、**出力スタイルを途中で差し替えて口調が変わるかの実測結果**が `evidence` にあること\n- `docs/requirements.md` 4.4 に決定が追記され、`grep -c '^#\\{2,3\\} ' docs/requirements.md` が編集の前後で変わらないこと（前後の値を `evidence` に書く）\n- `bun run check` が通ること\n\n## 注意\n\n- **ユーザーの選択が要る。サブエージェントに委譲せず、`/loop` に載せない**\n- **このタスクでは実装しない。`~/.claude/` も書き換えない**（決めるだけ）\n- 実測で tsukumo を起こすときは `TSUKUMO_VIEW_PORT` を変え、**常駐している 7327 番のプロセスを落とさない**。止めるのは自分が起こした pid だけ\n- **`bun run start` / `tsukumo` は本物の claude を子プロセスで起こす**（API の利用が発生する）\n- 口調の確認に使った応答の中身を `evidence` やログに写さない（`docs/coding-standards.md`「会話内容の扱い」）",
    "status": "todo",
    "difficulty": "opus",
    "passes": false,
    "evidence": "",
    "dependencies": ["T-058"]
  },
  {
    "id": "T-065",
    "summary": "決めた方式でキャラクターの切り替えを実装する",
    "task": "T-064 が決めた方式で、キャラクター（人格＋素材）の切り替えを実装する。\n\n## 背景\n\n**方式と実装手順は T-064 の `evidence` と `docs/requirements.md` 4.4 が正典**で、このタスクでは決め直さない。\n\nいまは切り替える余地が無い（2026-09-12 時点）:\n\n- `src/index.ts` がキャラクターディレクトリを起動時に1回だけ決め（`resolveBundledDir(process.env.TSUKUMO_CHARACTER_DIR, ...)`、154〜162行目付近）、`createViewPublisher` に固定の引数として渡している。立ち絵と差し色は配るたびに `readCharacterAssets(characterDir, ...)` でファイルから読み直しているので、**ディレクトリを持つ場所が変数になれば立ち絵側の差し替えは届く**\n- 表情名は `availableExpressions(...)` の結果が `startSession` の `expressions` に渡り、`speak` の zod enum として固定される（`src/session-driver.ts` の `speakServer`）。表情の集合が違うキャラに変えるなら `Query.setMcpServers` で作り直すか、セッションを起こし直すことになる\n- 人格（出力スタイル）は tsukumo から一切指定していない（`query()` のオプションに `outputStyle` が無い）\n\n画面から切り替える形にする場合、**前例がそのまま使える**: 許可モードとモデルの `<select>` は `src/view.ts` の `sessionInfoBody` が出し、`src/view-server.ts` の POST が受け、`src/index.ts` の配線が `driver.setPermissionMode` / `driver.setModel` を呼んでいる。同じ経路をもう1本足す形になる。\n\n## やること\n\n1. T-064 が決めた方式で実装する（起動時だけで済む方式なら、環境変数・引数の受け取りと一覧の読み取りだけで閉じる）\n2. 「決める」部分（どのディレクトリが選べるか・名前から定義を引く・選択が不正なときの落とし先）は**純粋関数に寄せて `test/` にテストを足す**（`src/character.ts` か新しい概念名のモジュール。`helpers` のような置き場所を名前にしたファイルは作らない）\n3. 表情の集合が変わる場合にどうなるかを確かめ、決めた通りに動かなければ**押し切らずに**理由を `evidence` に書いて止め、`develop/direction.md` に差し戻す\n4. `~/.claude/` を触る方式なら、**先にバックアップを取り、ユーザーの承認を得てから**触る\n5. `bun run check` を通す\n6. 目視（完了条件）\n\n## 完了条件\n\n- **キャラクターを2つ用意した状態で切り替えられること**（2体目は `characters/local/` など `.gitignore` 済みの場所に置いた確認用でよい。**リポジトリにコミットしない**）\n- 切り替え後に、**立ち絵が2体目のものに変わり、次のターンの口調が2体目の人格になる**こと（人格の切り替えを含む方式を選んだ場合。起動時だけの方式なら起こし直しで変わること）\n- 選択が不正なとき（無いディレクトリ名・壊れた `character.json`）に**プロセスが落ちず**、既定のキャラクターか立ち絵なしに落ちること\n- `bun run check` が通ること（テスト件数を `evidence` に書く）\n- **目視**: Orca のタブで切り替えを1回行い、立ち絵と吹き出しがどう変わったかを `evidence` に書く（**会話の中身は写さない**）。未実施なら「未実施」と明記する（T-050〜T-056 と同じ扱い）\n- `~/.claude/settings.json` を変更したなら、`hooks` のキー12件と `statusLine` が編集の前後で変わらないこと（前後の値を `evidence` に書く）\n\n## 注意\n\n- **キャラクターの中身をコードに書かない**（`docs/architecture.md` 原則4）。キャラ名・人格の文言・表情の対応はすべて定義ファイル側に置く\n- **環境変数の読み取りは `src/index.ts` の配線に集約する**（`docs/coding-standards.md`。モジュールのトップレベルで環境変数に触らない）\n- **`~/.claude/output-styles/` と `~/.claude/settings.json` はユーザーのグローバル設定。書き換えには必ず承認を得る。** 触る方式になった場合は**サブエージェントに委譲せず、`/loop` に載せない**。`~/.claude/` を触らない方式なら `/loop` に載せてよい\n- 2体目の確認用素材を**リポジトリにコミットしない**（`docs/requirements.md` 2.2「キャラクター素材そのものの同梱・配布」は対象外）\n- 動作確認で tsukumo を起こすときは `TSUKUMO_VIEW_PORT` を変え、**常駐している 7327 番のプロセスを落とさない**。止めるのは自分が起こした pid だけ\n- **`bun run start` / `tsukumo` は本物の claude を子プロセスで起こす**（API の利用が発生する）。テストから CLI を起動しきらない",
    "status": "todo",
    "difficulty": "sonnet",
    "passes": false,
    "evidence": "",
    "dependencies": ["T-064"]
  },
  {
    "id": "T-069",
    "summary": "HTML を描く技術の現状と理想を整理して方式を決める",
    "task": "HTML の描画に特定の技術（ライブラリ・フレームワーク）を入れるべきかを、現状と理想を分析して決める。\n\n## 背景\n\nユーザーの指示（2026-09-12）「HTMLを描画するために特定の技術を検討すべきかな? 一緒に現状と理想を分析して観点整理しよう」。**分析そのものがユーザーとの共同作業**で、実装はこのタスクに含めない。\n\n現物（2026-09-12 に読んで確認）:\n\n- `src/view.ts` は 3098 行の1ファイルで、HTML・CSS・ブラウザ側の JS を**すべてテンプレート文字列**として組み立てている（`buildLayoutPage` / `buildCharacterBody` / `sidebarSection` / `subscriptionScript` / `STYLE` 定数）。ブラウザ側のスクリプトは文字列の中なので、型検査（`tsc`）も lint（`oxlint`）も届かない\n- Markdown → HTML は自前の `renderMarkdownToHtml`（2548 行目付近）。**外部の Markdown ライブラリに依存しない**方針が同関数の doc コメントにある。描けないもの: 引用 `> `・ネストしたリスト・画像・水平線（T-063 が埋める予定）\n- レポートが直接書いた HTML は `src/report-html.ts` の `sanitizeReportHtml` の許可リストを通る\n- 更新は SSE で**領域まるごとの本文**を押し、ブラウザ側が `el.innerHTML = event.data` で差し替える（`subscriptionScript`、552 行目付近）。本文が前回と同じなら差し替えないガードと、領域単位のスクロール位置の復元が手当てとして入っている。**領域の内側にスクロールする要素があると位置が飛ぶ**（T-070 の不具合はこれ）\n- 外部ライブラリの前例はある: `vendor/` に mermaid 11.15.0 / Chart.js 4.4.1 / highlight.js 11.9.0 を同梱し、`127.0.0.1` の自分のサーバから配る。**CDN からは読まない**（会話の載るページに外部通信を持ち込まないため。`docs/architecture.md`「外部ライブラリは CDN から読まず、同梱して自分で配る」と `vendor/README.md`）\n- **ビルド工程は無い。** Bun が TypeScript をそのまま実行する（`package.json` の `start`）。ランタイム依存は SDK と zod の2つだけ\n\n## 解くべき論点\n\n- **理想を先に言葉にする。** 何が不足しているのかをユーザーと列挙する。候補: (a) 差し替えで DOM の状態（スクロール位置・`<details>` の開閉・フォーカス・選択範囲）が飛ぶ、(b) ブラウザ側 JS が文字列で壊れても気づけない、(c) CSS が1つの巨大な `STYLE` 定数、(d) Markdown で描けない記法、(e) レポートの表現力（T-062 の領分）\n- **層ごとに別々に決める。全部を1つのフレームワークで解く必要はない**: (1) DOM の更新（全置換 / 差分適用 / 領域をさらに細かく分けて別々に push）、(2) HTML の組み立て（テンプレート文字列 / タグ付きテンプレート / コンポーネント）、(3) Markdown 変換（自前 / ライブラリ）、(4) CSS の持ち方\n- **ビルド工程を持ち込むか。** JSX や単一ファイルコンポーネントはバンドラを要求する。`bun build` で足りるか、`bun run start` の起動経路と `bin/tsukumo`（`bun link` で入っている）が複雑にならないか\n- **依存を足す判断**: `vendor/` 同梱の前例に沿えるか（ライセンス・サイズ・**外部通信ゼロ**）。原則3（ホスト・外部依存は1つのアダプタに閉じ込める）と衝突しないか\n- **「入れない」も答えの1つ。** いまの構成のまま個別の小さな修正で埋める案を必ず候補に並べる\n- **React を名指しで評価する。** ユーザーの指示（2026-09-12）「HTMLの描画に使えそうなライブラリある?\n  Reactが筆頭だけど今回の要件には合わない?。検討して。」。**候補に React を必ず入れ、\n  合う／合わないをこのプロジェクトの制約（ビルド工程が無い・`vendor/` 同梱で外部通信ゼロ・\n  会話がページに載る・対象ユーザーは作者本人のみ）に照らして書く。** 合わないなら\n  その理由を調査メモに残す（後で同じ検討が再燃するため）\n\n## やること\n\n1. ユーザーと現状・理想を突き合わせる（**推測で進めない**。不足の正典はユーザーの体感）\n2. 現状を上の4層に分けて1枚にまとめ、不足を列挙する\n3. 候補を軸で比較した表を `docs/research/view-rendering.md` に書く。**主張ごとに一次情報の出典 URL を添える**。候補には**現状維持**を必ず入れる。`docs/` の他のファイルが持つ「節の索引」はこのファイルには作らない（調査メモであって正典ではない）\n4. 決めた結論と理由を `docs/architecture.md`「設計判断（なぜ今の形なのか）」に `#### ` の節として1つ足す（`docs/requirements.md` には書かない。技術の選択であって要件ではない）\n5. 結論が T-062 / T-063 / T-070 に与える影響を `evidence` に1行ずつ書く。**実装が要るなら `develop/direction.md` に書いてタスク化する**（このタスクでは実装しない）\n6. 比較の結果、現状維持が最善だと分かったら、**後続の実装タスクを作らず**その理由を `evidence` と調査メモに残して閉じる\n\n## 完了条件\n\n- `docs/research/view-rendering.md` があり、4層それぞれについて候補と軸の表があること。各行の主張に一次情報（公式ドキュメント・仕様・手元の実測）の出典 URL があること\n- `docs/architecture.md` に決定の節が1つ増えていること（`grep -c '^#\\{2,4\\} ' docs/architecture.md` の前後の値を `evidence` に書く。**節の索引の表に本文を流し込んでいない**こと）\n- **「自前の `renderMarkdownToHtml` を保つか、ライブラリに置き換えるか」への答えが1文で書かれている**こと（T-063 がこれを読む）\n- **「DOM の差分適用を入れるか」への答えが1文で書かれている**こと（T-070 がこれを読む）\n- 依存を足すと決めた場合、**ユーザーの承認を得たこと**と、`vendor/README.md` の表に足す予定の版・取得元・ライセンス・サイズが `evidence` にあること\n- `bun run check` が通ること（`oxfmt` が Markdown も整形するため）\n\n## 注意\n\n- **このタスクでは実装しない。** コードは変えず、ライブラリのダウンロードも `bun add` もしない\n- **ユーザーへの確認が要る。** サブエージェントに委譲せず、`/loop` に載せない\n- **調べるのは公開情報だけ。検索クエリに会話の中身を入れない**（`docs/coding-standards.md`「会話内容の扱い」）\n- **CDN から読む形は候補に入れない**（2026-09-10 の決定。`vendor/README.md`）\n- `~/.claude/settings.json` と `~/.claude/output-styles/asuna.md` は触らない",
    "status": "done",
    "difficulty": "opus",
    "passes": true,
    "evidence": "結論: **フレームワークは入れない**。層1=Idiomorph 0.8.0 を同梱して `innerHTML` を morph に、層2=ブラウザ側 JS 812行を `.ts` へ出して `bun build`、層3=**自前の `renderMarkdownToHtml` を保つ**、層4=CSS を `.css` に割って同じ工程でまとめる。比較は `docs/research/view-rendering.md`（4層 + 「サーバが HTML を作る」系の一族 htmx / Datastar / Turbo / Next.js、各行に出典 URL）、決定は `docs/architecture.md`「描画にフレームワークを入れず、morph とビルド1段で足りないところを埋める」（節数 25→26、索引は無傷）。\nユーザーに確認した不足は (a) DOM の状態が飛ぶ (b) ブラウザ側 JS が野放し (c) CSS が巨大な1定数 の3つで、**Markdown の記法とレポートの表現力は不足に挙がらなかった**。ビルド工程の追加は許容を得た。依存の追加（Idiomorph 0.8.0 / 取得元 jsdelivr（cdnjs には無い）/ 0BSD / min+gzip 3.7KB。非圧縮は未実測）は選択肢の説明にこの4点を明示したうえで承認を得た。\n後続への影響: **T-070** は Idiomorph で直る（本文を差し替えるか同梱作業に統合する）。**T-063** は「自前で足す」で確定（ライブラリ置き換えの分岐は消えた）。**T-062** は縛らない（表現力は不足に挙がらなかったので優先度は下がるが、判断は T-062 が持つ）。実装3件は `develop/direction.md` に書き出した。",
    "dependencies": []
  },
  {
    "id": "T-070",
    "summary": "Idiomorph で差し替えを morph にしスクロール飛びを直す",
    "task": "Idiomorph を同梱して SSE の差し替えを morph にし、再描画でスクロール位置が飛ぶのを直す。\n\n## 背景\n\n**元は「サイドバーの区画を差し替えると内側のスクロール位置が先頭に戻る」不具合のタスクだった**が、\nT-069 が直し方を決めたので本文を差し替えた（2026-09-12）。\n\n不具合の中身: サイドバーのタスク一覧をスクロールしている最中に「いま何をしているか」が更新されると、\nサイドバーの本文がまるごと届いて `<ul>` ごと作り直されるため、読んでいた位置が先頭に戻る。\n`src/view.ts` の `subscriptionScript`（548行目付近）は **`el.innerHTML = event.data` の全置換**で、\n**領域自身**のスクロール位置は復元するが、**領域の内側にスクロールする要素（`.sidebar-block-scroll`）の\n位置は復元しない**。同じ理由で `<details>` の開閉・フォーカス・入力中の選択範囲も失われる。\n\nT-069 の決定（`docs/architecture.md`「描画にフレームワークを入れず、morph とビルド1段で足りない\nところを埋める」）の層1がこれに当たる。**Idiomorph で morph すれば、一致した要素は DOM に\n残ったまま中身だけが直るので、位置も開閉もフォーカスも保たれる。**\n\n同梱の材料（2026-09-12 に確認）:\n\n| 項目 | 値 |\n| --- | --- |\n| 版 | 0.8.0（npm registry の `latest`） |\n| ライセンス | 0BSD |\n| サイズ | min+gzip 3.7KB（公称。**非圧縮は未実測**。jsdelivr / unpkg とも圧縮転送で `Content-Length` が返らない） |\n| 取得元 | jsdelivr（`https://cdn.jsdelivr.net/npm/idiomorph@0.8.0/dist/idiomorph.min.js`）。**cdnjs には無い**（API が 404）ので、`vendor/` の既存4件（すべて cdnjs）とは取得元が揃わない |\n\n配る仕組みは既にある: `src/view-server.ts` が `/vendor/<name>` を**名前の allowlist**\n（`VENDOR_ASSET_CONTENT_TYPES`）で配り（355〜391行目付近）、`src/bundled-files.ts` の\n`bundledFilePath` が cwd に依存せずパスを解く。**名前を1つ足すだけで配れる見込み。**\n\n## やること\n\n1. `vendor/idiomorph.min.js` を取得する（上の URL）。`vendor/README.md` の表に\n   版・取得元・ライセンス・用途を足し、更新用の `curl` 行も足す\n2. `VENDOR_ASSET_CONTENT_TYPES` に名前を足し、ページから読むようにする。\n   **mermaid / Chart.js のような遅延読み込みにはしない**（差し替えのたびに要るので常に読む。\n   highlight.js と同じ扱い）\n3. `subscriptionScript` の `el.innerHTML = event.data` を\n   `Idiomorph.morph(el, event.data, { morphStyle: \"innerHTML\" })` に置き換える\n4. **いま入っている手当てのうち、要らなくなるものを外す**: 差し替え前後のスクロール位置の\n   保存・復元は morph が保つなら不要になる。**ただし「いちばん下から24px以内なら追従して\n   いちばん下へスクロールする」挙動は残す**（レポートが伸びるときに読み続けられるため。\n   これは morph では再現されない別の意図）。本文が前回と同じなら差し替えないガードは残す\n5. `docs/architecture.md`「ビューの更新は Server-Sent Events で押す」に、差し替え方が morph に\n   なったことを1〜2文で足す（新しい節は作らない）\n6. `bun run check` を通す\n\n## 完了条件\n\n- **テスト**: `test/view.test.ts` に次を足し、`bun test` が通ること（増えた件数を `evidence` に書く）\n  1. 購読スクリプトの中身に `Idiomorph.morph(` が含まれ、`innerHTML =` での全置換が残っていない\n  2. ページが `/vendor/idiomorph.min.js` を読む `<script>` を含む\n  3. 「いちばん下から24px以内なら追従する」挙動のテストが残っている（消えていない）\n- **テスト件数が減らないこと**（前後の件数を `evidence` に書く。0 fail）\n- **目視（これが本題）**: `TSUKUMO_VIEW_PORT` を変えて tsukumo を起こし、次の2つを確かめて\n  `evidence` に1行ずつ書く\n  1. **サイドバーのタスク一覧を途中までスクロールした状態で、「いま何をしているか」が更新されても\n     スクロール位置が戻らない**\n  2. メインビューでレポートを読んでいる途中に更新が来ても、読んでいた位置が飛ばない\n- `vendor/README.md` の表に1行増えていること\n- `bun run check` が通ること\n\n## 注意\n\n- **外部通信ゼロを崩さない。** CDN から読む形にしない（`vendor/` に置いて自分のサーバから配る。\n  `docs/architecture.md`「外部ライブラリは CDN から読まず、同梱して自分で配る」）\n- **ダウンロードは1ファイルだけ**（`idiomorph.min.js`）。`bun add` はしない（実行時の依存に\n  しない。`vendor/` の既存4件と同じ扱い）\n- **常駐している 7327 番を落とさない。** 動作確認は `TSUKUMO_VIEW_PORT` を変えて起こす\n- **目視が要るので `/loop` には載せない。** 実装はサブエージェントに委譲してよい\n",
    "status": "done",
    "difficulty": "sonnet",
    "passes": true,
    "evidence": "`bun run check` 357→359 pass / 0 fail（+2）。`vendor/idiomorph.min.js` 10587 バイト（0.8.0 / 0BSD / jsdelivr）。中身は素の IIFE（`var Idiomorph=function(){...}()`）なのでグローバルで読み、highlight.js と同じ「常に読む」扱いにした。`subscriptionScript` は `Idiomorph.morph(el, event.data, { morphStyle: \"innerHTML\" })` に。**外したのは差し替え前後の scrollTop の保存・復元だけ**で、「いちばん下から24px以内なら追従」と「本文が同じなら差し替えない」ガードは残した。\n目視は合成データの使い捨てサーバ（7401。claude は起こしていない）を立て、**Chrome DevTools Protocol で機械的に確認**: `.sidebar-block-tasks .sidebar-block-scroll` を scrollTop=150 にし、SSE の更新を3回またいでも **150 のまま**（同時に `.activity-running` の中身は Running-10→27 に変わっており、差し替え自体は起きている）。入力欄のフォーカスと打ちかけの文字も保たれた。スクリーンショットでも一覧が先頭に戻っていないことを確認（1440x900、104回更新後）。\n**実機（Orca のタブ）での目視は未実施**。なお確認中に 7327 の常駐 tsukumo が動いていないことに気づいた（`lsof -iTCP:7327` も `pgrep -f claude-agent-sdk` も空。いつ止まったかは不明）。",
    "dependencies": ["T-069"]
  },
  {
    "id": "T-071",
    "summary": "キャラビューを立ち絵主役に作り直し最新を強調する",
    "task": "キャラビューの吹き出しが使える高さを取り戻す（T-066 の縦2段の見直し）。\n\n## 背景\n\nユーザーの報告（2026-09-12）「キャラ画面の吹き出しが見えづらくなっちゃったよ。見直してくれる?」。同日の確認で、見えづらさの中身は**「使える高さが半分になった」**（選択式で回答を得た）。\n\n- 原因は T-066（`013fb9a`）。`buildCharacterBody`（`src/view.ts` 1035 行目付近）が `.balloon-anchor` を `.balloon-track` と `.balloon-spacer` の2段に割り、`STYLE`（1863 行目付近）で `.balloon-track { flex: 0 0 50%; }` / `.balloon-spacer { flex: 1 1 auto; }` としている。**吹き出しが使えるのは領域の上半分だけで、下半分は常に空**\n- ねらいは「最新の吹き出し（＝並びの下端）を領域の縦中央に固定し、過去をその上へ積む」こと。決定は `docs/requirements.md` 4.2 と `docs/glossary.md`「吹き出し」に記録済み\n- `develop/progress.md`「未解決」にも「下半分の空きをこのままにするか」が残っている\n- 狭い画面で縦積みに折り返したときは `.balloon-spacer { display: none; }` で逃がしてある（2404 行目付近）。**そちらは今回の症状には効いていない**（横並びのときの話）\n\n## 解くべき論点\n\n- **何を優先するか。** 「最新が常に同じ高さにある（視線が動かない）」と「吹き出しに高さを使える」は、セリフが増えたときに衝突する。案を並べてユーザーに選んでもらう\n  - 案A: 中央固定をやめ、T-066 以前の下端寄せに戻す\n  - 案B: `.balloon-spacer` に上限を与える（例: 領域の 1/4 まで）。最新の位置は少し上がるが track の高さが確保される\n  - 案C: `.balloon-spacer` を縮められるようにし（`flex-shrink` を効かせる）、**余裕があるときだけ中央、あふれたら下端まで使う**\n  - 案D: 立ち絵と吹き出しの横幅の比率（`.balloon-anchor { flex: 1 1 11rem; }`）も併せて見直す\n- **「中央」は何のためだったか。** 立ち絵の顔の高さと最新の吹き出しが揃うのは T-066 の目視で確かめた利点。捨てるのか、条件付きで残すのか\n- 決めたら **`docs/requirements.md` 4.2 と `docs/glossary.md`「吹き出し」を新しい決定に書き換える**（T-066 の記述を残したままにしない）\n\n## やること\n\n1. 案をユーザーに見せて選ぶ（**見えづらさの正典はユーザーの体感**。推測で決めない）\n2. `src/view.ts` の `STYLE`（必要なら `buildCharacterBody` も）を直す。**CSS のコメントに残っている T-066 の意図の説明も、新しい決定に合わせて書き換える**\n3. `docs/requirements.md` 4.2 と `docs/glossary.md`「吹き出し」を更新する。**行頭を含めて位置を特定し**（`CLAUDE.md`「ドキュメントを編集するときの罠」）、節数が変わっていないことを確かめる\n4. `develop/progress.md`「未解決」から決着した項目を外す\n5. `bun run check` を通す\n\n## 完了条件\n\n- **目視**: Orca のタブで、セリフ1件のときと5件以上のときの両方を見て、(a) 吹き出しが縦に使える高さが領域の半分より広いこと、(b) 最新のセリフが立ち絵の隣で読めること、(c) ウィンドウを狭めて縦積みに折り返しても破綻しないこと。どの環境で何を見たかを `evidence` に1行で書く（**セリフの中身は書かない**）\n- `docs/requirements.md` / `docs/glossary.md` の吹き出しの記述が実装と一致していること（`grep -c '^#\\{2,3\\} ' docs/requirements.md` と `docs/glossary.md` の前後の値を `evidence` に書く）\n- `bun run check` が通り、テスト件数の前後が `evidence` に書かれていること\n\n## 注意\n\n- **ユーザーへの確認と目視が要る。** サブエージェントに委譲せず、`/loop` に載せない\n- **T-066 の決定を一部覆す可能性がある。** 覆すなら正典（`docs/requirements.md` / `docs/glossary.md`）も同時に直す。古い決定を残さない\n- 動作確認で別のプロセスを起こすときは `TSUKUMO_VIEW_PORT` を変え、自分が起こした pid だけを止める\n- `~/.claude/` 配下は触らない\n\n## 補記（2026-09-12）\n\n着手時にユーザーから「キャラ画面を再考してほしい。UX を最大にするために」と指示があり、**スコープを吹き出しの縦位置の調整からキャラビュー全体の再設計に広げた**。合成データで現状と試作3案を描いて比べたうえで、**案A（立ち絵を領域の高さいっぱいに出し、最新のセリフだけ濃く大きく、過去は小さく薄く）**を採用した。",
    "status": "done",
    "difficulty": "opus",
    "passes": true,
    "evidence": "キャラビューを作り直した（src/view.ts）: .balloon-anchor / .balloon-spacer を廃して .character-layout を立ち絵＋.balloon-track の2つに単純化。立ち絵は height: 100%（max-width 45%）、並びは下端揃え・高さ全開（max-height: 100%）、吹き出しは幅が中身なりで、最新（DOM 先頭）だけ 1.05rem・青枠・尻尾、過去は 0.85rem・opacity .5。狭い画面（760px 以下）の上書きも更新。\ndocs/requirements.md 4.2 と docs/glossary.md「吹き出し」を新しい決定に差し替え（節数 25→25 / 36→36）。bun run check: 353 pass / 0 fail（件数は同じ。構造テスト1件を anchor/spacer から新構造に書き換え）。\n目視（合成データのみ。Chrome headless 1512x900 と 700x1000。セリフの中身は合成）: 5件すべてが領域に入り（従来は3件で頭が切れていた）、最新が一目で分かる。立ち絵は幅 110px→170px。長いセリフ2件でも最新は全文見え、立ち絵なし・狭い幅の縦積みでも破綻なし。ユーザー向けに 127.0.0.1:7411 の一時ページを Orca のタブで提示。\n追い直し（2026-09-12 ユーザーの指摘「まだ吹き出しがダメ」）: 並びを column-reverse にして最新を下端・過去を上へ押し上げる形に変更（align-self: flex-end / align-items: flex-start）。アニメーションも balloon-push-down → balloon-push-up。立ち絵は --portrait-drop: 0.75rem ぶん下へずらし、はみ出した足元は .character-layout の overflow: hidden で切る。bun run check: 353 pass / 0 fail。目視（合成データ・Chrome headless 1440x900、セリフは合成）: セリフ0件のプレースホルダが立ち絵の隣・下端に出て尻尾が左下から立ち絵へ向く、6件では最新が下端で強調され過去5件が上へ積まれて最古が並びの外へ流れる、立ち絵は枠の下端に接する。",
    "dependencies": []
  },
  {
    "id": "T-072",
    "summary": "入力欄を Enter 改行・Cmd+Enter 送信にし記号も出す",
    "task": "入力欄のキー割り当てを「Enter で改行、Command+Enter で送信」に入れ替える。\n\n## 背景\n\nユーザーの指示（2026-09-12）「入力画面は改行をEnter、送信をCommand+Enterにしてほしい」。\n\nいまの実装（2026-09-12 に現物を読んで確認）:\n\n- `src/view.ts` の `dispatchScript`（756行目付近の `keydown` リスナー）が\n  **Enter で送信、Shift+Enter で改行**。IME の変換確定の Enter は `isComposing` /\n  `keyCode === 229` で弾いている\n- `/` 補完の候補が開いている間は同じリスナーの中で先に分岐し、**Tab は確定だけ、\n  Enter は確定して送信**（774行目付近。T-051 の決定で、Claude Code の TUI に合わせたもの）\n- 入力欄の `placeholder`（611行目付近）にも「Enter で送信、Shift+Enter で改行」と書いてある\n- 決定の記録は `docs/requirements.md` 4.2「入力欄」（326行目付近）\n\n`metaKey`（macOS の Command）は現状どこも見ていない。テストの偽 `keydown` イベント\n（`test/view.test.ts` の `makeFakeKeydownEvent`、337行目付近）も `key` / `shiftKey` /\n`isComposing` / `keyCode` しか持たないので、`metaKey` を足す必要がある。\n\n## やること\n\n1. `dispatchScript` の `keydown` を次の割り当てに変える\n   - **Command+Enter（`event.metaKey`）で送信**。IME 変換中は送信しない（いまの判定を流用）\n   - **Enter 単独は既定動作のまま**（`preventDefault` せず改行を入れる）\n   - Shift+Enter も改行のまま（挙動を変えない）\n2. `/` 補完の候補が開いている間の Enter を**確定だけ**にする（送信しない）。Tab は今までどおり\n   確定だけ。**送信は Command+Enter に一本化する**ので、確定と送信を同じキーで兼ねない\n3. **送信ボタンに Command+Enter を示す記号を出す**（ユーザーの追加要件 2026-09-12）。\n   ボタンのラベル（`DISPATCH_SEND_LABEL`、577行目付近）は `送信` の1語で、\n   進行中はブラウザ側が `中断`（`DISPATCH_INTERRUPT_LABEL`）へ差し替える\n   （`dispatchScript` の 667行目付近）。**記号は送信のときだけ出し、中断のときは出さない**\n   （中断は Command+Enter で起きないため）。記号は `⌘⏎` のように**キーが読み取れる形**にし、\n   ラベルの文字とは別の要素に入れて弱い色で添える（`STYLE` の `.dispatch-send` は 2442行目付近）\n4. `placeholder` の案内文を新しい割り当てに直す\n5. `docs/requirements.md` 4.2 の入力欄の記述（Enter / Tab の決定）を差し替える。\n   **T-051 の決定を覆す形になるので、覆した理由（ユーザーの指示 2026-09-12）を1行添える**\n6. `bun run check` を通す\n\n## 完了条件\n\n- **テスト**: `test/view.test.ts` に次を足し、`bun test` が通ること（増えた件数を `evidence` に書く）\n  1. `Command+Enter`（`metaKey: true`）で `requestSubmit()` が呼ばれる\n  2. Enter 単独では送信されず、`preventDefault` も呼ばれない\n  3. IME 変換中（`isComposing` / `keyCode 229`）の Command+Enter では送信しない\n  4. 候補が開いている間の Enter は確定だけで、`requestSubmit()` を呼ばない\n  5. 送信ボタンの HTML に Command+Enter を示す記号が含まれ、**中断のラベルには含まれない**\n- 既存の「Enter で送信する」「候補が開いている間の Enter は……確定して送信する」のテストは\n  **新しい割り当てに合わせて書き換える**（消すのではなく振る舞いの変更として直す）\n- **目視**: Orca のタブで入力欄に2行打てること（Enter で改行が入り、送信されない）と、\n  Command+Enter でターンが始まること、**送信ボタンに記号が見えていて、進行中の「中断」に\n  変わると記号が消えること**を確認し、何が起きたかを `evidence` に書く\n  （**入力した文面は写さない**）\n- `grep -c '^#\\{2,3\\} ' docs/requirements.md` が編集の前後で変わらないこと\n- `bun run check` が通ること\n\n## 注意\n\n- **目視が要るので `/loop` には載せない。** 実装はサブエージェントに委譲してよい\n- 目視で tsukumo を起こすときは `TSUKUMO_VIEW_PORT` を変え、**常駐している 7327 番を落とさない**\n- `docs/requirements.md` を編集するときは、**行頭を含めて位置を特定する**\n  （節の索引に本文を流し込む事故が 2026-09-10 にある。`CLAUDE.md`「ドキュメントを編集するときの罠」）\n",
    "status": "done",
    "difficulty": "sonnet",
    "passes": true,
    "evidence": "`bun run check` 353→357 pass / 0 fail（+4）。`src/view.ts`: keydown を `key !== \"Enter\" || composing || !metaKey` に変え、補完候補が開いている間の Enter から `requestSubmit()` を外し（Tab と同じ確定だけ）、送信ボタンに `data-shortcut=\"⌘⏎\"`（初期 HTML にも入れ、`applyButtonLabel` が中断時に外す）と `.dispatch-send[data-shortcut]::after` を足し、placeholder を差し替えた。`docs/requirements.md` 4.2 の入力欄を差し替え（節数 25→25）。\n目視は合成データの静的ページで実施（Chrome headless 1440x900）: 送信ボタンに「送信 ⌘⏎」が弱い色で出ること、placeholder が「Enter で改行、Command+Enter で送信」になっていることを確認。\n**実機の目視は未実施**（Enter で改行が入る・Command+Enter で送信・進行中に記号が消える、の3点はOrca のタブで要確認。記号が消えることはテストで検証済み）。",
    "dependencies": []
  },
  {
    "id": "T-073",
    "summary": "質問に自由入力欄を常に出して答えられるようにする",
    "task": "キャラクターからの質問に、選択肢とは別の自由入力欄を常に出す。\n\n## 背景\n\nユーザーの指示（2026-09-12）「質問が出たときに選択肢しか選べず Type Something がないのが不便に\n思ったよ」。Claude Code の TUI では `AskUserQuestion` の選択肢に加えて自由入力が常に選べる\n（ツールの説明にも \"Users will always be able to select \\\"Other\\\" to provide custom text input\"\nとある）が、tsukumo の画面では出ないことがある。\n\n原因（2026-09-12 に現物を読んで確認）: `src/view.ts` の `questionOptionHtml`（1390行目付近）は\n**ラベルが `その他`（`FREE_TEXT_OPTION_LABEL`、1276行目）の選択肢が届いたときだけ**\nテキスト欄（`.question-other-input` と `.question-other-send`）に差し替える。`canUseTool` に\n届く入力にはモデルが書いた選択肢しか入っていないので、モデルが `その他` を並べなければ\n**自由入力欄はどこにも出ない**。`src/question.ts` の `parseQuestions` も届いた `options` を\nそのまま読むだけで、選択肢を足さない。\n\n答えは `src/pending-answer.ts` の `answer(id, { kind: \"answers\", labels })` として SDK へ戻り、\n**ラベルの文字列がそのまま答えになる**（選択肢と照合していない）ので、自由入力の文字列を\nlabels に入れる経路は既にある。\n\n## やること\n\n1. `questionCardHtml`（1377行目付近）が、**選択肢の並びの末尾に必ず自由入力の行を1つ出す**\n   ようにする。モデルが `その他` を選択肢に入れてきたときは二重に出さない\n2. 複数質問（`question-card` が複数）のときも、カードごとに自由入力欄が1つずつ出ること。\n   既存の送信の配線（`pendingAnswerScript`）が質問ごとのラベルを集める形なので、\n   **その集め方を壊さない**\n3. `multiSelect` の質問で、選択肢のいくつかを選んだうえで自由入力も足せるかは、\n   **いまの送信の配線で自然に決まるほうに合わせる**（無理に作り込まない。どちらにしたかを\n   `evidence` に書く）\n4. `docs/requirements.md` 4.2 の質問の記述に、自由入力を常に出す決定を1行足す\n5. `bun run check` を通す\n\n## 完了条件\n\n- **テスト**: `test/view.test.ts` に次を足し、`bun test` が通ること（増えた件数を `evidence` に書く）\n  1. `その他` を含まない選択肢だけの質問でも、出力 HTML に `question-other-input` が1つ含まれる\n  2. `その他` を含む質問では `question-other-input` が2つに増えない\n  3. 質問が2件あるとき、`question-other-input` が2つ出る\n- **目視**: Orca のタブで質問を1つ出し、選択肢のボタンの下に自由入力欄があって、そこに打った\n  文字列で答えが返せることを確認する。**質問文と答えの中身は `evidence` に写さない**\n  （どの画面に何の要素が出たかだけ書く）\n- `bun run check` が通ること\n\n## 注意\n\n- **質問文・選択肢・答えは会話の内容そのもの。** ログにもテストのフィクスチャにも実物を使わない\n  （`docs/coding-standards.md`「会話内容の扱い」）\n- **目視が要るので `/loop` には載せない。** 実装はサブエージェントに委譲してよい\n- 目視で tsukumo を起こすときは `TSUKUMO_VIEW_PORT` を変え、**常駐している 7327 番を落とさない**\n",
    "status": "todo",
    "difficulty": "sonnet",
    "passes": false,
    "evidence": "",
    "dependencies": []
  },
  {
    "id": "T-074",
    "summary": "複数行の依頼をやり取りの見出しに全部出す",
    "task": "メインビューのやり取りの見出しに、複数行の依頼を1行だけでなく全部出す。\n\n## 背景\n\nユーザーの指示（2026-09-12）「ユーザーの発言が複数行あったときにタイトルに1行しか表示されなくて\n不便に思ったよ」。\n\n現物（2026-09-12 に確認）: `src/view.ts` の `truncateRequest`（1265行目付近）が\n**`request.split(\"\\n\")[0]` で1行目だけを取り**、120文字（`MAX_REQUEST_HEADING_LENGTH`）で\n切っている。出力は `turnPanel`（1220行目付近）の `<h2 class=\"turn-request\">` 1つ。\nやり取りを切り替えるタブのラベル（`turnTabHtml`、1259行目付近）も同じ見出しの文字列を使う。\n\n## やること\n\n1. 見出しに**依頼の全行**を出す。行の区切りが見えるように出す（`<h2>` の中で改行を保つか、\n   行ごとの要素にするかは実装しやすいほうでよい。**どちらにしたかを `evidence` に書く**）\n2. 長い依頼で画面が埋まらないよう、**高さの上限を決めて超えたら畳む**。既定で見せる高さと、\n   全文を見る手段（`<details>`・CSS の `max-height` とクリックでの展開など）を1つ選ぶ\n3. **タブのラベルは1行のままにする**（横に並ぶ要素なので全文を出すと壊れる）。見出しと\n   タブで別の切り方になるので、関数を分けるか引数で切り替える\n4. `bun run check` を通す\n\n## 完了条件\n\n- **テスト**: `test/view.test.ts` に次を足し、`bun test` が通ること（増えた件数を `evidence` に書く）\n  1. 3行の依頼を含むやり取りの HTML に、2行目・3行目の文字列が含まれる\n  2. タブのラベルには1行目だけが出る（2行目の文字列を含まない）\n  3. 長い依頼でも `MAX_REQUEST_HEADING_LENGTH` 相当の上限が効いている（切り詰めの印が付く）\n- **目視**: Orca のタブで3行以上の依頼を送り、見出しに全行が出て、画面がその1件で埋まらない\n  ことを確認する。**依頼の文面は `evidence` に写さない**（行数と見え方だけ書く）\n- `bun run check` が通ること\n\n## 注意\n\n- **依頼の文面は会話の内容そのもの。** テストのフィクスチャに実物を使わない\n  （`docs/coding-standards.md`「会話内容の扱い」）\n- **目視が要るので `/loop` には載せない。** 実装はサブエージェントに委譲してよい\n- 目視で tsukumo を起こすときは `TSUKUMO_VIEW_PORT` を変え、**常駐している 7327 番を落とさない**\n",
    "status": "todo",
    "difficulty": "sonnet",
    "passes": false,
    "evidence": "",
    "dependencies": []
  },
  {
    "id": "T-075",
    "summary": "経過時間を入力欄の送信ボタンと同じ行に移す",
    "task": "経過時間の表示を、サイドバーから入力欄の送信ボタンの行へ移す。\n\n## 背景\n\nユーザーの指示（2026-09-12）「経過時間は入力画面の送信ボタンと同じ行に表示したほうが良さそうだね。」\n\n現物（2026-09-12 に確認）:\n\n- 経過時間はいま**サイドバーの「セッション情報」**にある。`src/view.ts` の `sessionInfoBody`\n  （3155行目付近）が `.session-elapsed-row` を組み立て、`data-started-at` /\n  `data-finished-at` に時刻を載せるだけで、**カウントアップとラベルの出し分け（経過／所要）は\n  ブラウザ側の `sessionInfoScript`** が行う（view.ts は純粋関数だけを置く場所で `Date.now()` を\n  持たない。T-055 の決定）\n- 入力欄の送信ボタンは `dispatchRegionHtml`（607行目付近）の `.dispatch-row` にあり、\n  同じ行に `.dispatch-status`（送信の成否を出す `role=\"status\"`）が並んでいる\n- **サイドバーと入力欄は別の領域で、SSE の経路も別**（サイドバーは領域まるごとの差し替え、\n  入力欄は `TURN_STATUS_EVENT_PATH` などの更新）。開始・終了時刻をどちらの経路で届けるかが\n  実装の勘所になる\n\n## やること\n\n1. 経過時間の表示を `.dispatch-row`（送信ボタンと同じ行）へ移す。サイドバーの\n   `.session-elapsed-row` は**消す**（2箇所に出さない）\n2. 開始・終了時刻を入力欄側へ届ける経路を決めて配線する。**サイドバーの HTML を読みに行く形に\n   しない**（領域の差し替えで消えるため）。`TURN_STATUS_EVENT_PATH` に載せるのが素直なら\n   それでよい。**どの経路にしたかを `evidence` に書く**\n3. カウントアップとラベルの出し分け（経過／所要、`N秒` / `M分SS秒`）の振る舞いは\n   **T-055 のまま変えない**。実装が `sessionInfoScript` から入力欄側のスクリプトへ移るだけ\n4. `docs/requirements.md` 4.2 の該当箇所（経過時間の置き場所）を差し替える\n5. `bun run check` を通す\n\n## 完了条件\n\n- **テスト**: `test/view.test.ts` の経過時間まわりの既存テストを移設先に合わせて直し、\n  次を満たすこと（件数の増減を `evidence` に書く）\n  1. 入力欄の HTML に経過時間の要素が含まれる\n  2. サイドバーの「セッション情報」の HTML に経過時間の要素が**含まれない**\n  3. 書式（`N秒` / `M分SS秒`）とラベルの出し分けのテストが残っている\n- **目視**: Orca のタブで依頼を1件送り、送信ボタンの隣で秒数が増え、ターンが終わると「所要」に\n  変わることを確認する。どの画面で何が見えたかを `evidence` に書く\n- `grep -c '^#\\{2,3\\} ' docs/requirements.md` が編集の前後で変わらないこと\n- `bun run check` が通ること\n\n## 注意\n\n- **目視が要るので `/loop` には載せない。** 実装はサブエージェントに委譲してよい\n- 目視で tsukumo を起こすときは `TSUKUMO_VIEW_PORT` を変え、**常駐している 7327 番を落とさない**\n- `src/view.ts` は純粋関数だけを置く場所。**`Date.now()` のような時刻の取得を持ち込まない**\n",
    "status": "todo",
    "difficulty": "sonnet",
    "passes": false,
    "evidence": "",
    "dependencies": []
  },
  {
    "id": "T-076",
    "summary": "依頼を送って待っている間にメイン画面へ出すものを決める",
    "task": "依頼を送ってから答えが出るまでの間、メインビューに何を出すかを決める。\n\n## 背景\n\nユーザーの指示（2026-09-12）「入力後、待っている間にメイン画面に表示する内容って何がいいかな?\n考えてもらっていい?」。**何が待ち時間をつらくするかはユーザーの体感が正典**なので、\n推測で足さずに聞いてから決める。\n\nいまの到達点（2026-09-12 に現物を読んで確認）:\n\n- メインビューは `src/view.ts` の `buildMainBody`（1081行目付近）が組み立てる。\n  **利用者の依頼を境目にやり取りごとに区切り**、1やり取りの中は「レポート1件＋それに続く\n  ツールの実行」を1ステップとして縦に積む。過去のやり取りはタブで遡る\n- **「作業中」と「完了後」で状態を切り替えない**（同関数の doc コメント）。つまり\n  **待っている間に専用の見た目は無い**。出るのはツールの実行の行だけで、最初のレポートが\n  届くまでは実質ほとんど何も増えない\n- ツールの行は**利用者が見るべきものだけに絞ってある**（`toolVisibility`。ファイルを変えた操作・\n  サブエージェントの起動・失敗したツールの3種類だけ。コマンドとその出力・読み取り・検索は出ない）。\n  **待ち時間の大半は「出さないと決めたツール」で埋まっている**可能性がある\n- 進行中かどうかは `TURN_STATUS_EVENT_PATH` でブラウザへ届いており、入力欄のボタンが\n  「送信」／「中断」に切り替わる。サイドバーには「いま何をしているか」（`ToolActivity`）と\n  経過時間がある（T-054 / T-055。経過時間は T-075 で入力欄へ移る）\n- 表情は待っている間 `working` に変わる（`WORKING_EXPRESSION_DELAY_MS`）\n\n## 解くべき論点\n\n- **待ち時間の何がつらいのか。** 「進んでいるか分からない」のか「終わりが読めない」のか\n  「手持ち無沙汰」なのか。**ここを聞かずに何を出すかを決めない**（対策が変わる）\n- **メインビューの役目と衝突しないか。** メインビューは「読むもの」で、サイドバーが\n  「いま何をしているか」を持つ。待機中の表示をメインに置くと**サイドバーと二重になる**。\n  役割分担をここで決めて書き残す\n- **出さないと決めたツールを待機中だけ出すか。** `toolVisibility` の決定\n  （`docs/requirements.md` 4.2）を待機中に限って緩めるかどうか。緩めるなら\n  **コマンドの中身・出力は会話の内容**なので、どこまで出すかの線引きが要る\n- **キャラクターと一緒に仕事をする目的に沿うか。** 待ち時間をキャラビュー側（立ち絵・セリフ）で\n  埋めるほうが目的に近い可能性がある。**メインビューに足さない**という結論もあり得る\n- **足さない候補も並べる。** `CLAUDE.md`「機能を足そうとする前に `docs/requirements.md` 1と\n  2.2 を見る」\n\n## やること\n\n1. ユーザーに**待っている間の何が不便だったか**を聞く。体感が正典なので、ここを飛ばさない\n2. いま待機中に画面のどこに何が出ているかを**実物で棚卸しする**（メインビュー・キャラビュー・\n   サイドバー・入力欄の4箇所。tsukumo を `TSUKUMO_VIEW_PORT` を変えて起こし、依頼を1件送って観察する）\n3. 候補を出して、出すもの・出さないものを**理由付きで両方決める**\n4. 決めた実装は**このタスクではやらない**。後続タスクの案を `develop/direction.md` に書き出す\n   （T-069 と同じ進め方）\n5. 決定を `docs/requirements.md` 4.2 に記録する\n\n## 完了条件\n\n- ユーザーに聞いた「何がつらいか」と、それに対する結論が `docs/requirements.md` 4.2 に\n  節として記録されていること。**足さないと決めたものも理由付きで書かれている**こと\n- 棚卸しの結果（待機中に4箇所それぞれに何が出ているか）が `evidence` にあること。\n  **会話の中身は写さない**（どの要素が出たかだけ）\n- 実装が要るなら `develop/direction.md` に後続の案が書かれていること。\n  要らないと決めたならその理由が `evidence` にあること\n- `grep -c '^#\\{2,3\\} ' docs/requirements.md` が編集の前後で減っていないこと\n- コードを変えていれば `bun run check` が通ること（変えないなら不要）\n\n## 注意\n\n- **ユーザーの感想が要るので委譲せず、`/loop` にも載せない**\n- **このタスクで実装しない。** 決めるところまで\n- 観察のために tsukumo を起こすときは `TSUKUMO_VIEW_PORT` を変え、**常駐している 7327 番を落とさない**\n- `docs/requirements.md` を編集するときは**行頭を含めて位置を特定する**\n  （`CLAUDE.md`「ドキュメントを編集するときの罠」）\n",
    "status": "todo",
    "difficulty": "opus",
    "passes": false,
    "evidence": "",
    "dependencies": []
  },
  {
    "id": "T-077",
    "summary": "開き直したときにセッションを引き継ぐ方式を決める",
    "task": "tsukumo を起こし直したときに前のセッションを復元する方式を決める（実装はしない）。\n\n## 背景\n\nユーザーの指示（2026-09-12）「tsukumo を開き直したとき、セッションを復元するように変更しよう。」\n\n現物（2026-09-12 に確認）:\n\n- `src/session-driver.ts` の `startSession`（118行目付近）が `query({ options: { cwd,\n  permissionMode, mcpServers, ... } })` で**毎回まっさらなセッションを起こす**。\n  `resume` も `continue` も渡していない\n- Agent SDK の `Options` には **`resume?: string`（再開するセッションID）**、\n  `forkSession?: boolean`、`resumeSessionAt?: string` がある\n  （`node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts` の 1934 / 1584 / 1950 行目付近）\n- セッションIDは届いている。`src/session-event.ts`（169行目付近）が SDK のメッセージから\n  `session_id` を読んで `{ kind: \"session-info\", sessionId }` を流す\n- **画面の中身（やり取りの履歴）はプロセスのメモリにしかない。** `src/session-view.ts` の\n  `SessionView` が `applySessionEvent` で積み上がるだけで、どこにも保存していない\n- **`~/.tsukumo/` は旧方針の遺物で、もう読まれない**（T-042 で撤去。`develop/progress.md`\n  「注意」）。書き込む場所を作るなら、ここを再利用するか別の場所にするかを決めることになる\n\n## 解くべき論点\n\n- **何を復元するのか。** (a) claude 側の会話（`resume` で足りる）、(b) 画面のやり取りの履歴\n  （メインビューのタブ・レポート・ツールの行）、(c) 吹き出しに出ていたセリフ。\n  **(b)(c) は tsukumo 側で保存しないと戻らない**。どこまでやるかをここで決める\n- **会話内容の扱いとの衝突。** `docs/coding-standards.md`「会話内容の扱い」は\n  **「別の場所に複製しない」**と言っている。(b)(c) を保存するのはレポートとセリフの複製にあたる。\n  **セッションIDだけを保存して claude 側の transcript を正典にする**なら複製にならない。\n  この線引きが最大の論点で、**規約を曲げるなら規約側を先に直す**（`CLAUDE.md`\n  「用語を変えたくなったら先に用語集を直す」と同じ順序）\n- **何を鍵に復元するか。** 起動した作業ディレクトリ（`cwd`）ごとか、ホストの単位か。\n  `tsukumo` は `bun link` でグローバルに入っていて**別のプロジェクトからも起こせる**\n  （T-048）ので、鍵を間違えると他のプロジェクトの会話が出る\n- **いつ復元するか。** 常に自動で続きから始めるのか、起動時に選ばせるのか、\n  明示のフラグ（`--resume` / 環境変数）だけか。**事故（意図せず前の会話が続く）を防ぐ形**を選ぶ\n- **どこに保存するか。** `~/.tsukumo/` を復活させるか、`cwd` の中か。旧方針の遺物と\n  混ざらないようにする\n- **復元できなかったときどうするか。** セッションIDが古い・transcript が消えている場合に\n  **常駐プロセスが落ちない**こと（`docs/coding-standards.md`「常駐プロセスは描画1回の失敗で\n  落ちない」。起動時の前提不足だけが即時終了）\n\n## やること\n\n1. SDK の `resume` / `forkSession` / `resumeSessionAt` の意味を**型定義と公式ドキュメントで\n   確かめる**（推測で書かない。出典を控える）\n2. 上の論点をユーザーと突き合わせて決める。**(b)(c) を保存するかは規約に触れるので必ず確認を取る**\n3. 決めた方式を `docs/requirements.md` 4 に節として1つ足す。規約を変えるなら\n   `docs/coding-standards.md`「会話内容の扱い」も同時に直す\n4. **実装はこのタスクではやらない。** 実装の手順を T-078 の本文に書き足す\n\n## 完了条件\n\n- `docs/requirements.md` に「何を復元し、何を復元しないか」「鍵」「いつ復元するか」\n  「保存場所」「失敗時の振る舞い」が書かれた節があること\n- 会話内容の複製にあたるかの判断と、その結論が `evidence` に1行で書かれていること\n- `develop/tasks.json` の T-078 の本文が、決めた方式に沿って更新されていること\n- `grep -c '^#\\{2,3\\} ' docs/requirements.md` が編集の前後で減っていないこと\n- コードを変えていなければ `bun run check` は不要（変えたなら通すこと）\n\n## 注意\n\n- **ユーザーの選択が要るので委譲せず、`/loop` にも載せない**\n- **このタスクで実装しない。** 決めるところまで\n- **会話内容の扱いは他のどの規約よりも優先する**（`CLAUDE.md`）。保存を伴う案は、\n  規約を先に直してからでないと実装に進めない\n- `docs/requirements.md` を編集するときは**行頭を含めて位置を特定する**\n",
    "status": "todo",
    "difficulty": "opus",
    "passes": false,
    "evidence": "",
    "dependencies": []
  },
  {
    "id": "T-078",
    "summary": "決めた方式でセッションの復元を実装する",
    "task": "T-077 が決めた方式で、起こし直したときのセッション復元を実装する。\n\n## 背景\n\nユーザーの指示（2026-09-12）「tsukumo を開き直したとき、セッションを復元するように変更しよう。」\n**何を復元し、どこに何を保存し、いつ復元するかは T-077 が決める。**\n\n現物の出発点（2026-09-12 に確認）:\n\n- `src/session-driver.ts` の `startSession`（118行目付近）が `query()` に渡す `options` に\n  `resume` を足す口がある（SDK の `Options.resume`）\n- セッションIDは `src/session-event.ts`（169行目付近）が `session-info` として流している\n- 起動時の前提チェック・状態の保持・`try`/`catch` は配線の層（`src/index.ts`）の仕事\n\n## やること\n\n1. **T-077 の決定を読む**（`develop/tasks.json` の T-077 の `evidence` と\n   `docs/requirements.md` の該当節）。決まっていなければ着手しない\n2. 決まった方式で実装する。**外の世界に依存する値（保存先のパス・環境変数）の読み取りは\n   1モジュールに集約し、モジュールのトップレベルで環境変数に触らない**\n   （`docs/coding-standards.md`）\n3. 復元に失敗したときは**新しいセッションとして起動し、プロセスを落とさない**\n   （起動時の前提不足だけが即時終了）\n4. `bun run check` を通す\n\n## 完了条件\n\n- **テスト**: 復元の判断（保存された値を読んで `resume` に渡すかを決める部分）を\n  純粋な関数として切り出し、`test/` に次を足すこと（増えた件数を `evidence` に書く）\n  1. 保存された値があるときは復元する形になる\n  2. 保存された値が無い・壊れているときは新しいセッションとして起動する形になる\n  3. T-077 が「鍵」に決めた単位（`cwd` など）が違うときは復元しない\n- **テストから本物の claude を起動しない**（`develop/progress.md`「注意」）\n- **実機**: `TSUKUMO_VIEW_PORT` を変えて tsukumo を起こし、1往復したあと落として起こし直し、\n  T-077 が「復元する」と決めたものが戻っていることを確認する。**会話の中身は `evidence` に\n  写さない**（「前のやり取りのタブが N 個見えた」のように書く）\n- 起こし直したあと `pgrep -f claude-agent-sdk` で**自分が起こした pid だけ**を落としたことを確認\n- `bun run check` が通ること\n\n## 注意\n\n- **会話内容の扱いが最優先。** T-077 が許した範囲を超えて保存しない。ログに全文を出さない\n- **常駐している 7327 番を落とさない。** 動作確認は `TSUKUMO_VIEW_PORT` を変えて起こす\n- **実機の確認が要るので `/loop` には載せない。** 実装はサブエージェントに委譲してよい\n",
    "status": "todo",
    "difficulty": "sonnet",
    "passes": false,
    "evidence": "",
    "dependencies": ["T-077"]
  },
  {
    "id": "T-079",
    "summary": "段落や表の中に書いた HTML が生タグで出るのを直す",
    "task": "段落・表のセル・箇条書きの中に書いた HTML（`<span class=\"badge\">` など）が、\nタグの文字列のまま画面に出るのを直す。\n\n## 背景\n\nユーザーの指示（2026-09-12）「メイン画面に `<span class=\"badge badge-ok\">あり</span>` という\n表示がHTMLタグそのままに出ちゃってる...」。\n\n原因（2026-09-12 に現物を読んで確認）:\n\n- `src/view.ts` の `renderMarkdownToHtml`（2712行目付近）は、**行頭がタグの行から空行までを\n  1つの HTML ブロック**として `sanitizeReportHtml` に通す（2808行目付近）。この経路に乗った\n  HTML は正しく描かれる\n- 一方、**段落の途中・表のセル・箇条書きの項目の中に書かれた HTML はブロックにならず**、\n  `renderInline`（2970行目付近）→ `renderPlainInline`（3029行目付近）が\n  `escapeHtml` を通すので、**タグが文字として出る**\n- `src/report-html.ts` の許可リストには `span` も `class` 属性も入っている（15行目〜）ので、\n  **通す仕組みは既にあり、インラインの経路だけがそこに繋がっていない**\n- これは**出力スタイル側の規約と食い違っている**。`~/.claude/output-styles/asuna.md` の\n  「レポートの組み立て方」の表は「状態の印（OK / 要注意 / NG）」に\n  `<span class=\"badge badge-ok\">` を挙げていて、これは性質上**文の途中か表のセルに書く**もの。\n  書けと言われた記法が描けない状態にある\n\n**T-069（描画技術の選択）の結論に従う。** T-069 が Markdown ライブラリの同梱を決めた場合、\n自前の `renderInline` に手を入れる作業は無駄になる。\n\n## やること\n\n1. **T-069 の決定を読む**（`evidence` と `docs/architecture.md` の設計判断の節）。\n   「自前のレンダラのまま行く」と決まっていればこのタスクを進める。ライブラリ同梱に決まって\n   いたら、**実装せずに理由を `evidence` に書いて閉じる**（そのライブラリ側でインライン HTML を\n   どう扱うかを1行添える）\n2. インラインのテキストを描くとき、**許可リストを通った HTML のタグだけをタグとして残し、\n   それ以外はエスケープする**形にする。`sanitizeReportHtml` を再利用し、\n   **サニタイズを2箇所に書き分けない**\n3. 段落・表のセル・箇条書きの項目の3箇所すべてで効くこと。**リンク（`[text](url)`）や\n   `**太字**` / `` `code` `` の変換と衝突しない**こと（コードスパンの中の `<span>` は\n   文字のまま出る、が正しい）\n4. `docs/requirements.md` 4.2 のレポートの記法の記述を実態に合わせる\n5. `bun run check` を通す\n\n## 完了条件\n\n- **テスト**: `test/view.test.ts` に次を足し、`bun test` が通ること（増えた件数を `evidence` に書く）\n  1. 段落の途中の `<span class=\"badge badge-ok\">あり</span>` が `<span` を含む HTML として出る\n  2. 表のセルの中の同じ書き方でも同様に出る\n  3. 許可リストに無いタグ（`<script>` など）は**タグとして出ない**\n  4. `` `<span>` `` のようにコードスパンで囲んだものは**文字のまま出る**\n  5. 既存のリンク・太字・コードスパンのテストが通ったままであること\n- **目視**: Orca のタブで、段落の途中と表のセルにバッジを含むレポートを1つ出し、\n  色付きのバッジとして描かれていることを確認する。どの URL で何が見えたかを `evidence` に書く\n  （**レポートの中身は写さない**）\n- `bun run check` が通ること\n\n## 注意\n\n- **このページは会話の内容を持っている。** 許可リスト方式（載っているものだけ通す）を崩さない。\n  「タグらしきものを全部通す」実装にしない（`src/report-html.ts` の冒頭のコメントが理由の正典）\n- **目視が要るので `/loop` には載せない。** 実装はサブエージェントに委譲してよい\n- 目視で tsukumo を起こすときは `TSUKUMO_VIEW_PORT` を変え、**常駐している 7327 番を落とさない**\n- `~/.claude/output-styles/asuna.md` はこのタスクでは触らない（記法は既に正しく、\n  実装が追いついていないだけ）\n",
    "status": "todo",
    "difficulty": "sonnet",
    "passes": false,
    "evidence": "",
    "dependencies": ["T-069"]
  },
  {
    "id": "T-080",
    "summary": "ドメイン駆動で作り直すかを規約ごと見直して決める",
    "task": "アーキテクチャとコーディング規約を、ドメイン駆動・特定の技術に依存しない形へ\n作り直すかを決める（実装はしない）。\n\n## 背景\n\nユーザーの指示（2026-09-12）「アーキテクチャとコーディング規約を見直そう。具体的にはドメイン\n駆動で、特定のフレームワークに依存しないような形にリファクタしよう。」\n\n現物（2026-09-12 に確認）:\n\n- コードは `src/` の17ファイル・約7000行。**概念で名前が付いている**\n  （`character.ts` / `expression.ts` / `utterance.ts` / `question.ts` / `pending-answer.ts` /\n  `session-view.ts` / `session-event.ts` / `session-driver.ts` / `tasks.ts` / `view.ts` /\n  `view-server.ts` / `view-port.ts` / `report-html.ts` / `host.ts` / `orca-host.ts` /\n  `bundled-files.ts` / `index.ts`）。`utils` / `helpers` の類は無い（原則5）\n- **層は既に分けてある**（原則2）。「受け取る」＝`session-event.ts` / `session-driver.ts`、\n  「決める」＝`session-view.ts` / `view.ts` / `report-html.ts` / `expression.ts`、\n  「描く／配る」＝`view-server.ts`、「配線」＝`index.ts`\n- **外部依存の隔離も原則になっている**（原則3）。ホストは `host.ts` の1ポート＋`orca-host.ts`、\n  SDK の型を import するのは `session-driver.ts` の1ファイルだけ（`pending-answer.ts` の\n  冒頭コメントに明記）\n- **ランタイム依存は SDK と zod の2つだけ**でビルド工程が無い。`Bun.*` の固有APIにも寄せない\n  規約がある（`docs/coding-standards.md`「Bun固有APIに寄せない」）\n- **いちばん大きいのは `src/view.ts` の 3185 行**。HTML・CSS・ブラウザ側の JS を\n  すべてテンプレート文字列で組み立てており、ブラウザ側の JS には `tsc` も `oxlint` も届かない\n  （この問題は **T-069 の領分**）\n- 規約の正典は `docs/coding-standards.md`、設計判断の正典は `docs/architecture.md`\n\nつまり**「特定のフレームワークに依存しない」は既にかなり満たされている**一方、\n**ドメイン層とアプリケーション層の呼び分け・依存の向きを明示した形にはなっていない**。\n何を変えたいのかをユーザーと突き合わせないと、名前だけ変えて中身が同じになる。\n\n## 解くべき論点\n\n- **いま何が困っているのか。** 「読みにくい」「テストしづらい」「SDK を替えられない」\n  「別のホストに載せられない」のどれなのか。**困りごとを先に特定する**（ドメイン駆動は\n  手段であって目的ではない。`CLAUDE.md`「機能を足そうとする前に目的に沿うかで決める」と同じ）\n- **既にある原則（1〜5）との関係。** 原則2（受け取る／決める／描く）と原則3（外部依存を1つの\n  アダプタに）は、依存の向きという意味では既にドメイン駆動と同じ方向を向いている。\n  **上書きするのか、言い換えるのか、足すのか**をはっきりさせる。原則を増やすと\n  `CLAUDE.md` と `docs/coding-standards.md` の両方が動く\n- **ドメインは何か。** このプロジェクトのドメインは「キャラクターと一緒に仕事をするための\n  ターミナル環境」（`docs/requirements.md` 1）。**セッション・ターン・発話・セリフ・表情・\n  衣装・立ち絵・吹き出し**は `docs/glossary.md` にある概念で、コードの識別子もそれに\n  合わせてある。**用語集が実質のユビキタス言語**になっていることを踏まえる\n- **`src/view.ts` の 3185 行をどう扱うか。** これは T-069（描画技術の選択）と重なる。\n  **T-069 の結論が出る前にこのファイルを割る計画を立てない**（二重に決めることになる）\n- **やらない判断も答えの1つ。** 対象ユーザーは作者本人のみで、いまの構成は動いている。\n  **作り直しのコストに見合うか**を書き残す\n- **どこまでを1回でやるか。** 全面的な作り直しは大きすぎる。**段階に割って順番を付ける**\n\n## やること\n\n1. ユーザーと**何に困っているか**を突き合わせる（推測で進めない）\n2. いまの構成を層と依存の向きで1枚にまとめる（`docs/architecture.md` の全体図との差分でよい）\n3. 変える案・変えない案を軸で比較する。**変えない案を必ず候補に入れる**\n4. 決めた結論と理由を `docs/architecture.md`「設計判断（なぜ今の形なのか）」に `#### ` の節として\n   1つ足す。規約を変えるなら `docs/coding-standards.md` と `CLAUDE.md` の原則も**同時に**直す\n   （片方だけ直すと正典が二重になる）\n5. **実装はこのタスクではやらない。** 段階に割った後続タスクの案を `develop/direction.md` に書き出す\n6. 比較の結果、現状維持が最善だと分かったら、**後続タスクを作らず**その理由を `evidence` と\n   `docs/architecture.md` に残して閉じる\n\n## 完了条件\n\n- `docs/architecture.md` に結論の節が1つ増えていること。**変えないと決めた場合もその理由が\n  書かれている**こと\n- 規約を変えたなら、`docs/coding-standards.md` と `CLAUDE.md` の原則の記述が**互いに矛盾して\n  いない**こと（原則1〜5 のどれを差し替えたかが `evidence` に書かれていること）\n- 段階に割った後続の案が `develop/direction.md` にあること（作らないと決めたならその理由が\n  `evidence` にあること）\n- `grep -c '^#\\{2,4\\} ' docs/architecture.md` が編集の前後で減っていないこと\n- コードを変えていなければ `bun run check` は不要（変えたなら通すこと）\n\n## 注意\n\n- **ユーザーの選択が要るので委譲せず、`/loop` にも載せない**\n- **このタスクで実装しない。** 決めるところまで\n- **T-069 の結論を先に読む**（`src/view.ts` の扱いが重なる）\n- `docs/` を編集するときは**行頭を含めて位置を特定する**（`CLAUDE.md`「ドキュメントを編集する\n  ときの罠」）。編集の前後で節の数を数える\n- 会話内容の扱いの規約（`docs/coding-standards.md`）は**他のどの規約よりも優先する**。\n  作り直しの案がこれを緩める形になっていないか必ず確かめる\n",
    "status": "todo",
    "difficulty": "opus",
    "passes": false,
    "evidence": "",
    "dependencies": ["T-069"]
  },
  {
    "id": "T-081",
    "summary": "既定のモデルを Opus の effort high で起こす",
    "task": "セッションを起こすときの既定のモデルを Opus（effort: high）にする。\n\n## 背景\n\nユーザーの指示（2026-09-12）「デフォルトのモデルはOpus High にして」。\n\n現物（2026-09-12 に確認）:\n\n- `src/session-driver.ts` の `startSession`（118行目付近）が `query()` に渡す `options` は\n  `cwd` / `permissionMode` / `mcpServers` などで、**`model` を渡していない**\n  （claude 側の既定に従っている）。`effort` も渡していない\n- Agent SDK の `Options` には **`model?: string`**（1818行目付近）と\n  **`effort?: EffortLevel`**（`'low' | 'medium' | 'high' | 'xhigh' | 'max'`。1769行目付近）が\n  ある（`node_modules/@anthropic-ai/claude-agent-sdk/sdk.d.ts`）\n- 画面側は `src/view.ts` の `MODEL_LABELS`（1443行目付近）が `opus` / `sonnet` / `haiku` の\n  3つを持ち、**見た目上の既定値 `MODEL_FALLBACK` は `\"sonnet\"`**（1449行目付近）。\n  実際の値は `session-info` の `model` から `resolveModelAlias` が部分一致で決める。\n  **effort は画面に出ていない**\n- 切り替えは `session.setModel()`（`session-driver.ts` の 149行目付近）で、\n  `/model` は送っていない\n- `DEFAULT_PERMISSION_MODE` という既定値の置き方の前例が `session-driver.ts` にある\n\n## やること\n\n1. `session-driver.ts` に既定のモデル（`opus`）と既定の effort（`high`）を定数として置き、\n   `query()` の `options` に渡す\n2. `src/view.ts` の `MODEL_FALLBACK` を `\"opus\"` に合わせる（**`view.ts` は\n   `session-driver.ts` を import しない**。原則3。値が同じであることをコメントで示す既存の\n   書き方に倣う）\n3. **effort を画面に出すかは足さない**（`docs/requirements.md` 1・2.2。モデルの `<select>` は\n   エイリアス3つだけという既存の決定を変えない）。既定として渡すだけにする。\n   **`setModel` でモデルを切り替えたとき effort がどうなるかを実測して `evidence` に書く**\n   （SDK が保つのか落ちるのか。落ちるなら仕様としてそのまま受け入れ、要件に1行書く）\n4. `docs/requirements.md` 4 の該当箇所に既定のモデルと effort を記録する\n5. `bun run check` を通す\n\n## 完了条件\n\n- **テスト**: 既定値が `query()` に渡る形になっていることを確かめるテストを `test/` に足すこと\n  （`startSession` の `options` を組み立てる部分を検査できる形に切り出してよい。\n  **本物の claude を起動しない**）。増えた件数を `evidence` に書く\n- `test/view.test.ts` の `MODEL_FALLBACK` に依存する既存テストが新しい既定値で通ること\n- **実機**: `TSUKUMO_VIEW_PORT` を変えて tsukumo を起こし、サイドバーのモデルが Opus に\n  なっていることと、`session-info` の `model` に opus が来ていることを確認する\n- `grep -c '^#\\{2,3\\} ' docs/requirements.md` が編集の前後で変わらないこと\n- `bun run check` が通ること\n\n## 注意\n\n- **既定を上げると API の利用が増える。** 動作確認は最小限のやり取り1往復で済ませる\n- **常駐している 7327 番を落とさない。** 動作確認は `TSUKUMO_VIEW_PORT` を変えて起こし、\n  終わったら自分が起こした pid だけを落とす\n- `effort` は SDK のバージョンによっては型が無いことがある。**型を迂回するキャストを書かない**。\n  型が無ければ SDK を上げる前に**ユーザーに確認する**（`docs/coding-standards.md`）\n- **実機の確認が要るので `/loop` には載せない。** 実装はサブエージェントに委譲してよい\n",
    "status": "todo",
    "difficulty": "sonnet",
    "passes": false,
    "evidence": "",
    "dependencies": []
  },
  {
    "id": "T-082",
    "summary": "個別ビューのページを消してレイアウトを / で開く",
    "task": "個別ビューのページ（`/main` `/character` `/sidebar`）と `/` のリンク一覧を消し、\nレイアウトページを `/` で開くようにする。\n\n## 背景\n\nユーザーの指示（2026-09-12）「不要なものは消して `/` だけで開くようにするのが自然」。\n\n**役目を終えている。** 個別ページは当初案（3つを別々のタブで開いて `orca terminal split` で\nペインに並べる）の名残で、その案は 2026-09-10 に成立しないと分かって捨てられた\n（`docs/architecture.md`「ビューは1枚のページにまとめる」）。以降は「デバッグしやすさのため\n残す」と書いてあるが、**実際の目視確認は合成データの静的 HTML を吐いてブラウザで開く方法**で\n行っており（`buildLayoutPage` を呼んでファイルに書く。こちらのほうが実物に近い）、\nサーバの個別ページ経路は使われていない。\n\n現物（2026-09-12 に読んで確認）:\n\n- `src/view.ts`: `buildViewPage`（109行目付近、単体ページの組み立て）、`viewPath`（24行目付近）、\n  `/` のリンク一覧（289〜293行目付近）、`LAYOUT_PATH = \"/layout\"`（301行目付近）、\n  `VIEW_TITLE`（1814行目付近）\n- `src/view-server.ts`: `LAYOUT_PATH` の分岐（278行目付近）と、`viewPath` で個別ページを返す\n  分岐（283行目付近）。**`viewEventPath` で SSE を開く分岐（289行目付近）は別物で、これは残す**\n- `src/index.ts`: 起動ログの「（個別ビュー・デバッグ用）」3行（536〜543行目付近）と\n  `host.showView(server.layoutUrl)`（447行目付近）\n- `src/view-server.ts` の `urlOf`（209行目付近）は個別 URL を組み立てるためだけにある\n\n## やること\n\n1. `LAYOUT_PATH` を `\"/\"` にし、`/` でレイアウトページを返すようにする\n2. 個別ページの経路（`viewPath` とそれを使う `view-server.ts` の分岐、`buildViewPage`、\n   `/` のリンク一覧、`urlOf`）を消す。**`VIEW_NAMES` / `viewEventPath` / `/events/*` の SSE\n   経路 / `subscriptionScript` は残す**（レイアウトページが領域ごとに購読しているため）\n3. `src/index.ts` の起動ログから「（個別ビュー・デバッグ用）」の3行を消す。表示するのは\n   `/` の URL 1本だけにする\n4. `VIEW_TITLE` が個別ページのタイトルにしか使われていなければ消す。他で使っていれば残す\n   （**使途を確かめてから決める**）\n5. `test/view.test.ts` で `buildViewPage` を土台にしているテスト（9箇所。購読スクリプト・\n   レポートの描画・ターンのタブなど）を **`buildLayoutPage` ベースに書き換える**。\n   **消すのではなく書き換える**\n6. `docs/architecture.md`「ビューは1枚のページにまとめる」にある「`/main` `/character`\n   `/sidebar` はデバッグしやすさのため残す」旨の記述を、**消した事実と理由**に差し替える\n7. `docs/requirements.md` に個別ビューの URL が書かれていれば直す（`grep` で確かめる）\n8. `bun run check` を通す\n\n## 完了条件\n\n- **テスト件数が減らないこと。** 実行前は 357 pass / 0 fail。書き換え後も**同数以上**で\n  0 fail（実行前後の件数を `evidence` に書く）。減っていたら、それは書き換えでなく削除なので直す\n- **テスト**: 次を満たすこと\n  1. `/` を叩くとレイアウトページ（3領域 + 入力欄）が返る\n  2. `/main` `/character` `/sidebar` は**ページとしては返らない**（404 か、その経路が無いこと）\n  3. `/events/main` `/events/character` `/events/sidebar` の SSE は**今までどおり開ける**\n- **実機**: `TSUKUMO_VIEW_PORT` を変えて tsukumo を起こし、起動ログに出る URL が1本だけで、\n  それを開くと3領域と入力欄が見えることを確認する。**会話の中身は `evidence` に写さない**\n- `grep -c '^#\\{2,4\\} ' docs/architecture.md` が編集の前後で変わらないこと\n- `bun run check` が通ること\n\n## 注意\n\n- **SSE の経路（`/events/*`）を巻き込んで消さない。** ページの経路とイベントの経路は別物で、\n  後者はレイアウトページが3本とも使っている\n- **常駐している 7327 番を落とさない。** 動作確認は `TSUKUMO_VIEW_PORT` を変えて起こし、\n  終わったら自分が起こした pid だけを落とす\n- **実機の確認が要るので `/loop` には載せない。** 実装はサブエージェントに委譲してよい\n- `docs/` を編集するときは**行頭を含めて位置を特定する**（`CLAUDE.md`「ドキュメントを編集する\n  ときの罠」）\n",
    "status": "done",
    "difficulty": "sonnet",
    "passes": true,
    "evidence": "`bun run check` 359 pass / 0 fail（前後で同数。`it()` の数も view.test.ts 164→164、view-server.test.ts 36→36 で、削除ではなく `buildLayoutPage` ベースへの書き換えになっている）。消したのは `viewPath` / `buildViewPage` / `buildIndexPage` / `VIEW_TITLE` / `STANDALONE_VIEW_ELEMENT_ID` / `ViewServer.urlOf` / 起動ログの3行。`LAYOUT_PATH` は `\"/\"` に。\n経路を呼び出し側でも実測（`startViewServer` を直接起こした使い捨てスクリプト、7403。claude は起こしていない）: `/` → 200 で3領域の本文をすべて含む、`/layout` `/main` `/character` `/sidebar` → すべて 404、`/events/{main,character,sidebar}` → 200 `text/event-stream`（SSE は無傷）。\n`docs/architecture.md` 節数 26→26、`docs/requirements.md` 25→25。**実機（Orca のタブ）での目視は未実施**。**開きっぱなしの `/layout` のタブはリンクが切れるので開き直しが要る。**",
    "dependencies": []
  },
  {
    "id": "T-083",
    "summary": "ブラウザ側 JS を外に出すビルド工程を決めて1本通す",
    "task": "ブラウザ側 JS を独立した `.ts` ファイルへ出すためのビルド工程を決め、**1本だけ実際に\n移して経路を通す**。\n\n## 背景\n\nT-069 の決定（`docs/architecture.md`「描画にフレームワークを入れず、morph とビルド1段で\n足りないところを埋める」）のうち、層2の実装。ユーザーが挙げた不足は「**ブラウザ側 JS 812行が\nテンプレート文字列の中にあって `tsc` も `oxlint` も届かない**」。\n\n現物（2026-09-12 に実測）:\n\n- `src/view.ts` の中で、ブラウザ側 JS を文字列として返す関数が9つ・合計 812 行:\n  `layoutScript` 132 / `commandSuggestionsScript` 155 / `dispatchScript` 147 /\n  `pendingAnswerScript` 142 / `sessionInfoScript` 80 / `reportRenderersScript` 76 /\n  `mainTurnsScript` 50 / `subscriptionScript` 20 / `viewScript` 10\n- **ビルド工程は1つも無い。** `package.json` の `start` は `bun run src/index.ts`、\n  `bin/tsukumo` は `#!/usr/bin/env bun` と `import \"../src/index.ts\"` の2行だけ\n  （`bun link` で `~/.bun/bin/tsukumo` から参照されている）\n- **同梱物を配る仕組みは既にある**: `src/bundled-files.ts` の `bundledFilePath` が\n  「tsukumo 自身の場所から解く（cwd に依存しない）」形でパスを返し、`src/view-server.ts` が\n  `/vendor/<name>` を**名前の allowlist**（`VENDOR_ASSET_CONTENT_TYPES`）で配っている\n  （355〜391行目付近）。**ビルド成果物も同じ仕組みに乗せられる見込み**\n- `bun build` は `.ts` `.css` `.html` をエントリポイントにでき、`--watch` で差分ビルドできる\n  （`docs/research/view-rendering.md` 層2の表、出典 https://bun.com/docs/bundler ）\n\n## 解くべき論点\n\n- **ビルド成果物が無いときに起動できるか。** `tsukumo` は `bun link` でグローバルに入っていて\n  別プロジェクトから起こされる。`git clone` 直後や `bun install` 直後に成果物が無い状態で\n  `bun run start` を叩いたらどうなるべきか。候補: (a) 起動時に無ければビルドする、\n  (b) `prepare` / `postinstall` でビルドする、(c) 成果物をリポジトリにコミットする、\n  (d) 無ければ起動を止める（`docs/coding-standards.md`「起動時の前提不足だけが即時終了」）\n- **開発中の手触りを落とさないか。** いまは `.ts` を直して `bun run start` で即反映。\n  ビルドが挟まると1手増える。`--watch` を常用するのか、`start` がビルドを内包するのか\n- **配り方**: `/vendor/` の allowlist に相乗りするか、別の prefix（`/assets/` など）にするか。\n  **会話の載るページなので、外部通信ゼロは崩さない**\n- **どこまでを1本目にするか。** 812行を一度に移すと1コミットで説明が付かない。\n  **経路を通すのに十分な最小の1本**を選ぶ（`subscriptionScript` 20行が最小だが、\n  レイアウトページと単体ページの両方から使われている＝T-082 の後なら1箇所）\n- **`bun run check` に何を足すか。** 切り出した `.ts` が `tsc` と `oxlint` の対象に入ること。\n  ブラウザ用の型（`document` / `EventSource`）をどう通すか（`tsconfig` の `lib` に `DOM` が\n  入っているか確認する）\n\n## やること\n\n1. 上の論点を決める。**`bun run start` と `bin/tsukumo` が壊れないことが最優先**\n2. 決めた形で工程を作り、**1本だけ**（最小のスクリプト）を `.ts` に移して経路を通す\n3. 切り出した `.ts` が `tsc` と `oxlint` の対象に入っていることを、**わざと壊して確かめる**\n   （型エラーを1つ入れて `bun run check` が落ちること。確かめたら戻す）\n4. 決めた工程を `docs/architecture.md` の T-069 の節に追記する（新しい節は作らない。\n   「morph とビルド1段」の節の中に、実際に採った形を書く）\n5. 残りの JS を移すのは **T-084**、CSS は **T-085**。このタスクではやらない\n\n## 完了条件\n\n- `bun run start` が**ビルド成果物の有無にかかわらず**起動すること（決めた方式で。\n  「無ければビルドする」を選んだならその経路を通す。「起動を止める」を選んだなら、\n  止まる条件と出るメッセージを `evidence` に書く）\n- `tsukumo` コマンド（`bun link` 済み）が**別のディレクトリから起動できる**こと。\n  リポジトリ外の適当なディレクトリで `TSUKUMO_VIEW_PORT` を変えて起こして確かめ、\n  結果を `evidence` に書く\n- 切り出した `.ts` に**わざと型エラーを入れると `bun run check` が落ちる**ことを確認した記録が\n  `evidence` にあること（これが「検査が届いている」ことの証拠）\n- **テスト件数が減らないこと**（実行前は 357 pass / 0 fail。前後の件数を `evidence` に書く）\n- **実機**: `TSUKUMO_VIEW_PORT` を変えて起こし、移したスクリプトが動いていること\n  （何が動いていれば合格かは、選んだ1本に応じて `evidence` に書く）\n- `bun run check` が通ること\n\n## 注意\n\n- **ユーザーへの確認が要る論点を含む**（起動経路の設計）。委譲せず、`/loop` に載せない\n- **常駐している 7327 番を落とさない。** 動作確認は `TSUKUMO_VIEW_PORT` を変えて起こす\n- **外部通信ゼロを崩さない**（`docs/coding-standards.md`「会話内容の扱い」）。ビルド成果物も\n  `127.0.0.1` の自分のサーバから配る\n- `bun link` の張り直しが要る場合は**ユーザーの承認を得る**（グローバルなツールの導入・変更）\n",
    "status": "todo",
    "difficulty": "opus",
    "passes": false,
    "evidence": "",
    "dependencies": ["T-082"]
  },
  {
    "id": "T-084",
    "summary": "残りのブラウザ側 JS をすべて .ts へ移す",
    "task": "T-083 が通した経路に乗せて、残りのブラウザ側 JS をすべて `.ts` ファイルへ移す。\n\n## 背景\n\nT-069 の決定（層2）の続き。T-083 が「ビルド工程の形」と「1本だけ移して経路を通す」ところまでを\n済ませている。**このタスクは残りを機械的に移す作業**で、方針の判断は含まない。\n\n移す対象（2026-09-12 の実測。T-083 が1本を移したぶんは減っている）:\n\n| 関数 | 行数 | 役目 |\n| --- | --- | --- |\n| `commandSuggestionsScript` | 155 | `/` コマンド補完 |\n| `dispatchScript` | 147 | 入力欄（送信・中断・キー操作） |\n| `pendingAnswerScript` | 142 | 答え待ちの箱（許可・質問） |\n| `layoutScript` | 132 | 仕切りのドラッグと比率の保存 |\n| `sessionInfoScript` | 80 | モデル・許可モード・経過時間 |\n| `reportRenderersScript` | 76 | mermaid / Chart.js / highlight.js の起動 |\n| `mainTurnsScript` | 50 | やり取りのタブ切り替え |\n| `subscriptionScript` | 20 | SSE 購読と差し替え |\n| `viewScript` | 10 | 単体ページ用（**T-082 で消えている見込み**） |\n\n**移す先の分け方は「ファイル名が概念になっているか」で決める**（原則5。`helpers` / `utils` /\n`common` のような置き場所を名前にしたファイルは作らない）。上の表の「役目」の列が\nそのまま概念の候補になる。\n\n## やること\n\n1. **T-083 の決定を読む**（`evidence` と `docs/architecture.md` の該当節）。工程が通っていなければ\n   着手しない\n2. 残りのスクリプトを `.ts` へ移す。**1関数1ファイルにこだわらない**。概念でまとめる\n3. サーバ側から埋め込んでいた値（要素 id・経路の定数・ラベル）の渡し方を揃える。\n   いまは `JSON.stringify(定数)` をテンプレート文字列に差し込んでいるので、移したあとは\n   **定数を1箇所から import するか、`data-` 属性で渡すか**のどちらかに統一する\n   （**どちらにしたかを `evidence` に書く**）\n4. `src/view.ts` から消えた行数を実測して `evidence` に書く（812行のうち何行が移ったか）\n5. `bun run check` を通す\n\n## 完了条件\n\n- **`src/view.ts` にブラウザ側 JS を返す関数が残っていないこと**（`grep -n \"function .*Script(\"\n  src/view.ts` が何も返さない、または残ったものが `evidence` で理由付きで説明されていること）\n- **テスト件数が減らないこと**（着手前の件数と後の件数を `evidence` に書く。0 fail）\n- 移した `.ts` に**わざと型エラーを入れると `bun run check` が落ちる**こと（T-083 と同じ確認を\n  移した先のファイルで1つ行い、結果を `evidence` に書く）\n- **実機**: `TSUKUMO_VIEW_PORT` を変えて tsukumo を起こし、**ブラウザ側の機能が全部生きている**\n  ことを確認する。確かめる項目は次の6つで、それぞれ動いたかを `evidence` に1行ずつ書く\n  1. 依頼を送れる（Command+Enter）/ 進行中に「中断」に変わる\n  2. `/` を打つと補完の候補が出て、Tab で確定できる\n  3. 仕切りをドラッグすると比率が変わり、再読み込みしても保たれる\n  4. 許可要求か質問が出たとき、ボタンで答えられる\n  5. サイドバーのモデル・許可モードの `<select>` が効き、経過時間が動く\n  6. やり取りのタブを押すと表示が切り替わる\n- `bun run check` が通ること\n\n## 注意\n\n- **これは移す作業で、振る舞いを変える作業ではない。** 移すついでに直したくなったものは\n  別タスクにする（`develop/direction.md` に書く）\n- **常駐している 7327 番を落とさない。** 動作確認は `TSUKUMO_VIEW_PORT` を変えて起こす\n- **実機の確認が要るので `/loop` には載せない。** 実装はサブエージェントに委譲してよい\n- **会話の内容をログに出さない。** 実機確認の `evidence` には「何が動いたか」だけを書く\n",
    "status": "todo",
    "difficulty": "sonnet",
    "passes": false,
    "evidence": "",
    "dependencies": ["T-083"]
  },
  {
    "id": "T-085",
    "summary": "CSS を領域ごとの .css に割ってビルドでまとめる",
    "task": "`STYLE` 定数（約700行）を複数の `.css` ファイルに割り、T-083 のビルド工程でまとめて配る。\n\n## 背景\n\nT-069 の決定（層4）の実装。ユーザーが挙げた不足は「**CSS が1つの巨大な `STYLE` 定数**」。\n\n現物（2026-09-12 に実測）: `src/view.ts` の 1821〜2519 行目が `const STYLE = ` のテンプレート\n文字列1つ。中は区切りコメントで領域ごとにまとまっており、**割る線は既に引かれている**。\nおおよその並びは次のとおり（行番号は `STYLE` の先頭からの相対）:\n\n- キャラビュー（立ち絵・吹き出し・尻尾・アニメーション）: 12〜200 行あたり\n- メインビュー（ステップ・ツールのチップ・レポートの見た目・質問の記録）: 212〜400 行あたり\n- サイドバー（区画・スクロール・タスク一覧）: 409〜460 行あたり\n- レイアウト（grid・仕切り・既定比率に戻すボタン）: 469〜555 行あたり\n- 入力欄（textarea・補完の候補・ボタン・答え待ちの箱）: 560〜700 行あたり\n\n`bun build` は `.css` をエントリポイントにでき、複数の CSS を1つの出力にまとめられる\n（出典 https://bun.com/docs/bundler ）。\n\n## やること\n\n1. **T-083 の決定を読む**（ビルド工程の形）。通っていなければ着手しない\n2. `STYLE` を領域ごとの `.css` に割る。**ファイル名が概念になっていること**（原則5）。\n   上の並びをそのまま使う必要はなく、割ってみて概念にならない塊があれば分け方を変えてよい\n3. まとめた CSS を配る経路を T-083 の工程に合わせる。**ページからは1本の `<link>` か、\n   従来どおりインラインの `<style>` か**を選ぶ（インラインのままでもよい。\n   **どちらにしたかと理由を `evidence` に書く**）\n4. `src/view.ts` から消えた行数を実測して `evidence` に書く\n5. `bun run check` を通す\n\n## 完了条件\n\n- `src/view.ts` に `STYLE` の巨大なテンプレート文字列が残っていないこと\n- **テスト件数が減らないこと**（前後の件数を `evidence` に書く。0 fail）。\n  CSS の中身を検査しているテスト（`.dispatch-send[data-shortcut]::after` を含むかなど）が\n  **通ったままであること**\n- **目視**: 合成データのページを Chrome headless で撮り、**見た目が変わっていない**ことを\n  確認する。少なくとも次の4点を見て `evidence` に書く\n  1. 3領域 + 入力欄の grid が崩れていない\n  2. キャラビューの立ち絵が下端に接し、最新の吹き出しが強調されて尻尾が出ている\n  3. サイドバーの3区画がそれぞれ内側でスクロールする高さになっている\n  4. 送信ボタンに `⌘⏎` が薄い色で出ている\n- `bun run check` が通ること\n\n## 注意\n\n- **これは割る作業で、見た目を変える作業ではない。** 直したくなった見た目は別タスクにする\n- **目視が要るので `/loop` には載せない。** 実装はサブエージェントに委譲してよい\n- 目視で tsukumo を起こすときは `TSUKUMO_VIEW_PORT` を変え、**常駐している 7327 番を落とさない**\n  （合成データの静的ページで済むなら起こさなくてよい）\n",
    "status": "todo",
    "difficulty": "sonnet",
    "passes": false,
    "evidence": "",
    "dependencies": ["T-083"]
  }
]
