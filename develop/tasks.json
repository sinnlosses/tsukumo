[
  {
    "id": "T-510",
    "difficulty": "sonnet",
    "loopable": "Y",
    "dependencies": [],
    "summary": "レポートに出たファイルのパスを押すと、Orca のエディタで開けるようにする",
    "task": "## 背景\n\n- レポートは `src/browser/features/main-view/markdown/markdown.tsx` で描く。ファイルのパスが出るのは主に3か所で、どれもいまは押しても開けない:\n  - inline code（`src/foo.ts:12` の形。人格の規約が `file_path:line_number` で書かせている）\n  - フェンスの info 文字列のパス（```diff src/foo.ts。`code-file-name.ts` の `rehypeCodeFileName` が `CODE_FILE_NAME_PROPERTY` に移し、ブロックの左上に出す）\n  - Markdown の相対リンク `[x](src/foo.ts)`。`sanitize-schema.ts` の `protocols` はスキームの無い相対リンクを素通しするので、押すと tsukumo のページが `/src/foo.ts` へ遷移してしまう\n- Orca には `orca file open <path> [--worktree <selector>] [--json]` がある（2026-09-24 に `orca agent-context --json` で確認。`--worktree` を省くと cwd から作業ツリーを推す）。**行番号を渡す引数は無い**\n- `orca` を呼ぶのは `src/server/adapter/orca-host.ts` だけで、ホスト依存の操作は `src/server/core/host.ts` の `Host`（いまは `showView` 1つ）の裏に置く（`docs/architecture.md`「ホスト依存の操作は1つのポートにまとめる」）\n- git 管理下のファイル一覧は `src/server/adapter/repository-file.ts` の `listRepositoryFiles` が持っていて、`REPOSITORY_FILE_PATH`（`src/shared/repository-file.ts`。起動トークンが要る）で入力欄の `@` 補完に配っている。パスは cwd からの相対\n- ブラウザからサーバへの操作は `src/shared/command.ts` の `ClientCommand`（zod の判別合併）で送る\n\n## 決まっていること（蒸し返さない）\n\n- 開く先は Orca のエディタ（`orca file open`）だけ。VS Code（`code` コマンド）は足さない（2026-09-24 ユーザー決定）\n- 行番号へは飛ばない（Orca に口が無い）。表示の `:12` はそのまま残す\n\n## 解くべき論点\n\n- どの文字列をパスとみなすか。誤検出を避けるため、**git 管理下の一覧にあるものだけ**をリンクにする案が第一候補（`:行` / `:行:桁` の後ろを落として照合する。絶対パスで書かれたものを cwd 相対に直して拾うか）\n- 押す部品の形（`<a>` か `<button>` か）と見た目（`docs/screen-design.md` のトークンから）\n- サーバ側の検証: 届いたパスが git 管理下の一覧にあるときだけ `orca file open` を呼ぶ（任意の文字列を外部コマンドへ渡さない）。一覧に無い・`orca` が無い・失敗したときにどう知らせるか\n- ファイルを指さない相対リンクの扱い（押してもページが遷移しないようにするか）\n\n## やること\n\n1. `Host` に「ファイルを開く」を足し、`orca-host.ts` で `orca file open` を呼ぶ\n2. `ClientCommand` に開く依頼を足し、サーバで検証して `Host` へ渡す\n3. レポートの描画で、上の3か所のパスを押せる部品にする\n4. `docs/display.md`（レポートの節）・`docs/design.md`（プロトコル）・`docs/architecture.md`（`orca-host.ts` の Orca CLI の対応関係）に書き足す。行頭を含めて位置を特定する（`CLAUDE.md`「ドキュメントを編集するときの罠」）\n\n## 完了条件\n\n- git 管理下のパスを書いた inline code・フェンスのパス・相対リンクが押せる部品になり、一覧に無い文字列はならない（部品のテスト）\n- 押すと開く依頼が1回送られる（部品のテスト）。一覧に無いパスの依頼ではホストが呼ばれない（サーバのテスト）\n- 描画に関わるので、`bun run build` のうえで tsukumo を起こし、レポートのパスを押して Orca のエディタにそのファイルが開いたことを `evidence` に書く（`docs/architecture.md`「手で確かめること」）\n- `bun run check` が通る\n\n## 注意\n\n- `orca` 以外の外部コマンドを足さない（`CLAUDE.md`「セットアップ / 環境構築」）\n- `orca-host.ts` 冒頭の境界の注記と `test/architecture.test.ts` の絞りを崩さない",
    "status": "todo",
    "passes": false,
    "evidence": ""
  },
  {
    "id": "T-511",
    "difficulty": "opus",
    "loopable": "Y",
    "dependencies": [],
    "summary": "main に入った成果を日ごとに振り返る画面の、数え方と画面の形を決めて docs に書く",
    "task": "## 背景\n\n2026-09-24 に「成果が見えて、それをキャラクターと一緒に喜べるようにしたい」という指示が来た（`docs/history/direction.md` の同日の節）。材料はリポジトリの中にある:\n\n- コミット: `main` の git 履歴（参考: `main` の日ごとのコミット数は 9/20 が107、9/21 が168、9/22 が104、9/23 が636）\n- `done` になったタスク: `main` の `develop/tasks.json`（`src/server/adapter/task-summary.ts` が `main` の先端を見張って読んでいる）と、アーカイブ済みの `docs/history/tasks.md`\n- 進めたこと: `develop/progress.md` の完了した小節と、アーカイブ済みの `docs/history/progress.md`\n- 日の境目は `src/server/adapter/local-time.ts` が OS のローカル時刻で決めている\n- 画面は `src/browser/stores/location-hash.ts` の `SCREENS`（会話・キャラクター・トークン消費）と、帯の口（`src/browser/features/screen-nav/`。`docs/screen-design.md` 13.9）。トークン消費の画面（`src/browser/features/token-usage/`）が「別の画面 + 会話へ1ターンの依頼を送るボタン」の先例（送り方は `src/browser/features/task-board/components/task-run-confirm.tsx` の `dispatch({ type: \"prompt\", ... })`）\n\n## 決まっていること（蒸し返さない）\n\n- 見せ方は「区切りでまとめて振り返る」だけ。その場で反応するもの（`done` の瞬間・節目のコミット数）は作らない（2026-09-24 ユーザー決定）\n- 数えるのは `main` に入った分だけ。並行する作業ツリーの分も `main` に入れば数え、二重に数えない\n- 区切りはローカル時刻の日（0時で切る）。前の日へ遡れる\n- 置き場は帯に足す5枚目の画面（会話・キャラクター・トークン消費と並ぶ「成果」の口）\n- キャラクターは、画面の「つくもと振り返る」（仮の文言）を押したときに会話へ1ターンの依頼を送り、`speak` で感想を言う。画面を開いただけではターンを使わない\n\n## 解くべき論点\n\n- コミットの「その日」をどの時刻で決めるか。`--ff-only` で送るので、作業ツリーで積んだ時刻（コミットの日付）と `main` に入った時刻がずれる。reflog はクローンのローカルにしか無く期限で消える。どれを採るか\n- `done` になった日をどう知るか（`tasks.json` に日付の欄は無い。`done` にしたコミットの日付を git から引くか）。アーカイブ済みのタスクも同じ規則で拾えるか\n- 「進めたこと」に `progress.md` の小節を並べるか、タスクの `summary` で足りるか\n- 数えるコミットに merge commit・`doing` にするだけのコミット・アーカイブのコミットを含めるか\n- 画面に並べるもの（数の札・タスクの一覧・日の切り替え）と空の日の見せ方。見本を `docs/history/mockup/` に HTML で置くか\n- 振り返りの依頼の文面（何を渡し、キャラクターに何を言わせるか）\n- 雑談モードのとき・ターンが進んでいるときのボタンの扱い\n- 集めた成果をサーバからブラウザへ渡す形（push か、画面を開いたときに取りに行くか）\n\n## やること\n\n1. 上の論点を決め、`docs/requirements.md`（機能の節）・`docs/screen-design.md`（画面の節と 13.9 の帯の表）・`docs/design.md`（サーバが集めてブラウザへ渡す形）に書く。行頭を含めて位置を特定する（`CLAUDE.md`「ドキュメントを編集するときの罠」）\n2. 後続の T-512（集める）・T-513（画面）・T-514（振り返りの依頼）が読む節を、`evidence` に節の名前で並べる（後続タスクの本文は書き換えない）\n\n## 完了条件\n\n- 上の論点それぞれの結論がどの docs のどの節に書かれたかを `evidence` に並べる\n- `bun run check` が通る（`test/section-reference.test.ts` を含む）\n\n## 注意\n\n- 実装はしない（T-512 以降の担当）",
    "status": "todo",
    "passes": false,
    "evidence": ""
  },
  {
    "id": "T-512",
    "difficulty": "sonnet",
    "loopable": "Y",
    "dependencies": ["T-511"],
    "summary": "main の履歴とタスクから日ごとの成果を集め、ブラウザへ渡す",
    "task": "## 背景\n\nT-511 で、`main` に入った成果を日ごとに数える規則と、サーバからブラウザへ渡す形を決めた（書いた節は T-511 の `evidence`）。このタスクはその集める側を作る。\n\n- `main` の上を読む先例は `src/server/adapter/task-summary.ts`（`main` の先端の変化を見張り、`git` で読む。読めないときは「不明」にして作業ツリーのファイルへは落とさない）\n- `node:child_process` を import してよいファイルは `test/architecture.test.ts` が絞っている（原則3。1ファイル = 1つの境界）\n- 日の境目は `src/server/adapter/local-time.ts`\n- 両側で共有する型と読み取りは `src/shared/` に置く（`docs/design.md` 2章）\n\n## やること\n\n1. T-511 の結論どおりに、日ごとのコミット・`done` になったタスク・進めたことを集める adapter を作る\n2. 共有の型と、届いた値の読み取りを `src/shared/` に置く\n3. T-511 が決めた形でブラウザへ渡す経路をつなぐ\n4. `docs/design.md` の該当箇所を実装に合わせて直す（ずれが出たときだけ）\n\n## 完了条件\n\n- 一時ディレクトリに作った git リポジトリで、T-511 の規則どおりに日ごとの数と一覧が出る（テスト）\n- `git` が無い・`main` が無い・`develop/tasks.json` が無いときに例外を投げず、画面で分かる形の「不明」になる（テスト）\n- `bun run check` が通る\n\n## 注意\n\n- 読むのは git の履歴と `develop/`・`docs/history/` のファイルだけ。会話の中身は扱わない（`docs/coding-standards.md`「会話内容の扱い」）\n- テストのフィクスチャに実物のリポジトリの履歴を使わない",
    "status": "todo",
    "passes": false,
    "evidence": ""
  },
  {
    "id": "T-513",
    "difficulty": "sonnet",
    "loopable": "Y",
    "dependencies": ["T-512"],
    "summary": "帯に「成果」の口を足し、日ごとの成果を並べる画面を作る",
    "task": "## 背景\n\nT-511 で画面の形を決め（`docs/screen-design.md` の節。書いた節は T-511 の `evidence`）、T-512 で成果がブラウザに届くようになった。このタスクは画面を作る。\n\n- 画面の名前は `src/browser/stores/location-hash.ts` の `SCREENS`、帯の口は `src/browser/features/screen-nav/`（`docs/screen-design.md` 13.9 の帯の表）\n- 別の画面の先例はトークン消費の画面（`src/browser/features/token-usage/`。container `token-usage-screen.tsx` と器 `presentational-token-usage-screen.tsx`）\n- container / presenter の分け方は `docs/design.md` 2章「機能の中を分ける」\n\n## 決まっていること（蒸し返さない）\n\n- 帯に5枚目の口として足す。区切りはローカル時刻の日で、前の日へ遡れる（2026-09-24 ユーザー決定）\n- 「つくもと振り返る」ボタンは T-514 の担当で、このタスクでは置かない\n\n## やること\n\n1. `SCREENS` と帯の口に「成果」を足す\n2. T-511 の見本どおりに、日ごとの成果を並べる画面を作る（日の切り替え・空の日の見せ方を含む）\n3. `docs/screen-design.md` を実装に合わせて直す（ずれが出たときだけ）\n\n## 完了条件\n\n- 帯の口から開け、`location.hash` で直接開いても同じ画面になる（テスト）\n- 日を切り替えると並ぶ中身が変わり、成果の無い日は T-511 の決めた見せ方になる（部品のテスト）\n- 描画に関わるので、`bun run build` のうえで tsukumo を起こし、幅 1440 と 390 で見た結果（何が見えたか・横スクロールが出ないか）を `evidence` に書く（`docs/architecture.md`「手で確かめること」）\n- `bun run check` が通る",
    "status": "todo",
    "passes": false,
    "evidence": ""
  },
  {
    "id": "T-514",
    "difficulty": "sonnet",
    "loopable": "Y",
    "dependencies": ["T-513"],
    "summary": "成果の画面に「つくもと振り返る」を置き、会話へ1ターンの依頼を送る",
    "task": "## 背景\n\nT-513 で成果の画面ができた。押すとキャラクターが感想を言うボタンを足す。依頼の文面・押せない条件は T-511 で決めた（書いた節は T-511 の `evidence`）。\n\n- 先例はトークン消費の画面の「減らし方を見てもらう」（`src/browser/features/token-usage/`）。送り方は `src/browser/features/task-board/components/task-run-confirm.tsx` の `dispatch({ type: \"prompt\", ... })`\n- キャラクターのセリフは `speak` で言う。レポートの記法と間合いは `src/server/core/report-notation.ts` / `src/server/core/speech-cadence.ts`\n\n## 決まっていること（蒸し返さない）\n\n- 押したときだけ会話へ1ターンの依頼を送る。画面を開いただけではターンを使わない（2026-09-24 ユーザー決定）\n\n## やること\n\n1. ボタンを置き、T-511 の文面で依頼を送る\n2. 押せない条件（ターンが進んでいる・雑談モードなど。T-511 の結論）で押せなくし、理由を添える\n3. `docs/screen-design.md` を実装に合わせて直す（ずれが出たときだけ）\n\n## 完了条件\n\n- 押すと依頼が1回送られ、押せない条件では送られない（部品のテスト）\n- 実際の tsukumo で押し、キャラクターが `speak` で感想を言ったことを `evidence` に書く（会話の中身は写さず、何が起きたかだけ書く）\n- `bun run check` が通る",
    "status": "todo",
    "passes": false,
    "evidence": ""
  },
  {
    "id": "T-515",
    "difficulty": "opus",
    "loopable": "Y",
    "dependencies": [],
    "summary": "待ち時間にほかのキャラが訪ねてくる仕組みを考え、案とタスクのドラフトを書く",
    "task": "## 背景\n\n2026-09-24 に「長いテストやビルドを待っているあいだに、帳ちゃんなどほかのキャラクターが画面に訪ねてきて、つくもと話す時間があると楽しい」という指示が来た（`docs/history/direction.md` の同日の節）。いまの待ち時間は、キャラクターが `bored`（たいくつ）の表情で待つだけ。\n\n- キャラクターパックは1セッションに1つで、切り替え（`switch-character`）は別のパックでセッションを起こし直す（`docs/design.md` 7章）。`speak` の `expression` の enum も1パックぶん\n- `systemPrompt` の append を組むのは `src/server/core/system-prompt.ts` の `takeSystemPromptAppend` 1つ。セリフの間合いは `src/server/core/speech-cadence.ts`\n- 帳ちゃんのパックは T-516 で作る（訪ねてくる1人目。古い帳面の付喪神で膝くらいの背丈の記録係、つくもは世話を焼く兄貴分）\n- 成果の振り返り（T-511〜T-514）とつながりうる: 帳ちゃんは記録係なので、訪ねてきたときに今日の成果を持ってくる形もありうる\n- `docs/requirements.md`「2.2 対象外とすること」に「複数セッション・常駐（client–daemon）へ広げること」がある\n\n## 決まっていること（蒸し返さない）\n\n- このタスクは考えるだけで、作らない（作るのは結論が出てから。2026-09-24 ユーザーの指示）\n- セッションは1つのまま、画面に出るキャラクターが増えるだけ\n\n## 解くべき論点\n\n- 訪問の契機（どの待ちで、どのくらい待ったら来るか。いま tsukumo が「待っている」を知れる信号は何か — 背景のタスク・長く走るツールなど）\n- 会話を誰が作るか（いま動いている claude に2人ぶん話させるのか、ほかの手段か。トークンの費用と、`docs/requirements.md` 2.2 の会話の外部送信に当たらないこと）\n- 2人目の立ち絵と吹き出しを画面のどこに出すか（`docs/screen-design.md`）\n- キャラクターパックを2つ同時に読む形（`docs/design.md` 7章の「1つを選んで切り替える」との関係。`speak` に誰が言ったかを載せるか）\n- 利用者が話しかけたら訪問をどう畳むか\n- 2.2 の「複数セッション・常駐」に当たらないことの確認\n- 訪問のためにパックに要るもの（登場の絵・訪問用の表情。設定画 `assets/chou-design-sheet.png` に棚の陰から「ひょこっ」と覗く絵がある）\n\n## やること\n\n1. 現物のコードとドキュメントを読み、上の論点それぞれに案を並べて推す案とその理由を書く。書く場所（既存の docs の節か、新しいファイルか）も決める\n2. 作るためのタスクの案を `develop/direction.md` の `## エージェントのドラフト` に積む（ユーザーの承認を経てタスクになる）\n\n## 完了条件\n\n- 論点それぞれに推す案と理由が書かれた文書が docs にある（`evidence` にパスと節）\n- `develop/direction.md` の `## エージェントのドラフト` に作るタスクの案がある\n- `bun run check` が通る\n\n## 注意\n\n- コードは変えない\n- `docs/requirements.md` の決定事項（2.2 など）は書き換えない。変える必要があると分かったら、提案とドラフトにそう書く",
    "status": "todo",
    "passes": false,
    "evidence": ""
  },
  {
    "id": "T-516",
    "difficulty": "sonnet",
    "loopable": "Y",
    "dependencies": [],
    "summary": "設定画から帳ちゃんの立ち絵を切り出し、キャラクターパックを作る",
    "task": "## 背景\n\n- パックの形は `characters/tsukumo/`（`character.json` + `persona.md` + 素材）。定義の型と読み取りは `src/shared/character-definition.ts`（`portraits` は「あるものだけでよい」。`docs/requirements.md` 4.4）、パックの置き方は `characters/README.md`\n- 表情の enum は `src/shared/expression.ts` の9つ（default / thinking / proud / flustered / serious / curious / sad / excited / bored）。パックごとの呼び名は `character.json` の `expressions`\n- 1つのパックが持てる画像は `MAX_IMAGE_FILES_PER_PACK`（表情の数 + 2。`src/server/adapter/character-edit.ts`）\n- 設定画は `assets/chou-design-sheet.png`（ユーザーが用意した1枚）。右上の格子に表情6つ（通常・ねむい・びっくり・にっこり・きょとん・しょんぼり）が、**灰色のまだらな地の上に**描かれている。ほかに三面図・人のシルエットとの背丈の比較・仕草3つ・棚の陰から「ひょこっ」と覗く絵がある\n- 画像の加工には `magick`（ImageMagick。`/opt/homebrew/bin/magick`）が使える。素材を作る道具として使うだけで、tsukumo の実行時の依存にはしない。python の PIL はこのマシンでは架構違いで読み込めない（2026-09-24 確認）\n- 帳ちゃんの設定: 古い帳面の付喪神で、膝くらいの背丈の記録係。つくもは世話を焼く兄貴分の側に回る（`characters/tsukumo/persona.md`「覚えたこと」）\n\n## 決まっていること（蒸し返さない）\n\n- 素材は設定画から切り出す。地の除去を試み、仕上がりが粗ければ仮の素材として置いて、あとで差し替える（2026-09-24 ユーザー決定）\n- 自作のキャラクターなので、`characters/tsukumo/` と同じくパックごとコミットする（`characters/README.md`「既定のキャラクター」）\n- 設定画はパックのディレクトリに置かない（画像の数の上限に数えられる）\n\n## 解くべき論点\n\n- 6つの表情を9つの enum のどれに当てるか（例: 通常→default、ねむい→bored、びっくり→flustered、にっこり→proud か excited、きょとん→curious、しょんぼり→sad）。当たる絵の無い enum は置かない\n- `mini` / `face` を用意するか（`face` は帯に出る顔）\n- 地の除去のやり方と、仮の素材で置くかどうかの見切り\n- パックのディレクトリ名（`characters/chou/` など）・`accent`・`persona.md` の口調と一人称（上の設定と設定画から起こす叩き台。利用者があとで直す前提）\n\n## やること\n\n1. 表情を切り出し、地を除去してパックのディレクトリに置く\n2. `character.json` と `persona.md` を書く\n3. `characters/README.md` の既定のキャラクターの記述に帳ちゃんを足す\n\n## 完了条件\n\n- 新しいパックの `character.json` が定義の読み取りを通る（テストで確かめる。既存のパックを読むテストがあればそこに足す）\n- 描画に関わるので、`bun run build` のうえで tsukumo を起こし、キャラクターの画面で帳ちゃんに切り替えて各表情の立ち絵が出ること・地の残り具合を `evidence` に書く（`docs/architecture.md`「手で確かめること」）\n- `bun run check` が通る\n\n## 注意\n\n- `assets/chou-design-sheet.png` は書き換えない\n- ホームの `~/.tsukumo/characters/` に同じ名前のパックがあると同梱のパックを覆う。目視の前に確かめる\n- 訪問用の素材（「ひょこっ」の絵など）は T-515 の結論が出てから。このタスクでは切り出さない",
    "status": "todo",
    "passes": false,
    "evidence": ""
  },
  {
    "id": "T-518",
    "difficulty": "opus",
    "loopable": "Y",
    "dependencies": [],
    "summary": "token-usage-diet をトークン量で考える形にし、cost-optimize の分析を取捨する",
    "task": "## 背景\n\n- `token-usage-diet` スキル（`~/.claude/skills/token-usage-diet/`。`SKILL.md` と `scripts/summarize_usage.py`）は tsukumo の使用量の記録（`$TSUKUMO_HOME` の `token-usage/` と `context-usage/`）を集計して、減らし方を提案する。見積もりの割合の分母は `costUsd`（期間の費用。無いときだけ `cacheReadTokens`。`SKILL.md` の見積もりの節の `denominator`）で、提案の文面も「約 N トークン・約 $X、期間の費用の約 P%」と費用で言う\n- 利用者の契約は Claude Max 20x（サブスクリプション）で、ドルは API の単価で換算した目安にすぎない。減らしたいのは利用枠を食うトークンの消費量\n- 2026-09-24 に `/claude-api cost-optimize`（Claude API のアプリ向けの費用削減の手順）をこのリポジトリに当てた。そこで diet に無い分析として出てきたもの:\n  - 種類別の内訳（キャッシュ読み 66%・出力と思考 17%・キャッシュ作成 14%。**ドルで割った数**）\n  - モデルの単価の違い（Opus 5 → Opus 5.5 でキャッシュ読みの単価が下がった分）。diet は `model-choice` を「持ち場ごとのモデルの内訳が記録に無い」として見積もっていない（`summarize_usage.py` の `not_estimated`）が、`totals.by_model` にはモデル別の数がある\n  - 歩数の見積もり（記録に残らない `speak` だけの歩が、1歩ごとに文脈を丸ごと読み直す）\n  - 無料の削り方とトレードオフ（effort・モデル）の区別、見送った候補とその理由\n- 見直しの結果は `mcp__tsukumo__usage_review_result` でトークン消費の画面の提案の札に渡る（`SKILL.md` 7章。提案の型は効きめ・見出し・根拠・やること）\n\n## 決まっていること（蒸し返さない）\n\n- 費用ではなくトークン消費量で考える。そのうえで、cost-optimize が出していた分析がトークンの物差しでも使えるかを1つずつ判断して、使えるものだけ取り入れる（2026-09-24 ユーザー決定）\n- diet の集計と見直しの画面（提案の札）は残す\n\n## 解くべき論点\n\n- 物差しにする「トークン消費量」の定義。種類（入力・キャッシュ読み・キャッシュ作成・出力・思考）をそのまま足すのか、重みを付けるのか。**サブスクリプションの利用枠が種類ごとにどう数えられるかは公開されていない**ので、確かめられないことは確かめていないと書き、重みを推測で置かない\n- 割合の分母を `costUsd` からトークンへ替えたとき、効きめ（大・中・小）のしきい値と、画面の提案の札の文面（「約 $X」）をどう変えるか\n- cost-optimize 由来の4つ（種類別の内訳・モデルと effort・歩数・無料とトレードオフの区別と見送りの一覧）それぞれが、トークンの物差しで意味を持つか。例: モデルの単価の違いはトークンの数を変えないので、トークンで考えると効きが消える可能性がある\n- 取り入れたものを `SKILL.md` の「4. 何を候補にするか」とスクリプトの `--json` の形のどちらに置くか（画面の提案の型を変えずに済むか）\n\n## やること\n\n1. 上の論点を決め、`SKILL.md` とスクリプトを直す。取り入れないと判断したものは、その理由を `SKILL.md` に1行ずつ残す\n2. スクリプトの出力の形を変えたら、それを読む画面側（`src/browser/features/token-usage/` と `usage_review_result` の検証）が壊れないことを確かめる。直す必要があれば直す\n3. 手元の記録でスクリプトを走らせ、トークンで言い直した見積もりが出ることを確かめる\n\n## 完了条件\n\n- 見積もりの割合と提案の文面が、既定でトークンの消費量で出る（スクリプトの出力で確かめ、`evidence` に数を書く）\n- cost-optimize 由来の4つについて、取り入れた・取り入れないとその理由が `SKILL.md` にある\n- `bun run check` が通る\n\n## 注意\n\n- スキルはリポジトリの外（`~/.claude/skills/`）にあり、コミットの対象外。変えた内容は `evidence` に書く\n- 記録の JSONL の生の行を読まない・写さない（`SKILL.md` 6章。会話の中身は入っていないが、集計の表だけを見る決まり）",
    "status": "todo",
    "passes": false,
    "evidence": ""
  },
  {
    "id": "T-526",
    "difficulty": "sonnet",
    "loopable": "N",
    "dependencies": ["T-519", "T-524", "T-525"],
    "summary": "全作業ツリーを止めて新しいタスク運用へ切り替える",
    "task": "## 背景\n\n`task` コマンド（T-521〜T-523）、書き直したスキル（T-524。claude-skills の枝 `feature/tsukumo-T-524` にあり `main` には未投入）、新しい形式を読むタスク板（T-525）がそろったので、一度に切り替える。承認済みの方針は `docs/history/direction.md` の 2026-09-24 の「タスク運用の作り直し」の項にある（1件1ファイル・着手の印は共有の `.git` の中・完了は作業のコミットに同梱して rebase → ff-only で1回送る・錠の中で採番・`progress.md` をなくす・登録は粗く着手時に確かめ直す・手順は `task` コマンドに）。\n\n途中の作業ツリーが古い `develop/tasks.json` に done を書くと行き場がなくなるので、**tsukumo の全作業ツリー（`git worktree list`。登録時は本体と tsukumo-2〜6・tsukumo-task）が手を止めている間に**行う。\n\n## やること\n\n1. 利用者に全作業ツリーの手が止まっていることを確かめてもらう。`main` の `develop/tasks.json` に doing が無く、各作業ツリーに `main` へ入っていないコミットや未コミットの変更が無いことを確かめる（あれば止めて預ける）\n2. claude-skills の枝 `feature/tsukumo-T-524` を claude-skills の `main` へ入れる。claude-skills 自身の `develop/tasks.json` も `task migrate` で移す\n3. tsukumo の `main` で `task migrate` をかけ、`develop/tasks.json` を `develop/task/` に移す\n4. T-519 の検査の読み元を `develop/task/` と `docs/history/tasks.md` に差し替える\n5. `todo` のタスク本文と tsukumo の `CLAUDE.md`「## タスク運用」から `develop/tasks.json` を名指ししているところを、新しい形で読める程度に直す（全面の書き直しは T-528）\n6. `/list-tasks` と `task status` の一覧が移行前と ID・status で一致すること、タスク板に一覧が出ることを確かめる\n\n## 完了条件\n\n- 移行前後の一覧が一致する（`evidence` に件数）\n- tsukumo の `bun run check` と claude-skills の `./check.sh` が通る\n- このタスク自身の完了が新しい形式（`develop/task/T-526.md` の `## 結果`）で記録されている\n\n## 注意\n\n- `loopable` が N なのは、全作業ツリーが手を止めていることを確かめられるのが利用者だけだから\n- 他のプロジェクト（Git-Bulk-Maestro・gitlab-watari-dori・helm-yadokari）は移さない。次にスキルを呼んだときに移行の案内で止まることを、完了報告で利用者に伝える\n- 各作業ツリーは再開するとき、`git rebase main` で追い付いてから始める\n",
    "status": "todo",
    "passes": false,
    "evidence": ""
  },
  {
    "id": "T-527",
    "difficulty": "opus",
    "loopable": "Y",
    "dependencies": ["T-526"],
    "summary": "progress.md の未解決と注意を、タスクと正典の docs へ振り分けて消す",
    "task": "## 背景\n\n`develop/progress.md` は「未解決」14.5k字と「注意」23.9k字（登録時点で計86項目）を持ち、共通の規則が「未解決・注意は移さない」なので減らない。セッションを始めるたびに読まれる。新しい運用では `progress.md` をなくし、知見は done の前に正典かタスクへ置く。承認済みの方針は `docs/history/direction.md` の 2026-09-24 の「タスク運用の作り直し」の項にある（1件1ファイル・着手の印は共有の `.git` の中・完了は作業のコミットに同梱して rebase → ff-only で1回送る・錠の中で採番・`progress.md` をなくす・登録は粗く着手時に確かめ直す・手順は `task` コマンドに）。\n\n## やること\n\n1. 項目を1つずつ今のコードとドキュメントに照らし、次のどれかに振り分ける: 人の判断待ち → `hold` のタスク（`task new`）/ 直すもの → `todo` のタスク / 長く効く知見 → 正典（`docs/architecture.md`「既知の制約」、`docs/workflow.md`、`docs/coding-standards.md` など該当する節）/ すでに解消・古い → 捨てる\n2. `develop/progress.md` を消す。`scripts/merge-progress.ts`・`scripts/progress-done-section.ts`・それらのテストとフィクスチャ（`test/scripts/merge-progress-git.test.ts`・`test/scripts/progress-done-section.test.ts`・`test/fixture/progress-doc.ts`）・`.gitattributes` の `merge=progress` の行を消す。`CLAUDE.md` のマージドライバの登録手順を消す\n3. `grep -rn 'progress.md' CLAUDE.md docs src scripts test`（`docs/history/` を除く）で残る参照を直す\n\n## 完了条件\n\n- 全項目の行き先（タスクID・docs の節・捨てた理由）を `## 結果` に表で書く（件数が登録時の86と合わない場合は、そのときの実数を書く）\n- `bun run check` が通る（節の参照の検査を含む）\n\n## 注意\n\n- 捨てる判断は「今のコードで解消していることを確かめた」ときだけ。確かめられないものは `hold` にする\n- `.git/config` の `merge.progress.driver` の登録は、利用者が `git config --unset merge.progress.driver` で外す（完了報告で伝える）\n",
    "status": "todo",
    "passes": false,
    "evidence": ""
  },
  {
    "id": "T-528",
    "difficulty": "sonnet",
    "loopable": "Y",
    "dependencies": ["T-527"],
    "summary": "tsukumo の運用の記述を新しい形に一本化し、旧形式の読み口を消す",
    "task": "## 背景\n\n切り替え（T-526）と `progress.md` の解体（T-527）のあと、tsukumo の `CLAUDE.md`（「## タスク運用」「進捗管理とHandoff」「Git運用」「導入済みスキル」）と `docs/workflow.md` にはまだ旧運用（`doing` のコミット、アーカイブを単独で送る、`git merge main`、マージドライバ）の説明が残っている。共通スキル（T-524）が手順を持つようになったので、tsukumo 側には共通版が知らない事情だけを残す。承認済みの方針は `docs/history/direction.md` の 2026-09-24 の「タスク運用の作り直し」の項にある（1件1ファイル・着手の印は共有の `.git` の中・完了は作業のコミットに同梱して rebase → ff-only で1回送る・錠の中で採番・`progress.md` をなくす・登録は粗く着手時に確かめ直す・手順は `task` コマンドに）。\n\n## やること\n\n1. `docs/workflow.md` を、tsukumo 固有の事情（`loopable` の判定、目視の道具、`summary` の40字、参考画像の残し方、確認手順が触る資源の書き方など）だけに縮める。旧運用の決定の経緯は `docs/history/` 側へ寄せる\n2. `CLAUDE.md` の上の4節を、共通スキルの設定（検証コマンド・整形コマンド・ブランチ）と `docs/workflow.md` への参照に縮める。`git merge main` を `task ship`（rebase → ff-only）に置き換える\n3. `docs/architecture.md` の worktree と `doing` の設計判断を新しい形に直す\n4. T-525 で残した旧形式（`develop/tasks.json`）の読み口をタスク板から消す\n\n## 完了条件\n\n- `grep -rn 'tasks.json\\|progress.md\\|merge main' CLAUDE.md docs --exclude-dir=history` の残りが、経緯の説明として意図したものだけになっている（`evidence` に残した行と理由を書く）\n- `CLAUDE.md` の行数の前後を `evidence` に書く\n- `bun run check` が通る（節の参照の検査を含む）\n\n## 注意\n\n- `docs/` の編集は `CLAUDE.md`「ドキュメントを編集するときの罠」に従い、節の数の前後を `grep -c '^#\\{2,3\\} '` で確かめる\n- 終わったら利用者に `/retrospect` を1回回すよう完了報告で勧める（`/loop` に載せない決まり）\n",
    "status": "todo",
    "passes": false,
    "evidence": ""
  },
  {
    "id": "T-529",
    "difficulty": "sonnet",
    "loopable": "Y",
    "dependencies": [],
    "summary": "依頼の履歴の一覧で、依頼をコピーでき、飛ぶのは別の口にする",
    "task": "## 背景\n\nメインビューで利用者の依頼をコピーできない。依頼の1行目はターンの札の見出し（`src/browser/features/main-view/presentational-turn-header.tsx` の `h2` の中の `<button>`）にしか出ず、ボタンの中の文字は選択できない。1行に収まらない分は CSS で省略され、全文はボタンの `title` にしかない。2行目以降は `turn.tsx` の `RequestRest` が出す。見出しのボタンを押すと開く履歴の一覧（`TurnHistory`。`turn-history-row`）も、1行がまるごと `<button>` なので選択できない。\n\n## 決まっていること（蒸し返さない）\n\n- 利用者の案: 見出しの依頼はボタンのまま。押すと依頼の履歴の一覧が出て、**そこで依頼をコピーできる**ようにし、**そのターンへ飛ぶのは行の中の選べる別の口**にする\n- コピーボタンは付けない。本文に依頼の全文を足すこともしない（どちらも利用者が不採用とした）\n\n## やること\n\n1. 履歴の一覧の行を、「依頼の文字（選択できるテキスト）」と「そのターンへ飛ぶ口（ボタン）」に分ける。飛ぶ口の形（番号・印など）は、今の行の見た目（`turn-header.module.css`）から大きく変えない範囲で決める\n2. 行の文字を選択してコピーしたとき、**依頼の全文**（2行目以降も含む）が入るようにする。ブラウザに届いている依頼が途中で切られていないかを確かめる（`truncateRequestText`）\n3. 外側を押すと閉じる（`useDismissSignal`）が、文字を選ぶためのドラッグで閉じないことを確かめる\n\n## 完了条件\n\n- 疑似セッション（`TSUKUMO_DRIVER=fake`）で起こし、playwright で一覧を開いて行の文字をトリプルクリックとドラッグで選び、`window.getSelection().toString()` が依頼の全文と一致する\n- 飛ぶ口を押すとそのターンへ移る。キーボード（Tab と Enter）でも移れる\n- `bun run check` が通る。`evidence` に上の確認と撮った画像のパスを書く\n",
    "status": "doing",
    "passes": false,
    "evidence": ""
  },
  {
    "id": "T-530",
    "difficulty": "sonnet",
    "loopable": "Y",
    "dependencies": [],
    "summary": "キャラ画面を小さくしたとき、吹き出しも立ち絵に合わせて縮める",
    "task": "## 背景\n\nキャラ画面（キャラビュー）の領域を仕切り（`src/browser/features/layout/layout-resizer.tsx`）で小さくすると、立ち絵に比べて吹き出しが大きすぎる（利用者の報告）。`src/browser/features/character-view/character-view.module.css` で、立ち絵は `.portrait` の `height: var(--portrait-height)`（領域の高さの 88%。`.character-region` は `container-type: size`）で領域に合わせて伸び縮みする。一方、吹き出しの文字（`--font-body` 0.9375rem など）、余白（`padding: 10px 14px`）、列の幅（`.balloon-track` の `flex-basis: 11rem`）は固定量で、領域の大きさに連動しない。\n\n## 決まっていること（蒸し返さない）\n\n- 崩れ方は「小さくすると吹き出しが大きい」（利用者の回答）。大きくしたときの見え方は今のままでよい\n- 既定の大きさでの見え方は変えない\n\n## やること\n\n1. 領域の高さを段階的に変えて（既定、仕切りで縮めた中間、縮められる最小）撮り、立ち絵の高さと最新の吹き出しの高さ・幅・文字の大きさを `scripts/capture-view.ts --measure` で測る\n2. 領域が既定より小さいときだけ、吹き出しの文字・余白・列の幅を領域の大きさ（`cqh` など）に合わせて縮める。読める下限（文字の大きさの最小）を決め、それより下は縮めない\n3. 狭い画面（`@media (max-width: 760px)`）の既定が崩れないことを確かめる\n\n## 完了条件\n\n- 既定の大きさで、吹き出しの寸法が変更前と同じ（測った値を並べる）\n- 縮めた2段階で、「吹き出しの高さ ÷ 立ち絵の高さ」が変更前より小さく、既定の大きさでの比に近づいている（3段階の測定値を変更前と後で表にして `evidence` に書く）\n- 画像を読んで、最小の大きさで吹き出しの文字が切れずに読めることを確かめる\n- `bun run check` が通る\n",
    "status": "todo",
    "passes": false,
    "evidence": ""
  }
]
