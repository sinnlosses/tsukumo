---
id: T-617
summary: `test-audit` のキャンペーンで server/session のテストを刈り込む
status: done
difficulty: sonnet
loopable: Y
dependencies: []
---

## 目的

`test-audit` スキルのキャンペーンモード（同スキルの `CAMPAIGN.md` の8段）で、`test/server/session/`（3ファイル・約4,000行。大半は `session-manager.test.ts`） のテストを刈り込む。価値の低いテスト・実装に結合したテスト・より強い証明と重なるテストと、テストしか呼ばない本番コードを消し、残す契約はより強い境界のテストに寄せる。

## 完了条件

- 対象範囲のテストの宣言ごとに `R` / `F` / `C` / `D` の印と1行の証拠を付けた台帳を作り、印ごとの件数と、`C` / `D` の宣言ごとの残す側（keeper）を タスクの結果 に書く（台帳の全文はリポジトリに置かない）
- `D` / `C` の編集と、それで消せるようになったテスト専用の本番コードの継ぎ目の削除を済ませる
- 移した（`C` / `F`）契約ごとに、本番側の持ち主にわざと1つ変異を入れて keeper が落ちることを確かめ、元に戻す
- 基準線（着手時）と完了時の、対象範囲のテストの `it` の数と行数、本番コードの増減を タスクの結果 に書く
- `bun run check` が通る
- 確信の持てる候補が無ければ、台帳の件数だけ書いて編集なしで `done` にしてよい

## 背景

- 2026-09-25 に `test-audit` スキル（openclaw/openclaw の同名スキルを日本語化して claude-skills に入れたもの）を取り込み、監査モードの試運転で、テストだけが呼んでいた `parseAnswer`（`src/shared/pending-ask.ts`）と `newFormatTaskSummaries`（`src/shared/task-summary.ts`）を消した。固有の検査は `test/shared/command.test.ts` と `test/server/repository/adapter/task-summary.test.ts` に移した（`0b3ef70a`）
- テスト全体は 205 ファイル・約4.7万行で、機能ごとに1タスクに分けた。このタスクはserver/sessionの範囲

## 決まっていること（蒸し返さない）

- 消すかどうかは `docs/coding-standards.md`「テスト」節の「消すかどうか」「埋めないと決めた穴」と突き合わせる。スキルの残す基準とリポジトリの規約が食い違ったら、リポジトリの規約を優先し、食い違いを タスクの結果 に書く
- 残す基準は、スキルの `SKILL.md`「残す基準」のとおり（とくに、出力の書式を固定するテストと過去のバグの退行テストは残す）
- `CAMPAIGN.md` の段7（プロダクトの欠陥）で見つけた欠陥は、このタスクでは直さず、`develop/direction.md` の 「エージェントのドラフト」節 に積む。段8（`main` の merge）はタスクが短いので要らない
- 1タスク＝1コミット。対象範囲の外のテストは、範囲内の契約を吸収する keeper になるときだけ触る

## やること

前提を確かめ直した（2026-09-25）。`test/server/session/` は今も3ファイル・約4,000行
（`session-manager.test.ts` 3044行・92宣言、`session-launch.test.ts` 534行・17宣言、
`adapter/remembered-default.test.ts` 400行・35宣言。合計144宣言）のままで、タスクの前提は崩れて
いない。`CAMPAIGN.md` の段どおりに進める:

1. 基準線: 対象3ファイルを `bun test --isolate` で走らせ、144宣言・369テストがすべて通ることを
   確認する
2. レーンと棚卸し: 本番側の持ち主の境界で3レーンに分ける
   （`session-manager.ts` の `dispatch`/`receive`、`session-launch.ts` の
   `createSessionLaunch`、`remembered-default.ts` の3つの覚える口）
3. レーンごとの読み取り専用の台帳: 各レーンの宣言を読み、本番側の持ち主・兄弟の実装・重なる
   テスト（`test/server/adapter/lib/json-file.test.ts`・
   `test/server/character-pack/core/character-selection.test.ts`・
   `test/shared/command.test.ts` など）を突き合わせて印を付ける
4. レーンごとの層の計画: 台帳から冗長な層を探し、keeper を名指しする
5. 切り替え: `D`/`C` の編集と、消せるようになったテスト専用の継ぎ目の削除
6. 保全のレビュー: 消した契約ごとに本番側へ変異を入れ、keeper が落ちることを確認してから
   バイト単位で元に戻す
7. プロダクトの欠陥: 基準線で落ちたものは無い（全通過）ので対象なし
8. 突き合わせと引き継ぎ: `bun run format` / `bun run check` を通し、タスクの結果に台帳の件数・
   keeper・変異確認・行数の増減を書く

## 注意

- 変異を入れて確かめるときは、退避したコピーから戻す（`git checkout --` で戻すと、同じファイルへの先行の編集ごと消える）
- `CAMPAIGN.md` の段3・段6は、別のエージェントに読ませることを求めている。サブエージェントの中で別のエージェントを立てられないなら、レーンごとに順に自分で読み、段6の保全のレビューは受け入れのときにメインが行う
- ブラウザに出る絵は自動テストで守らない方針（`docs/architecture.md`）なので、描画のテストを消すときも、足すときも、その方針の範囲で判断する

## 結果

- 台帳（144宣言）: R 133 / F 0 / C 2（→ `it.each` 1件。keeper は同ファイルの set-portrait・set-background の2件と `test/shared/command.test.ts`）/ D 9（「覚えた値の使いどころ」の3件はテスト内で再実装した関数を検査していた。keeper は `test/server/character-pack/core/character-selection.test.ts`。3つの write の「ディレクトリ作成」「塞がれても投げない」6件は keeper が `test/server/adapter/lib/json-file.test.ts`）
- 変異: `selectInitialCharacterPack` の条件反転 → 2件 red／`writeJsonFile` の mkdir と write の順を入れ替え → red／`isCharacterEditCommand` から set-profile を外す → `it.each` の行が red。どれも戻した。規約との食い違い・段7の欠陥はなし
- 基準線 3978行・144宣言 → 完了時 3889行・134宣言（-89行）。本番コードは増減なし（テスト専用の継ぎ目は無かった）
- 検証コマンド: 2765 pass / 0 fail
- 振り返り: ツールのエラー4件・同じファイル×4（ドラフト0件）
