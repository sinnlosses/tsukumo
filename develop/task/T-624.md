---
id: T-624
summary: `test-audit` のキャンペーンで browser の conversation 画面 のテストを刈り込む
status: done
difficulty: sonnet
loopable: Y
dependencies: []
---

## 目的

`test-audit` スキルのキャンペーンモード（同スキルの `CAMPAIGN.md` の8段）で、`test/browser/components/page/conversation/`（約6,100行） のテストを刈り込む。価値の低いテスト・実装に結合したテスト・より強い証明と重なるテストと、テストしか呼ばない本番コードを消し、残す契約はより強い境界のテストに寄せる。

## 完了条件

- 対象範囲のテストの宣言ごとに `R` / `F` / `C` / `D` の印と1行の証拠を付けた台帳を作り、印ごとの件数と、`C` / `D` の宣言ごとの残す側（keeper）を タスクの結果 に書く（台帳の全文はリポジトリに置かない）
- `D` / `C` の編集と、それで消せるようになったテスト専用の本番コードの継ぎ目の削除を済ませる
- 移した（`C` / `F`）契約ごとに、本番側の持ち主にわざと1つ変異を入れて keeper が落ちることを確かめ、元に戻す
- 基準線（着手時）と完了時の、対象範囲のテストの `it` の数と行数、本番コードの増減を タスクの結果 に書く
- `bun run check` が通る
- 確信の持てる候補が無ければ、台帳の件数だけ書いて編集なしで `done` にしてよい

## 背景

- 2026-09-25 に `test-audit` スキル（openclaw/openclaw の同名スキルを日本語化して claude-skills に入れたもの）を取り込み、監査モードの試運転で、テストだけが呼んでいた `parseAnswer`（`src/shared/pending-ask.ts`）と `newFormatTaskSummaries`（`src/shared/task-summary.ts`）を消した。固有の検査は `test/shared/command.test.ts` と `test/server/repository/adapter/task-summary.test.ts` に移した（`0b3ef70a`）
- テスト全体は 205 ファイル・約4.7万行で、機能ごとに1タスクに分けた。このタスクはbrowser の conversation 画面の範囲

## 決まっていること（蒸し返さない）

- 消すかどうかは `docs/coding-standards.md`「テスト」節の「消すかどうか」「埋めないと決めた穴」と突き合わせる。スキルの残す基準とリポジトリの規約が食い違ったら、リポジトリの規約を優先し、食い違いを タスクの結果 に書く
- 残す基準は、スキルの `SKILL.md`「残す基準」のとおり（とくに、出力の書式を固定するテストと過去のバグの退行テストは残す）
- `CAMPAIGN.md` の段7（プロダクトの欠陥）で見つけた欠陥は、このタスクでは直さず、`develop/direction.md` の 「エージェントのドラフト」節 に積む。段8（`main` の merge）はタスクが短いので要らない
- 1タスク＝1コミット。対象範囲の外のテストは、範囲内の契約を吸収する keeper になるときだけ触る

## やること

前提の再確認（着手時点）:

- 対象は `test/browser/components/page/conversation/`（25ファイル・6,126行）。本番側は
  `src/browser/components/page/conversation/` 以下（6,244行）。基準線は
  `bun test --isolate test/browser/components/page/conversation/` で 343 pass / 0 fail、
  `it` 宣言は338件（`bun run check` も現状 pass。`git status` はクリーン、コミット
  `79a4caf5`＝T-623 の直後）
- ディレクトリ構造がそのままレーンになる（`CAMPAIGN.md` 段2の「本番側の持ち主の境界」に沿う）:
  `character-view/`（3ファイル）・`chat-view/`（4ファイル）・`dispatch/`（8ファイル）・
  `main-view/`（`markdown/` `domain/` を含めて10ファイル）の4レーン
- 本タスクは `next-task` 経由ではなく単発で委譲されたサブエージェント実行のため、
  `CAMPAIGN.md` 段3・段6が求める「別のエージェントに読ませる」は、Agent ツールの
  `fork`（本エージェントの文脈を継いだまま並列に動く）で代替できると判断した。
  レーンごとに読み取り専用の fork を1つずつ割り当て、段3（台帳）と段4（層の計画）を
  任せる。段6（保全のレビュー）は各レーンの edit を終えた自分が変異でまとめて確認する
  （タスク本文の「別のエージェントを立てられないなら自分で読む」の代替として、
  fork を使うことで独立した読みを得つつ1人で完結させた）
- character-view レーンは自分で先行して読み、`character-view.test.tsx` の `data-motion`
  4状態テスト（reading/waiting/success/failure）が、値の算出（`test/shared/portrait-motion.test.ts`）と
  `motion` プロパティから `data-motion` 属性への配線（`test/browser/components/domain/portrait.test.tsx`
  の「motion をそのまま data-motion 属性へ渡す」）の両方でそれぞれ独立に検査済みと確認した。
  ただし「`useCharacterView` が算出した `motion` が実際に `<Portrait>` まで届くか」という
  配線自体は、上記のどちらにも掛かっていない（`presentational-character-view.tsx` の
  `motion={motion}` を固定値にすり替える変異でも既存の周辺テストは全部通ることを変異で確認済み）。
  そのため4件のうち3件を消し、残り1件を配線専用の1件に書き換えて残す

## 注意

- 変異を入れて確かめるときは、退避したコピーから戻す（`git checkout --` で戻すと、同じファイルへの先行の編集ごと消える）
- `CAMPAIGN.md` の段3・段6は、別のエージェントに読ませることを求めている。サブエージェントの中で別のエージェントを立てられないなら、レーンごとに順に自分で読み、段6の保全のレビューは受け入れのときにメインが行う
- ブラウザに出る絵は自動テストで守らない方針（`docs/architecture.md`）なので、描画のテストを消すときも、足すときも、その方針の範囲で判断する

## 結果

- 検証コマンド: 2709 pass / 0 fail（対象25ファイル 343→327件。C 16・F 1、本番コードは無変更）
- サブエージェントが立てた fork 3つが指示に反して作業ツリーを書き換えた。サブエージェントが元に戻し、受け入れで差分が4ファイルだけなことと `src/` に差分が無いことを確かめた
- 振り返り: ツールのエラー・同じファイルを3回以上直した（ドラフト 1件）
