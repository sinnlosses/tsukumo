---
id: T-594
summary: `session/` と `view-server/` を移し、共有の箱の検査を掛けて移行を閉じる
status: todo
difficulty: sonnet
loopable: Y
dependencies: [T-593]
---

## 目的

残りの2機能を移し、`server/core/` と `server/adapter/` の直下を共有の箱（6ファイル）だけにして、移行を終える。

## 完了条件

- `docs/design.md` 2章「いまの `src/server/` から移す先（移行の対応表）」で段が「8」の12行が移っている。テストも同じ置き換えで移っている
- `server/core/` と `server/adapter/` の直下には、表で「動かさない（共有の箱）」の6ファイルしか無い
- 同じ節の検査の表で、直す段に「8」を含む行が入っている
  - 共有の箱は機能を読まない
  - 共有の箱に1つの機能だけが読むファイルが無い
  - `node:child_process` の許可リストの `bundle.ts`
- `SERVER_FEATURE_IMPORTS` が 2章の辺の表と同じになっている
- 移したパスを指す記述が直っている（`scripts/` の `server.ts` `bundle.ts` `port-resolution.ts` の import、`docs/design.md` 5章「session-manager.ts（core）」「server.ts と session-socket.ts（adapter）」を含む）
- 移し終えたので、`docs/design.md` 2章「いまの `src/server/` から移す先（移行の対応表）」を `docs/history/decision.md` へ移し、2章の「移行中」の注記から server の文を外している
- `bun run check` が通る
- 着手時に対応表の前提が崩れていたら、表を直してから移す

## 背景

- 束ねる `session-manager.ts` を `session/core/` に置く理由と、共有の箱の基準は `docs/design.md` 2章「サーバの機能と、機能どうしの辺」

## 注意

- 節を `docs/history/` へ移すときは、`test/section-reference.test.ts` が見る「ファイル名＋番号＋「句」」の参照が切れていないかを見る
