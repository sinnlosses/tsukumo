---
id: T-580
summary: plan-tasks に、設計待ちの実装タスクの本文は設計の着地後に揃える、を足す
status: todo
difficulty: sonnet
loopable: Y
dependencies: []
---

## 目的

設計を書くタスクと、その設計に従う実装タスクを同時に登録したときに、後段の本文が設計の結論を先取りして書かれ、設計の着地後に何件も書き直しになるのをなくす。

## 完了条件

- claude-skills の `plan-tasks/SKILL.md` 手順3「方針決めが要るものは前段のタスクに切り出して」の後ろに、「前段が設計を書くなら、後段の `## 背景` には設計の結論を先取りして書かず、前段の完了条件に後段の本文の追随を入れる」旨が入っている（言い回しは合わせてよい）
- claude-skills の `./check.sh` が通り、変更が claude-skills の `main` に入っている
- tsukumo の `bun run check` が通る

## 背景

2026-09-25 の T-561・T-562 の振り返りで見つけたもの（`docs/history/direction.md` の 2026-09-25「振り返りのドラフト3件」）。T-561〜T-568 を1コミット（+1041行）で一度に登録し、T-561 が設計を書いたあとに T-562〜T-568 の7件を +36/-27 で書き直した（`a34a98a5`）。T-561 の結果の節にも「後続の本文は未反映」とある。T-569〜T-575（T-573 が出し方をデザインし、T-574 がそれに従って描く）も同じ形で登録されている。

## 注意

- 作業するのは claude-skills のリポジトリ（`~/ghq/github.com/sinnlosses/claude-skills`。`~/.claude/skills/` の各スキルはそこへの symlink で、書き換えは全プロジェクトへ即座に効く）。**着手時に `git -C ~/ghq/github.com/sinnlosses/claude-skills status --short` が空でなければ止めて預ける**
- claude-skills の枝は本体の作業ツリーで checkout せず、`git worktree add` で別の作業ツリーを切ってそこで作業する（本体で切り替えると全プロジェクトのスキルが変わる）。枝の名前は `feature/tsukumo-<このタスクのID>`。検証コマンドは claude-skills の `CLAUDE.md` に従う（`./check.sh`）
- コミットは claude-skills の側に入る。tsukumo のタスクの結果には claude-skills のコミットハッシュを書く
- 調べて、手順3かほかの節に同じ趣旨が既に入っていたら `dropped` で閉じてよい
