---
id: T-698
summary: `docs/design.md` の経緯と実測を history へ移し、残りの章を締めて約1000行にする
status: todo
difficulty: opus
loopable: Y
dependencies: [T-697]
---

## 目的

`docs/design.md` を絞る一続きの最終段。残った章（2〜4・6・8〜11 ほか）から経緯と実測を
`docs/history/` へ出し、文を締めて全体を約1000行にする。

## 決まっていること（蒸し返さない）

- 役割と返し先の判定は T-694 で `docs/design.md` の冒頭に書いた宣言に従う
- 経緯（日付つきの決定・ユーザーの発言の引用・「当初は〜だった」・SDK の版の実測）→ `docs/history/`、
  「なぜこの形か」で読み手の判断を変えるもの → `docs/architecture.md`「設計判断」。正典には
  「いまの形」と1行の参照だけ（`docs/requirements.md`「正典に残すもの・`docs/history/` へ移すもの」）
- 10章「E2E の走らせ方」は CLAUDE.md が引いているので残す。11章は承認済みの依存の一覧を残す

## 完了条件

- `wc -l docs/design.md` が 1200 行以下（目安は約1000行）
- 本文に残る日付（`20[0-9][0-9]-[0-9][0-9]-[0-9][0-9]`）が、「その値を見直すべき条件」など
  いまの形に必要なものだけ（着手時は119箇所）
- 「節の索引」が本文と1対1
- 移した記述を指していた参照を張り替えてある
- 一続き（T-694〜T-697 と、このタスク）を通して `docs/design.md` の冒頭の役割の宣言と中身が
  食い違っていないかを通読で確かめた（このタスクだけは通読してよい）
- `bun run check` が通る（`scripts/find-stray-reference.ts` を含む）

## 解くべき論点

- 3章「動きの流れ」と4章「shared」の、どの段落が不変条件でどれが実装の写しか
- 10章「E2E の成果物と再現」（69行）を `docs/architecture.md`「手で確かめること」へ寄せるか、
  design.md に締めて残すか

## 背景

- T-697 まで済んだ時点の design.md が対象。着手時の行数と日付の数を最初に測る
- 置き先の `docs/history/decision.md` は「design.md <章>」の見出しで既に経緯を受けている

## 注意

- T-686（Node への移行のドキュメント追随）が 11章を書き換える予定。先に済んでいればその形を、
  未着手なら 11章は Bun の記述を触らずに締めるだけにする
- 編集の前後で見出しの数を数える（CLAUDE.md「ドキュメントを編集するときの罠」）
