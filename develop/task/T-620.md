---
id: T-620
summary: `test-audit` で view-server・visit・character-pack のテストを刈り込む
status: done
difficulty: sonnet
loopable: Y
dependencies: []
---

## 目的

`test-audit` スキルのキャンペーンモード（同スキルの `CAMPAIGN.md` の8段）で、`test/server/view-server/` `test/server/visit/` `test/server/character-pack/`（15ファイル・約4,300行） のテストを刈り込む。価値の低いテスト・実装に結合したテスト・より強い証明と重なるテストと、テストしか呼ばない本番コードを消し、残す契約はより強い境界のテストに寄せる。

## 完了条件

- 対象範囲のテストの宣言ごとに `R` / `F` / `C` / `D` の印と1行の証拠を付けた台帳を作り、印ごとの件数と、`C` / `D` の宣言ごとの残す側（keeper）を タスクの結果 に書く（台帳の全文はリポジトリに置かない）
- `D` / `C` の編集と、それで消せるようになったテスト専用の本番コードの継ぎ目の削除を済ませる
- 移した（`C` / `F`）契約ごとに、本番側の持ち主にわざと1つ変異を入れて keeper が落ちることを確かめ、元に戻す
- 基準線（着手時）と完了時の、対象範囲のテストの `it` の数と行数、本番コードの増減を タスクの結果 に書く
- `bun run check` が通る
- 確信の持てる候補が無ければ、台帳の件数だけ書いて編集なしで `done` にしてよい

## 背景

- 2026-09-25 に `test-audit` スキル（openclaw/openclaw の同名スキルを日本語化して claude-skills に入れたもの）を取り込み、監査モードの試運転で、テストだけが呼んでいた `parseAnswer`（`src/shared/pending-ask.ts`）と `newFormatTaskSummaries`（`src/shared/task-summary.ts`）を消した。固有の検査は `test/shared/command.test.ts` と `test/server/repository/adapter/task-summary.test.ts` に移した（`0b3ef70a`）
- テスト全体は 205 ファイル・約4.7万行で、機能ごとに1タスクに分けた。このタスクはserver の view-server・visit・character-packの範囲

## 決まっていること（蒸し返さない）

- 消すかどうかは `docs/coding-standards.md`「テスト」節の「消すかどうか」「埋めないと決めた穴」と突き合わせる。スキルの残す基準とリポジトリの規約が食い違ったら、リポジトリの規約を優先し、食い違いを タスクの結果 に書く
- 残す基準は、スキルの `SKILL.md`「残す基準」のとおり（とくに、出力の書式を固定するテストと過去のバグの退行テストは残す）
- `CAMPAIGN.md` の段7（プロダクトの欠陥）で見つけた欠陥は、このタスクでは直さず、`develop/direction.md` の 「エージェントのドラフト」節 に積む。段8（`main` の merge）はタスクが短いので要らない
- 1タスク＝1コミット。対象範囲の外のテストは、範囲内の契約を吸収する keeper になるときだけ触る

## やること

前提を調べ直した（2026-09-25、HEAD `61075120`）。対象は現状も `test/server/view-server/`
`test/server/visit/` `test/server/character-pack/` の15ファイル・4,348行のままで、`bun test --isolate`
で対象範囲は231件（`it.each` 展開込み）すべて通る（基準線に落ちているテストは無い）。前提は崩れて
いないので作業する。

- `test-audit` スキルと `CAMPAIGN.md` を読み、レーンを本番側の持ち主の境界で3つに切る:
  `view-server`（adapter 5ファイル + core 1ファイル）・`visit`（adapter 1ファイル + core 5ファイル）・
  `character-pack`（adapter 2ファイル + core 1ファイル）
- レーンごとに、対象のテストファイルとその本番側の持ち主（`src/server/<機能>/...`）を1件ずつ読み、
  宣言ごとに `R`/`F`/`C`/`D` を判定する台帳を作る（台帳はリポジトリに置かず、この作業のメモとして
  だけ持つ）。兄弟のテスト（同じ契約を別の境界で守っていそうなもの。例: `test/shared/command.test.ts`）
  も突き合わせる
- 見つかった `C`/`D` を編集し、消せるようになったテスト専用の本番コードの継ぎ目があれば消す
- 移した契約ごとに、本番側の持ち主にわざと1つ変異を入れて keeper が落ちることを確かめ、
  `git diff` が空になることを確認してから元に戻す
- `bun run test` と `bun run check` を通す
- 台帳の件数・keeper・基準線と完了時の `it` 数と行数を、この報告（呼び出し元への引き継ぎ）に書く
  （タスクファイルには `## 結果` 節を書かない指示があるため、台帳の全文も件数の要約もここには置かない）

## 注意

- 変異を入れて確かめるときは、退避したコピーから戻す（`git checkout --` で戻すと、同じファイルへの先行の編集ごと消える）
- `CAMPAIGN.md` の段3・段6は、別のエージェントに読ませることを求めている。サブエージェントの中で別のエージェントを立てられないなら、レーンごとに順に自分で読み、段6の保全のレビューは受け入れのときにメインが行う
- ブラウザに出る絵は自動テストで守らない方針（`docs/architecture.md`）なので、描画のテストを消すときも、足すときも、その方針の範囲で判断する

## 結果

- 検証コマンド: 2770 pass / 0 fail（対象15ファイル 231→229件。C 2件を `test/shared/command.test.ts` と同ファイルの受け口の2件へ、本番コードは無変更）
- 受け入れで、消したテストの跡に残った keeper の在りかを説明するコメントを外した
- 振り返り: 受け入れのときにメインが直したもの（ドラフト 1件）
