---
id: T-674
summary: `docs/coding-standards.md`「React」に TanStack Query の `retry` 既定値の扱いを書く
status: todo
difficulty: haiku
loopable: Y
dependencies: []
---

## 目的

手書きの `fetch` を TanStack Query の `queryOptions`（oRPC など）へ置き換えると、`retry` の既定（3回）で失敗時の挙動が黙って変わる。次に置き換える人が同じ取りこぼしをしないよう規約に書く。

## 完了条件

- `docs/coding-standards.md` の `## React` 節に、「`fetch` を `queryOptions` へ置き換えるときは、失敗時にすぐ控えへ倒していた読み取りに `retry: false` を付けるか、既定の再試行で待つ挙動が変わらないかを見る」旨を1〜2行で書く（コードから読めない理由として、既定だと約7秒骨組みのまま待つことを添える）
- 冒頭の「節の索引」の `React` 行の説明を要るなら直す
- `bun run check` が通る

## 背景

- T-653 で、落ちた応答をすぐ控えに倒していた3つの読み取り（`@` 補完の一覧・コンテキストの内訳・灯りの暦）が、既定の再試行のために約7秒骨組みのまま待つようになり、受け入れで `retry: false` を足した
- いま `retry: false` があるのは `src/browser/components/page/achievement/hooks/use-achievement-calendar.ts`・`src/browser/components/page/conversation/components/hooks/use-repository-file-paths.ts`・`src/browser/domain/context-usage.ts`
- `docs/coding-standards.md` には TanStack Query・`queryOptions` の記述がまだ無い
- 振り返りのドラフトは T-654・T-655 の注意に書く案も挙げていたが、どちらも done なので規約に書く
