---
id: T-610
summary: 文字の部品を `components/ui/` に作り、`domain/` と `features/` の文字の段・色の規則を置き換える
status: done
difficulty: sonnet
loopable: Y
dependencies: [T-606]
---

## 目的

`color: var(--ink-quiet)`・`font-size: var(--font-secondary)` / `var(--font-label)` のような「文字の段と色だけ」の規則が各機能の CSS に散っている。文字の部品を作って大きさ・色（静か・強調・状態）を variant で表し、まず `components/domain/` と `features/` から置き換える。空の表示（「まだ何もない」の一文）もここに寄せる。

## 完了条件

- `docs/design.md` 2章「`components/ui/` の部品（variant の作法と一覧）」の一覧どおりの `Text`（`element`・`size`・`tone`・`weight`・`className`。props はすべて必須）が `src/browser/components/ui/text/text.tsx`・`text.module.css` にある。size・tone・weight の対応表は `satisfies Record<合併型, string | undefined>` で検査し、**見出しの部品（T-609）が同じ表を読めるように置く**（`ui/text/` の中。形は実装で決めてよいが、表は1つ）。既定の `margin: 0` は `:where()` に書く。`test/browser/components/ui/text/text.test.tsx` に、軸ごとのすべての値 → 付く class、`"inherit"` で class が付かないこと、`element` → 描く要素、`className` が足されることのテストがある
- `src/browser/components/domain/` と `src/browser/features/` で、**字の段・色・太さ（と `margin`・文字の組み）だけを持つ規則が付いた `<p>` / `<span>`** が `Text` に置き換わり、規則が消えている（`margin` と文字の組み——1行で切る・字間・桁揃え・書体——は `className` に残す）。計画時の実測で対象になるのは、空の表示（`.task-empty` ×5・`.sidebar-empty` ×2・`.screen-nav-work-empty`）、`.task-run-question`、`.task-ready` / `.task-blocked`、`.protocol-mismatch-title`、`.screen-nav-work-failure-mark`、`<p>` に付いた見出し風の字（`.screen-nav-work-heading`・`.screen-nav-work-full-heading` など・`.screen-nav-settings-heading` ×4）
- **置き換えないもの**: 表のセル・行・リストの項目に付く状態の色（`domain/task-status.ts` が選ぶ `.task-status-*`、`.task-board-row.task-done`、`.screen-nav-work-step-done` / `-failed`。要素が `<td>` / `<li>` で、`Text` を挟むと DOM が1段増える）。ほかに残したものがあれば理由を結果に1行ずつ書く
- `Text` を読むのが2つ以上の領域・機能になっている（`test/architecture.test.ts`「browser/ の機能をまたぐ箱」が通る）
- `test/architecture.test.ts`「components/ui/ の部品の className」が `Text` を対象にして通る（検査がまだ無ければこのタスクで足す。足し方は T-607 の完了条件と同じ）
- `docs/design.md` 6.1「部品の置き場」の段落に `<Text>` を足してある
- 置き換えた画面（サイドバー・画面のナビの帯の「いまの作業」と設定の歯車・タスクの一覧と表・実行の確認・版違いの知らせ）を起動して目視し、字の大きさ・色・太さ・間隔が置き換え前と同じであることを確かめている（何を見たかを結果に書く）
- `bun run check` が通る

## 背景

- 計画時の実測（2026-09-25）で、字の段と色（と太さ・`margin: 0`）しか持たない単独の class の規則は 68（`page/` 52・`features/` 10・`domain/` 6）。うち 17 は `--usage-*` の色（トークン消費の画面）
- 色のトークンは `src/browser/styles/theme.css` の `--ink` / `--ink-quiet` / `--accent` / `--state-ok` / `--state-warn` / `--state-ng` / `--state-ask`
- `.sidebar-block p { margin: 0.2rem 0 }` のような親からの要素の選択子は `:where()` の既定より強いので、置き換えても効き続ける
- `components/page/` の置き換えは後段（T-611）が受け持つ
- 作法は `docs/design.md` 2章「`components/ui/` の部品（variant の作法と一覧）」が正典

## 決まっていること（蒸し返さない）

- 語彙は size（`--font-*` の6段 + `inherit`）・tone（`ink` / `ink-quiet` / `accent` / `state-*` + `inherit`）・weight（`normal` / `semibold` / `bold` + `inherit`）の3軸。書体（等幅・明朝）は語彙に入れず `className` で渡す
- **見た目を変えない。** 太さの 600 と 700 は分けたまま
- `element` は `"p" | "span"` から始め、要るものが出たらテストの行と一緒に足す（汎用の `as` は持たない）

## やること

いまのコードで前提を取り直した結果（2026-09-25 実行時）:

- 対象の12クラスの現物と使用箇所を確認した。`.task-empty`（`task-list.tsx` ×3・`task-table.tsx` ×2）・
  `.sidebar-empty`（`persona-memory-section.tsx`・`recent-topic-section.tsx`）は
  すべて `SidebarSection`（`components/domain/sidebar/section.tsx`）経由で
  `.sidebar-block` の中に描かれ、`.sidebar-block p { margin: 0.2rem 0 }`（背景に書かれた
  親からの要素選択子）が margin を持ち続ける。`task-table.tsx` の2件だけは `.task-board`
  ダイアログの直下で、この親選択子が効かない——**ブラウザ既定の `<p>` の margin
  （`1em 0`。Chrome で実測: font-size 13px の `<p>` で marginTop/marginBottom とも 13px）に
  頼っている**。`Text` は既定の `margin: 0` を `:where()` に持つ（`:where()` の詳細度は0だが、
  author 由来の規則は UA スタイルシートに常に勝つ）ので、そのまま置き換えると `.task-empty` の2件だけ margin が
  消えて見た目が変わる。**対策**: `.task-empty` を削らず `margin: 1em 0;` だけ残す
  （`.sidebar-block p` が効く3件には影響しない——detail specificity で常にそちらが勝つ）
- `.screen-nav-work-empty` / `.task-run-question` はもともと `margin: 0` 明示なので、`Text` の
  既定 `margin: 0` とそのまま一致する。クラスごと削除してよい
- `.protocol-mismatch-title` は margin を持たないが、`.protocol-mismatch p { margin: 0 }`
  （親からの要素選択子）が効き続けるので、クラスごと削除してよい
- `.task-ready` / `.task-blocked` / `.screen-nav-work-failure-mark` は `<span>` に付く色・太さだけの
  規則で margin を持たない。クラスごと削除してよい
- 見出し風の `<p>`（`.screen-nav-work-heading` ・ `.screen-nav-work-full-heading` ・
  `.screen-nav-work-background-heading` ・ `.screen-nav-work-steps-heading` ・
  `.screen-nav-settings-heading`）はどれも周りに margin を肩代わりする親からの要素選択子が
  無いので、**margin の宣言だけ残す**（色・font-size・font-weight は `Text` の props へ）。
  `.screen-nav-settings-heading` は兄弟結合子のルール
  （`.screen-nav-settings-heading ~ .screen-nav-settings-heading { margin-top: 0.8rem }`）も
  そのまま残す
- `Text` の対応表（`TEXT_SIZE_CLASS` / `TEXT_TONE_CLASS` / `TEXT_WEIGHT_CLASS`）は
  `ui/text/text.tsx` 自身から export し、T-609 の `Heading` がそこから import する形にする
  （`ui/text/` の中に表は1つだけなので、別ファイルへ分けない）
- `test/architecture.test.ts`「components/ui/ の部品の className」は対象を
  `readonly className: string` の字面で動的に拾うので、`Text` を足すだけで自動的に対象へ入る
  （検査自体の変更は不要）

置き換える12クラス・8ファイルの対応（すべて確認済み）:

| クラス                                | ファイル                                                                           | 残す CSS                           | Text の props（size / tone / weight） |
| ------------------------------------- | ---------------------------------------------------------------------------------- | ---------------------------------- | ------------------------------------- |
| `.task-empty`（×5）                   | `features/task-board/task-list.tsx`・`components/task-table.tsx`                   | `margin: 1em 0;`                   | inherit / ink-quiet / inherit         |
| `.sidebar-empty`（×2）                | `components/domain/sidebar/persona-memory-section.tsx`・`recent-topic-section.tsx` | 削除                               | inherit / ink-quiet / inherit         |
| `.screen-nav-work-empty`              | `components/domain/screen-nav/components/screen-nav-current-work.tsx`              | 削除                               | inherit / ink-quiet / inherit         |
| `.task-run-question`                  | `features/task-board/components/task-run-confirm.tsx`                              | 削除                               | heading / inherit / semibold          |
| `.task-ready`                         | `features/task-board/components/readiness-cell.tsx`                                | 削除                               | inherit / state-ok / inherit          |
| `.task-blocked`                       | 同上                                                                               | 削除                               | inherit / state-warn / inherit        |
| `.protocol-mismatch-title`            | `components/domain/protocol-mismatch.tsx`                                          | 削除                               | subheading / inherit / bold           |
| `.screen-nav-work-failure-mark`       | `screen-nav-current-work.tsx`                                                      | 削除                               | inherit / state-ng / semibold         |
| `.screen-nav-work-heading`            | 同上                                                                               | `margin: 0 0 0.4rem;`              | inherit / inherit / semibold          |
| `.screen-nav-work-full-heading`       | 同上                                                                               | `margin: 0 0 0.2rem;`              | inherit / ink-quiet / inherit         |
| `.screen-nav-work-background-heading` | 同上                                                                               | `margin: 0.4rem 0 0.2rem;`         | inherit / ink-quiet / inherit         |
| `.screen-nav-work-steps-heading`      | 同上                                                                               | `margin: 0.4rem 0 0.2rem;`         | inherit / ink-quiet / inherit         |
| `.screen-nav-settings-heading`（×4）  | `components/domain/screen-nav/components/screen-nav-settings.tsx`                  | `margin: 0 0 0.4rem;` + 兄弟結合子 | label / ink-quiet / inherit           |

## 注意

- 見出し（`<h*>`）は見出しの部品（T-609）が受け持つので、ここでは置き換えない

## 結果

- `components/ui/text/` に `Text`（`element`・`size`・`tone`・`weight`・`className`。対応表は `Heading` が読めるよう export、既定の `margin: 0` は `:where()`）を作り、`domain/`・`features/` の文字の段・色の規則12クラスを置き換えた。`<td>`・`<tr>`・`<li>` に付く状態色と兄弟結合子の margin は残した。親の要素選択子が効かない文脈の margin だけ CSS に残した
- 検証コマンド: 2725 pass / 0 fail（+22）、`bun run build` 成功。変更前（`serve-revision.ts HEAD`）と並べ、「いまの作業」の見出しと空の表示・設定の見出し4つの文字の大きさ・色・太さ・余白が一致し、着手可否の色がトークンと一致することを測った。実行中・失敗・背景の見出しなど疑似セッションで出せない分は宣言の突き合わせで代えた
- 振り返り: ツールのエラー・同じファイルを3回以上直した（ドラフト 1件）
