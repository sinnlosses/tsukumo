---
id: T-628
summary: 振り返りで積んだ教訓6件を docs に書く
status: done
difficulty: sonnet
loopable: Y
dependencies: []
---

## 目的

これまでのタスクの振り返りで見つかった、同じ失敗を繰り返さないための教訓6件を、それぞれの正典のドキュメントに1項ずつ書く。

## 完了条件

- 次の6件が、指定したドキュメントの節に1項（1〜3行）ずつ入っている
  1. `docs/workflow.md`「タスクを書くとき・受け入れるとき」: わざと壊して落ちるか確かめるときは、このタスクで編集していないファイルか一時ファイルに壊しを入れる。退避したコピーから戻す（`git checkout --` で戻すと、同じファイルへの先行の編集ごと消える）
  2. `docs/coding-standards.md` の非同期・常駐プロセスの節: 入口の判定（ターン中なら断る など）は `await` をまたいで保たれない。`await` のあとで取り直し、駆動へ「前提を渡す口」を「依頼を送る口」より先に呼ぶ
  3. `docs/architecture.md`「手で確かめること」の起動の手順: 起こしたままの tsukumo は `bun run build` を打ち直しても古い組み立てを配り続ける。直しながら目視するなら `TSUKUMO_WATCH_UI=1` で起こすか、組み立てのたびに上げ直す
  4. `docs/coding-standards.md`「React」の state の置き場: 画面を離れるとアンマウントされる部品（`main.tsx` の `OVERLAY_SCREEN`）では、一回限りの合図や「もう見せた」の印を部品の state に持たず、React の外（モジュールの store）に持つ
  5. `docs/design.md` 2章「段と、test/architecture.test.ts の直し方」: ファイルを移したときにパスを指す記述は `.ts` / `.tsx` だけでなく `.module.css` のコメントにもあるので、`grep -rn` の対象から外さない
  6. `docs/architecture.md`「手で確かめること」: 目視のために起こす tsukumo は `TSUKUMO_VIEW_PORT` を 39000 番台に固定し、ふだん使いの既定（7327 から始まる帯）と重ねない。`scripts/stop.ts --port` は自分で起こしたポートにだけ打つ
- 節を足していない（`grep -c '^#\{2,3\} '` の数が編集の前後で同じ）
- `bun run check` が通る
- 調べて前提が成り立たなければ、理由を書いて `dropped` で閉じてよい

## 背景

- 出典はそれぞれ T-552・T-565・T-567・T-568・T-582・T-609 の振り返り（`docs/history/direction.md` の 2026-09-25 の節に根拠がある）
- 2026-09-25 時点では、どのドキュメントにもこれらは書かれていない（`docs/workflow.md` に `checkout --`、`docs/architecture.md` に `TSUKUMO_WATCH_UI` と 39000 番台は出てこない）

## 決まっていること（蒸し返さない）

- 目視用のポートは 39000 番台（振り返りの提案のまま）

## やること

前提を調べ直した結果（2026-09-26時点）:

1. `docs/workflow.md`「## タスクを書くとき・受け入れるとき」（12行目）は現存。`checkout --` の
   文言は無い。ここへ1項足す
2. `docs/coding-standards.md` に「非同期・常駐プロセス」という見出しの節は無い。`常駐プロセス`
   を扱う節は「## エラーハンドリング」（146行目）だけなので、ここへ1項足す
3. `docs/architecture.md`「## 手で確かめること」（916行目）は現存。番号付きの起動手順（1〜9）の
   直前に段落があり、`TSUKUMO_WATCH_UI` の文言は無い。ここへ1項足す
4. `docs/coding-standards.md`「## React」の中に「state の置き場」という見出しの小節は無い。
   `### useEffect の代わりに使うもの`（471行目）の中に段落として足す
5. **前提が崩れている**: `docs/design.md` 2章に「段と、test/architecture.test.ts の直し方」という
   節は現存しない（`grep -n` で design.md 全体を検索してもヒット無し）。同名の節は
   `docs/history/decision.md` に2件あるが、いずれも別の移行（2026-09-25決定のサーバ機能分割）が
   段8で完了した際に「移し終えたのでこの節を docs/history/decision.md へ移す」と明記されたうえで
   design.md から退避されたもの。代替先として挙がっていた「残りの移動のタスク（T-583〜T-585）の
   `## 注意`」も、`develop/task/` に存在しない（既に完了・削除済み）。
   よって指定された2つの置き場はどちらも現存しない。
   ただし助言の中身（ファイル移動時に `.module.css` のコメントを `grep -rn` の対象から
   外さない）は、CLAUDE.md「現在の状態」に残る段8（キャラクターパック本体の移動）・段9
   （セッションの復元）でも再発しうる一般的な注意なので、**タスクを dropped にはせず**、
   design.md 2章「### ディレクトリ」節（214行目、ファイルツリーのあとの箇条書き部分）に、
   特定の「段」に紐付けない一般的な注意として1項足す（新しい見出しは追加しない）
6. `docs/architecture.md`「## 手で確かめること」の起動の手順の直前段落は現存。`39000` の文言は
   無い。3と同じ段落に1項足す

## 注意

- `docs/` の各ファイルの冒頭には節の索引がある。見出し名で位置を探すと索引の行に当たるので、行頭の `\n## ` / `\n### ` から位置を特定する（CLAUDE.md「ドキュメントを編集するときの罠」）
- 節の句を変えていないか `bun run check` の `test/section-reference.test.ts` が見る

## 結果

- 検証コマンド: 2705 pass / 0 fail（docs 4ファイルに教訓6件。節の数は編集前後で一致）
- 5件目の書き先「段と、test/architecture.test.ts の直し方」は `docs/history/decision.md` へ退避済みだったので、`docs/design.md` 2章「ディレクトリ」へ段に紐付けない形で書いた
- 振り返り: 兆候なし
