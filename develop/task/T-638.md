---
id: T-638
summary: `page/token-usage/` を `components/page/` の規約に合わせて移す
status: todo
difficulty: sonnet
loopable: Y
dependencies: [T-635]
---

## 目的

使用量の画面（`src/browser/components/page/token-usage/`）を、`docs/design.md` 2章の
`components/page/` の規約に合わせる。

## 完了条件

- `page/token-usage/` の直下に残るのは `domain/` `hooks/` `components/` と `token-usage.tsx`
  `presentational-token-usage.tsx`（と、使うなら `token-usage.module.css`）だけ
- 部品は `components/<部品>/<部品>.tsx` の1部品1ディレクトリになっている
- 画面の見た目と振る舞いが変わっていない（目視で確かめ、何を見たかを結果に書く）
- `bun run check` が通る

## 背景

- いまの container は `token-usage-screen.tsx`、presenter は `presentational-token-usage-screen.tsx`
- 直下の部品: `context-usage-card.tsx` `period-usage-card.tsx` `usage-review-card.tsx`。
  部品でない `.ts`: `context-usage-category.ts` `usage-format.ts`。CSS: `token-usage.module.css`
- `main.tsx` が `TokenUsageScreen` を直に import している

## 注意

- 移し先の表は T-635 が本文に書き込む。書き込まれていなければ `docs/design.md` 2章の規約から決め、
  調べて規約に合っていれば `dropped` で閉じてよい
- テストは `test/browser/components/page/` の下にソースと同じ形で並んでいるので、一緒に移す。
  `docs/` と `.module.css` のコメントにあるパスの参照も `grep -rn` で拾って直す
- `T-624` / `T-625`（テストの刈り込み）と同じテストを触る。先に入ったほうに rebase で合わせる
