---
id: T-592
summary: `visit/` を機能のディレクトリへ移す
status: todo
difficulty: sonnet
loopable: Y
dependencies: [T-591]
---

## 目的

訪問の7ファイル（`sdk-visit-script.ts` を含む）を `src/server/visit/{core,adapter}/` へ移す。

## 完了条件

- `docs/design.md` 2章「いまの `src/server/` から移す先（移行の対応表）」で段が「6」の7行が移っている。テストも同じ置き換えで移っている
- `SERVER_FEATURES` に `visit` が足されている（葉なので辺の表には読む先を足さない）
- 移した `sdk-visit-script.ts` が、`SDK_BOUNDARY_FILE` の新しい位置のほうで通っている
- 移したパスを指す記述が直っている（`docs/design.md` 5章「訪問の契機と状態」「訪問の台本」を含む。`docs/research/` は除く）
- `bun run check` が通る
- 着手時に対応表の前提が崩れていたら、表を直してから移す。移す意味が無くなっていたら `dropped` で閉じてよい

## 背景

- SDK の境界を機能ごとに置く理由は `docs/history/decision.md`「design.md 2. 全体構成 / ディレクトリ（`src/server/` を機能で割った）」

## 注意

- `docs/research/` は記録なので直さない
