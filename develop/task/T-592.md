---
id: T-592
summary: `visit/` を機能のディレクトリへ移す
status: done
difficulty: sonnet
loopable: Y
dependencies: [T-591]
---

## 目的

訪問の7ファイル（`sdk-visit-script.ts` を含む）を `src/server/visit/{core,adapter}/` へ移す。

## 完了条件

- `docs/design.md` 2章「いまの `src/server/` から移す先（移行の対応表）」で段が「6」の7行が移っている。テストも同じ置き換えで移っている
- `SERVER_FEATURES` に `visit` が足されている（葉なので辺の表には読む先を足さない）
- 移した `sdk-visit-script.ts` が、`SDK_BOUNDARY_FILE` の新しい位置のほうで通っている
- 移したパスを指す記述が直っている（`docs/design.md` 5章「訪問の契機と状態」「訪問の台本」を含む。`docs/research/` は除く）
- `bun run check` が通る
- 着手時に対応表の前提が崩れていたら、表を直してから移す。移す意味が無くなっていたら `dropped` で閉じてよい

## 背景

- SDK の境界を機能ごとに置く理由は `docs/history/decision.md`「design.md 2. 全体構成 / ディレクトリ（`src/server/` を機能で割った）」

## やること

段6の7行の前提を調べ直した結果、崩れていなかった（対応表は正しいまま移せる）。

- 7ファイルは全部いまの場所に実在する: `src/server/adapter/sdk-visit-script.ts`
  `src/server/adapter/visit-clock.ts` `src/server/core/visit-guest.ts`
  `src/server/core/visit-script-writer.ts` `src/server/core/visit-script.ts`
  `src/server/core/visit-timing.ts` `src/server/core/visit-watch.ts`
- テストの有無も表どおり: `sdk-visit-script.ts` だけテスト無し、残り6本は
  `test/server/{adapter,core}/<同名>.test.ts` がある
- 7ファイルの import 先は shared・訪問どうし・SDK だけで、他機能を読んでいない（`visit` は
  対応表・機能表どおり葉になれる）
- 外から visit を読む側（移す側で直す必要がある箇所）:
  - `src/server/core/session-manager.ts`（`./visit-watch.ts` → `../visit/core/visit-watch.ts`。
    コメントの `src/server/core/visit-timing.ts` も含む）
  - `src/server/core/config.ts`（コメントの `src/server/core/visit-timing.ts` への言及）
  - `src/session-start.ts`（6本の相対 import）
  - `src/shared/session-event.ts` `src/shared/session-state.ts` `src/shared/visit.ts`
    （コメントの `src/server/core/visit-timing.ts` への言及。実 import ではない）
  - `test/fixture/manual-clock.ts`（コメントと import）
  - `test/server/core/session-manager.test.ts`（3本の import。session-manager.ts 自体は
    まだ移さないので、このテストファイルは移さず import 先の深さだけ直す）
  - `docs/design.md` 5章「訪問の契機と状態」「訪問の台本」の2つの表（`src/server/core/…`
    `src/server/adapter/…` の実パスを書いている箇所。2章の移行対応表〔797〜872行〕は
    移行が終わるまで残す決まりなので触らない）
  - `docs/screen-design.md` の2箇所（`src/server/core/visit-timing.ts` への言及）
- `scripts/`・`CLAUDE.md`・`package.json`・`.oxlintrc.json` に visit のファイルパスを指す記述は
  無かった（`scripts/task-mention.ts` の許可リストは `orca-host.ts` `server.ts` `bundle.ts`
  `port-resolution.ts` だけで visit を含まない）

## 注意

- `docs/research/` は記録なので直さない

## 結果

- 段6の7ファイル（`visit/`）とそのテストを移し、`SERVER_FEATURES` に `visit` を葉として足した。`sdk-visit-script.ts` は `SDK_BOUNDARY_FILE` の新しい位置で通っている
- 検証コマンド: 2663 pass / 0 fail、`scripts/find-stray-reference.ts` 0件
- 振り返り: 兆候なし
