---
id: T-646
summary: `recall` を候補の一覧にし、`recall_episode` を足し、`index` と `keep` を消す
status: todo
difficulty: sonnet
loopable: Y
dependencies: [T-643]
---

## 目的

キャラクターが古い雑談を2段階で引けるようにする。`recall(言葉)` はエピソードの候補の一覧を返し、`recall_episode(id)` がその1件の逐語を返す。索引はもう定着が書くので、キャラクターが書く `index` と、`weight` に吸収される `keep` を消す。

## 完了条件

- 雑談モードのとき、`recall` が候補の一覧（`id`・`title`・`gist`）を、`recall_episode` が1件の逐語を返す。量と1ターンに開ける数は容量の表に従うことをテストで確かめている
- `index` と `keep` のツール、その口（`ChatRecall` の `index`・`ChatKeep`）と `index.jsonl`・`kept.jsonl` を読み書きするコードが消えている
- 作法の条（`src/server/chat/core/chat-manner.ts`）とツールの説明が、2段階の引き方に合わせて書き直されている
- `docs/display.md` 4.2 などに「戻り値が `"ok"` だけではないツール」の数え方があれば、`recall_episode` を足して直している
- `bun run check` が通る

## 背景

- ツールの定義は `src/server/session-driver/adapter/sdk-tool.ts`（`KEEP_TOOL_NAME`・`INDEX_TOOL_NAME`・`RECALL_TOOL_NAME`）
- `recall` の戻り値の文面は `src/server/chat/core/chat-memory-prompt.ts` の `chatRecallText`
- 口の型は `src/server/session-driver/core/session-driver.ts`
- `recall` は戻り値が `"ok"` だけではない最初のツール（`docs/chat-mode.md` 4.9「古い雑談は索引を引いて思い出す」）

## 注意

- 引いた逐語は画面に出さない（モデルの文脈に入るだけ）
- 消すツールが用語集（`docs/glossary.md`）にあれば直す
