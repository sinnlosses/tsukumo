---
id: T-697
summary: `docs/design.md` 2章と6章からコードの写し（ディレクトリの木・一覧・割り振り表・部品の木）を消す
status: todo
difficulty: sonnet
loopable: Y
dependencies: [T-696]
---

## 目的

`docs/design.md` を絞る一続きの4段目。2章「全体構成」と6章「browser」に積もったコードの写しを
消し、置き場所の**基準**だけを残す。

## 決まっていること（蒸し返さない）

- 役割と返し先の判定は T-694 で `docs/design.md` の冒頭に書いた宣言に従う
- 消すもの: 「ディレクトリ」節の1ファイル1行の木（369行）、「コマンドの受け手と手続きの置き方」の
  「28種の割り振り」表と「置くもの」表のうちファイルの中身の説明、`components/ui/` の部品の一覧、
  「領域の機能と、置かれる機能」「機能の中を分ける」「`lib/` と `utils/` に置く基準」の例の列挙、
  6.1「部品の木」。**history へは移さない**（git の履歴に残る）
- 残すもの: 層と依存の向き・機能どうしの辺の表（`test/architecture.test.ts` と対）・手続きの辿り方
  （4段）と断る条件の置き場所・variant の作法・container / presenter と `hooks/` の基準・
  `lib/` と `utils/` の基準・6.2 以降の方針

## 完了条件

- 2章が目安 380 行前後、6章が目安 100 行前後
- 「ディレクトリ」節は、木の代わりに「どの種類のファイルをどこに置くか」の基準（数十行）になっている。
  CLAUDE.md の原則5 が引く「`docs/design.md` 2章」の置き場所の基準が消えていない
- 消した節・表を指していた参照を張り替えてある（`test/architecture.test.ts` のメッセージ、
  `docs/coding-standards.md`、`docs/workflow.md`「ページの形」など）
- `bun run check` が通る（`scripts/find-stray-reference.ts` を含む）

## 背景

- `docs/design.md` 2章は 78 行目付近から。節の行数: 層と依存の向き 38・サーバの機能と辺 65・
  コマンドの受け手 91・ディレクトリ 369・`components/ui/` 139・領域の機能 81・機能の中を分ける 168・
  `lib/` と `utils/` 105。6章は 2185 行目付近で 6.1 部品の木 110
- 調べて、基準が例の列挙と絡み合っていて外せない節は、例を1〜2個に減らすだけでよい

## 注意

- T-681（zustand への置き換え）が 6.2 を書き換える予定。6.2 は方針として残るので、文を締めるだけに
  留める
- 編集の前後で見出しの数を数える（CLAUDE.md「ドキュメントを編集するときの罠」）
