---
id: T-615
summary: ダイアログの部品を `components/ui/` に作り、10箇所の `<dialog>` を置き換える
status: done
difficulty: sonnet
loopable: Y
dependencies: [T-606]
---

## 目的

`<dialog>` を `showModal()` で開く箇所が10ファイルあり、`useModalDialog` の呼び出し・`::backdrop` の規則・閉じる口の置き方を各自が書いている。ダイアログの部品を作って背景の暗さ・置き方を variant で表し、置き換える。

## 完了条件

- `docs/design.md` 2章「`components/ui/` の部品（variant の作法と一覧）」の一覧どおりの `Dialog`（`open`・名前（`aria-label` か `aria-labelledby` の合併型）・`backdrop`・`placement`・`onClose`・`className`・`children`。props はすべて必須）が `src/browser/components/ui/dialog/dialog.tsx`・`dialog.module.css` にある。`useModalDialog` の呼び出し・Esc の `close`・backdrop のクリックの読み替え（`event.target` が `<dialog>` 自身のときだけ）を部品が持ち、どれも `onClose` を呼ぶ。`backdrop` は `dim`（`ground` の 70%）/ `deep`（88%）/ `clear`（透明）、`placement` は `{ kind: "auto" }` / `{ kind: "at"; top; left }`。顔と箱（幅・余白・地・枠・角丸・影）は持たない
- `test/browser/components/ui/dialog/dialog.test.tsx` に、`open` への追随（開く・閉じる）、Esc（`close` イベント）と backdrop のクリックで `onClose` が呼ばれること、中のクリックでは呼ばれないこと、`backdrop` のすべての値 → 付く class、`placement: "at"` の座標、名前の付き方のテストがある
- 下の「背景」の10個の `<dialog>` が `Dialog` に置き換わり、各機能の `::backdrop` の規則と「backdrop を押したら閉じる」の読み替え（フックの `onDialogClick` など）と `useModalDialog` の呼び出しが消えている。残したものは理由を結果に1行ずつ書く
- `ImageZoom` が `Dialog` を使っている（`components/ui/` どうしの import）
- `test/architecture.test.ts`「components/ui/ の部品の className」が `Dialog` を対象にして通る（検査がまだ無ければこのタスクで足す。足し方は T-607 の完了条件と同じ）
- `docs/design.md` 6.1「部品の置き場」の段落に `<Dialog>` を足してある
- 置き換えたダイアログをすべて開いて目視し、背景の暗さ・置き方・閉じ方（閉じる口・Esc・外側クリック）が置き換え前と同じであることを確かめている（何を見たかを結果に書く。重ねて開く2箇所は、下のダイアログの上に出ることと、上だけが閉じることも見る）
- `bun run check` が通る

## 背景

- `<dialog>` を持つファイル（計画時は12と数えたが、`task-table.tsx`・`task-run-button.tsx` はコメントの中だけだった）: `features/task-board/presentational-task-board.tsx`・`components/task-run-confirm.tsx`、`components/domain/sidebar/persona-memory-section.tsx`、`page/achievement/diary-book.tsx`、`page/character/components/character-delete-confirm.tsx`・`character-profile-edit-dialog.tsx`・`portrait-clear-confirm.tsx`、`page/character/presentational-character-create.tsx`、`page/conversation/character-view/presentational-speech-log.tsx`、`components/ui/image-zoom/image-zoom.tsx`
- 10個すべてが `useModalDialog` と backdrop のクリックの読み替えを書き写している（フックに持つもの: `use-task-board.ts`・`use-diary-book.ts`・`use-character-create.ts`・`use-speech-log.ts`）
- `::backdrop` の規則は 9。`ground` の 70% が6、88% が日記帳、透明が2（`.character-clear-confirm`・`.speech-log`）
- `portrait-clear-confirm.tsx` は位置を inline style の座標で指定する（`placement: "at"`）。`.speech-log` は CSS の anchor positioning で置く（`placement: "auto"` のまま `className` で置く）
- `use-speech-log.ts` は開いた直後にスクロールを下端へ送る effect を持ち、`showModal()` のあとに走る必要がある（コメントあり）。`Dialog` は子なので、その effect は親のフックより先に走る
- 開閉は `src/browser/hooks/use-modal-dialog.ts` の `useModalDialog(open)`。重ねて開く（表のモーダルの上に確認を開く）箇所がある（`task-run-confirm.tsx`・`persona-memory-section.tsx`）
- 作法は `docs/design.md` 2章「`components/ui/` の部品（variant の作法と一覧）」が正典

## 決まっていること（蒸し返さない）

- `Dialog` が持つのは振る舞い（開閉・Esc・外側クリック）と背景（`backdrop`）と置き方（`placement`）だけ。顔と箱は呼び出し側の `className`（顔を揃えるかは別の判断として持ち越し）
- **見た目を変えない。** 日記帳の 88% は `deep` として残す
- 外側クリックで閉じるのは全部のダイアログ共通なので、variant にしない
- 部品の顔に揃えることで生じる小さな見た目の差（押せないときの減光・段に無い文字の大きさを近い段へ・hover の出方）は受け入れる。変わった箇所は結果に1行ずつ書く（2026-09-25 ユーザー決定。T-613 の4点を受け入れたとき）

## やること

現状のコードで前提を取り直した結果（着手時点）:

- `<dialog>` を持つファイルは背景の10個（`presentational-task-board.tsx`・`task-run-confirm.tsx`・
  `persona-memory-section.tsx`〔`PersonaMemoryForgetConfirm`〕・`diary-book.tsx`・
  `character-delete-confirm.tsx`・`character-profile-edit-dialog.tsx`・`portrait-clear-confirm.tsx`・
  `presentational-character-create.tsx`・`presentational-speech-log.tsx`・`image-zoom.tsx`）と実物が一致した
- `useModalDialog(true)` で「開いた状態でだけ組み立てる」形が6箇所（`task-run-confirm.tsx`・
  `persona-memory-section.tsx`・`character-delete-confirm.tsx`・`character-profile-edit-dialog.tsx`・
  `portrait-clear-confirm.tsx`・`image-zoom.tsx`）、`useModalDialog(open)` で「常にマウントして `open` を
  追随させる」形が4箇所（`use-task-board.ts`・`use-diary-book.ts`・`use-character-create.ts`・
  `use-speech-log.ts`）。後者4つは `ref`/`onDialogClick` をモデルに持たせて presentational へ渡している
  ので、`Dialog` に移すとこれらのフックから `ref`/`onDialogClick`/`useModalDialog` の呼び出しが消え、
  代わりに `open: boolean` をモデルに足して presentational から `<Dialog open={...}>` へ渡す形になる
- `::backdrop` の規則は9箇所（`task-board.module.css` に2〔`.task-board`・`.task-run-confirm`〕、
  `image-zoom.module.css`・`achievement.module.css`〔`.diary-book`〕・`character-screen.module.css`に3
  〔`.character-clear-confirm`・`.character-delete-dialog`・`.character-create-dialog`〕・
  `character-view.module.css`〔`.speech-log`〕・`sidebar.module.css`〔`.sidebar-persona-memory-confirm`〕）。
  `ground` 70% が6（task-board・task-run-confirm・image-zoom・character-delete-dialog・
  character-create-dialog・sidebar-persona-memory-confirm）、88% が日記帳の1、透明が2
  （character-clear-confirm・speech-log）
- `portrait-clear-confirm.tsx` の `anchoredStyle` は `{ top: "…px", left: "…px" }` という
  `CSSProperties` を返して `<dialog style={...}>` に渡している。`Dialog` の `placement: "at"` は
  `top`/`left` を数値で受けるので、`anchoredStyle` の戻り値を `{ top: number; left: number }` へ変える
- `use-speech-log.ts` の「開いた直後に下端へ転がす」`useEffect` は、`useModalDialog` の効果より後に
  宣言する必要がある（`showModal()` の前は高さが 0 のため）。`Dialog` に `useModalDialog` を移しても、
  `Dialog` は `PresentationalSpeechLog` のさらに子なので、React は子から親の順に効果を実行し、
  `Dialog` 内の `useModalDialog` の効果は `useSpeechLog`（コンテナの外側）のスクロール効果より
  必ず先に走る。挙動を変えずに移せる
- `test/architecture.test.ts`「components/ui/ の部品の className」は `UI_COMPONENT_FILE_PATTERN`
  （`browser/components/ui/<dir>/<dir>.tsx`）と `readonly className: string` の字面を機械的に検出する
  ため、`dialog.tsx` を作るだけで自動的に対象に入る（T-607 のときと同じで、このテストファイル自体は
  編集不要）

作業の順番:

1. `src/browser/components/ui/dialog/dialog.tsx`・`dialog.module.css`・
   `test/browser/components/ui/dialog/dialog.test.tsx` を先に作る（TDD）
2. `useModalDialog(true)` 系の6箇所を `Dialog` に置き換える（`onDialogClick` の組み立てと
   `useModalDialog` の呼び出しを消す）
3. `useModalDialog(open)` 系の4箇所（と背後のフック）を、`open` をモデルへ足す形に直して
   `Dialog` に置き換える
4. 各 `*.module.css` から `::backdrop` の規則を消し、箱の CSS だけ残す
5. `docs/design.md` 6.1「部品の置き場」の一覧に `<Dialog>` を足す
6. `bun run format` → `bun run check` → `bun run build`、目視確認

## 注意

- `Dialog` の読み手には機能の外（`ImageZoom`）も数えられる。「browser/ の機能をまたぐ箱」の検査はそのまま通る

## 結果

- `components/ui/dialog/` に `Dialog`（`open`・`name`（label/labelledby の合併型）・`backdrop`（dim/deep/clear）・`placement`（auto/at）・`onClose`・`className`）を作り、10か所の `<dialog>` をすべて置き換えた。常に描く4つのフックから `ref`・`onDialogClick`・`useModalDialog` が消え、各 CSS から `::backdrop` の規則9つを消した。`test/architecture.test.ts` の className 検査が部品側の `::backdrop` を数えていた漏れを直した。見た目・振る舞いの変化は無い
- 検証コマンド: 2792 pass / 0 fail（+12）、`bun run build` 成功。ポート 39420/39421 で9つを実際に開き、背景の暗さ・Esc・外側クリック・`at` の座標を確かめ、タスク一覧・新しく作る・セリフのログは変更前と並べて見分けが付かなかった。覚えたことを消す確認だけは開かず、同じ形の2つと差分で代えた
- 振り返り: 兆候なし
