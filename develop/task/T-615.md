---
id: T-615
summary: ダイアログの部品を `components/ui/` に作り、12箇所の `<dialog>` を置き換える
status: todo
difficulty: sonnet
loopable: Y
dependencies: [T-606]
---

## 目的

`<dialog>` を `showModal()` で開く箇所が12ファイルあり、`useModalDialog` の呼び出し・`::backdrop` の規則・閉じる口の置き方を各自が書いている。ダイアログの部品を作って背景の暗さ・置き方を variant で表し、置き換える。

## 完了条件

- T-606 が `docs/design.md` 2章に書いた作法どおりのダイアログの部品が `src/browser/components/ui/` にあり、`test/browser/components/ui/` に開閉（`open` の追随・Esc で閉じたときの呼び先）と variant → class のテストがある
- 下の「背景」の `<dialog>` のうち T-606 が対象にしたものが部品に置き換わり、各機能の `::backdrop` の規則が消えている。残したものは理由を結果に1行ずつ書く
- 置き換えたダイアログをすべて開いて目視し、背景・置き方・閉じ方（閉じる口・Esc・外側クリック）が置き換え前と同じであることを確かめている（何を見たかを結果に書く）
- `bun run check` が通る

## 背景

- `<dialog>` を持つファイル: `features/task-board/presentational-task-board.tsx`・`components/task-table.tsx`・`components/task-run-button.tsx`・`components/task-run-confirm.tsx`、`components/domain/sidebar/persona-memory-section.tsx`、`page/achievement/diary-book.tsx`、`page/character/components/character-delete-confirm.tsx`・`character-profile-edit-dialog.tsx`・`portrait-clear-confirm.tsx`、`page/character/presentational-character-create.tsx`、`page/conversation/character-view/presentational-speech-log.tsx`、`components/ui/image-zoom.tsx`
- 開閉は `src/browser/hooks/use-modal-dialog.ts` の `useModalDialog(open)`。Esc の `close` は呼び出し側が `<dialog onClose>` で拾う作り
- `::backdrop` の規則は6ファイル9箇所。`.character-clear-confirm::backdrop` は暗く覆わず外側クリックの読み替えだけ使う。`portrait-clear-confirm.tsx` は位置を inline style で指定する
- 重ねて開く（表のモーダルの上に確認を開く）箇所がある（`task-run-confirm.tsx`・`persona-memory-section.tsx`）

## 注意

- T-606 で採らないと決まったら `dropped` で閉じてよい
