---
id: T-699
summary: コメントの強調 `**` を一括で消し、`test/architecture.test.ts` で検査する
status: done
difficulty: sonnet
loopable: Y
dependencies: []
---

## 目的

`docs/coding-standards.md`「コメント」の「Markdown の記法を使わない」に、既存のコードを合わせる。
強調の `**…**` は機械的に消せるので、一括で消したうえで、再び入り込まないよう検査を足す。

## 完了条件

- `src` / `test` / `scripts` の `*.ts` / `*.tsx` のコメント行から、強調の `**` がなくなっている
  （着手時の件数は `grep -rcE '^\s*(//|\*).*\*\*[^*]+\*\*' src test scripts` で数え直す。
  2026-09-26 の時点では 446 ファイル・2654 行）
- `test/architecture.test.ts` に、コメント行が強調の `**` を含まないことを確かめる検査がある。
  既にある「コメント中の日付」（`COMMENT_LINE_START`）と同じく、行頭がコメントの行だけを対象にする
- 変わっているのはコメントだけ（コードの差分が無い）。`git diff` の `+`/`-` 行が、どれもコメント行であることを確かめてある
- `bun run check` が通る

## 背景

- 規約: `docs/coding-standards.md`「コメント」の「書き方は2つ守る」（2026-09-26 に足した）
- 検査の手本: `test/architecture.test.ts` の `describe("コメント中の日付")`（`COMMENT_LINE_START = /^\s*(\/\/|\*|\/\*)/`）

## 決まっていること（蒸し返さない）

- 消すのは強調の `**` だけ。囲まれていた文字は残す（2026-09-26 ユーザーの決定）
- 意味の切れ目での改行は機械で検査できないので、このタスクでは直さない。そのファイルを触るときに直す
  （同日ユーザーの決定。規約に書いてある）

## やること

- 着手時に前提を数え直した。`grep -rcE '^\s*(//|\*).*\*\*[^*]+\*\*' src test scripts` は
  446ファイル・2643行（拡張子を絞らないため `src/browser/styles/theme.css` の1件を含む。
  完了条件の対象は `*.ts` / `*.tsx` のコメント行だけなので、CSS のその1件は対象外）
- 文字列リテラル・テンプレートリテラル・正規表現リテラルの中身を強調の削除対象から正しく除く
  ために、行頭の文字だけで「コメント行」を決める素朴な変換では誤爆する
  （`CHAT_MANNER_PROMPT` / `REPORT_NOTATION` のような、システムプロンプト用テンプレート
  リテラルの本文行が `*` で始まることがあり、`COMMENT_LINE_START` に当たってしまうため）。
  実際のコメント範囲（`//` の行コメント・`/* … */` のブロックコメント）だけを文字単位で
  判定する簡易トークナイザを `/tmp` に書き、その範囲内かつ行頭が `COMMENT_LINE_START` に
  当たる行だけを対象に強調 `**` を除去する
- 除去後、`src` / `test` / `scripts` の `*.ts` / `*.tsx` のコメント行に強調の `**` が
  残っていないことを確認する。テンプレートリテラル側に残る2箇所
  （`chat-manner.ts` / `report-notation.ts` のプロンプト文面）はコメントではないので対象外
- `test/architecture.test.ts` に、既存の「コメント中の日付」に倣って強調の `**` を検査する
  `describe` を足す
- `git diff` の変わった行について、変更前の行がすべて `COMMENT_LINE_START` に当たること、
  変更後の行が「変更前から `**` を除いたもの」と一致することを、`git show HEAD:<file>` との
  突き合わせで確かめる

## 注意

- **JSDoc の開き `/**` を消さない。** 行頭が `/**` の行や、`/** … */` の1行コメントの開きは強調ではない
- 強調は2行にまたがることがある（行末で開き、次の行で閉じる）。行ごとに `**` の対を探すのではなく、
  コメント行の中の強調の `**` をすべて消す形にする。消したあとに `**` が1つだけ残る行が無いかを見る
- コメントでない行（文字列リテラル・コード）の `**`（べき乗など）には触らない
- 400ファイルを超える差分になり、並行する作業ツリーと衝突しやすい。着手したら寄り道せずに1コミットにまとめて送る
- 人に委ねる判断は無いので `Y`

## 結果

- 検証コマンド: 2687 pass / 0 fail、E2E 24 pass / 0 fail
- コメント行の強調 `**` を 473 ファイル・4099 行から除去。残る2行は `chat-manner.ts` / `report-notation.ts` のプロンプト文面（テンプレートリテラル）で対象外
- 受け入れで、検査の字句解析器（約150行）を「`//` の行と `/*`〜`*/` の行だけを見る」行単位の判定に置き換えた。違反を仕込んで拾うことを確認
- 振り返り: 受け入れのときにメインが直したもの（ドラフト 0件。「読む人が把握しやすい案を選ぶ」は CLAUDE.md に既にある）
