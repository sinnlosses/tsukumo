---
id: T-566
summary: 成果の画面を日記の吹き出し・しおり・小さな驚き・灯りの暦の形に作り直す
status: todo
difficulty: sonnet
loopable: Y
dependencies: [T-563, T-565]
---

## 目的

見本 SEIKA-4 の (a) ふだん と (b) 書いている の場面を、成果の画面に作る。

## 完了条件

- (a): `docs/screen-design.md` 13.10「並べるもの」「灯りの暦」のとおり。**終えたタスクの一覧はこの画面から外す**（見開きの左ページへ移る。T-567）
- (b): 同じ節「ボタンを押せないとき・押したあと」のとおり。ボタンは `reflect-achievement` を送り（T-565）、画面は移らない。書き上がったら本文を書き上げの演出で頭から見せる（途中の本文は流さない）
- まだ振り返っていない日・成果が0の日・書けなかったとき・取れなかったときの見え方が 13.10「空の日・数えられないとき」の表のとおりにある
- 灯りの暦は `GET /achievement-calendar`（T-563）から描き、日記が書き上がったら1日ぶんと暦を取り直す（`docs/design.md`「成果の集め方と配り方」の「取り直す契機」）
- しおり・小さな驚き・鈴の金色は `--state-warn` を流用せず、この画面の中でだけ使う固定の色として `theme.css` に足し、値を 13.10「色」に書いてある
- 明朝体は端末の明朝体に寄せる（外から書体を取らない）
- 部品の振る舞いのテストがあり、`bun run check` が通る
- 目視確認（疑似セッションか本物で、(a)(b) の場面を見て、何が見えたか）を結果に書く

## 背景

- いまの画面は `src/browser/features/achievement/`（container `achievement-screen.tsx`、器 `presentational-achievement-screen.tsx`、`hooks/use-achievement.ts`）
- 見本は `docs/history/mockup/achievement-diary-2026-09-25/SEIKA-4a-Base.dc.html` と `SEIKA-4b-Writing.dc.html`（`python3 -m http.server` で開く。色・寸法の値はインラインの style にある。値は既存のトークンに寄せ、新しい色を足すなら `docs/screen-design.md` に書く）
- 書き上げの演出は `src/browser/features/main-view/reveal/`。成果の画面が2つ目の読み手になるので、置き場所を `docs/design.md` 6章の「2つ目の読み手が出たら」の規則で決め直す
- 目視の手順は `docs/architecture.md`「手で確かめること」

## 注意

- キャラクターの名前・セリフをコードに書かない（`CLAUDE.md` 原則4）。見本のサンプル文はそのまま使わない
