---
id: T-558
summary: タスクを1件終えるたびにその1件を振り返り、兆候のときだけドラフトに積む
status: done
difficulty: opus
loopable: Y
dependencies: [T-556, T-557]
---

## 目的

タスクを1件やり終えるたびに、その1件を振り返る。まとめて振り返ると間が空いて薄くなり、別の作業ツリーで動いたタスクのトランスクリプトも拾えない。同じ作業ツリー・同じセッションのうちに振り返れば、サブエージェントのトランスクリプトから手数まで取れる。

## 完了条件

- `/next-task` の最後（`task ship` のあと）に、その1件だけを対象にした振り返りの手順がある。`/loop` から回っているときも走る
- 振り返りは**兆候に当たったときだけ** `develop/direction.md` の `## エージェントのドラフト` に積み、当たらなければ `develop/retrospective.md` の記録だけ進める。兆候の一覧（下の「決まっていること」）が `retrospect` の SKILL.md にある
- `retrospect` の SKILL.md と `WORKFLOW.md` から「`/loop` に載せない」の決まりが、今回の決定に合わせて書き換わっている（手で呼ぶまとめての振り返りも残すかは論点で決める）
- 下の「解くべき論点」の1つ1つに結論があり、完了時の結果に論点 → 結論を1行ずつ書く
- claude-skills の `./check.sh` が通り、変更が claude-skills の `main` に入っている
- tsukumo の `CLAUDE.md`「導入済みスキル」の `retrospect` の説明（「手で呼ぶ。`/loop` には載せない」）が新しい形に直っていて、`bun run check` が通る

## 背景

2026-09-24 に利用者が決めた（`docs/history/direction.md` の 2026-09-24 の項）。同日の振り返り（`f1adaf96`..`7ef5a7ba`、175コミット・71タスク）では、範囲内のタスクに割り付くトランスクリプトが1件も無く、手数を見られなかった。原因は `skills/retrospect/scripts/transcript.py` の `project_dir` が、起こした作業ツリーの slug（`~/.claude/projects/-Users-...-tsukumo-4/`）の下しか探さないこと。同じ作業ツリーで動いた T-533 は `material.py` で手数（ツール呼び出し60件・`character-edit.ts` を8回直した）まで取れた。

いまの `retrospect` は `scan.py` が `develop/retrospective.md` の「最後に振り返ったコミット」から `HEAD` までを範囲にし、コミット件名のタスクIDでタスクに割り付ける。

## 決まっていること（蒸し返さない）

- `/loop` から回っているときも走らせる（「無人だと根拠の薄い気づきが溜まる」への答えは、兆候の縛りで積む量を絞ること）
- 兆候の例: 検証コマンドの打ち直し・`task ship` の失敗（`VERIFY_FAILED` など）・同じファイルを3回以上直した・ツールのエラー・完了条件に無い差分・受け入れのときにメインが直したもの
- 当たらなければドラフトには何も積まない（「うまくいった」は書かない）

## 解くべき論点

1. **基準点の共有**: `develop/retrospective.md` の「最後に振り返ったコミット」は1行で、複数の作業ツリーが並行してタスクごとに振り返ると、互いのタスクを飛ばす・同じ行で衝突する。範囲を「そのタスクのコミット」に絞る形（例: `scan.py` / `material.py` にタスクIDを渡す）にするか、基準点の持ち方を変えるか
2. **記録の置き場**: タスクごとに振り返った事実を `develop/retrospective.md` に積むのか、タスクファイルの結果の節に1行で書くのか（コミットを増やさないなら後者。`task ship` のあとに振り返るとタスクファイルはもう送り済み）
3. **手順の位置**: `task ship` のあとに振り返ると、ドラフトを積んだ `direction.md` を別のコミットで送ることになる。`task done` の前（受け入れの直後）に振り返って同じコミットに入れるほうがよいか
4. **まとめての振り返り**: 手で呼ぶ `/retrospect`（範囲まとめて）を残すか。残すなら、タスクごとの振り返りと範囲が重ならない形にする
5. **費用**: 1件ごとに `material.py` を走らせるトークンの量。兆候の判定を `material.py` の出力（数だけ）で済ませ、兆候に当たったときだけ diff を読む形にできるか

## やること

調べ直した前提（claude-skills `6c34930`）: `material.py <T-xxx>` はコミットを件名のタスクIDで拾い、手数はトランスクリプトの先頭にIDがあるものから数える（範囲の基準点を使わない）。`scan.py` だけが `develop/retrospective.md` の1行を読む。`next-task` の手順は 6 受け入れ → 7 `task done` → 整形 → 1コミット → 8 `task ship`。`task ship` が止まったら `/loop` も止まり人に預かる。`WORKFLOW.md` に「`/loop` に載せない」の字面は無く、それに当たるのは「指示メモ」の表の `## エージェントのドラフト` の行（「`/loop` からは触らない」）。

論点の結論:

1. **基準点**: 1件ごとの振り返りは基準点を使わず、`material.py` にタスクIDを渡して範囲をそのタスクに絞る（いまもそう動く）。`develop/retrospective.md` の1行は手で呼ぶまとめての振り返りだけが進める。並行する作業ツリーが同じ行を書くことはなくなる
2. **記録の置き場**: タスクファイルの `## 結果` に `- 振り返り: 兆候なし` / `- 振り返り: <兆候>（ドラフト N件）` の1行。行頭 `- 振り返り:` がそのまま「1件ごとの振り返りが済んだ」の機械の判定になる（T-560 は、この行がある、または `done`/`dropped` にしたコミットが `develop/retrospective.md` の基準点の祖先にある、で判定できる）。完了条件の「当たらなければ `develop/retrospective.md` の記録だけ進める」は、この1行に置き換える
3. **手順の位置**: 受け入れ（手順6）の直後・`task done` の前に置く。ドラフトと記録が作業と同じ1コミットに入り、送りは1回。`task ship` の失敗はこの時点では見えないが、止まった時点で `/loop` も止まり人に預かるので、1件ごとの振り返りでは扱わない（完了条件の「`task ship` のあと」からずらす）
4. **まとめての振り返り**: 手で呼ぶ `/retrospect` は残す。`scan.py` が範囲内のタスクのうち `- 振り返り:` の行を持つものを `reviewed` として `tasks` から外すので、範囲は重ならない。見るのは記録の無いタスク（この変更の前のもの・`/next-task` を通らずに終えたもの）と、タスクをまたぐ形
5. **費用**: `material.py --signals` を足し、手数（数だけの数行）だけを出す。兆候の判定はその数と、メインが受け入れで見た diff・直したもので済ませ、当たったときだけ diff を読み直す

作業（claude-skills の `feature/tsukumo-T-558` の作業ツリーで）:

- `retrospect/scripts/material.py`: `--signals` で手数の節だけを出す
- `retrospect/scripts/scan.py`: 範囲内のタスクのうち、そのコミットの版のタスクファイルの `## 結果` に `- 振り返り:` があるものを `reviewed` 行に出し、`tasks` から外す
- `retrospect/SKILL.md`: 節「1件だけ振り返る（`/next-task` から）」を足す（兆候の一覧・閾値・記録の1行）。まとめての手順は `reviewed` を飛ばす。「`/loop` に載せない」を「まとめての振り返りは手で呼ぶ。1件ごとは `/loop` からも走る」に書き換え、description も合わせる
- `next-task/SKILL.md`: 手順6と7のあいだに振り返りを足し、手順7の結果の例と `git add`、完了報告に振り返りの1行を足す
- `task-workflow/WORKFLOW.md`: 「ファイル配置」の `develop/retrospective.md` の行、「1サイクル」、「結果の書き方」に `- 振り返り:` の行、「指示メモ」の表の書き手と「`/loop` からは触らない」の意味（タスク化のこと）を直す
- `README.md` の `retrospect` の説明を合わせる
- tsukumo の `CLAUDE.md`「導入済みスキル」の `retrospect` の説明を直す（コミットしない）

## 注意

- 作業するのは claude-skills のリポジトリ（`~/ghq/github.com/sinnlosses/claude-skills`。`~/.claude/skills/` の各スキルはそこへの symlink で、書き換えは全プロジェクトへ即座に効く）。**着手時に `git -C ~/ghq/github.com/sinnlosses/claude-skills status --short` が空でなければ止めて預ける**
- claude-skills の枝は本体の作業ツリーで checkout せず、`git worktree add` で別の作業ツリーを切ってそこで作業する（本体で切り替えると全プロジェクトのスキルが変わる）。枝の名前は `feature/tsukumo-<このタスクのID>`。検証コマンドは claude-skills の `CLAUDE.md` に従う（`./check.sh`）
- コミットは claude-skills の側に入る。tsukumo のタスクの結果には claude-skills のコミットハッシュを書く
- `next-task` の SKILL.md は T-556・T-557 も書き換えるので、それらのあとに着手する

## 結果

- claude-skills `411742a`: `/next-task` に手順6a（受け入れの後・`task done` の前に、その1件だけ振り返る）を足した。記録は `## 結果` の `- 振り返り:` の1行で、`scan.py` はこれを `reviewed` として扱い、まとめての振り返りから外す。`material.py --signals` を足し、`transcript.py` は先頭で最初に出るタスクIDだけで当てる形にした。完了条件の「ship の後」「retrospective.md を進める」は、論点3・2の結論に合わせて変えた
- 検証コマンド: 2285 pass / 0 fail（+1）。受け入れの1回目だけ `bun run check` が exit 1 になり、続けた2回はどちらも通った（落ちたテストは特定できていない）。claude-skills の `./check.sh` も通過
- 振り返り: 完了条件に無い差分（`transcript.py`）・受け入れでの検証の打ち直し（ドラフト 0件）
