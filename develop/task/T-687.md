---
id: T-687
summary: Storybook を入れ、`capture-catalog.ts` との役割分担を決める
status: todo
difficulty: opus
loopable: Y
dependencies: [T-684]
---

## 目的

部品を場面ごとに並べて見られるカタログを持つ。2026-09-21 に見送った Storybook を、組み立てが
Vite に揃ったことで採り直す。

## 完了条件

- `storybook` と `@storybook/react-vite` が devDependencies に入り、`package.json` に起動の script がある
- Storybook の組み立てが本体と同じ Vite の設定を使い、CSS Modules の class 名が本体の成果物と同じ綴りになる
- レポートの記法の部品（note・badge・表・mermaid・Chart.js を含むレポート）と、立ち絵・吹き出しの
  部品に story がある
- `scripts/capture-catalog.ts`（目視の手順）との分担の結論が `docs/architecture.md`「手で確かめること」に
  書かれ、`docs/research/ui-catalog.md` の見送りに「2026-09-26 に採り直した」旨の追記がある
- 検証コマンド（移した後の `check`）が通り、Storybook を開いて上の story が描けることを目で見て確かめている

## 背景

- `docs/research/ui-catalog.md`（2026-09-20）は Storybook を見送った。理由は2つで、Vite が焼く class 名が
  `bun build` と別の綴りになり「本物を見る道」でなくなること（`T-684` で解消）と、推移で新規 126
  パッケージが増えること（こちらは残る）
- 代わりに採った案2 で、`scripts/capture-catalog.ts` が疑似セッションの場面を Playwright で撮っている

## 決まっていること（蒸し返さない）

- Storybook を入れる。依存が増えることは受け入れる（2026-09-26 ユーザーの判断）

## 解くべき論点

- `capture-catalog.ts` を残すか、Storybook の story を撮る形に寄せるか
- story の置き場所（`docs/design.md` 2章の `src/browser/` の置き場所の基準との整合）と、
  `test/architecture.test.ts` の辺の扱い
