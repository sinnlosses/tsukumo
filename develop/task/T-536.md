---
id: T-536
summary: 字句の同じ重複4組を1つずつに減らす
status: todo
difficulty: haiku
loopable: Y
dependencies: [T-535]
---

## 背景

2026-09-24 に `/simplify` の4観点（再利用・単純化・効率・根本対処）でリポジトリ全体を洗った結果の1件（`docs/history/direction.md` の 2026-09-24「改修コストを下げる直し9件」）。

中身が字句まで同じ関数・処理が4組ある。どれも片方だけ直すと挙動が食い違う。

- `clamp`: `src/browser/features/main-view/reveal/paint.ts` と `band.ts`
- `fetchTokenUsageSummary`: `src/browser/features/token-usage/hooks/use-token-usage.ts` と `use-usage-review.ts`（後者のコメントも「同じ経路」と言っている）
- `run()`（`execFileSync` の失敗を空文字にする）: `scripts/lib/port-listener.ts` と `scripts/open-room-grid.ts`（後者はすでに前者から `findListener` などを import している）
- `src/server/adapter/claude-account.ts` が `JSON.parse(readFileSync(...))` を try/catch で手書きしている。同じことをする `src/server/adapter/lib/json-file.ts` の `readJsonFile` がある

T-535 が `claude-account.ts` の同じあたりを触るので、その後に置く。

## やること

1. `clamp` は `reveal/` の中の1つのファイルに置き、両方から import する（置き場所の名前のファイルは作らない。`CLAUDE.md` 原則5。既存のファイルに同居させてもよい）
2. `fetchTokenUsageSummary` は片方から export してもう片方が import する
3. `run()` は `scripts/lib/port-listener.ts` から export して `open-room-grid.ts` が import する
4. `claude-account.ts` は `readJsonFile` を呼び、戻り値の検証はいまの形のまま続ける

## 完了条件

- `grep -rn "function clamp\|function fetchTokenUsageSummary" src` がそれぞれ1件、`grep -n "function run(" scripts/open-room-grid.ts` が0件
- `grep -n "JSON.parse" src/server/adapter/claude-account.ts` が0件
- `bun run check` が通る
