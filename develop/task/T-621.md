---
id: T-621
summary: `test-audit` のキャンペーンで server の小さな機能と共有の箱 のテストを刈り込む
status: done
difficulty: sonnet
loopable: Y
dependencies: []
---

## 目的

`test-audit` スキルのキャンペーンモード（同スキルの `CAMPAIGN.md` の8段）で、`test/server/` の report・token-usage・context-usage・usage-review・system-prompt・repository と、共有の `adapter/` `core/` `host/`（22ファイル・約3,100行） のテストを刈り込む。価値の低いテスト・実装に結合したテスト・より強い証明と重なるテストと、テストしか呼ばない本番コードを消し、残す契約はより強い境界のテストに寄せる。

## 完了条件

- 対象範囲のテストの宣言ごとに `R` / `F` / `C` / `D` の印と1行の証拠を付けた台帳を作り、印ごとの件数と、`C` / `D` の宣言ごとの残す側（keeper）を タスクの結果 に書く（台帳の全文はリポジトリに置かない）
- `D` / `C` の編集と、それで消せるようになったテスト専用の本番コードの継ぎ目の削除を済ませる
- 移した（`C` / `F`）契約ごとに、本番側の持ち主にわざと1つ変異を入れて keeper が落ちることを確かめ、元に戻す
- 基準線（着手時）と完了時の、対象範囲のテストの `it` の数と行数、本番コードの増減を タスクの結果 に書く
- `bun run check` が通る
- 確信の持てる候補が無ければ、台帳の件数だけ書いて編集なしで `done` にしてよい

## 背景

- 2026-09-25 に `test-audit` スキル（openclaw/openclaw の同名スキルを日本語化して claude-skills に入れたもの）を取り込み、監査モードの試運転で、テストだけが呼んでいた `parseAnswer`（`src/shared/pending-ask.ts`）と `newFormatTaskSummaries`（`src/shared/task-summary.ts`）を消した。固有の検査は `test/shared/command.test.ts` と `test/server/repository/adapter/task-summary.test.ts` に移した（`0b3ef70a`）
- テスト全体は 205 ファイル・約4.7万行で、機能ごとに1タスクに分けた。このタスクはserver の小さな機能と共有の箱の範囲

## 決まっていること（蒸し返さない）

- 消すかどうかは `docs/coding-standards.md`「テスト」節の「消すかどうか」「埋めないと決めた穴」と突き合わせる。スキルの残す基準とリポジトリの規約が食い違ったら、リポジトリの規約を優先し、食い違いを タスクの結果 に書く
- 残す基準は、スキルの `SKILL.md`「残す基準」のとおり（とくに、出力の書式を固定するテストと過去のバグの退行テストは残す）
- `CAMPAIGN.md` の段7（プロダクトの欠陥）で見つけた欠陥は、このタスクでは直さず、`develop/direction.md` の 「エージェントのドラフト」節 に積む。段8（`main` の merge）はタスクが短いので要らない
- 1タスク＝1コミット。対象範囲の外のテストは、範囲内の契約を吸収する keeper になるときだけ触る

## やること

前提を確認した（2026-09-25 着手）:

- 対象を数え直すと `test/server/report/`（4ファイル）・`test/server/token-usage/`（2ファイル）・
  `test/server/context-usage/`（1ファイル）・`test/server/usage-review/`（3ファイル）・
  `test/server/system-prompt/`（2ファイル）・`test/server/repository/`（2ファイル）・共有の
  `test/server/adapter/`（5ファイル）・`test/server/core/config.test.ts`・
  `test/server/host/core/tracked-file.test.ts` の計 **21ファイル・3,128行**（本文の見立ては
  22ファイルだが、`0b3ef70a`（このキャンペーンの試運転）で `test/shared/task-summary.test.ts` を
  `test/server/repository/adapter/task-summary.test.ts` へ移す前提の変化を含めても21ファイルにしか
  ならず、内訳は崩れていない）。基準線は `bun test --isolate` でこの21ファイルだけを指定して
  208 pass / 0 fail（`it`/`test` 宣言も208件）
- `test-audit` スキルと `CAMPAIGN.md` の8段のうち、段3〜6（レーンごとの読み取り専用の台帳→層の
  計画→切り替え→保全のレビュー）を、本文の「注意」の指示どおり自分でレーンを順に読んで行う
  （別エージェントを立てない）。段7（プロダクトの欠陥）は直さず `develop/direction.md` へ、
  段8は本文の指示どおり省く
- レーンは本番側の持ち主の境界で切る: (1) `report/core`（4ファイル、記法・差し戻し・関所・
  規約違反の純関数とプロンプトのバイト列）(2) `token-usage/core`＋`token-usage/adapter`
  （純関数の集計とターン内訳、実ファイルシステム境界）(3) `context-usage/adapter`
  （実ファイルシステム境界。書き込みのみで読み口を持たない）(4) `usage-review/core`＋
  `usage-review/adapter`（ツールの受け口の純関数と、見送り一覧・前回結果の実ファイルシステム
  境界）(5) `system-prompt/core`（プロンプトの組み立てとバイト列）(6) `repository/adapter`
  （`git` を実際に起こす境界。`task-summary.ts` は本物の worktree を使う監視の境界）
  (7) 共有の `adapter/`（`bundled-path` / `lib/json-file` / `lib/jsonl` / `local-time` /
  `tsukumo-home`）と `core/config.ts`・`host/core/tracked-file.ts`
- 全21ファイルを本番側の持ち主と突き合わせて読んだ結果、report・system-prompt・repository・
  bundled-path・local-time・tsukumo-home・core/config・host/tracked-file の各レーンは、
  プロンプトのバイト列や実際の `git`／実ファイルシステムを当てた強い境界のテストが並び、
  重複は見当たらなかった。見つけた確信の持てる候補は、**共有ライブラリ
  （`src/server/adapter/lib/json-file.ts` / `lib/jsonl.ts`）へ丸ごと委譲しているだけの
  アダプタが、委譲先の口（読めない/壊れている・書けない・ディレクトリが無い）をもう一度
  個別に検査している**ケースのみ（8件）:
  - `token-usage-log.test.ts` の「書けないときも例外を投げない」1件、
    `context-usage-log.test.ts` の同名1件は、どちらも `appendJsonLine` へ素通しするだけ
    （自前の分岐が無い）で、`lib/jsonl.test.ts` の「書けなくても例外を投げない
    （常駐プロセスを落とさない）」と同じ失敗経路（`mkdirSync` が塞がれる）を再演している
  - `previous-usage-review.test.ts` の「ディレクトリが無ければ作って書く」
    「書き込み先がディレクトリで塞がっていても例外を投げない」と、
    `usage-proposal-dismissal.test.ts` の同名2件は、どちらも `writeJsonFile` への素通しで、
    `lib/json-file.test.ts` の同名テストと同じ経路を再演している
  - `previous-usage-review.test.ts` の「JSON が壊れているときは none」と
    `usage-proposal-dismissal.test.ts` の「JSON が壊れているときは空」は、
    `readJsonFile` が「ファイルが無い」ときも「壊れている」ときも同じ `undefined` を返す
    （`lib/json-file.test.ts` がどちらも個別に検査済み）ため、呼び出し側のこの2ファイルでは
    同じファイル内の「ファイルが無いときは none/空」と同じ分岐を二度検査しているだけ
    （版違い・形違いのテストは、共有ライブラリが知らないこのファイル固有のスキーマなので残す）
  - `usage-review/core/usage-review-tool.test.ts` は見送り一覧の実ファイル配線だけを見る
    設計で、規約違反（`blank-headline` / `too-many-proposals` など）はこのレーンに無く
    `test/server/session-driver/adapter/sdk-tool.test.ts`（範囲外、MCP 登録込みのより強い境界）
    が持つ。範囲内に重複が無いのでこのレーンは触らない
- 作業として、上の8件を消す（残す側は `lib/json-file.test.ts` / `lib/jsonl.test.ts` の既存テスト、
  および同じファイル内の「ファイルが無いとき」のテスト）。本番コードの継ぎ目は無く
  （どのアダプタも委譲だけで自前の try/catch を持たない）、本番コードの変更は無し

## 注意

- 変異を入れて確かめるときは、退避したコピーから戻す（`git checkout --` で戻すと、同じファイルへの先行の編集ごと消える）
- `CAMPAIGN.md` の段3・段6は、別のエージェントに読ませることを求めている。サブエージェントの中で別のエージェントを立てられないなら、レーンごとに順に自分で読み、段6の保全のレビューは受け入れのときにメインが行う
- ブラウザに出る絵は自動テストで守らない方針（`docs/architecture.md`）なので、描画のテストを消すときも、足すときも、その方針の範囲で判断する

## 結果

- 検証コマンド: 2762 pass / 0 fail（対象21ファイル 208→200件。C 8件はどれも `lib/json-file` `lib/jsonl` への委譲を再検査していたもので、keeper はその lib のテスト。本番コードは無変更）
- 振り返り: 同じファイルを3回以上直した（変異とその戻し・複数件の削除によるもの。ドラフト 0件）
