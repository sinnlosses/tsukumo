---
id: T-540
summary: イベントを足すタスクには、それを起こす経路を列挙させる
status: done
difficulty: sonnet
loopable: Y
dependencies: []
---

## 背景

2026-09-24 の振り返り（T-448, T-475, 修正コミット 7b06eec3・1cdf60fd）から、利用者の承認を得て登録した。

T-448 は続きのターン（ターンの外の `init`）を「背景のタスクが終わった知らせ」の1経路で、委譲1回につき1回起きると見て、吹き出しを空にする仕様を書いた（`docs/screen-design.md` 13.9）。一方で T-157 以来、`src/server/core/speech-cadence.ts` は委譲中に `SendMessage` の合図を送らせていて、実際には委譲1回で3回（合図2通と完了の返事）起きた。その結果、中間レポートが消えては出直し、吹き出しが「（まだ発話がありません）」に戻った。

## やること

1. `docs/workflow.md`「タスクを書くとき・受け入れるとき」に、「SDK のイベントを足す・扱いを変えるタスクは、そのイベントを起こす経路を本文に列挙し、1回のやり取りで何回起きうるかを書く」という項を1つ足す。上の事故を根拠として短く添える

前提の調べ直し（着手前）:

- `docs/workflow.md`「タスクを書くとき・受け入れるとき」節（12行目〜）はまだあり、同種の項（経路の列挙・回数を書け、というもの）はまだ無い（`grep -rn "SDKのイベントを足す\|イベントを起こす経路" docs/workflow.md` はヒット無し）。作業は要る
- `src/server/core/speech-cadence.ts` は今も委譲中に `SendMessage({ to: "main" })` の合図を送らせている（67〜72行目）。合図は「ひと区切りついたとき」「長いコマンドを流す前」に飛び、委譲1回で複数回起きうる契約のまま。背景の話は今も正しい
- `docs/screen-design.md` 13.9「画面のナビゲーション」の「背景のタスク」節（1591〜1596行目）は、2026-09-24 付けで「吹き出しは空にせず、前のセリフと表情を残したまま続きのセリフを積む」に直っており、直す前の「（まだ発話がありません）」に戻る不具合の経緯もそこに残っている。T-540 の根拠として今も参照できる
- `develop/task/T-540.md` の「注意」にある「T-528 が `docs/workflow.md` を tsukumo 固有の事情だけに縮める予定」は、T-528 が既に完了済み（`docs/history/decision.md`「workflow.md「`doing` をコミットする」「アーカイブも `main` へ単独で送る」」節）で前提が古い。ただし縮めたあとの現在の `docs/workflow.md` も既にタスク運用の tsukumo 固有の事情だけを持つ形になっており、「タスクを書くとき・受け入れるとき」節は残す側に入っている。この項をそこへ足す作業に支障は無い

## 完了条件

- `docs/workflow.md` の該当の節に項が1つ増えている
- `docs/workflow.md` の節の数が編集の前後で変わっていない（`CLAUDE.md`「ドキュメントを編集するときの罠」）
- `bun run check` が通る

## 注意

- T-528 が `docs/workflow.md` を tsukumo 固有の事情だけに縮める予定。この項は tsukumo 固有の事情なので、残す側に入る書き方にする

## 結果

- 検証コマンド: 2367 pass / 0 fail
- `docs/workflow.md`「タスクを書くとき・受け入れるとき」に、イベントの経路と回数を列挙させる項を1つ足した（節の数は 6 のまま）
- 振り返り: 兆候なし
