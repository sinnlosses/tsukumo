---
id: T-649
summary: `test-audit` の CAMPAIGN.md と `/next-task` に、委譲先の縛り2つを書く
status: done
difficulty: sonnet
loopable: Y
dependencies: []
---

## 目的

`test-audit` のキャンペーンを委譲したときに起きた2つの失敗を、スキルの本文で防ぐ。① 消したテストの跡に「どこへ移したか」のコメントを残した ② サブエージェントが fork を立てて読ませ、fork が「読むだけ」を破って作業ツリーを書き換えた。

## 完了条件

- claude-skills の `skills/test-audit/CAMPAIGN.md` の切り替えの段（段5。C/D を編集する段）に、「消したテストの跡に keeper の在りかを説明するコメントを残さない。keeper は報告とタスクの結果の節に書く」の1行がある
- 同じファイルの段3・段6と、`skills/next-task/SKILL.md` の手順5（委譲）の指示に、「サブエージェントの中では別のエージェント（fork を含む）を立てず、レーンを順に自分で読む」が**条件なしで**書かれている
- tsukumo の `develop/task/` に残っているキャンペーンのタスク（T-625〜T-627 のうち `todo` のもの）の `## 注意` にある「別のエージェントを立てられない**なら**自分で読む」を、条件なしの書き方に直す。**他のセッションが着手中（`task status` で `CLAIMED`）のタスクは触らず、結果の節にその旨を書く**
- claude-skills の `./check.sh` が通り、claude-skills の `main` に入っている。tsukumo 側は `bun run check` が通る

## 背景

- 出典: T-620 と T-624 の振り返り（`develop/direction.md` のエージェントのドラフト、2026-09-25 にユーザー承認）
- T-624: 本文に「別のエージェントを立てられない**なら**自分で読む」と条件付きで書いてあり、サブエージェントが fork を3つ立てた。3つとも「読むだけ」を破って作業ツリーを書き換え、1つは `git checkout --` で先の編集を消した。3つとも同じ誤った「重複」の判定を返した
- T-620: `session-socket.test.ts` の2件を消した跡に keeper の在りかを説明する6行のコメントが残り、受け入れでメインが外した
- `CAMPAIGN.md` の段3は「レーンごとに、読み取り専用のエージェントを1つずつ割り当てる」と書いている（この書き方自体を直す）

## やること

前提を確かめ直した（2026-09-26）: claude-skills は `git status --short` 空・`main` ブランチで着手可。
tsukumo 側 `develop/task/` に T-625〜T-627 は無い（完了条件は該当なしとして確認済み）。CAMPAIGN.md の
段3・段5・段6にも `skills/next-task/SKILL.md` の手順5にも、まだ該当の文言は入っていない。作業は要る。

- claude-skills を `~/ghq/github.com/sinnlosses/claude-skills` の別の作業ツリー
  （`worktree add -b feature/tsukumo-T-649 <scratchpad> main`）で編集する
  - `skills/test-audit/CAMPAIGN.md` 段5（切り替え）に「消したテストの跡に keeper の在りかを説明する
    コメントを残さない。keeper は報告とタスクの結果の節に書く」の1行を足す
  - 同ファイル段3・段6と `skills/next-task/SKILL.md` 手順5（委譲）に「サブエージェントの中では別の
    エージェント（fork を含む）を立てず、レーンを順に自分で読む」を**条件なしで**書く（段3の
    「レーンごとに、読み取り専用のエージェントを1つずつ割り当てる」という書き方自体も直す）
  - `./check.sh` を通し、コミット（署名行なし）→ 本体 `main` へ `merge --ff-only` → 作業ツリーと枝を
    片付ける
- develop/task/ の T-625〜T-627 は該当なし（3件とも既に無い。触る作業は無い）
- tsukumo 側は `develop/task/T-649.md` のみ編集し、`bun run check` を通す

## 注意

- 作業するのは claude-skills のリポジトリ（`~/ghq/github.com/sinnlosses/claude-skills`。`~/.claude/skills/` の各スキルはそこへの symlink）。**本体の作業ツリーで枝を checkout しない**（その瞬間に全プロジェクトのスキルが入れ替わる）。`git -C ~/ghq/github.com/sinnlosses/claude-skills worktree add -b feature/tsukumo-<このタスクのID> <一時ディレクトリ> main` で別の作業ツリーを作って編集・`./check.sh`・コミットし、本体の `main` へ fast-forward で入れる
- 着手時に `git -C ~/ghq/github.com/sinnlosses/claude-skills status --short` が空でなければ止めて預ける
- claude-skills の運用はそのリポジトリの `CLAUDE.md` に従う。tsukumo のタスクの結果の節には claude-skills のコミットハッシュを書く

## 結果

- 検証コマンド: 2717 pass / 0 fail、E2E 15 pass / 0 fail。claude-skills の `./check.sh` も通った
- claude-skills `e0b03b3`（main に fast-forward 済み）: `test-audit/CAMPAIGN.md` の段3・段6と `next-task/SKILL.md` の手順5に「サブエージェントの中では別のエージェント（fork を含む）を立てず、レーンを順に自分で読む」を条件なしで、段5に「消したテストの跡に keeper の在りかのコメントを残さない」を書いた
- T-625〜T-627 は完了済みで `develop/task/` に無く、直す注意書きは無かった
- 振り返り: 兆候なし
