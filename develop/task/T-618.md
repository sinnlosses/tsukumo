---
id: T-618
summary: `test-audit` のキャンペーンで server/session-driver のテストを刈り込む
status: done
difficulty: sonnet
loopable: Y
dependencies: []
---

## 目的

`test-audit` スキルのキャンペーンモード（同スキルの `CAMPAIGN.md` の8段）で、`test/server/session-driver/`（12ファイル・約3,600行） のテストを刈り込む。価値の低いテスト・実装に結合したテスト・より強い証明と重なるテストと、テストしか呼ばない本番コードを消し、残す契約はより強い境界のテストに寄せる。

## 完了条件

- 対象範囲のテストの宣言ごとに `R` / `F` / `C` / `D` の印と1行の証拠を付けた台帳を作り、印ごとの件数と、`C` / `D` の宣言ごとの残す側（keeper）を タスクの結果 に書く（台帳の全文はリポジトリに置かない）
- `D` / `C` の編集と、それで消せるようになったテスト専用の本番コードの継ぎ目の削除を済ませる
- 移した（`C` / `F`）契約ごとに、本番側の持ち主にわざと1つ変異を入れて keeper が落ちることを確かめ、元に戻す
- 基準線（着手時）と完了時の、対象範囲のテストの `it` の数と行数、本番コードの増減を タスクの結果 に書く
- `bun run check` が通る
- 確信の持てる候補が無ければ、台帳の件数だけ書いて編集なしで `done` にしてよい

## 背景

- 2026-09-25 に `test-audit` スキル（openclaw/openclaw の同名スキルを日本語化して claude-skills に入れたもの）を取り込み、監査モードの試運転で、テストだけが呼んでいた `parseAnswer`（`src/shared/pending-ask.ts`）と `newFormatTaskSummaries`（`src/shared/task-summary.ts`）を消した。固有の検査は `test/shared/command.test.ts` と `test/server/repository/adapter/task-summary.test.ts` に移した（`0b3ef70a`）
- テスト全体は 205 ファイル・約4.7万行で、機能ごとに1タスクに分けた。このタスクはserver/session-driverの範囲

## 決まっていること（蒸し返さない）

- 消すかどうかは `docs/coding-standards.md`「テスト」節の「消すかどうか」「埋めないと決めた穴」と突き合わせる。スキルの残す基準とリポジトリの規約が食い違ったら、リポジトリの規約を優先し、食い違いを タスクの結果 に書く
- 残す基準は、スキルの `SKILL.md`「残す基準」のとおり（とくに、出力の書式を固定するテストと過去のバグの退行テストは残す）
- `CAMPAIGN.md` の段7（プロダクトの欠陥）で見つけた欠陥は、このタスクでは直さず、`develop/direction.md` の 「エージェントのドラフト」節 に積む。段8（`main` の merge）はタスクが短いので要らない
- 1タスク＝1コミット。対象範囲の外のテストは、範囲内の契約を吸収する keeper になるときだけ触る

## やること

前提を調べ直した結果、`test/server/session-driver/` は12ファイル・約3,600行・`it` 225件
（`adapter/` 4ファイル・`core/` 8ファイル）で、タスク本文の前提と一致した。作業は要る。
`CAMPAIGN.md` の8段のうち、段1〜段7を次の順で進める（段8は本タスクでは対象外・タスク本文の
「決まっていること」のとおり）。

1. 基準線: `bun test --isolate test/server/session-driver` を走らせ、全ファイルが通ることを記録する
2. レーン分け: 本番側の持ち主の境界で切る（`core/` の8ファイルと `adapter/` の4ファイルは
   すでにディレクトリで割れているので、それぞれ1ファイル=1境界を1レーンとして扱うか、
   関連の強いもの（例: `sdk-driver` と `fake-driver` と `sdk-tool` のような adapter 群、
   `sdk-message` と `session-restore` のような core 群）をまとめてレーンにする
3. レーンごとの台帳: サブエージェントを立てられない制約下では自分がレーンを順に読み、
   宣言ごとに `R`/`F`/`C`/`D` と1行の証拠を付ける
4. レーンごとの層の計画: 台帳から冗長な層を探し、契約ごとの keeper を名指しする
5. 切り替え: `C`/`D` の編集とテスト専用の本番コードの継ぎ目の削除
6. 保全レビュー: メインが受け入れで行う。移した契約ごとにわざと1変異を入れ keeper が
   落ちることを確認し、退避コピーから戻す（`git checkout --` は使わない）
7. プロダクトの欠陥: 基準線で落ちるものがあれば別記録し、直さず `develop/direction.md` へ
   積む提案として報告に書く（このタスクではファイルを触らない）

## 注意

- 変異を入れて確かめるときは、退避したコピーから戻す（`git checkout --` で戻すと、同じファイルへの先行の編集ごと消える）
- `CAMPAIGN.md` の段3・段6は、別のエージェントに読ませることを求めている。サブエージェントの中で別のエージェントを立てられないなら、レーンごとに順に自分で読み、段6の保全のレビューは受け入れのときにメインが行う
- ブラウザに出る絵は自動テストで守らない方針（`docs/architecture.md`）なので、描画のテストを消すときも、足すときも、その方針の範囲で判断する

## 結果

- 台帳（225宣言）: R 225 / F 0 / C 0 / D 0。どれも SDK・transcript の形を境界で確かめるもの、実測した癖に合わせた分岐、SDK の型との一致の検査で、同じ契約を別の場所で繰り返すテストもテスト専用の継ぎ目も無かった。変異確認は対象なし。規約との食い違い・段7の欠陥はなし
- 基準線・完了時とも 12ファイル・3600行・`it` 225件。本番コードは増減なし（編集なしで完了）
- 検証コマンド: 2765 pass / 0 fail
- 振り返り: 兆候なし
