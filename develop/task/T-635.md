---
id: T-635
summary: `components/page/` の規約を `docs/design.md` 2章に書き、各ページの移し先を決める
status: done
difficulty: opus
loopable: Y
dependencies: []
---

## 目的

ユーザーが決めた `src/browser/components/page/` の下の置き方の規約を正典（`docs/design.md` 2章）に書き、
いまのファイルを規約に合わせて移す後続のタスクが、迷わずに動ける移し先の表を用意する。

## 完了条件

- `docs/design.md` 2章「ディレクトリ」（`components/page/` の行と、単数形・1部品1フォルダの例外を
  言う段落）と「機能の中を分ける（container / presenter と `hooks/`）」に、下の「決まっていること」の
  規約が書かれている。2章のほかの記述（「PascalCase・1部品1フォルダは真似しない」「例外は
  `components/ui/` だけ」など）と矛盾しない形に直してある
- 会話の画面を1ページ（`page/conversation/conversation.tsx` と `presentational-conversation.tsx`）に
  するときの形が決まって書かれている: `main.tsx` と `components/domain/layout/` の `<Layout>` の
  差し込み口（`main` / `character` / `dispatch` と、雑談モードの `<ChatView>`）との分担、
  4領域（`main-view` / `character-view` / `chat-view` / `dispatch`）を `conversation/components/` の下の
  部品として扱うときの中身の置き方（`markdown/` のような部品の中の道具、部品ごとの `hooks/`）、
  2章「領域の機能と、置かれる機能」の表と `test/architecture.test.ts` の機能の一覧をどう直すか
- `achievement` / `character` / `token-usage` / `conversation` の各ページについて、いまの各ファイルの
  移し先（`domain/` / `hooks/` / `components/<部品>/` / `components/hooks/` / 直下）を表にして、
  後段のタスク（ページごとの移動と、規約を検査に焼くタスク）の本文へ書き込んである
- 後段（このタスクを `dependencies` に持つタスク）の本文を設計の結論に合わせて直す。揃えるときは
  実装の広がり（動かすファイルの数・`main.tsx` や `<Layout>` の変更・触る機能の数）を見て、後段の
  `difficulty` も見直す（1コミットで説明できない大きさなら分ける）
- `bun run check` が通る

## 背景

- いまの `components/page/` の直下（2026-09-25 の `main`）:
  - `achievement/`: container は `achievement-screen.tsx`、presenter は
    `presentational-achievement-screen.tsx`。部品（`bookmark-section.tsx` `day-switch.tsx`
    `diary-book.tsx` `diary-notice.tsx` `diary-section.tsx` `lantern-calendar.tsx`
    `surprise-section.tsx`）、部品でない `.ts`（`diary-book-request.ts` `diary-writer.ts`）、CSS 2つが
    直下にある。`domain/kanji-date.ts` と `hooks/` は規約どおり。`diary-notice.tsx` は `main.tsx` が
    直に置いている（2章「領域の機能と、置かれる機能」）
  - `character/`: container 相当が `character-screen.tsx` `character-create.tsx`
    `character-edit.tsx` `character-list.tsx` の4つ、presenter が `presentational-character-create.tsx`
    `presentational-character-edit.tsx` の2つ。`components/` はあるが部品はファイルを直に置いている
  - `token-usage/`: container は `token-usage-screen.tsx`、presenter は
    `presentational-token-usage-screen.tsx`。部品（`context-usage-card.tsx` `period-usage-card.tsx`
    `usage-review-card.tsx`）と `.ts`（`context-usage-category.ts` `usage-format.ts`）が直下
  - `conversation/`: 自分の container は無く、`main.tsx` が4領域（`MainView` `CharacterView`
    `ChatView` `Dispatch`）を別々に import して `<Layout>` に差し込む。各領域は container /
    presenter の対・`hooks/`・部品をそれぞれの直下に持つ（`main-view/markdown/` は Markdown 一式）
- 画面の名前は `stores/location-hash.ts` の `Screen` の値に合わせる決まり（2章「ディレクトリ」）
- テストは `test/browser/components/page/` の下にソースと同じ形で並んでいる
- `test/architecture.test.ts` の機能の一覧（`components/page/conversation/main-view` など）が
  領域のパスを持っている

## 決まっていること（蒸し返さない）

ユーザーの指示（2026-09-25）と、そのときの問答で決まったこと:

- ページの形:
  - `page/<ページ>/domain/` … そのページだけで使うドメインの機能。2つ以上のページで使うものは共通の側へ
    （部品なら `components/domain/`、部品でないものは `src/browser/domain/`。2章の既存の線に合わせる）
  - `page/<ページ>/hooks/` … そのページだけで使うフック。共通のものは `src/browser/hooks/`
  - `page/<ページ>/components/` … そのページで使う部品。**1部品1ディレクトリ**
    （`components/<部品>/<部品>.tsx`。その部品の CSS も同じディレクトリ）
  - `page/<ページ>/components/hooks/` … `components/` の下の部品だけが使うフック
  - `page/<ページ>/presentational-<ページ>.tsx` … presenter
  - `page/<ページ>/<ページ>.tsx` … container（例: `achievement.tsx`。いまの `achievement-screen.tsx` の
    ような名前は、構成要素として `components/` へ行く）
- **ページの直下に置くのは上のディレクトリと container / presenter の対だけ**。例外として、
  container / presenter が使う `<ページ>.module.css` は直下に置いてよい（部品専用の CSS は部品の隣）
- 直下の container / presenter は**ページに1対だけ**。`character` の create / edit / list のような
  2つ目以降の画面は `components/` の下の部品にする（部品が自分の container / presenter の対を持つのはよい）
- 会話の画面は**1ページ**にする（`conversation.tsx` / `presentational-conversation.tsx` を作り、4領域は
  `conversation/components/` の下へ入れる）

## 解くべき論点

- 会話の画面を1ページにするとき、`<Layout>` の差し込み口を誰が埋めるか（`main.tsx` のまま領域を
  差し込むのか、`conversation.tsx` が `<Layout>` を描くのか）と、2章「領域の機能と、置かれる機能」の
  「領域は `main.tsx` だけが import する。互いに import しない」との折り合い
- `components/<部品>/` の中に部品の container / presenter / 専用の hooks・道具（`markdown/` の
  `split-blocks.ts` など）を置くときの形。ページの `domain/` へ出すものとの線
- `diary-notice.tsx` のように、ページの部品なのに `main.tsx` が画面の外に置くものの扱い
- 規約を `test/architecture.test.ts` で落とす形（直下に許す名前の決め方）

## やること

### 調べ直した前提（2026-09-26）

- 背景の4ページのファイル構成は `main` のいまと一致（崩れていない）。`main.tsx` は
  `AchievementScreen` / `DiaryNotice` / `CharacterScreen` / `TokenUsageScreen` と会話の4領域を直に
  import し、`<Layout>`（`components/domain/layout/`）に差し込んでいる。枠（`layout` / `screen-nav` /
  `sidebar`）から画面への import は無い
- 会話の4つを1つの領域にまとめると、読み手が1つに戻るものが出る:
  `components/domain/prompt-image.tsx`（chat-log・presentational-composer・turn）と
  `domain/api-error-label.ts`（turn・use-turn-status）は、`test/architecture.test.ts`「1つの機能だけが
  読むファイルは無い」が落とす。`hooks/use-repository-file-paths.ts`（file-suggestions・
  use-suggestion・repository-link）は検査の外だが同じ理由で下ろす
- 読み手の数は `import`（`import type` を含む）を辿って数えた。下の表はその結果

### 決めたこと（正典は `docs/design.md` 2章に書いた）

- 「ディレクトリ」に**ページの形**の段落と木、置き場所の表を足した。要点:
  - ページの直下は container / presenter の対・`<ページ>.module.css`・`domain/` `hooks/`
    `components/` だけ。**対はどのページも必ず作る**（「2種類以上で割る」は中の部品に掛け、入口には
    掛けない）。state・副作用があれば `hooks/use-<ページ>.ts`、ストアを読むだけなら作らない
  - **置き場所は「読み手すべてを含む、いちばん近い箱」**（部品・フック・語彙・CSS 共通。CSS は
    ファイル単位で数え、クラスごとには割らない）
  - **入れ子はページの `components/` から2段まで**（子部品は `components/` を持たず、孫は親の
    `components/` に並べる）。部品のディレクトリの中に置けるのは `<部品>.tsx`・
    `presentational-<部品>.tsx`・`<部品>.module.css`・`hooks/`・`domain/`・`components/`（1段目だけ）・
    概念のディレクトリ（`markdown/`）
  - **部品のディレクトリの外から引いてよいのは `<部品>.tsx` だけ**（例外は `main.tsx` とテスト）
  - 名前: `<ページ>.tsx` / `presentational-<ページ>.tsx`、部品名は PascalCase（`Achievement`・
    `Character`・`TokenUsage`・`Conversation`）。`-screen` は付けない
- 「1部品1フォルダは真似しない」の例外を `components/ui/` とページの `components/` の2つにした
- 「領域の機能と、置かれる機能」に**会話の画面を1ページにしたあとの形**を足した:
  `presentational-conversation.tsx` が `<Layout>` と `<Sidebar>` を import して差し込み口を埋め、
  `conversation.tsx` は `chatMode` をストアから読んで渡すだけ（`hooks/` は作らない）。`main.tsx` は
  `<Activity>` の中に `<Conversation />` を置き、`<ScreenNav>`・`<DiaryNotice>`・
  `usePortraitPreload`・`OVERLAY_SCREEN` は入口に残す。辺は**「画面 → 枠」を1本足す**
  （検査の一覧を 枠 / 画面 / 置かれる機能 の3種類にし、許す辺は「枠・画面 → 置かれる機能」と
  「画面 → 枠」）。`diary-notice` は成果の画面の部品のまま `main.tsx` が直に置く
- 「機能の中を分ける」の冒頭に、ページではこの節を中の部品に掛けることを足した
- 表と木の「いまの中身」は移す前の形のまま残し、「まだ移す前の形」と書いた（書き換えは移すタスク）

### 移し先の表（後段が読む）

パスは `src/browser/components/page/<ページ>/` からの相対。テストは
`test/browser/components/page/` の下の同じ相対パスへ（`src/<p>.ts` → `test/<p>.test.ts`。いまページの
直下にある `use-*.test.tsx` も移し先の `hooks/` の下へ）。

**achievement**

| いま                                                                                                                       | 移し先                                                                                                                              |
| -------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `achievement-screen.tsx`（`AchievementScreen`）                                                                            | `achievement.tsx`（`Achievement`）                                                                                                  |
| `presentational-achievement-screen.tsx`                                                                                    | `presentational-achievement.tsx`（`PresentationalAchievement`）                                                                     |
| `achievement.module.css`                                                                                                   | 直下のまま（presenter と6部品が読む）                                                                                               |
| `domain/kanji-date.ts`                                                                                                     | そのまま                                                                                                                            |
| `diary-writer.ts`                                                                                                          | `domain/diary-writer.ts`（diary-section・use-achievement・use-diary-book）                                                          |
| `diary-book-request.ts`                                                                                                    | `hooks/use-diary-book-open-request.ts`（`useSyncExternalStore` の小さな store とそのフック1本。use-diary-book と use-diary-notice） |
| `hooks/use-achievement.ts` `use-achievement-calendar.ts` `use-diary-book.ts`                                               | そのまま（container / presenter が読む）                                                                                            |
| `hooks/use-diary-notice.ts`                                                                                                | `components/diary-notice/hooks/use-diary-notice.ts`                                                                                 |
| `hooks/use-fit-diary-page.ts`                                                                                              | `components/diary-book/hooks/use-fit-diary-page.ts`                                                                                 |
| `diary-notice.tsx` `diary-notice.module.css`                                                                               | `components/diary-notice/`（`main.tsx` が直に import）                                                                              |
| `bookmark-section.tsx` `day-switch.tsx` `diary-book.tsx` `diary-section.tsx` `lantern-calendar.tsx` `surprise-section.tsx` | `components/<名前>/<名前>.tsx`（`lantern-calendar` は presenter・diary-book・diary-notice が読むのでページの `components/` 直下）   |

**character**

| いま                                                                                                                                                                                                                                               | 移し先                                                                                                                                                        |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `character-screen.tsx`（`CharacterScreen`）                                                                                                                                                                                                        | `character.tsx`（`Character`）+ `presentational-character.tsx`（2段組の JSX）+ `hooks/use-character.ts`（`createOpen` / `createKey` の state と開閉の呼び先） |
| `character-screen.module.css`                                                                                                                                                                                                                      | `character.module.css`（直下。ほぼ全部品が読む）                                                                                                              |
| `character-list.tsx`                                                                                                                                                                                                                               | `components/character-list/character-list.tsx`                                                                                                                |
| `character-create.tsx` `presentational-character-create.tsx`                                                                                                                                                                                       | `components/character-create/`                                                                                                                                |
| `hooks/use-character-create.ts`                                                                                                                                                                                                                    | `components/character-create/hooks/use-character-create.ts`（読み手は create とその子の portrait-drop）                                                       |
| `character-edit.tsx` `presentational-character-edit.tsx`                                                                                                                                                                                           | `components/character-edit/`                                                                                                                                  |
| `hooks/use-character-edit.ts`                                                                                                                                                                                                                      | `components/hooks/use-character-edit.ts`（edit・accent-swatch・use-character-create が読む）                                                                  |
| `hooks/use-selected-pack.ts`                                                                                                                                                                                                                       | `components/hooks/use-selected-pack.ts`（character-list・use-character-edit）                                                                                 |
| `components/action-icon.tsx` `components/accent-swatch.tsx`                                                                                                                                                                                        | `components/<名前>/<名前>.tsx`（list / create / edit をまたぐ）                                                                                               |
| `components/portrait-drop.tsx`                                                                                                                                                                                                                     | `components/character-create/components/portrait-drop/portrait-drop.tsx`                                                                                      |
| `components/background-field.tsx` `character-delete.tsx` `character-delete-confirm.tsx` `character-profile.tsx` `character-profile-edit.tsx` `character-profile-edit-dialog.tsx` `face-field.tsx` `portrait-card.tsx` `portrait-clear-confirm.tsx` | `components/character-edit/components/<名前>/<名前>.tsx`（孫の delete-confirm・profile-edit・profile-edit-dialog・clear-confirm も同じ段に並べる）            |

**token-usage**

| いま                                                                            | 移し先                                                           |
| ------------------------------------------------------------------------------- | ---------------------------------------------------------------- |
| `token-usage-screen.tsx`（`TokenUsageScreen`）                                  | `token-usage.tsx`（`TokenUsage`）                                |
| `presentational-token-usage-screen.tsx`                                         | `presentational-token-usage.tsx`（`PresentationalTokenUsage`）   |
| `token-usage.module.css` `hooks/use-token-usage.ts` `hooks/use-usage-review.ts` | そのまま                                                         |
| `usage-format.ts`                                                               | `domain/usage-format.ts`（2本のフックと presenter が読む）       |
| `context-usage-card.tsx`                                                        | `components/context-usage-card/context-usage-card.tsx`           |
| `context-usage-category.ts`                                                     | `components/context-usage-card/domain/context-usage-category.ts` |
| `period-usage-card.tsx` `usage-review-card.tsx`                                 | `components/<名前>/<名前>.tsx`                                   |

**conversation**（直下は新しく `conversation.tsx`（`Conversation`）と `presentational-conversation.tsx`）

| いま                                                                                                                                                     | 移し先                                                                                                               |
| -------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------- |
| `src/browser/domain/api-error-label.ts`                                                                                                                  | `domain/api-error-label.ts`                                                                                          |
| `src/browser/hooks/use-repository-file-paths.ts`                                                                                                         | `components/hooks/use-repository-file-paths.ts`                                                                      |
| `src/browser/components/domain/prompt-image.tsx` `prompt-image.module.css`                                                                               | `components/prompt-image/`                                                                                           |
| `main-view/main-view.tsx` `main-view.module.css` `domain/turn-title.ts` `hooks/use-active-turn-scroll.ts` `markdown/`（中身ごと）                        | `components/main-view/` の同じ相対パス                                                                               |
| `main-view/turn-header.tsx` `presentational-turn-header.tsx` `turn-header.module.css` `hooks/use-turn-header.ts`                                         | `components/main-view/components/turn-header/`（フックは `hooks/` の下）                                             |
| `main-view/question-record.tsx` `presentational-question-record.tsx` `question-record.module.css` `hooks/use-question-record.ts`                         | `components/main-view/components/question-record/`（turn の子だが2段までなので main-view の `components/` に並べる） |
| `main-view/question-ask.tsx` `question-ask.module.css` `hooks/use-question-ask-scroll.ts`                                                                | `components/main-view/components/question-ask/`                                                                      |
| `main-view/turn.tsx` / `report.tsx` / `mini-portrait.tsx`（+`mini-portrait.module.css`）                                                                 | `components/main-view/components/<名前>/`                                                                            |
| `character-view/character-view.tsx` `presentational-character-view.tsx` `character-view.module.css` `hooks/use-character-view.ts`                        | `components/character-view/` の同じ相対パス                                                                          |
| `character-view/balloon-track.tsx` `balloon.tsx`                                                                                                         | `components/character-view/components/<名前>/`                                                                       |
| `character-view/speech-log.tsx` `presentational-speech-log.tsx` `hooks/use-speech-log.ts`                                                                | `components/character-view/components/speech-log/`                                                                   |
| `chat-view/chat-view.tsx` `presentational-chat-view.tsx` `chat-view.module.css` `hooks/use-chat-view.ts` `use-speech-reveal.ts` `use-stick-to-bottom.ts` | `components/chat-view/` の同じ相対パス                                                                               |
| `chat-view/components/chat-log.tsx` `chat-day.tsx` `chat-time.tsx` `chat-typing.tsx` `nudge-portrait.tsx`                                                | `components/chat-view/components/<名前>/<名前>.tsx`                                                                  |
| `chat-view/components/chat-speech.tsx` `hooks/use-chat-speech.ts`                                                                                        | `components/chat-view/components/chat-speech/`（フックは `hooks/` の下）                                             |
| `dispatch/dispatch.tsx` `dispatch.module.css`                                                                                                            | `components/dispatch/`                                                                                               |
| `dispatch/composer.tsx` `presentational-composer.tsx` `hooks/use-composer.ts` `use-suggestion.ts` `use-prompt-image.ts` `prompt-image.ts`                | `components/dispatch/components/composer/`（フックは `hooks/`、`prompt-image.ts` は `domain/`）                      |
| `dispatch/command-suggestions.tsx` `file-suggestions.tsx`                                                                                                | `components/dispatch/components/<名前>/<名前>.tsx`（composer の子だが2段までなので dispatch の段に並べる）           |
| `dispatch/pending-answer.tsx` `presentational-pending-answer.tsx` `hooks/use-pending-answer.ts`                                                          | `components/dispatch/components/pending-answer/`                                                                     |
| `dispatch/turn-status.tsx` `presentational-turn-status.tsx` `hooks/use-turn-status.ts`                                                                   | `components/dispatch/components/turn-status/`                                                                        |

### 後段への申し送り（本文はこのタスクでは直していない）

- T-636 / T-637 / T-638: 上の表どおりに移す。規模は各 20 前後のファイル + テスト + `main.tsx` の
  import で、`sonnet` のままでよい。`docs/design.md` 2章の木・「領域の機能と、置かれる機能」の
  `diary-notice.tsx` / `diary-book-request.ts` / `lantern-calendar.tsx` のパス、
  `docs/screen-design.md` のパスも直す
- T-639 は1コミットで説明できる大きさを超える（ソース約70 + テスト約25 + 箱の外の3ファイル +
  検査の作り替え）ので、2つか3つに分けるのがよい:
  1. 骨組み（`opus`）: `conversation.tsx` / `presentational-conversation.tsx` を作り `main.tsx` を
     `<Conversation />` にする。4領域を**中の形を変えずに** `conversation/components/<領域>/` へ
     ディレクトリごと移す。`prompt-image` / `api-error-label` / `use-repository-file-paths` を下ろす。
     `test/architecture.test.ts` の一覧を 枠 / 画面 / 置かれる機能 に分け「画面 → 枠」を許す。
     2章の表と木、6.1（部品の木）を直す。**6.3 の見出しがパス
     （`components/page/conversation/main-view/markdown/markdown.tsx`）を持っている**ので、見出しを
     変えるときは `test/section-reference.test.ts` が拾う参照も直す
  2. 中の組み替え（`sonnet`）: `main-view` を上の表どおり1部品1ディレクトリへ
  3. 中の組み替え（`sonnet`）: `character-view` / `chat-view` / `dispatch` を上の表どおりに
- T-640: 許す名前は**ディレクトリ名から作る**（`<dir>.tsx` / `presentational-<dir>.tsx` /
  `<dir>.module.css` / `domain` / `hooks` / `components`）。部品のディレクトリも同じ作り方で、ほかに
  概念のディレクトリを**名前の一覧で**許す（いまは `markdown` だけ。一覧に無いディレクトリは落とす）。
  加えて「子部品が `components/` を持たない」「部品のディレクトリの外から中の `<部品>.tsx` 以外を
  import しない（`main.tsx` とテストは除く）」も落とせる。「いちばん近い箱より浅く置きすぎている」
  向きは検査しない（読む人が表で守る）。`dependencies` に 2・3 の分割後のタスクを足す

## 注意

- 指示の原文の最後に「container/presenter」の1語だけの行が残っている（書きかけの可能性がある）。
  規約の読みに効くなら、設計を決める前にユーザーに確かめる
- `T-624`（conversation 画面のテストの刈り込み）・`T-625`（achievement・character・token-usage 画面の
  テストの刈り込み）が同じテストのファイルを触る。移動のタスクと並んで走ると rebase でぶつかる

## 結果

- 検証コマンド: 2714 pass / 0 fail（E2E 1 pass）
- `docs/design.md` 2章に「ページの形」と会話の画面を1ページにしたあとの形を書いた。各ページの移し先の表は T-636〜T-639 の本文へ写し、T-639 を骨組み（opus）に絞って中の組み替えを T-663・T-664（sonnet）に分け、T-640 の依存に足した
- 振り返り: 受け入れのときにメインが直したもの（後段の本文と分割。ドラフト 1件）
