---
id: T-585
summary: 残りの3画面を `components/page/<画面>/` へ移し `features/` を畳む
status: done
difficulty: sonnet
loopable: Y
dependencies: [T-584]
---

## 目的

会話以外の3画面を `components/page/<画面>/` に移し、移行の最後の段として `features/` に残るものを T-581 の対応表どおりにする。

## 完了条件

- `src/browser/features/character-screen/` `token-usage/` `achievement/` が、`docs/design.md` の対応表の段4の行どおり中身ごと（ファイル名は変えない）`components/page/character/` `components/page/token-usage/` `components/page/achievement/` にある。`achievement/` は `diary-notice.tsx`・`diary-notice.module.css`・`hooks/use-diary-notice.ts` も含めて一緒に移っている。テストも `test/browser/` の同じ相対パスへ移っている
- `src/browser/features/` の直下に残っているのが `task-board/` だけになっている
- `test/architecture.test.ts` の領域の一覧の3つのパスが `components/page/<画面>/` になり、**移行の途中のために許した `features` → `components/domain` の辺が消えている**（箱の辺が `docs/design.md` 2章の箱の表と一致する）
- `docs/design.md` の対応表の節（「いまの `src/browser/` から移す先（移行の対応表）」）と、2章の「移行中」の段落が `docs/history/decision.md` へ移っている（`test/section-reference.test.ts` が見る参照が切れていない）
- 古いパスが残っていない（`grep -rn "features/character-screen\|features/token-usage\|features/achievement\|features/main-view\|features/layout\|features/screen-nav\|features/sidebar\|features/dispatch\|features/chat-view\|features/character-view\|browser/components/[a-z-]*\.tsx" docs CLAUDE.md src test scripts` で `docs/history/` 以外に当たらない）
- `bun run check` と `bun run build` が通る。キャラクター画面・使用量の画面・成果の画面（と、どの画面にも出る書き終わりの知らせ）を開き、以前と同じに見えることを目視で確かめ、何を見たかを結果に書く

## 背景

- 3画面は `main.tsx` の `OVERLAY_SCREEN`（`satisfies Record<Exclude<Screen, "conversation">, ReactElement>`）で引かれ、`main.tsx` に残す。画面の一覧の正典は `stores/location-hash.ts` の `Screen` で、ディレクトリの名前はその値に揃える（`character-screen` → `character`）
- `features/achievement/diary-notice.tsx` は画面ではなく `main.tsx` が常に出す知らせだが、見開きを開く合図（`diary-book-request.ts`）と `lantern-calendar.tsx` の `Bell` を成果の画面と共有するので、成果の画面と一緒に移す（`docs/design.md` 2章「領域の機能と、置かれる機能」）
- `features/character-screen/` は `components/` に13部品を持ち、`hooks/` に3本
- 移す先・依存の向きの正典は `docs/design.md` 2章（T-581 が書いた対応表・箱の表）

## やること

`docs/design.md` 2章の対応表（段4の行）・箱の表を読み直した。タスク本文の完了条件と食い違わないので、そのまま作業する。

- **移す（`git mv`）**: `src/browser/features/character-screen/`（中身ごと。`components/` 8部品・`hooks/` 3本）→ `src/browser/components/page/character/`。`src/browser/features/token-usage/`（中身ごと。`hooks/` 2本）→ `src/browser/components/page/token-usage/`。`src/browser/features/achievement/`（中身ごと。`diary-notice.tsx`・`diary-notice.module.css`・`hooks/use-diary-notice.ts` を含む）→ `src/browser/components/page/achievement/`。テストも `test/browser/features/character-screen/` `token-usage/` `achievement/` を同じ振り分けで `test/browser/components/page/character/` `token-usage/` `achievement/` へ
- **`main.tsx` の import を直す**: 3画面への import パスを新しい場所に合わせる（ディレクトリ名は `character-screen` → `character` に変わる。ファイル名は変えない）
- **移した3ディレクトリの中の相対 import を直す**: `features/<画面>/` から `components/page/<画面>/` へ1段深くなる分、外を指す `../` を1段足す（`./` で始まる内部 import は変わらない）
- **`test/architecture.test.ts` を直す**: `BROWSER_REGIONS` の3行を新しいパスに替え、移行の途中だけ許していた `features` → `components/domain` の辺（`ALLOWED_BROWSER_BOX_IMPORTS.features` の中の `"components/domain"` とその注釈コメント）を外す。あわせて migration 中である旨の古い注釈も現状に合わせる
- **記述を直す**（`docs/` の `history/` 以外・`CLAUDE.md`・`src/` と `test/` のコメント・`docs/research/` の実物記述）: `features/character-screen` `features/token-usage` `features/achievement` への言及を新しいパスに直す
- **`docs/design.md` の対応表の節（「いまの `src/browser/` から移す先（移行の対応表）」）と、2章の「移行中」の段落を `docs/history/decision.md` へ移す**（`src/server/` の対応表は別タスクの対象なのでそのまま残す）
- **`docs/research/architecture-placement.md`**: この文書の差分表にある `src/ui/features/character-screen/*` 等は、T-581 で採らなかった別案（`src/ui/` 側の命名）の行で、いまの `src/browser/features/character-screen/` を指してはいない。文字列としてだけ完了条件の grep に当たるので、`docs/history/` へ丸ごと移し（正典3箇所からの言及も新しい場所へ直す）、内容は書き換えない
- **`docs/research/configuration-and-state.md`・`docs/research/test-coverage.md`**: `appearance-color.ts` の実装欄がすでに古い（実際は `src/browser/domain/appearance-color.ts`）ので、この機会に実物へ合わせる
- **`docs/research/ui-catalog.md`**: `features/character-screen/` への言及を新しいパスに直す
- **テストのフィクスチャ**: `test/browser/components/page/conversation/dispatch/{composer,file-suggestions}.test.tsx` の架空のパス一覧が `src/browser/features/dispatch/...`（旧パス）を使っている。テストの意図（前方一致・部分一致）を保ったまま、いまも存在しない架空の名前（`festival`）に揃える

## 注意

- **このタスクの本文は T-581 の設計より前に書いた。** 着手時に `docs/design.md` の対応表を読み、食い違うところは表のほうに従う。表の上で成り立たなければ `dropped` で閉じてよい
- ソースのコメントに書かれた古いパス（`features/...` を指す参照）も直す
- ファイルの移動は `git mv` で行う

## 結果

- 残りの3画面（`character`・`token-usage`・`achievement`。書き終わりの知らせを含む）とそのテストを `components/page/` の下へ移した。`features/` には `task-board/` だけが残り、移行の途中だけ許していた `features` → `components/domain` の辺を消した。対応表の節は `docs/history/decision.md` へ移した。古い提案の表は `docs/history/architecture-placement.md` へ移し、dispatch のテストの架空のパスは `features/festival/` に揃えた。完了条件の grep は `docs/history/` 以外で 0件
- 検証コマンド: 2659 pass / 0 fail、`bun run build` 成功。疑似セッション（`TSUKUMO_FAKE_SCENE=diary-written`）を撮り、会話・キャラクター・トークン消費・成果の各画面と書き終わりの知らせが崩れず、横のはみ出しが 0px なことを確かめた（成果の画面の日記の本文は、データを置かなかったので空の表示だけ見た）
- 振り返り: 同じファイルを3回以上直した（ドラフト 0件）
