---
id: T-585
summary: 残りの3画面を `components/page/<画面>/` へ移し `features/` を畳む
status: todo
difficulty: sonnet
loopable: Y
dependencies: [T-584]
---

## 目的

会話以外の3画面を `components/page/<画面>/` に移し、移行の最後の段として `features/` に残るものを T-581 の対応表どおりにする。

## 完了条件

- `src/browser/features/character-screen/` `token-usage/` `achievement/` の中身が、`docs/design.md` の対応表（T-581）どおりの場所にある。テストも `test/browser/` の同じ相対パスへ移っている
- `src/browser/features/` の直下に残っているのが、対応表が `features/` に残すとしたものだけになっている
- `test/architecture.test.ts` の機能の一覧が表どおりで、移行の途中のために残した分岐があれば消えている
- `docs/design.md` の対応表・`CLAUDE.md` 原則5・`docs/architecture.md` のファイルの一覧に、古いパス（`features/<移した機能>/`）が残っていない（`grep -rn "features/character-screen\|features/token-usage\|features/achievement\|features/main-view\|features/layout\|features/screen-nav\|features/sidebar\|features/dispatch\|features/chat-view\|features/character-view" docs CLAUDE.md src test` で `docs/history/` 以外に当たらない。表が `features/` に残すとしたものは除く）
- `bun run check` と `bun run build` が通る。キャラクター画面・使用量の画面・成果の画面を開き、以前と同じに見えることを目視で確かめ、何を見たかを結果に書く

## 背景

- 3画面は `main.tsx` の `OVERLAY_SCREEN`（`satisfies Record<Exclude<Screen, "conversation">, ReactElement>`）で引かれる。画面の一覧の正典は `stores/location-hash.ts` の `Screen`
- `features/achievement/diary-notice.tsx` は画面ではなく `main.tsx` が常に出す知らせで、画面と別に置き場を決める必要がある
- `features/character-screen/` は `components/` に13部品を持ち、`hooks/` に3本
- 移す先・依存の向きの正典は T-581 が書く `docs/design.md` 2章

## 注意

- **このタスクの本文は T-581 の設計より前に書いた。** 着手時に `docs/design.md` の対応表を読み、食い違うところは表のほうに従う。表の上で成り立たなければ `dropped` で閉じてよい
- ソースのコメントに書かれた古いパス（`features/...` を指す参照）も直す
- ファイルの移動は `git mv` で行う
