---
id: T-585
summary: 残りの3画面を `components/page/<画面>/` へ移し `features/` を畳む
status: todo
difficulty: sonnet
loopable: Y
dependencies: [T-584]
---

## 目的

会話以外の3画面を `components/page/<画面>/` に移し、移行の最後の段として `features/` に残るものを T-581 の対応表どおりにする。

## 完了条件

- `src/browser/features/character-screen/` `token-usage/` `achievement/` が、`docs/design.md` の対応表の段4の行どおり中身ごと（ファイル名は変えない）`components/page/character/` `components/page/token-usage/` `components/page/achievement/` にある。`achievement/` は `diary-notice.tsx`・`diary-notice.module.css`・`hooks/use-diary-notice.ts` も含めて一緒に移っている。テストも `test/browser/` の同じ相対パスへ移っている
- `src/browser/features/` の直下に残っているのが `task-board/` だけになっている
- `test/architecture.test.ts` の領域の一覧の3つのパスが `components/page/<画面>/` になり、**移行の途中のために許した `features` → `components/domain` の辺が消えている**（箱の辺が `docs/design.md` 2章の箱の表と一致する）
- `docs/design.md` の対応表の節（「いまの `src/browser/` から移す先（移行の対応表）」）と、2章の「移行中」の段落が `docs/history/decision.md` へ移っている（`test/section-reference.test.ts` が見る参照が切れていない）
- 古いパスが残っていない（`grep -rn "features/character-screen\|features/token-usage\|features/achievement\|features/main-view\|features/layout\|features/screen-nav\|features/sidebar\|features/dispatch\|features/chat-view\|features/character-view\|browser/components/[a-z-]*\.tsx" docs CLAUDE.md src test scripts` で `docs/history/` 以外に当たらない）
- `bun run check` と `bun run build` が通る。キャラクター画面・使用量の画面・成果の画面（と、どの画面にも出る書き終わりの知らせ）を開き、以前と同じに見えることを目視で確かめ、何を見たかを結果に書く

## 背景

- 3画面は `main.tsx` の `OVERLAY_SCREEN`（`satisfies Record<Exclude<Screen, "conversation">, ReactElement>`）で引かれ、`main.tsx` に残す。画面の一覧の正典は `stores/location-hash.ts` の `Screen` で、ディレクトリの名前はその値に揃える（`character-screen` → `character`）
- `features/achievement/diary-notice.tsx` は画面ではなく `main.tsx` が常に出す知らせだが、見開きを開く合図（`diary-book-request.ts`）と `lantern-calendar.tsx` の `Bell` を成果の画面と共有するので、成果の画面と一緒に移す（`docs/design.md` 2章「領域の機能と、置かれる機能」）
- `features/character-screen/` は `components/` に13部品を持ち、`hooks/` に3本
- 移す先・依存の向きの正典は `docs/design.md` 2章（T-581 が書いた対応表・箱の表）

## 注意

- **このタスクの本文は T-581 の設計より前に書いた。** 着手時に `docs/design.md` の対応表を読み、食い違うところは表のほうに従う。表の上で成り立たなければ `dropped` で閉じてよい
- ソースのコメントに書かれた古いパス（`features/...` を指す参照）も直す
- ファイルの移動は `git mv` で行う
