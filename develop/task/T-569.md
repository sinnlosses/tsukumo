---
id: T-569
summary: 待ち時間の訪問について決まったことを requirements.md の 2.2 と 4.3 に書き足す
status: done
difficulty: sonnet
loopable: Y
dependencies: []
---

## 目的

待ち時間にほかのキャラクターが訪ねてくる仕組み（提案 `docs/research/character-visit.md`）について、2026-09-25 にユーザーが決めたことを正典の要件へ移し、実装のタスクが要件と食い違わないようにする。

## 背景

- 提案は `docs/research/character-visit.md`。2026-09-25 のユーザー判断は冒頭の「2026-09-25 のユーザー判断」と、論点2・論点6・「表情の源」の節に反映済み
- `docs/requirements.md` 2.2 には「分離や整形のために会話を外部のモデルへ送ることも含めて対象外」と「複数セッション・常駐（client–daemon）へ広げること」がある。4.3 には「表情の源は `speak` の1つだけ（2026-09-17 決定）」がある

## 決まっていること（蒸し返さない）

- 訪問の台本を作る使い捨ての `query()`（同じ claude）に仕事の会話を渡してよい。2.2 の外部送信には当たらない（2026-09-25 ユーザー決定）
- claude の子プロセスが一時的に2つになるのは問題にしない。2.2 の「複数セッション」には当たらない（2026-09-25 ユーザー決定）
- 4.3 に「訪問のあいだだけ台本の表情を出す」例外を足す。条件は提案書「表情の源」の節の3つ（`speechExpression` と `records` に書かない／過去のターンを見ているあいだは上書きしない／本物の `speak` と待ちの終わりで必ず帰る）（2026-09-25 ユーザー承認）
- **あとで元に戻せる形で入れる**（2026-09-25 ユーザー決定: 「上手くいかず元に戻すかもしれないから revert 可能なようにすることが条件」）
  - このタスクの変更は1コミットにまとめ、訪問と関係の無い変更（ついでの整理・名前の変更・整形だけの差分）を混ぜない。`git revert` 1回でこのタスクの分だけ戻せるようにする
  - 訪問のコードは訪問専用の新しいファイルに置き、既存のファイルへの差し込みは呼び出し・分岐・型の足し込みなど最小にとどめる
  - 完了時の結果の節に「差し込んだ既存のファイル」を列挙する（戻すときに見る場所）
  - 訪問のタスクは後ろから順に revert すれば戻る並びにする（前のタスクが後のタスクの部品に依存しない）

## 完了条件

- `docs/requirements.md` 2.2 と 4.3 に上の3点が日付つきで書かれている（決定済みの既存の文言は消さず、補足として足す）
- 節の数が変わっていない（`grep -c '^#\{2,3\} ' docs/requirements.md` が前後で同じ）
- `bun run check` が通る

## やること

前提を調べ直した結果:

- `docs/requirements.md` 2.2 には「分離や整形のために会話を外部のモデルへ送ることも含めて対象外」（176〜178行目）と「複数セッション・常駐（client–daemon）へ広げること」（182〜188行目）の2つの既存の否定節がある。どちらにも訪問（`query()` への仕事の会話の受け渡し・子プロセスが一時的に2つになること）への言及は無い。4.3 の「表情の源は `speak` の1つだけ」（320〜323行目）にも例外の記述は無い。**3点とも要件にまだ無いので、作業する**（dropped にはしない）
- `docs/research/character-visit.md` の「2026-09-25 のユーザー判断」（13〜19行目）・論点2（73〜116行目）・論点6（200〜210行目）・「表情の源」（229〜274行目）を読み、3点それぞれの決定内容と理由を確認した

足した場所:

- 2.2 の「分離や整形のために会話を外部のモデルへ送ることも含めて対象外」の既存の一文（2026-09-21 補足）の直後に、訪問の台本を作る使い捨ての `query()` に仕事の会話を渡すことは外部送信に当たらないという 2026-09-25 の補足を追加
- 2.2 の「複数セッション・常駐（client–daemon）へ広げること」の既存の箇条書きの末尾に、訪問のあいだ claude の子プロセスが一時的に2つになることはこれに当たらないという 2026-09-25 の補足を追加
- 4.3 の「表情の源は `speak` の1つだけ」の箇条書きの下に、訪問のあいだだけ台本の表情を出す例外（3条件つき）を 2026-09-25 の決定として子箇条書きで追加

「あとで元に戻せる形で入れる」の条件について: **要件（`docs/requirements.md`）には書かない。** この条件（1コミットにまとめる・訪問専用の新ファイルに寄せる・差し込んだ既存ファイルを結果に列挙する・後ろから順に revert できる並びにする）は実装タスクの進め方の規律であって、tsukumo が何をする／しないというスコープの記述ではないため。実装タスク（後続の T-）側の `## 決まっていること（蒸し返さない）` や作業手順に引き継ぐべき事項として扱う。

## 注意

- 見出し名で位置を探すと冒頭の「節の索引」に先に当たる。行頭から特定する（CLAUDE.md「ドキュメントを編集するときの罠」）
- 提案書の該当節の上に「requirements.md へ移した」と1行添える

## 結果

- 検証コマンド: 2521 pass / 0 fail（±0。docs のみ）
- `docs/requirements.md` 2.2 に2点、4.3 に例外と3つの条件を日付つきの補足で足した（節の数 26 のまま）。提案書の該当3節の上に「移した」の1行。「あとで元に戻せる形」は実装タスクの規律なので要件には書かず、実装タスク側が引き継ぐ
- 差し込んだ既存のファイル: `docs/requirements.md`・`docs/research/character-visit.md`（このコミットだけで revert できる）
- 振り返り: 兆候なし
