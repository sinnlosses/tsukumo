---
id: T-574
summary: 承認された出し方で、訪ねてきたキャラクターをキャラビューに描く
status: hold
difficulty: opus
loopable: Y
dependencies: [T-571, T-573]
---

## 目的

サーバが持つ訪問の状態（`SessionState.visit`）を、T-573 でユーザーが承認した出し方でブラウザに描き、待ち時間に客が訪ねてきて話す場面を画面に出す。

## 背景

- 訪問の状態とイベント（`visit-started` / `visit-ended`）は T-571 で足す
- キャラビューの表情は `src/browser/features/character-view/hooks/use-character-view.ts` が、今のターンは `state.speechExpression`、過去のターンは `records` から引き直した表情で選んでいる
- 雑談モードの顔（`use-chat-view.ts`）とミニ立ち絵（`mini-portrait.tsx`、固定の表情）は訪問の影響を受けない（提案 `docs/research/character-visit.md`「表情の源」の表）
- 疑似セッションは `src/server/adapter/fake-driver.ts`（`TSUKUMO_FAKE_SCENE`）

## 決まっていること（蒸し返さない）

- あるじの表情は、訪問中かつ今のターンを見ているときだけ台本の行の表情にする。過去のターンを見ているあいだは上書きしない
- 訪問のセリフはメインビュー・ログ・記録に残さない
- 客の吹き出しの色は客のパックの `accent`
- **あとで元に戻せる形で入れる**（2026-09-25 ユーザー決定: 「上手くいかず元に戻すかもしれないから revert 可能なようにすることが条件」）
  - このタスクの変更は1コミットにまとめ、訪問と関係の無い変更（ついでの整理・名前の変更・整形だけの差分）を混ぜない。`git revert` 1回でこのタスクの分だけ戻せるようにする
  - 訪問のコードは訪問専用の新しいファイルに置き、既存のファイルへの差し込みは呼び出し・分岐・型の足し込みなど最小にとどめる
  - 完了時の結果の節に「差し込んだ既存のファイル」を列挙する（戻すときに見る場所）
  - 訪問のタスクは後ろから順に revert すれば戻る並びにする（前のタスクが後のタスクの部品に依存しない）

## 解くべき論点

- T-573 の案をユーザーが承認したか（承認されるまで `hold`。承認された内容を決まっていることに焼いてから `todo` に戻す）

## 完了条件

- 訪問中・訪問の前後・過去のターンを見ているときのあるじの表情が部品のテストで守られている
- 疑似セッションに訪問の場面を足し、広い画面と狭い画面で出入りを目視した結果（どの端末で何が見えたか）が evidence にある
- `bun run check` が通る
