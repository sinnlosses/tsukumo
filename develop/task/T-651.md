---
id: T-651
summary: コマンドの受け手の表と、機能ごとの契約・手続きの置き方を正典に書く
status: todo
difficulty: opus
loopable: Y
dependencies: []
---

## 目的

`docs/research/server-procedure-proposal.md`（2026-09-26 採用）の段1〜3の形を正典に決める。後段のタスクが「どのファイルに何を置くか」を迷わず写せるようにする。

## 完了条件

- `docs/design.md` 2章（「サーバの機能と、機能どうしの辺」「ディレクトリ」）と 5章（「session-manager.ts（core）」「server.ts と session-socket.ts（adapter）」）に、次が書かれている
  - 段1: 機能ごとのコマンドの受け手の表（置き場所・ファイル名・受け手の引数の形・断る条件を行に書く書き方）と、それを束ねて断る条件を1箇所で見るファイル
  - 段2〜3: 契約（`src/shared/contract/<機能>.ts`）・手続き（`<機能>/adapter/<機能>-procedure.ts`）・束ねるルータ（配線）・照合と断る条件のミドルウェアの置き場所と、許す依存の辺（`shared` → `@orpc/contract`、`@orpc/server` は `adapter` だけ）
- `docs/glossary.md` に「契約」「手続き」を足し、「コマンド」に追記している（文面案は提案書 5章）
- `docs/research/server-procedure-proposal.md` の冒頭に採用の旨と正典の移し先を1行書いている
- **後段（T-652〜T-655）の本文を、ここで決めた結論に合わせて直している。** 揃えるときは実装の広がり（消す口・プロトコルの変更・触る機能の数）を見て後段の `difficulty` も見直す
- 提案の前提が `main` で成り立たなくなっていたら、その点をタスクの結果に書き、形を直してから進める
- `bun run check` が通る

## 背景

- いまの受け口: `src/server/view-server/adapter/session-socket.ts` の `receive` → `parseClientCommand`（`src/shared/command.ts`、28種の zod の和）→ `src/server/session/core/session-manager.ts` の `dispatch`（672行目あたり。「雑談の外なら断る」「ターン中なら断る」の `switch` 2つと、振り分けの `switch`）→ `driver-command.ts` の `dispatchToDriver` / `restart` / `write(options.*)`
- `SessionManagerOptions` の口は約20本で、中身は `src/session-start.ts`（配線）が渡している
- 手本は opencode の `server/routes/instance/httpapi/{groups,handlers,middleware}/` と t3code の `packages/contracts/src/rpc.ts`（提案書 2章）

## 決まっていること（蒸し返さない）

- oRPC（`@orpc/server` `@orpc/contract` `@orpc/client` `@orpc/tanstack-query`）を入れる。依存の追加は 2026-09-26 にユーザーが承認済み
- 3層と、機能の中の `core` / `adapter` の割りは動かさない。Effect・Hono・tRPC・NestJS・DI コンテナは採らない（提案書 4章・8章）

## 解くべき論点

- 受け手の表の受け手が、`session-manager` の内側の `restart` / `write` / 駆動（`generation.driver`）をどう受け取るか（引数の文脈オブジェクトか、機能ごとの口か）。`codebase-design` の語彙で深いモジュールになる形を選ぶ
- 機能ごとの表をどの機能に割るか（`driver-command` の6種・起こし直し3種・書き込み8種・キャラ編集が、どの機能のファイルに住むか）
- 段3で断る条件を契約の `meta` に移すとき、段1の表の列と同じ語彙にできるか
