---
id: T-588
summary: `token-usage/`・`context-usage/`・`usage-review/` を機能のディレクトリへ移す
status: todo
difficulty: sonnet
loopable: Y
dependencies: [T-587]
---

## 目的

記録と見直しの3機能を `src/server/<機能>/{core,adapter}/` へ移す（`docs/design.md` 2章「サーバの機能と、機能どうしの辺」）。

## 完了条件

- `docs/design.md` 2章「いまの `src/server/` から移す先（移行の対応表）」で段が「2」の7行が移っている。テストも同じ置き換えで移っている
- `test/architecture.test.ts` の `SERVER_FEATURES` と `SERVER_FEATURE_IMPORTS` に、この段の3機能の行（2章の辺の表のとおり）が足されている
- 移したパスを指す記述が直っている（`src/` `test/` `scripts/` の import とコメント、`docs/` のうち `history/` と `research/` 以外、`CLAUDE.md`）
- `bun run check` が通る
- 着手時に対応表の前提が崩れていたら、表を直してから移す。移す意味が無くなっていたら `dropped` で閉じてよい

## 背景

- 設計は `docs/design.md` 2章の2つの節。`sdk-context-usage.ts` は `context-usage/` ではなく `session-driver/` へ行く（段7）。理由は `docs/history/decision.md`「design.md 2. 全体構成 / ディレクトリ（`src/server/` を機能で割った）」

## 注意

- ファイル名は変えない。共有の箱（`tsukumo-home.ts` `local-time.ts` `lib/`）は動かさない
