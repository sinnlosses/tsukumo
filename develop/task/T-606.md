---
id: T-606
summary: `components/ui/` に置く variant 部品の作法と候補を決め、設計書に書く
status: done
difficulty: opus
loopable: Y
dependencies: [T-616]
---

## 目的

`src/browser/components/ui/` には `<Select>` と `<ImageZoom>` の2つしか無く、見出し・文字・縦横の並べ・ボタン・バッジ・ダイアログの見た目が各機能の `*.module.css` に手書きで散っている。これらを語彙を持たない部品として切り出し、見た目の違いを variant（props の合併型）で表す。後段のタスクがばらばらの API を作らないよう、先に作法と候補の一覧を決めて正典に書く。

## 完了条件

- `docs/design.md` 2章（`components/ui/` の箱の説明の近く）に、variant 部品の作法が書いてある。少なくとも次を決めている:
  - variant の表し方（props の合併型 → CSS Modules の class の対応表。対応表は `satisfies` で検査する）
  - 呼び出し側からの見た目の上書きをどこまで許すか（`className` の逃げ道を持つか、持つなら何を渡してよいか。`<Select>` の `frameClassName` の扱いと揃えるか）
  - 描く要素を変える口（`as` など）を持つか
  - variant の値の語彙（`theme.css` のトークン `--font-heading` / `--font-subheading` / `--font-body` / `--font-secondary` / `--font-label` / `--font-action`、`--ink` / `--ink-quiet` / `--accent` / `--state-*` にどう対応させるか）
  - テストの範囲（`test/browser/components/ui/select.test.tsx` に倣うか。絵は目視、の原則との線）
- 同じ節か `docs/screen-design.md` に、切り出す部品の一覧（下の「背景」の候補のうち採るもの・採らないもの＋理由）がある
- 後段（このタスクを `dependencies` に持つタスク）の本文を、設計の結論に合わせて直してある（採らない部品のタスクは `dropped` にする）
- `bun run check` が通る

## 決まっていること（蒸し返さない）

- `components/ui/` の部品は部品ごとのディレクトリに置く（`ui/select/select.tsx` + `select.module.css`。`index.tsx` は作らない）。T-616 で設計書に例外として書き、既存の2部品を移す。後段の部品もこの形で作る

## 解くべき論点

- **Text と Heading の境目**: 見出しは `<h1>`〜`<h4>` の意味（文書の構造）と見た目の大きさが一致しない箇所がある（例: `.character-section-heading` は `--font-body`、`.step-heading` は `--font-label`）。level と size を別の props にするか、level から size を決めるか
- **Stack の粒度**: `Stack`（direction を props）1つか、`HStack` / `VStack` の2つか。gap の値を段（トークン）に丸めるか、数値を受けるか（いまは `0.2rem`〜`0.75rem` 程度がばらばらに書かれている）。**段に丸めると見た目が少しずつ変わる**ので、許すかどうか
- **表示の小さな差をどこまで吸収するか**: ボタンの高さ（`30px` / `34px` / `2.25rem` / `2.5rem` / `2.75rem`）や角丸（`0.4rem` / `0.5rem` / `0.5625rem` / `8px`）は画面ごとに違う。size の段に寄せて揃えるか、画面固有の見た目は各機能の CSS に残すか
- **Markdown から出るレポートの印**（`report-notation.module.css` の `.report-badge-*` など）は unified が生む HTML に当たる class で、React の部品ではない。Badge の対象に入れるか外すか
- **ダイアログの形**: `useModalDialog`（`src/browser/hooks/use-modal-dialog.ts`）を包む `<Dialog>` を作るか。背景を暗くするもの／透明にするもの（`.character-clear-confirm::backdrop`）、中央／位置指定（`portrait-clear-confirm.tsx` の inline style）を variant にするか
- **Card / Surface（地の段 ground / surface / surface-raised）と Disclosure（`<details>`、4箇所）** を候補に入れるか
- **「読み手が1つだけのものは機能の中へ下ろす」との兼ね合い**: `test/architecture.test.ts` の「browser/ の機能をまたぐ箱」は、`components/ui/` のファイルを読むのが1つの機能だけなら落とす。作り始めの段（読み手がまだ1機能）に部品が落ちないか、読み手が1つしかない汎用部品（Dialog の置き方の variant など）を ui/ に置いてよいかを決める
- `components/ui/` は `stores` を読めない（`test/architecture.test.ts`）。部品は値と呼び先を全部 props で受け取る、の原則を崩さない形になっているか

## 背景

2026-09-25 時点の実測（`src/browser` の `*.module.css` 31ファイル・約7,900行、`*.tsx` 116ファイル）。

**候補と、いま散っている量**

| 候補                    | いまの書かれ方                                                                                                                                                                                                                                                                                                                                                                                                                | 量                                                                    |
| ----------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| Heading                 | `.task-board-heading` `.achievement-title` `.token-usage-title` `.usage-table-title` `.character-create-heading` `.turn-history-heading` `.step-heading` など。`margin: 0` + `font-size: var(--font-*)` + `font-weight` が `normal` / `600` / `700` / `bold` とばらつく                                                                                                                                                       | `<h1>`〜`<h6>` が 21ファイル33箇所、見出しの規則が約35                |
| Text                    | `color: var(--ink-quiet)` が 21ファイル119規則、`--font-secondary` が 22ファイル94規則、`--font-label` が 13ファイル86規則。空の表示（`.task-empty` `.sidebar-empty` `.screen-nav-work-empty` `.speech-log-empty` `.chat-empty`）もほぼ「静かな色の文字」                                                                                                                                                                     | —                                                                     |
| Stack / HStack / VStack | `display: flex; flex-direction: column; gap` が 15ファイル72規則、`display: flex; align-items: center; gap` が 19ファイル46規則                                                                                                                                                                                                                                                                                               | —                                                                     |
| Button                  | `.task-board-close` と `.image-zoom-close` は中身が同一。ほかに `.character-button`（`-danger`）、`.character-link-button`、`.achievement-review-button`、`.diary-book-review-button`（塗り）、`.diary-book-topbar-button`、`.turn-nav-button`、`.speech-log-close`、`.screen-nav-chat-mode-button`（`aria-pressed`）、`.usage-review-result-close`（リンク風）。outline / solid / ghost / link / danger の顔が十数通り手書き | `<button>` が 41ファイル92箇所、`cursor: pointer` が 23ファイル74規則 |
| Badge / Chip            | `.task-running-badge` `.character-card-badge` `.token-usage-review-badge`（accent の塗り）、`.question-ask-chip`（warn の塗り）、`.question-ask-option-badge`（warn の枠）、`.turn-newest-badge`、`.task-count-chip`、`.report-badge-ok/warn/ng`（Markdown）                                                                                                                                                                  | 約10                                                                  |
| Dialog                  | `<dialog>` が12ファイル（`task-board`・`task-run-confirm`・`character-delete-confirm`・`character-profile-edit-dialog`・`portrait-clear-confirm`・`presentational-character-create`・`presentational-speech-log`・`image-zoom`・`persona-memory-section`・`diary-book` ほか）。`::backdrop` の規則が6ファイル9箇所                                                                                                            | —                                                                     |

- 既存の `components/ui/select.tsx` は「どのプルダウンでも同じ見た目の分だけ持ち、寸法・枠・地は呼び出し側が `className` で渡す」形。variant は持たない
- `theme.css` だけがグローバルで、16進の色・`font-size`・`font-family` の値を書いてよいのもそこだけ（冒頭のコメント）
- 置き場の決まりは `docs/design.md` 2章「`src/browser/` の箱と、置く基準」、`components/ui` は `hooks` / `lib` / `utils` / `shared` だけを読める

## やること

いまのコード（T-616 のあと）で前提を調べ直した結果:

- **`components/ui/` の今の形**: `ui/select/select.tsx`・`select.module.css` と `ui/image-zoom/image-zoom.tsx`・`image-zoom.module.css` の2部品。テストは `test/browser/components/ui/select/select.test.tsx` の1本（`image-zoom` は無い）。`Select` は variant を持たず、`frameClassName`（置き方と字の色）と `className`（寸法・枠・地・字の段）を必須の props で受ける。`select.module.css` の冒頭が「置く側が持つ property を共有しない（同じ強さの class の勝ち負けが読み込み順で決まる）」と書いている。`ImageZoom` は `useModalDialog(true)` と backdrop のクリックの読み替えを自分で持つ
- **`test/architecture.test.ts` の検査**: 箱の辺（`components/ui` → `components/ui` / `hooks` / `lib` / `utils`。**`ui` どうしは読める**が、`docs/design.md` 2章の箱の表の `components/ui` の行には `components/ui` が書かれていない）、「browser/ の機能をまたぐ箱」（`ui/` のファイルで、読み手が**1つの領域・機能だけ**かつ機能の外の読み手が0なら落ちる。読み手0は落ちない。`ui` の部品が読む分は機能の外の読み手に数える）、「components/ui/ の置き方」（直下にファイルを置かない・`ui/<部品>/<部品>.tsx` がある）。CSS と JSX の中身を見る検査は無い
- **CSS の実測**（`src/browser` の `*.module.css` 30ファイル・7,905行、`*.tsx` 116ファイル）:
  - 見出し: `<h1>`〜`<h4>` が 20ファイル29箇所（`markdown.tsx` の2つは Markdown が生むもので対象外）。規則の太さは `normal` / `600` / `700`（= `bold`）の3通り。トークン消費の画面の見出しは `--usage-ink`、日記帳は `--diary-gold-*` と `--font-serif`
  - 文字: 字の段と色（と太さ・`margin: 0`）しか持たない単独の class の規則が 68（`page/` 52・`features/` 10・`domain/` 6）。うち `--usage-*` の色が 17
  - 並べ方: `display: flex` の規則 186 のうち、flex の向き・間隔・揃え・折り返しに置き方（margin・flex・min-width など）だけを足した規則が 72（`page/` 60・`domain/`+`features/` 12）。その gap は 17通りで、0 / 0.25 / 0.5 / 0.75 / 1 / 1.25rem（`8px` を含む）に乗るのが 46、乗らないのが 26（0.4rem ×9・0.6rem ×5・0.625rem ×3・1.1rem ×2 ほか）
  - ボタン: `<button>` 93箇所。顔の束は「枠だけ（`--rule`）」「accent の塗り」「`--state-ng` の塗り」「`--state-warn` の塗り」「accent の枠」「地も枠も無い（静かな字）」「リンク風（accent の字・余白0）」に分かれる。リンク風は 9箇所でほぼ同一（字の段が label か action かだけ違う）。箱は高さ 1.5rem / 30px / 34px / 2.25rem / 2.5rem / 2.75rem と余白指定、角丸 0.4 / 0.5 / 0.5625 / 0.625 / 0.875rem / 9px / 999px がばらつく。押せないは `aria-disabled`（5ファイル）と `disabled` の2通り
  - 札: 6つ（`task-running-badge`・`character-card-badge`・`token-usage-review-badge`・`question-ask-chip`・`question-ask-option-badge`・`turn-newest-badge`）。面が重なるのは accent の塗りの3つだけで、箱（ピル・0.5rem・0.3rem・位置指定）は全部違う
  - ダイアログ: 実際の `<dialog>` は 10（背景にある `task-table.tsx`・`task-run-button.tsx` はコメントの中）。10個すべてが `useModalDialog` と「backdrop を押したら閉じる」の読み替えを書き写している。`::backdrop` は 9規則で、`ground` の 70%（6）・88%（日記帳）・透明（2）。顔（地 `--surface`・枠・角丸 0.75 / 0.875rem）は7つが近く、キャラビューのログと日記帳は別物
  - `<details>` は 3領域6箇所。`<summary>` の見た目は字の段と色だけで、持つべき振る舞いは無い
- `:where(.x)` が `bun build` の CSS Modules を通ってハッシュ付きのまま残ることを、scratchpad で確かめた（`:where(.text_LZV4Kg)`）。`test/css-module-loader.ts` は選択子の中の class 名を拾うので `:where()` の中でも引ける

## 注意

- このタスクでは部品を作らず、既存の CSS も動かさない（作るのは後段）
- 後段の本文に書いた候補ごとの「置き換える対象」は計画時の実測。設計で採らないと決めた部品のタスクは `dropped` で閉じてよい

## 結果

- `docs/design.md` 2章に「`components/ui/` の部品（variant の作法と一覧）」を足した（1つの prop に1つの軸・値はトークン名・`satisfies` の対応表・props は必須、`className` は `:where()` に入れた既定値だけを上書きでき後段で検査を足す、汎用の `as` は持たない、見た目は変えない側を既定）。採るのは Text・Heading・Stack・Button・Dialog、採らないのは HStack/VStack・Badge・Disclosure・Card/Surface（理由を表に書いた）
- 後段の T-607〜T-615 の本文を設計に揃え、受け入れで T-609 の依存に T-610 を足し、T-615 の summary を実測の10箇所に直した。T-614（Badge）は採らないので dropped にする
- 検証コマンド: 2675 pass / 0 fail
- 振り返り: 兆候なし
