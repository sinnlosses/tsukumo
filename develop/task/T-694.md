---
id: T-694
summary: `docs/design.md` の冒頭に役割を書き、題を戻し、1章「何を変え、何を残すか」を撤去する
status: todo
difficulty: sonnet
loopable: Y
dependencies: []
---

## 目的

`docs/design.md`（3605行）を約1000行へ絞る一続き（このタスクから T 番号が続く5件）の1段目。
役割を冒頭で宣言し、移行の設計書としての枠組み（題・冒頭の注記・1章）を外す。以降の段は
この宣言を判定の基準にして仕分ける。

## 決まっていること（蒸し返さない）

- **design.md の役割**（2026-09-26 ユーザー承認）: 「**コードを1ファイル読んでも分からない構造の規則
  だけを持つ**」— 層と機能の辺・置き場所の基準・プロトコルの不変条件・動きの順序・安全の境界
- ほかは持ち主へ返す: コードの写し → **削除**（history へ移さない）、機能の仕様 →
  `docs/chat-mode.md` / `docs/requirements.md`、経緯と実測 → `docs/history/`、「なぜこの形か」で
  残す価値があるもの → `docs/architecture.md`「設計判断（なぜ今の形なのか）」
- 1段1コミット。段の順は: 役割と1章（このタスク）→ 7章の雑談の記憶 → 5章 → 2章と6章のコードの写し
  → 経緯と実測の移送と全体の締め

## 完了条件

- `docs/design.md` の冒頭（題の下・「このドキュメントの読み方」）に、上の役割と「何をどこへ返すか」が
  1段落＋短い表程度で書かれている。題が「設計書（描く層をブラウザ側へ移す）」ではなくなっている
- 1章「何を変え、何を残すか」が無い。1章の「決めたこと」のうち、いまも効く決定（React 19・WebSocket
  1本・Markdown の構成・立ち絵は話さない・PTY を持たない・TypeScript と Bun の位置づけ）は
  `docs/architecture.md`「設計判断」にある（既にあれば足さない）。移行前後の対照表は
  `docs/history/decision.md` へ移すか、既にあるなら消す
- 冒頭の「残っているのはキャラクターパック（7章）の段だけ」など移行の進み具合の注記が無い
- 「節の索引」が本文の `## <番号>.` と1対1。**章番号は詰めない**（外から `4.1`・`7.1`・`10章` などで
  引かれているため。1章が欠番になる旨を索引の上に1行）
- `docs/design.md` の1章を指していた参照（`git grep -n 'design\.md.*1章\|何を変え、何を残すか'`）を
  張り替えてある
- `bun run check` が通る（`scripts/find-stray-reference.ts` を含む）

## 背景

- `docs/design.md` 1〜77行が冒頭・読み方・索引・1章。1章は「残すもの | 変えるもの」の移行の対照表と、
  「決めたこと」6項（2026-09-13〜17 の経緯つき）
- 置き分けの規則は `docs/requirements.md`「正典に残すもの・`docs/history/` へ移すもの」。
  `docs/architecture.md` の読み方の表が design.md を「**移行後の形**（3層・プロトコル・部品・段階）」と
  紹介しているので、そこも役割に合わせて直す
- design.md を指す参照は history・research・develop を除いて約1200箇所

## 注意

- T-686（Node への移行のドキュメント追随）の完了条件が design.md 1章の「TypeScript は本質的な選択、
  Bun はそうではない」の段を指している。移した先を T-686 の `## 注意` に1行書き足す
- 編集の前後で `grep -c '^#\{2,3\} ' docs/design.md` の差が意図どおりかを見る（CLAUDE.md
  「ドキュメントを編集するときの罠」）
