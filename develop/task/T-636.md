---
id: T-636
summary: `page/achievement/` を `components/page/` の規約に合わせて移す
status: todo
difficulty: sonnet
loopable: Y
dependencies: [T-635]
---

## 目的

成果の画面（`src/browser/components/page/achievement/`）を、`docs/design.md` 2章の `components/page/` の
規約（直下は `domain/` `hooks/` `components/` と container / presenter の対だけ）に合わせる。

## 完了条件

- `page/achievement/` の直下に残るのは `domain/` `hooks/` `components/` `achievement.tsx`
  `presentational-achievement.tsx`（と、使うなら `achievement.module.css`）だけ
- 部品は `components/<部品>/<部品>.tsx` の1部品1ディレクトリになっている
- 画面の見た目と振る舞いが変わっていない（成果の画面・日記の見開き・知らせを目視で確かめ、何を見たかを
  結果に書く）
- `bun run check` が通る

## 背景

- いまの container は `achievement-screen.tsx`、presenter は `presentational-achievement-screen.tsx`
- 直下の部品: `bookmark-section.tsx` `day-switch.tsx` `diary-book.tsx` `diary-notice.tsx`
  `diary-section.tsx` `lantern-calendar.tsx` `surprise-section.tsx`。部品でない `.ts`:
  `diary-book-request.ts` `diary-writer.ts`。CSS: `achievement.module.css` `diary-notice.module.css`
- `main.tsx` が `AchievementScreen` と `DiaryNotice` を直に import している

## 決まっていること（蒸し返さない）

- 移し先は T-635 で決めた下の表のとおり（規約の正典は `docs/design.md` 2章「ディレクトリ」の「ページの形」）。パスは `src/browser/components/page/<ページ>/` からの相対。テストは `test/browser/components/page/` の下の同じ相対パスへ（`src/<p>.ts` → `test/<p>.test.ts`。いまページの直下にある `use-*.test.tsx` も移し先の `hooks/` の下へ）。
- あわせて `docs/design.md` 2章の木と「領域の機能と、置かれる機能」にある `diary-notice.tsx` / `diary-book-request.ts` / `lantern-calendar.tsx` のパス、`docs/screen-design.md` のパスも直す

**achievement**

| いま                                                                                                                       | 移し先                                                                                                                              |
| -------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| `achievement-screen.tsx`（`AchievementScreen`）                                                                            | `achievement.tsx`（`Achievement`）                                                                                                  |
| `presentational-achievement-screen.tsx`                                                                                    | `presentational-achievement.tsx`（`PresentationalAchievement`）                                                                     |
| `achievement.module.css`                                                                                                   | 直下のまま（presenter と6部品が読む）                                                                                               |
| `domain/kanji-date.ts`                                                                                                     | そのまま                                                                                                                            |
| `diary-writer.ts`                                                                                                          | `domain/diary-writer.ts`（diary-section・use-achievement・use-diary-book）                                                          |
| `diary-book-request.ts`                                                                                                    | `hooks/use-diary-book-open-request.ts`（`useSyncExternalStore` の小さな store とそのフック1本。use-diary-book と use-diary-notice） |
| `hooks/use-achievement.ts` `use-achievement-calendar.ts` `use-diary-book.ts`                                               | そのまま（container / presenter が読む）                                                                                            |
| `hooks/use-diary-notice.ts`                                                                                                | `components/diary-notice/hooks/use-diary-notice.ts`                                                                                 |
| `hooks/use-fit-diary-page.ts`                                                                                              | `components/diary-book/hooks/use-fit-diary-page.ts`                                                                                 |
| `diary-notice.tsx` `diary-notice.module.css`                                                                               | `components/diary-notice/`（`main.tsx` が直に import）                                                                              |
| `bookmark-section.tsx` `day-switch.tsx` `diary-book.tsx` `diary-section.tsx` `lantern-calendar.tsx` `surprise-section.tsx` | `components/<名前>/<名前>.tsx`（`lantern-calendar` は presenter・diary-book・diary-notice が読むのでページの `components/` 直下）   |

## 注意

- テストは `test/browser/components/page/` の下にソースと同じ形で並んでいるので、一緒に移す。
  `docs/` と `.module.css` のコメントにあるパスの参照も `grep -rn` で拾って直す
- `T-624` / `T-625`（テストの刈り込み）と同じテストを触る。先に入ったほうに rebase で合わせる
