---
id: T-637
summary: `page/character/` を `components/page/` の規約に合わせて移す
status: todo
difficulty: sonnet
loopable: Y
dependencies: [T-635]
---

## 目的

キャラクターの画面（`src/browser/components/page/character/`）を、`docs/design.md` 2章の
`components/page/` の規約（直下の container / presenter はページに1対だけ、部品は1部品1ディレクトリ）に
合わせる。

## 完了条件

- `page/character/` の直下に残るのは `domain/` `hooks/` `components/`（あるものだけ）と
  `character.tsx` `presentational-character.tsx`（と、使うなら `character.module.css`）だけ
- create / edit / list の画面は `components/` の下の部品になり、各部品は `components/<部品>/` の
  1部品1ディレクトリになっている
- 画面の見た目と振る舞いが変わっていない（一覧・作成・編集・削除の確認を目視で確かめ、何を見たかを
  結果に書く）
- `bun run check` が通る

## 背景

- container 相当が4つ（`character-screen.tsx` `character-create.tsx` `character-edit.tsx`
  `character-list.tsx`）、presenter が2つ（`presentational-character-create.tsx`
  `presentational-character-edit.tsx`）直下にある。CSS は `character-screen.module.css`
- `components/` の下の部品（`portrait-card.tsx` など13個）はファイルを直に置いている
- `hooks/` の `use-character-create.ts` `use-character-edit.ts` は create / edit の部品だけが使う
  （規約では `components/hooks/` か部品の中が候補）
- `main.tsx` が `CharacterScreen` を直に import している

## 決まっていること（蒸し返さない）

- 移し先は T-635 で決めた下の表のとおり（規約の正典は `docs/design.md` 2章「ディレクトリ」の「ページの形」）。パスは `src/browser/components/page/<ページ>/` からの相対。テストは `test/browser/components/page/` の下の同じ相対パスへ（`src/<p>.ts` → `test/<p>.test.ts`。いまページの直下にある `use-*.test.tsx` も移し先の `hooks/` の下へ）。
- あわせて `docs/design.md` 2章と `docs/screen-design.md` にあるこのページのパスも直す

**character**

| いま                                                                                                                                                                                                                                               | 移し先                                                                                                                                                        |
| -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `character-screen.tsx`（`CharacterScreen`）                                                                                                                                                                                                        | `character.tsx`（`Character`）+ `presentational-character.tsx`（2段組の JSX）+ `hooks/use-character.ts`（`createOpen` / `createKey` の state と開閉の呼び先） |
| `character-screen.module.css`                                                                                                                                                                                                                      | `character.module.css`（直下。ほぼ全部品が読む）                                                                                                              |
| `character-list.tsx`                                                                                                                                                                                                                               | `components/character-list/character-list.tsx`                                                                                                                |
| `character-create.tsx` `presentational-character-create.tsx`                                                                                                                                                                                       | `components/character-create/`                                                                                                                                |
| `hooks/use-character-create.ts`                                                                                                                                                                                                                    | `components/character-create/hooks/use-character-create.ts`（読み手は create とその子の portrait-drop）                                                       |
| `character-edit.tsx` `presentational-character-edit.tsx`                                                                                                                                                                                           | `components/character-edit/`                                                                                                                                  |
| `hooks/use-character-edit.ts`                                                                                                                                                                                                                      | `components/hooks/use-character-edit.ts`（edit・accent-swatch・use-character-create が読む）                                                                  |
| `hooks/use-selected-pack.ts`                                                                                                                                                                                                                       | `components/hooks/use-selected-pack.ts`（character-list・use-character-edit）                                                                                 |
| `components/action-icon.tsx` `components/accent-swatch.tsx`                                                                                                                                                                                        | `components/<名前>/<名前>.tsx`（list / create / edit をまたぐ）                                                                                               |
| `components/portrait-drop.tsx`                                                                                                                                                                                                                     | `components/character-create/components/portrait-drop/portrait-drop.tsx`                                                                                      |
| `components/background-field.tsx` `character-delete.tsx` `character-delete-confirm.tsx` `character-profile.tsx` `character-profile-edit.tsx` `character-profile-edit-dialog.tsx` `face-field.tsx` `portrait-card.tsx` `portrait-clear-confirm.tsx` | `components/character-edit/components/<名前>/<名前>.tsx`（孫の delete-confirm・profile-edit・profile-edit-dialog・clear-confirm も同じ段に並べる）            |

## 注意

- テストは `test/browser/components/page/` の下にソースと同じ形で並んでいるので、一緒に移す。
  `docs/` と `.module.css` のコメントにあるパスの参照も `grep -rn` で拾って直す
- `T-624` / `T-625`（テストの刈り込み）と同じテストを触る。先に入ったほうに rebase で合わせる
