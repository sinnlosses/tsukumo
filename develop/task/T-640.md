---
id: T-640
summary: `components/page/` の直下に置けるものを `test/architecture.test.ts` で落とす
status: todo
difficulty: sonnet
loopable: Y
dependencies: [T-636, T-637, T-638, T-639]
---

## 目的

`components/page/` の規約（ページの直下は `domain/` `hooks/` `components/` と container / presenter の
対（と `<ページ>.module.css`）だけ、部品は1部品1ディレクトリ）を、読んで守る約束から検査で落ちる
仕組みにする。

## 完了条件

- `test/architecture.test.ts`（か同じ種類の検査）が、`src/browser/components/page/<ページ>/` の直下に
  規約の外のファイル・ディレクトリがあると落ちる。`components/` の直下にディレクトリでないもの
  （`hooks/` を除く）があるときも落ちる
- 規約の外のファイルを一時ファイルで置いて落ちることを確かめ、確かめ方を結果に書く
  （壊しはこのタスクで編集していないファイルか一時ファイルに入れる）
- `docs/design.md` 2章の規約の段落から、この検査の名前を引いてある
- `bun run check` が通る

## 背景

- 規約は T-635 が `docs/design.md` 2章に書く。各ページを合わせるのは T-636〜T-639
- `test/architecture.test.ts` にはすでに機能の一覧や import の向きの検査がある

## 注意

- 許す名前の決め方（`<ページ>.tsx` を `Screen` の値から作るか、ディレクトリ名から作るか）は T-635 の
  結論に従う。調べて検査がすでにあれば `dropped` で閉じてよい
