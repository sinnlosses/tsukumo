---
id: T-695
summary: `docs/design.md` 7章の雑談の記憶まわりを `docs/chat-mode.md` へ移し、7章をパックの構造だけにする
status: todo
difficulty: opus
loopable: Y
dependencies: [T-694]
---

## 目的

`docs/design.md` を絞る一続きの2段目。7章「キャラクターパック」（約850行）のうち機能の仕様を
持ち主へ返し、7章を「パックの形・探索順・`systemPrompt` の append の並び」の構造だけにする。

## 決まっていること（蒸し返さない）

- 役割と返し先の判定は T-694 で `docs/design.md` の冒頭に書いた宣言に従う
- 仕様（上限の値・何を覚えて何を忘れるか・いつ定着させるか）は `docs/chat-mode.md`、
  置き場所の規則（どの機能の `core/` / `adapter/` か、`~/.tsukumo/` のどこか）は design.md に
  短く残す。コードの写し（関数名の列挙・型の全文）は削除
- 経緯（「2026-09-21 決定。ユーザーの指示『…』」の類）は `docs/history/` へ。正典には「いまの形」と
  1行の参照だけ

## 完了条件

- 7章が目安 120 行前後（厳密な上限ではない。残した節ごとに「コードを読んでも分からない構造の規則か」を
  説明できること）
- 7.1 の「覚えたことを人格に書き足す・1行だけ忘れる」「雑談の記憶の要約はどこに置くか」「雑談の
  会話のアーカイブはどこに置くか」「定着はどこで走るか」「エピソード索引はどこに置くか」の仕様が
  `docs/chat-mode.md` の該当節にあり、`docs/chat-mode.md` の索引が本文と合っている
- 移した・消した節を指していた参照（コードのコメント・`docs/glossary.md`・`docs/chat-mode.md`・
  `docs/requirements.md` など）を張り替えてある
- `bun run check` が通る（`scripts/find-stray-reference.ts` を含む）

## 解くべき論点

- 7.1 の各節で、どこまでが仕様（chat-mode.md）でどこからが置き場所の規則（design.md）か。
  例: 「覚えたこと」の上限の表は仕様、「`persona.md` の末尾の節だけを書く」は安全の境界として
  9章に寄せるか
- `docs/chat-mode.md` 4.9 と重なっている記述の、どちらを正典にするか（重複は片方を消す）

## 背景

- `docs/design.md` の7章は 2436 行目付近から。7.1「画面から作るときの置き場と受け取り方」（197行）の下に
  `####` の5節（118・111・105・79・125行）、7.2「パックの一覧と素材の URL」（50行）
- `docs/chat-mode.md` は 4.9「雑談モード」を持つ正典（579行）
- 調べて、移す先が既に同じことを書いているなら移さずに design.md 側を消すだけでよい

## 注意

- T-690（仕事の会話も記憶に入れる決定を正典に書く）が `docs/design.md` 7章を書き換える予定。
  先に T-690 が済んでいたらその形を土台にし、未着手なら T-690 の `## 注意` に「7章の雑談の記憶は
  `docs/chat-mode.md` へ移した」と1行書き足す
- 編集の前後で見出しの数を数える（CLAUDE.md「ドキュメントを編集するときの罠」）
