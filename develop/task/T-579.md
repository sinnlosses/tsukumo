---
id: T-579
summary: 規約に、bun test は UTC で走ることと、覚えの入れ物は口だけ渡すことを足す
status: done
difficulty: sonnet
loopable: Y
dependencies: []
---

## 目的

T-562・T-563 で受け入れのときに直した2つの罠を規約に書き、次に同じ形のコードを書くサブエージェントが最初から避けられるようにする。

## 完了条件

- `docs/coding-standards.md` に次の2項目が入っている（置く節は着手時に決め、言い回しは合わせてよい）
  - **`bun test` はプロセスのタイムゾーンを UTC にして走らせる**（`process.env.TZ` は未設定のまま、`Temporal.Now.timeZoneId()` が `UTC` を返す。2026-09-25 に実測）。日付の境目を見るテストは、オフセットを `+09:00` のように固定せず、`Temporal.Now.timeZoneId()` を基準に時刻を組む。候補は「テスト」節か「`Date` を使わない」節
  - **状態を持つ入れ物（キャッシュなど）を配線から渡すときは、作る側の閉包に閉じ込めて、覚える・引く口だけを渡す**（中の `Map` を型に出して呼び出し先に `.set` させない）。「変数は基本イミュータブル」節の「引数として渡した入れ物が呼び出し先で書き変わる契約にしない」の例として
- 冒頭の「節の索引」と `grep -c '^#\{2,3\} ' docs/coding-standards.md` の節の数が、節を足したときはそれに合わせて揃っている
- `bun run check` が通る

## 背景

2026-09-25 の振り返りで見つけた2件（`docs/history/direction.md` の 2026-09-25「振り返りのドラフト3件」）。

- T-562 で `test/server/adapter/main-history.test.ts` のコミット日時を `+09:00` 固定で組んだら日の境目がずれ、`isoDateAt`（`Temporal.Now.timeZoneId()` 基準）に組み直した。`docs/coding-standards.md` と `docs/architecture.md` に UTC の記述は無い
- T-563 で `src/server/adapter/main-history.ts` の `AchievementCommitCache` が `readonly dailyCounts: Map<string, number>` を型に出し、`readCommitCalendar` の中で `.set` していた。受け入れのときに `dailyCountOf` / `rememberDailyCount` などの口を持つ形（`createAchievementCommitCache` の閉包）に包み直した。設計書が「入れ物は配線で作って渡す」と指定していたことと規約がぶつかる例が、規約に書かれていなかった

## やること

着手時に前提を調べ直した結果:

- **2項目とも `docs/coding-standards.md` にまだ入っていない**（`grep -n "UTC\|timeZoneId" docs/coding-standards.md docs/architecture.md` は無ヒット。「書き変わる」で当たるのは「変数は基本イミュータブル」節の一般論だけで、入れ物を閉包に閉じ込める具体例は無い）。両方とも足す
- `Temporal.Now.timeZoneId()` の実測を取り直した: `bun test --isolate` 配下で `process.env.TZ` は `undefined` のまま、`Temporal.Now.timeZoneId()` は `"UTC"` を返す（ホストの `date` コマンドは JST）。タスク本文の記述どおり
- `src/server/adapter/main-history.ts` の `AchievementCommitCache` は**すでに** T-563 の受け入れで閉包の形に直っている（61-75行目）: 型は `dailyCountOf` / `rememberDailyCount` の口だけを持ち、`createAchievementCommitCache()` の中で `Map` を閉じ込めている。コード側の修正は不要で、**規約に書き足すだけ**でよい
- `test/server/adapter/main-history.test.ts` の `isoDateAt`（44-52行目）に、UTC 固定の理由を説明する doc comment が既にある。規約に書く文面はこれと矛盾しないように揃える

やる作業:

1. `docs/coding-standards.md`「テスト」節の「置き場所とモック」に、`bun test` が UTC で走ることと、日付の境目を見るテストは `Temporal.Now.timeZoneId()` を基準に時刻を組む旨を1項目追加する
2. 同ファイル「変数は基本イミュータブル」節の「引数として渡した入れ物が呼び出し先で書き変わる契約にしない」の直後に、状態を持つ入れ物は作る側の閉包に閉じ込めて覚える/引く口だけを渡す旨を追記し、`AchievementCommitCache`（`src/server/adapter/main-history.ts`）を実例として挙げる
3. 見出しは増やさない見込み（既存節への追記で足りる）ので、索引の行は変更不要。念のため編集前後で `grep -c '^#\{2,3\} ' docs/coding-standards.md` の数を確認する
4. `bun run format` → `bun run check`

## 注意

- 規約の中身を二重に書かない。CLAUDE.md の「ルールの一覧」へ足すかどうかは、既存の行の粒度に合わせて判断する（足さなくてよい）
- 調べて、2項目のどちらかが既に規約に入っていたら、その項目は足さずに結果に書く（両方入っていたら `dropped` で閉じてよい）

## 結果

- `docs/coding-standards.md` の「テスト」節（置き場所とモック）に `bun test` が UTC で走ることを、「変数は基本イミュータブル」節に入れ物を閉包に閉じ込めて口だけ渡すことを足した（見出しは増やしていない。33節のまま）
- 検証コマンド: 2653 pass / 0 fail
- 振り返り: 受け入れのときにメインが直したもの（やることの誤字1か所。ドラフト 0件）
