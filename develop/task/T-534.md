---
id: T-534
summary: 画面の一覧を1つの表にまとめ、足すときの登録を1箇所にする
status: done
difficulty: sonnet
loopable: Y
dependencies: []
---

## 背景

2026-09-24 に `/simplify` の4観点（再利用・単純化・効率・根本対処）でリポジトリ全体を洗った結果の1件（`docs/history/direction.md` の 2026-09-24「改修コストを下げる直し9件」）。

画面（`conversation` / `character` / `token-usage` / `achievement`）を1枚足すたびに、次の4箇所を揃える必要がある。1つ抜けると「hash には乗るがメニューに出ない」「メニューにはあるが描かれない」が黙って起きる。

- `src/browser/stores/location-hash.ts` の `SCREENS`
- 同じファイルの画面名の表（`achievement: "achievement"` などが並ぶもの）
- `src/browser/features/screen-nav/hooks/use-screen-nav.ts` の帯のメニューの配列（`{ screen, label }`）
- `src/browser/main.tsx` の画面部品の import と `screen === "..." ? <X /> : null` の分岐

着手時に `grep -rn '"achievement"' src/browser` で、ほかに画面の一覧を持つ場所が無いか取り直す。

## 解くべき論点

- 表を置く場所。`stores/` は部品（React）を知らないので、画面の名前とラベルの表（`stores/` か `domain/`）と、名前から部品を引く表（`main.tsx` の側）の2つに分けてもよい。そのときも「名前の一覧」は1つにし、部品の表は `Record<Screen, ...>` で抜けを型エラーにする

## やること

1. 画面の名前・ラベルの一覧を1つにし、`SCREENS`・帯のメニュー・`main.tsx` の分岐をそこから導く
2. 部品の表は `satisfies Record<Screen, ...>` などで、画面を足したのに部品を登録し忘れたら型エラーになるようにする

着手時（2026-09-24）に `grep -rn '"achievement"' src/browser` を取り直した。背景に挙がった4箇所（`location-hash.ts` の `SCREENS` と `SCREEN_PATH`、`use-screen-nav.ts` の `NAV_SCREENS`、`main.tsx` の import と3つの三項演算子）以外に画面の一覧を持つ場所は無く、前提は崩れていない。ただし `main.tsx` の分岐は「`conversation` は常時マウントの `<Layout>` を `<Activity>` の可視/不可視で切り替え、残り3画面だけがマウント/アンマウントの三項」という非対称な形だったため、部品の表は `Record<Screen, ...>` ではなく `Record<Exclude<Screen, "conversation">, ReactElement>` にした（`conversation` は表に乗らない特別枠のまま）。この表に screen を1つ足すと自動的にキーが増えるので、登録漏れの型エラーは変わらず効く。

`SCREEN_PATH` の中身（`character: "character"` のような対応）は `conversation` だけ例外で残りは画面名そのものなので、表を持たず `screen === "conversation" ? "" : screen` の関数に置き換えた（表がもう1つ増えるのを避けるため）。

## 完了条件

- 画面を1つ足すときに触る場所が「名前とラベルの一覧」と「部品の表」の2つだけになり、部品の登録漏れが `bun run typecheck` で落ちる（`evidence` に仮の画面で確かめた結果を書き、仮の画面は戻す）
- 帯のメニューの並び順と表示名が変わっていない
- `bun run check` が通る

## 結果

- 画面の一覧を `stores/location-hash.ts` の `SCREEN_LIST` 1つにし、`Screen` 型・帯のメニュー・hash のパスをそこから導いた。部品は `main.tsx` の `OVERLAY_SCREEN`（`satisfies Record<Exclude<Screen, "conversation">, ReactElement>`。会話の画面は `<Activity>` の別枠のまま）
- 仮の画面 `temp-check` を足すと `bun run typecheck` が `main.tsx` の TS2741 / TS7053 で落ちることを確かめ、戻した。帯の並び・表示名は変えていない
- 検証コマンド: 2281 pass / 0 fail
