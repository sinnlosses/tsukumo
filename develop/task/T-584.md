---
id: T-584
summary: 会話の画面の4領域を `components/page/conversation/` へ移す
status: todo
difficulty: sonnet
loopable: Y
dependencies: [T-583]
---

## 目的

会話の画面を構成する4つの領域を、手本の `components/page/<画面>/` に合わせて `components/page/conversation/` へ移す。

## 完了条件

- `src/browser/features/main-view/` `character-view/` `chat-view/` `dispatch/` の中身が、`docs/design.md` の対応表（T-581）どおりの場所にある。テストも `test/browser/` の同じ相対パスへ移っている
- `test/architecture.test.ts` の機能の一覧が表どおりに更新されている
- 古いディレクトリにファイルが残っていない。`main.tsx`（と、表が画面の組み立てを移すとしたならその部品）の import が新しいパスを指す
- `bun run check` と `bun run build` が通る。画面を開いてメインビューのレポート（mermaid と chart を含むもの）・キャラビューの吹き出し・入力欄・雑談モードの切り替えが以前と同じに見えることを目視で確かめ、何を見たかを結果に書く

## 背景

- `features/main-view/` はいちばん大きい（`markdown/` の下に11ファイル、`domain/turn-title.ts`、`hooks/` に4本、`.module.css` が5枚）。`markdown/vendor-script.ts` と `chart.ts` は `src/server/adapter/vendor-asset.ts` が配る経路を読む
- 雑談モードでは `main.tsx` の `<Root>` がメインビューを `chat-view` に差し替え、キャラビューを畳む（`docs/chat-mode.md` 4.9）
- 移す先・依存の向きの正典は T-581 が書く `docs/design.md` 2章

## 注意

- **このタスクの本文は T-581 の設計より前に書いた。** 着手時に `docs/design.md` の対応表を読み、食い違うところは表のほうに従う。表の上で成り立たなければ `dropped` で閉じてよい
- 4つの領域どうしを import してよいかは T-581 の規則に従う（いまは領域どうしの import は禁止）
- ファイルの移動は `git mv` で行う
