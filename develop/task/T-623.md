---
id: T-623
summary: `test-audit` のキャンペーンで browser の部品以外 のテストを刈り込む
status: done
difficulty: sonnet
loopable: Y
dependencies: []
---

## 目的

`test-audit` スキルのキャンペーンモード（同スキルの `CAMPAIGN.md` の8段）で、`test/browser/` の `domain/` `features/` `lib/` `stores/` `utils/`（27ファイル・約3,600行） のテストを刈り込む。価値の低いテスト・実装に結合したテスト・より強い証明と重なるテストと、テストしか呼ばない本番コードを消し、残す契約はより強い境界のテストに寄せる。

## 完了条件

- 対象範囲のテストの宣言ごとに `R` / `F` / `C` / `D` の印と1行の証拠を付けた台帳を作り、印ごとの件数と、`C` / `D` の宣言ごとの残す側（keeper）を タスクの結果 に書く（台帳の全文はリポジトリに置かない）
- `D` / `C` の編集と、それで消せるようになったテスト専用の本番コードの継ぎ目の削除を済ませる
- 移した（`C` / `F`）契約ごとに、本番側の持ち主にわざと1つ変異を入れて keeper が落ちることを確かめ、元に戻す
- 基準線（着手時）と完了時の、対象範囲のテストの `it` の数と行数、本番コードの増減を タスクの結果 に書く
- `bun run check` が通る
- 確信の持てる候補が無ければ、台帳の件数だけ書いて編集なしで `done` にしてよい

## 背景

- 2026-09-25 に `test-audit` スキル（openclaw/openclaw の同名スキルを日本語化して claude-skills に入れたもの）を取り込み、監査モードの試運転で、テストだけが呼んでいた `parseAnswer`（`src/shared/pending-ask.ts`）と `newFormatTaskSummaries`（`src/shared/task-summary.ts`）を消した。固有の検査は `test/shared/command.test.ts` と `test/server/repository/adapter/task-summary.test.ts` に移した（`0b3ef70a`）
- テスト全体は 205 ファイル・約4.7万行で、機能ごとに1タスクに分けた。このタスクはbrowser の部品以外の範囲

## 決まっていること（蒸し返さない）

- 消すかどうかは `docs/coding-standards.md`「テスト」節の「消すかどうか」「埋めないと決めた穴」と突き合わせる。スキルの残す基準とリポジトリの規約が食い違ったら、リポジトリの規約を優先し、食い違いを タスクの結果 に書く
- 残す基準は、スキルの `SKILL.md`「残す基準」のとおり（とくに、出力の書式を固定するテストと過去のバグの退行テストは残す）
- `CAMPAIGN.md` の段7（プロダクトの欠陥）で見つけた欠陥は、このタスクでは直さず、`develop/direction.md` の 「エージェントのドラフト」節 に積む。段8（`main` の merge）はタスクが短いので要らない
- 1タスク＝1コミット。対象範囲の外のテストは、範囲内の契約を吸収する keeper になるときだけ触る

## やること

前提を確認した（2026-09-25 着手、基準線は `36a40696`）:

- 対象は `test/browser/` の `domain/` `features/` `lib/` `stores/` `utils/` の **27ファイル・
  3,569行・`it` 230件**（`bun test --isolate` で 230 pass / 0 fail）。本文の見立てと一致し、
  前提は崩れていない
- レーンは本番側の持ち主のディレクトリ単位で切り、1ファイルずつ本番コードとテストを組で読んだ
  （別エージェントは立てず、自分で順に読んだ）。`domain/reveal/`（Z字の演出。band / brush-scroll /
  brush-tip / measure / plan / use-report-reveal）、`features/task-board/`（一覧・表・実行の確認）、
  `stores/`（hash・姿の store・答え待ち・見ているターン）は、実機の不具合の再現を含む厚みのある
  回帰テストが多く、`CLAUDE.md`「消すかどうか」の「同じ入力分岐を別のテストが既に通している」
  「実装の内部構造をなぞるだけ」に当たる候補が見つからなかった
- 見つかった候補は3件（すべて高確信、範囲内で完結）:
  - `test/browser/domain/reveal-speed.test.ts`: `describe("saveRevealSpeed / loadRevealSpeed")` の
    「保存した値を読み戻せる」が、`describe("loadRevealSpeed")` の「保存した値をそのまま読み戻す」
    （`saveRevealSpeed("fast")` → `loadRevealSpeed()` を確かめる、一字一句同じ手順）と完全に重複。
    **D**。keeper: 同ファイルの「loadRevealSpeed」>「保存した値をそのまま読み戻す」
  - `test/browser/lib/tool-summary.test.ts`: `describe("toolInputText")` の「summarizeToolInput と
    同じ欄を読む（Edit は file_path）」が、`summarizeToolInput` 側の「Edit はファイルパスを出す」
    （欄の選び方）と同じ describe 内の「120文字を超えても切り詰めない」（非切り詰め）の組み合わせを
    言い直しているだけで、両関数が同じ内部欄選択ロジックを共有する以上、新しい分岐を試していない。
    **D**。keeper: 「summarizeToolInput」>「Edit はファイルパスを出す」＋「toolInputText」>
    「120文字を超えても切り詰めない」
  - `test/browser/features/task-board/domain/task-sidebar-filter.test.ts`: 「想定外の status…は
    3つのチップのどれを選んでも出ない」が `not.toContainEqual` で todo / doing / done の3通りを
    検査していたが、todo / doing 側は既存の「選んだ状態のタスクだけを残す」「進行中を選ぶと doing
    だけになる」の完全一致アサーションに包含済みで、done 側だけが未証明のまま弱いアサーション
    （不在の確認のみ、`done` の正しい中身は確認していない）だった。**F**。`filterTasksForSidebar`
    に変異（`selected === "done"` のとき `status === "archived"` も通す）を入れて、書き直した
    「完了を選ぶと done だけになる」が落ちることを確認し、ソースは元に戻した（差分無し）
  - 他の24ファイルは `R`（見つけた高確信の重複・埋没候補が無し）
- テスト専用の本番コードの継ぎ目は見つからなかった。本番コードは変えない（変異の確認以外は無編集、
  変異は確認後に元へ戻し `git diff` で残差なしを確認済み）
- 作業: 上のD 2件を消し、F 1件のアサーションを強め、対象を絞って `bun test` と
  `bun run check` を通す

## 注意

- 変異を入れて確かめるときは、退避したコピーから戻す（`git checkout --` で戻すと、同じファイルへの先行の編集ごと消える）
- `CAMPAIGN.md` の段3・段6は、別のエージェントに読ませることを求めている。サブエージェントの中で別のエージェントを立てられないなら、レーンごとに順に自分で読み、段6の保全のレビューは受け入れのときにメインが行う
- ブラウザに出る絵は自動テストで守らない方針（`docs/architecture.md`）なので、描画のテストを消すときも、足すときも、その方針の範囲で判断する

## 結果

- 検証コマンド: 2725 pass / 0 fail（対象27ファイル 230→228件。D 2・F 1、本番コードは無変更）
- 振り返り: 兆候なし
