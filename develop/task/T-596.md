---
id: T-596
summary: 決めた形で、振り返りを会話のセッションから切り離して書かせる
status: done
difficulty: sonnet
loopable: Y
dependencies: [T-595]
---

## 目的

成果の画面の「<キャラクターの名前>と振り返る」で、日記を会話のセッションの外で書かせる。依頼文もレポートも会話のメインビューに残らないようにする（形は T-595 で決める）。

## 完了条件

- 振り返りのボタンを押しても、会話のメインビューに依頼文・レポート・ターンが増えず、キャラビューにセリフが出ない。会話の SDK セッションに `diary` ツールが載っていない
- 会話のターン中でも振り返りを頼め、書いている最中の2回目は断られる
- 日記は今までどおり `diary` で受け取られ、成果の画面と日記帳に出る。書けなかったとき（受け付けずに終わった・時間切れ）は「書けなかった」が出てボタンが戻る
- `docs/requirements.md` 4.11「振り返りの依頼」と `docs/design.md`「日記の受け取りと保存」の形に沿うテストがある（少なくとも: 書き手が窓口を問い合わせごとに作って `diary-written` / `diary-failed` を流す・session-manager がターン中に受けて書いている最中は断る・会話の `turn-finished` / `session-ended` で `diaryWriting` が動かない・代を閉じると中断する）
- `docs/design.md` 2章「サーバの機能と、機能どうしの辺」の機能の表と辺の表が、移した後のファイルと辺に揃っている（`test/architecture.test.ts` が通る）
- `CLAUDE.md` 原則3の括弧（SDK の境界を置く機能の例: 「駆動は `session-driver/`、訪問の台本は `visit/`」）に、日記の問い合わせ（`diary/`）が足されている
- `bun run check` が通り、`bun run build` のあと実際に振り返りを1回押して、会話の画面に何も増えず成果の画面に日記が出たことを目視で確かめる

## 背景

- 形は `docs/requirements.md` 4.11「決まっていること」「日記」「振り返りの依頼」（とその中の「会話から切り離す」）、`docs/design.md`「日記の受け取りと保存」（問い合わせの起こし方の表・関所・コマンドと依頼・状態とイベント）、画面は `docs/screen-design.md` 13.10「ボタンを押せないとき・押したあと」と 13.9「いまの作業」の表。採らなかった案は `docs/history/decision.md` の先頭の節
- いまの送り口は `src/server/session/core/session-manager.ts` の `reflectAchievement`（数え直し → ターン中なら断る → `generation.driver` に `beginDiaryDay` → `prompt` → `diary-requested`）。ターン中の断りは同じファイルの `dispatch` にもある
- 窓口と3段目の合図は `src/server/diary/core/diary-tool.ts`（`createDiaryIntake` は `beginDay` / `forgetDay` を持つ）。会話へ載せているのは `src/server/session-driver/adapter/sdk-tool.ts` の `diaryTool`、`diary-drafting` は `session-driver/core/sdk-message.ts` の `diaryDraftingEvents`、窓口と追跡を代ごとに持つのは `session-driver/adapter/sdk-driver.ts`、契約は `session-driver/core/session-driver.ts` の `beginDiaryDay`（fake は `fake-driver.ts`）
- 使い捨ての `query()` の先例は訪問の台本: `src/server/visit/adapter/sdk-visit-script.ts`（起こし方）と `src/server/visit/core/visit-script-writer.ts`（書けた／書けなかったに畳む・中断・reject しない）、配線は `src/session-start.ts`（`queryVisitScript` と `VisitScriptSource`）
- 依頼文は `src/shared/achievement.ts` の `achievementReflectionRequestText`（`chatMode` の引数と report の1行を外す）。理由の文は `src/shared/frame.ts` の `FRAME_ERROR_REASON.achievementReflectionDuringTurn`
- `diaryWriting` の畳み込みは `src/shared/session-state.ts`（`turn-finished` の `settleDiaryWriting` と `session-ended` の `idle`）。画面側は `src/browser/components/page/achievement/hooks/use-achievement.ts`（押せない条件・「会話の画面で様子を見る ›」）と `src/browser/components/domain/screen-nav/hooks/use-current-work.ts`（「振り返り中」の順）

## 決まっていること（蒸し返さない）

- 日記は**会話とは別の使い捨ての `query()`** で書かせる。持たせるのは `diary` だけ（`tools: []`・プロセス内の MCP に `diary` 1つ・`strictMcpConfig: true`・`allowedTools` は `mcp__tsukumo__diary` だけ・`permissionMode: "dontAsk"`）。`settingSources: []`・`persistSession: false`・`includePartialMessages: true`・`maxTurns: 4`（仮）・`thinking: { type: "disabled" }`（`effort` は渡さない）・120 秒で諦める（仮）
- 指示文は `systemPrompt` を文字列で丸ごと置き換え、人格（`persona.md` の全文）と日記を書く役目の短い指示だけを入れる。雑談の記憶の読み戻しは載せない。依頼文はモードで変えない
- モデルは会話のいまのモデル（`SessionState.model`。分からなければ既定）。作業ディレクトリと環境変数は会話と同じ
- 会話の SDK セッションから `diary` を外し、`SessionDriver.beginDiaryDay` と `diary-drafting` の変換（`sdk-message.ts`）・窓口と追跡（`sdk-driver.ts`）を消す。`session-driver` → `diary` の辺は無くなり、`diary` が `session-driver/core/sdk-message.ts` の `tsukumoToolFullName` を読む
- 新しいファイルは `src/server/diary/core/diary-writer.ts`（書き手。`visit-script-writer.ts` と同じ形）と `src/server/diary/adapter/sdk-diary.ts`（使い捨ての `query()`。`diary` の zod の形もここへ移す）。窓口 `createDiaryIntake` は書く日を1つ受け取って問い合わせごとに作る（`beginDay` / `forgetDay` を無くす）
- 会話のターン中・答え待ちでも受ける。断るのは書いている最中（`diaryWriting.kind === "writing"`。理由は新しく足し、ターン中の理由は外す）と、成果が読めない・空の日
- 書き手は代の持ち物。起こし直しで代を閉じたら中断し、閉じたあとに届いたイベントは捨てる。受け付けずに終わった（時間切れ・失敗）ときは書き手が `diary-failed { date }` を流す。`diary-failed` を足すので `PROTOCOL_VERSION` を上げる
- `diaryWriting` は会話の `turn-finished` / `session-ended` で動かさない。起こし直しで初期値に戻るのは今までどおり
- 使用量（token-usage / context-usage）は記録しない（訪問の台本と同じ）
- 疑似セッション（`TSUKUMO_DRIVER=fake`）では起こさず、`diary-requested` のすぐ後に `diary-failed` を流す
- 画面: 押せないのは書いている最中だけ（書いている日は「振り返り中…」、ほかの日は「いま<日付>の日記を書いているので送れない」）。「会話の画面で様子を見る ›」を外す。帯の「いまの作業」は「作業中」を「振り返り中」より上にする

## やること

着手時点で前提を洗い直した結果（`main` はまだ T-565/T-590 時点の「会話の中で書かせる」実装のまま。
`docs/design.md`「日記の受け取りと保存」は T-595 で新方針に書き換え済みだが、コードは未着手）。

**shared**:

- `src/shared/session-event.ts`: `diary-failed { date }` を追加。`diary-requested` はそのまま
  session-manager が出す。`diary-drafting` / `diary-stage` は出し手を「駆動」から「`diary` の
  使い捨ての `query()`（`sdk-diary.ts`）」へ doc コメントを直す
- `src/shared/frame.ts`: `PROTOCOL_VERSION` を 19→20。`FRAME_ERROR_REASON.achievementReflectionDuringTurn`
  を外し、書いている最中に断る理由を新設（`achievementReflectionWriting`）
- `src/shared/achievement.ts`: `achievementReflectionRequestText` から `chatMode` 引数と
  `report` の1行を外す
- `src/shared/session-state.ts`: `diaryWriting` を `turn-finished` / `session-ended` で動かさない
  （`settleDiaryWriting` の呼び出しと `session-ended` の明示リセットを外す。`settleDiaryWriting`
  関数ごと削除）。`case "diary-failed"` を足して `{ kind: "failed", date }` へ畳む

**server/diary**:

- `core/diary-tool.ts`: `createDiaryIntake` を「書く日を1つ受け取って作る」形に変える
  （`beginDay` / `forgetDay` を外し、`DiaryIntake` は `submit` だけに）。`SaveDiaryParagraph` に
  `writer` を足す（書いた時点のパックを窓口の外から注入する経路を無くし、生成時にまとめて
  受け取る）。`createDiaryStageTracker`（生メッセージを追う状態機械）は `sdk-diary.ts` 側の
  役目にし、ここには純関数 `diaryArgumentHasBookmarkKey` だけを残す
- `core/diary-writer.ts`（新規）: 振り返り1回ぶんの書き手。`visit-script-writer.ts` と同じ形
  （reject しない・`signal` で中断）。窓口を1つ作り、`sdk-diary.ts` の `query` 口を呼び、
  `diary-written` が流れなければ `diary-failed` を流す。書く時点のパック・人格・作業先・環境変数
  （`DiaryWriterContext`）は呼び出し側が `readContext()` で渡す
- `adapter/sdk-diary.ts`（新規）: 使い捨ての `query()`。`diary` だけを持つプロセス内の MCP
  サーバを載せ（`tools: []` / `strictMcpConfig: true` / `allowedTools` は `mcp__tsukumo__diary`
  だけ / `permissionMode: "dontAsk"` / `settingSources: []` / `persistSession: false` /
  `includePartialMessages: true` / `maxTurns: 4` / `thinking: disabled`）、`stream_event` の断片から
  `diary-drafting` / `diary-stage` を組み立てて流す。`session-driver/core/sdk-message.ts` の
  `tsukumoToolFullName` / `TSUKUMO_MCP_SERVER_NAME` を読む（diary → session-driver の新しい辺）

**server/session-driver**（会話から `diary` を完全に外す）:

- `core/session-driver.ts`: `SessionDriverOptions.diaryWriter` と `SessionDriver.beginDiaryDay` を削除
- `core/sdk-message.ts`: `diaryDraftingEvents` とその呼び出し、`DIARY_TOOL_NAME` の import を削除
- `adapter/sdk-tool.ts`: `diaryTool` と `diaryIntake` 引数、`DIARY_*` の import を削除
  （`tsukumoServer` のシグネチャから `diaryIntake` を外す）
- `adapter/sdk-driver.ts`: `diaryIntake` / `diaryStageTracker` の生成・`beginDiaryDay` メソッド・
  `relayMessages` 内の `diaryStageTracker.observe` と `diaryIntake.forgetDay()`・`appendDiaryParagraph`
  の import を削除
- `adapter/fake-driver.ts`: `beginDiaryDay` のスタブを削除（contract から無くなるため）

**server/session**（振り返りの送り口を作り直す）:

- `core/session-manager.ts`: `SessionManagerOptions` に `diary: DiaryWriterSource` を追加。
  `reflectAchievement` を書き直す（ターン中でも受け付ける。断るのは
  `state.diaryWriting.kind === "writing"` と、成果が読めない・空の日だけ）。`dispatch` の
  ターン中ガードの `switch` から `reflect-achievement` の分岐を外す。`GenerationTally` に
  `diarySignal`（代ごとの `AbortController`）を足し、`close()` で中断する。疑似セッション
  （`options.diary.kind === "dont-write"`）では `diary-requested` の直後に `diary-failed` を流す

**配線（`src/session-start.ts`）**:

- `startDriver` から `diaryWriter` フィールドを削除
- 直近に起こした代のパック情報（persona・表情・pack/name）を閉じ込めた変数を用意し、
  `startDriver` を呼ぶたびに更新。`createSessionManager` の `diary` オプションへ、この変数を
  読む `readContext` と `queryDiary`・`appendDiaryParagraph` を束ねた `DiaryWriterSource` を渡す
  （疑似セッションのときは `{ kind: "dont-write" }`）

**browser**（`docs/screen-design.md` 13.9/13.10 の確定済みの形にコードを合わせる。
現状は旧方針＝会話の中で書かせる形のまま実装されていて、確定した形と食い違っている）:

- `components/domain/screen-nav/hooks/use-current-work.ts`: 状態の判定順を「答え待ち →
  **作業中** → **振り返り中** → 背景で作業中」に直す（いまは振り返り中が作業中より強く、
  13.9 の表と逆）
- `components/page/achievement/hooks/use-achievement.ts`: `reviewButtonOf` からターン中の
  ブロックを外し、代わりに「ほかの日を書いている最中」のブロック（理由
  「いま<日付>の日記を書いているので送れない」）を足す。`onWatchConversation` を削除
- `components/page/achievement/diary-section.tsx`: 「会話の画面で様子を見る ›」ボタンと
  `onWatchConversation` props を削除
- `components/page/achievement/presentational-achievement-screen.tsx`: `onWatchConversation` の
  受け渡しを削除

**docs**:

- `docs/design.md` 2章の機能の表・辺の表を実装後の形に直す（`diary` の `core`/`adapter` 欄、
  `diary → session-driver` の辺を足す、`session-driver` の読む機能一覧から `diary` を外す）
- `CLAUDE.md` 原則3の括弧に「日記の問い合わせは `diary/`」を足す

**テスト**（完了条件の4本柱 + 既存テストの移設）:

- `test/server/diary/core/diary-writer.test.ts`（新規）: 書き手が窓口を問い合わせごとに作る・
  `diary-written` / `diary-failed` を流す・`signal` で中断できる
- `test/server/session/core/session-manager.test.ts`: `reflectAchievement` がターン中でも受け付ける・
  書いている最中は断る・代を閉じると中断する（`diarySignal` が abort されるのを確かめる）
- `test/shared/session-state.test.ts`: `turn-finished` / `session-ended` で `diaryWriting` が
  動かないこと、`diary-failed` の畳み込み
- `test/server/session-driver/adapter/sdk-tool.test.ts` / `sdk-driver.test.ts` /
  `test/server/session-driver/core/sdk-message.test.ts`: diary 関連のテストを削除（載らなくなるため）
- `test/browser/components/domain/screen-nav/current-work.test.tsx`: 「振り返り中はターンが
  動いていても作業中にならない」テストを「作業中が振り返り中より強い」に直す
- `test/browser/components/page/achievement/*.test.tsx`・`test/shared/achievement.test.ts`:
  `onWatchConversation` / `chatMode` 関連のテストを削除・更新

## 注意

- 着手の時点で T-595 の決定が正典に無ければ、進めずに止まる
- 成果の画面はいま `src/browser/components/page/achievement/` にある（T-582〜T-585 で移った）。着手時の `main` の場所に合わせる

## 結果

- 振り返りを会話とは別の使い捨ての `query()`（`diary/adapter/sdk-diary.ts`、書き手は `diary/core/diary-writer.ts`）で書かせる形にした。会話の駆動から `diary` ツールと `beginDiaryDay` を外し、`diary-failed` を足して `PROTOCOL_VERSION` を 20 にした。ターン中でも頼め、書いている最中の2回目は断る。browser は「いまの作業」の並びを直し、「会話の画面で様子を見る」を外した。2章の機能・辺の表と `CLAUDE.md` 原則3も揃えた
- 検証コマンド: 2665 pass / 0 fail、`bun run build` 成功。疑似セッションで押して会話の画面が変わらず「書けなかった」が出てボタンが戻ること、本物の claude（一時の `TSUKUMO_HOME`）で押して会話の画面が変わらず約1分で日記が保存され「日記帳で読む」に変わることを確かめた
- 振り返り: 同じファイルを3回以上直した（ドラフト 1件）
