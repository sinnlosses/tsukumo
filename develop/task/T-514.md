---
id: T-514
summary: 成果の画面に「つくもと振り返る」を置き、会話へ1ターンの依頼を送る
status: done
difficulty: sonnet
loopable: Y
dependencies: [T-513]
---

## 背景

T-513 で成果の画面ができた。押すとキャラクターが感想を言うボタンを足す。依頼の文面・押せない条件は T-511 で決めた（書いた節は T-511 の `evidence`）。

- 先例はトークン消費の画面の「減らし方を見てもらう」（`src/browser/features/token-usage/`）。送り方は `src/browser/features/task-board/components/task-run-confirm.tsx` の `dispatch({ type: "prompt", ... })`
- キャラクターのセリフは `speak` で言う。レポートの記法と間合いは `src/server/core/report-notation.ts` / `src/server/core/speech-cadence.ts`

## 決まっていること（蒸し返さない）

- 押したときだけ会話へ1ターンの依頼を送る。画面を開いただけではターンを使わない（2026-09-24 ユーザー決定）

## やること

1. ボタンを置き、T-511 の文面で依頼を送る
2. 押せない条件（ターンが進んでいる・雑談モードなど。T-511 の結論）で押せなくし、理由を添える
3. `docs/screen-design.md` を実装に合わせて直す（ずれが出たときだけ）

## 完了条件

- 押すと依頼が1回送られ、押せない条件では送られない（部品のテスト）
- 実際の tsukumo で押し、キャラクターが `speak` で感想を言ったことを `evidence` に書く（会話の中身は写さず、何が起きたかだけ書く）
- `bun run check` が通る

## 結果

成果の画面に「<パック名>と振り返る」ボタンを置き、押すと依頼 prompt を1回送って会話画面へ移る。ターン進行中・空の日は送らず理由を出す（test/browser/features/achievement/ の部品テスト）
実機（TSUKUMO_VIEW_PORT=4790・一時 TSUKUMO_HOME、Playwright）で押し、依頼が会話に出てキャラクターが speak で感想を言った
bun run check 2291 pass / 0 fail
