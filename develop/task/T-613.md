---
id: T-613
summary: `components/page/` の各画面のボタンを、ボタンの部品に置き換える
status: done
difficulty: sonnet
loopable: Y
dependencies: [T-612]
---

## 目的

前段で作ったボタンの部品を、残る `components/page/` の画面にも行き渡らせ、画面ごとに手書きされたボタンの顔を無くす。

## 完了条件

- `src/browser/components/page/` の `<button>` のうち、**顔が前段の `Button` の `variant` のどれかと同じもの**が `Button` に置き換わり、顔の規則が消えている（箱——高さ・余白・角丸——と置き方は `className` に残す）。計画時の実測で候補になるのは、枠だけ（`.character-button`・`.diary-book-topbar-button`・`.achievement-day-switch-button` / `-today`・`.turn-nav-button`・`.pending-action`）、accent の枠（`.achievement-review-button`）、accent の塗り（`.character-create-submit`・`.diary-book-review-button`・`.diary-notice-open`・`.usage-review-start`・`.dispatch-send`）、`--state-ng` の塗り（`.character-delete-ok`・`.character-clear-confirm-ok`）、`--state-warn` の塗り（`.question-ask-answer`）、リンク風（`.character-link-button`・`.achievement-diary-open-book`・`.usage-review-previous`・`.usage-review-result-close`・`.usage-table-more`）、地も枠も無い（`.dispatch-tool`・`.diary-notice-dismiss`）
- **置き換えないもの**: 押せる行・押せる文字（`.achievement-calendar-day`・`.diary-book-toc-day`・`.chat-poke`・`.turn-title-toggle`・`.turn-history-jump`・`.character-list-new`・`.character-card-action`・吹き出し）、トークン消費の画面の `--usage-*` の顔（`.usage-review-stop`・`.usage-review-retry`・`.usage-review-proposal-*`・`.token-usage-choice`）、顔が一覧に無いもの（`.speech-log-open` / `-close`）。地の色が少し違うもの（`.pending-action` の `--surface` の地など）は、置いた場所で見た目が変わらないなら置き換え、変わるなら残す。残したものは理由を結果に1行ずつ書く
- 部品の API を広げたなら、`test/browser/components/ui/button/button.test.tsx` にその行を足してある（顔を足すなら `docs/design.md` 2章の一覧も直す）
- 押せないの書き方を `aria-disabled` に揃えたことで、もと `disabled` だったボタンがフォーカスできるようになった箇所を結果に書く
- `test/architecture.test.ts`「components/ui/ の部品の className」が通る
- 会話の画面（入力欄の送信・道具のボタン・許可・質問の札・札の頭の ‹ ›）・キャラクターの画面（作成・名前の編集・削除の確認・立ち絵を外す確認を含む）・成果の画面（日記帳の見開きと書き終わりの知らせを含む）・使用量の画面を起動して目視し、押す前・hover・focus-visible・押せないときの見た目を確かめている（何を見たかを結果に書く。hover・押せないときの見た目が顔に揃って変わったものは、どう変わったかを書く）
- `bun run check` が通る

## 背景

- 画面ごとに高さ（`30px` / `34px` / `2.25rem` / `2.5rem` / `2.75rem`）と角丸（`0.4rem` / `0.5rem` / `0.5625rem` / `8px` / `9px` / `0.875rem`）が違う。これは箱なので `className` に残る
- 作法は `docs/design.md` 2章「`components/ui/` の部品（variant の作法と一覧）」が正典

## 決まっていること（蒸し返さない）

- **箱（高さ・余白・角丸）は段に丸めない**（揃えるかは別の判断として持ち越し）
- トークン消費の画面の `--usage-*` の顔は `Button` の語彙に入れない
- 押せる行・押せる文字は `Button` にしない

## やること

いまのコードで前提を取り直した結果（着手前の実測）:

- 候補の一覧にある class は概ねそのまま存在する。ただし実際に置き換えるとき、以下の追加判断が要る:
  - **`.character-button` は `<button>` だけでなく `<label>`（顔・背景の「差し替える」）にも使われ、
    `.character-button-danger` と組んだ「消す」にも使われる。** `Button` の className の検査は
    class 名ごとにファイル全体の規則を見るので、`.character-button` に残った `:disabled` /
    `border` / `color` の顔を消すと、`Button` 化しない `<label>` 側の見た目も道連れで消える。
    そこで **箱だけを持つ新しい class `.character-button-outline` を足し、`Button` 化する
    6箇所だけをそちらへ移す**（`.character-button` 自体は `<label>` と `-danger` の組のために
    手を付けない）
  - **`.pending-action` は必ず `.pending-allow` / `.pending-deny`（state-ok / state-ng の枠色）と
    組んで使われ、単独の「枠だけ」の姿では存在しない。** `Button` の variant に state-ok の枠が
    無く、揃えると色が消えるので**見送り**（報告に理由を書く）
  - **`.dispatch-send` は `type="submit"` で、`[data-shortcut]::after` が付ける
    「⌘⏎」のヒントが `data-shortcut` 属性を要る。** `Button` は決まった props しか持たず属性を
    素通しする口が無いので、既存の見た目（ヒント）を保ったまま置き換える手段が無い。**見送り**
  - **`.character-button-danger`（顔を消す・パックを消すの帯）は state-ng の「枠だけ」で、
    `Button` の variant に state-ng の枠（`outline-danger` 相当）が無い。** `outline-warn` は
    state-warn で色が違う。**見送り**
  - 顔の CSS に `font-weight: 700` を持つもの（`.diary-notice-open` など9箇所）は、太さが
    `Button` の顔（`border`/`background`/`color`/`cursor`/`opacity`/`text-decoration` に加え
    `.button-variant-ghost[aria-pressed="true"]` の `font-weight` も含む）に既に含まれるため、
    呼び出し側の class に残すと検査に落ちる。**`Text`（`weight="bold"`）で文字を包んで太さを保つ**
    （`dispatch.module.css` の `.pending-tool` が同じ手口を既に使っている）
  - リンク風の `.achievement-diary-open-book` は**常時**下線（他のリンク風は hover だけ）。
    `Button` の `link` は hover だけ下線なので、**揃えると見た目が変わる**（報告に書く）

## 注意

- 1コミットに収まらない量だと分かったら、画面単位で後続タスクに切り出してよい

## 結果

- `components/page/` のボタン34か所を `Button` に置き換えた（成果13・キャラクター11・使用量4・会話6）。太さ700の顔は `Text` の `weight="bold"` で包んで保ち、`<label>` と共有する `.character-button` は箱だけの `.character-button-outline` を分けた。state-ok/ng の枠・`data-shortcut` の要る送信・`<label>`・押せる行・`--usage-*` の顔は残した
- 見た目が変わった4点（ユーザーに確認中）: 押せないときが個別の色から `opacity: 0.6` の減光に揃った（11か所）、書き終わりの知らせの閉じるが 16px→15px、「日記帳を開く」が常時下線→hover の下線、入力欄の道具の hover で地が浮かなくなった。`disabled` から `aria-disabled` にした7か所はフォーカスを受け、押しても呼ばれないことをテストで守った
- 検証コマンド: 2780 pass / 0 fail（+6）、`bun run build` 成功。ポート 39411/39412 で変更前と並べ、会話・キャラクター・使用量の画面は既定の見た目が一致し、違いは上の4点だけなことを確かめた。成果の画面は変更前が `serve-revision.ts` で出せず、変更後だけ見た
- 振り返り: 兆候なし
