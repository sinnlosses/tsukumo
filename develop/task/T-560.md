---
id: T-560
summary: 振り返り済みの done / dropped のタスクファイルを消す `task prune` を足す
status: done
difficulty: opus
loopable: Y
dependencies: [T-558, T-559]
---

## 目的

`develop/task/` が際限なく増えないように、振り返りが済んだ `done` / `dropped` のタスクファイルを `git rm` で消す仕組みを入れる。本文は git の履歴から読む。

## 完了条件

- `task` にサブコマンド `prune` があり、消してよいタスクファイルだけを `git rm` して stage する（コミットはしない。ほかのサブコマンドと同じく出力の1語で結果が分かる）。`--dry-run` で消す対象の一覧だけを出せる
- 消してよいかの判定が、T-558 のあとの振り返りの記録の持ち方に合っている（下の論点1）
- `WORKFLOW.md`「ファイル配置と CLAUDE.md」の「アーカイブの段は無い」が、振り返り済みの `done` / `dropped` は `task prune` で消す、に書き換わっていて、サブコマンドの参照の表に `prune` が載っている
- `prune` を誰がいつ呼ぶかが、該当するスキル（`next-task` か `retrospect` の手順、など）に書いてある（論点2）
- tsukumo で1回走らせ、消したファイルを1コミットにして `main` へ送っている。そのあと `task status` の `invalid` が0で、依存の解決が変わっていない
- 下の「解くべき論点」の1つ1つに結論があり、完了時の結果に論点 → 結論を1行ずつ書く
- claude-skills の `./check.sh` が通り、変更が claude-skills の `main` に入っている。tsukumo 側は `bun run check` が通る

## 背景

2026-09-24 に利用者が決めた（`docs/history/direction.md` の 2026-09-24 の項）。直近の振り返りでは1日に71件・76件のタスクを見ていて、いまの「`done`・`dropped` も同じ場所に残す。アーカイブの段は無い」（`WORKFLOW.md`「ファイル配置と CLAUDE.md」）のままだと `develop/task/` が際限なく増える。

- 依存の解決は既に「`done`・`dropped` と、タスクファイルに無い ID は解決済み」（`WORKFLOW.md`「状態」の節）なので、消しても依存は壊れない
- 採番は台帳の `last-id` なので、消した番号が再利用されることはない
- `task.py` のサブコマンドは `status` / `new` / `claim` / `release` / `done` / `ship` / `migrate`（`cmd_*` 関数と `add_parser`）
- いまの `retrospect` は `develop/retrospective.md` の「最後に振り返ったコミット」1行を基準点にしている。T-558 がこの基準点の持ち方を変えうる（T-558 の論点1・2）ので、このタスクは T-558 のあとに置いた
- 振り返りが消えたファイルを読めるようにするのは前段のタスク（依存に置いた）

## 決まっていること（蒸し返さない）

- 移すのではなく消す（`develop/task/done/` のような置き場は作らない）
- 消すのは `/retrospect` ではない。`retrospect` の SKILL.md の「`develop/task/` を直接書き換えない」を守る
- 消す手段は `task` のサブコマンド（`task prune`）にする
- `/retrospect` が読み終える前のタスクは消さない

## 解くべき論点

1. **消してよいかの判定**: T-558 のあとの形で「振り返りが済んだ」をどう判定するか。基準点のコミットの祖先に `done` / `dropped` にしたコミットがある、で足りるか。タスクごとに振り返る形になっていれば、結果の節の記録で判定するほうが確かか
2. **呼ぶ契機**: 人が手で呼ぶのか、`/next-task` や `/retrospect` の手順の最後で呼ぶのか。`/loop` で回っているときに消してよいか
3. **コミットの単位**: 消したファイルを単独の1コミットにするか、振り返りの記録のコミットと一緒にするか。並行する作業ツリーが同じファイルを触っていて `task ship` の rebase でぶつかる場合の扱い

## やること

調べ直した前提（claude-skills `411742a`）: `task.py` のサブコマンドは背景のとおり7つ。依存の解決（`is_resolved`）はファイルの無い ID を解決済みにする。`task ship` は `main` の版で `done`/`dropped` になった自分の印だけを消す（`_release_own_claims_when_shipped`）ので、**印の立っているタスクのファイルを同じ送りで消すと、印が消えずに残る**。`scan.py` の `has_review_line` はコミットの版を読むので、あとで消したファイルでも `reviewed` を判定できる。`material.py` は消したファイルを最後の版から読む（`0012398`）。tsukumo の `develop/retrospective.md` の基準点は `7ef5a7ba`。採番は台帳の `last-id` が主で、台帳は git の外（別の機械で台帳が無いと、消した番号より大きい番号の決め方がタスクファイル・`docs/history/tasks.md` だけに頼る。単一の機械で使う前提なので手当てしない）。

論点の結論:

1. **判定**: `HEAD` の版で `done`/`dropped`、かつ台帳に印が立っていない、かつ次のどちらか——(a) `## 結果` に `- 振り返り:` の行がある（1件ごとの振り返り済み）、(b) `develop/retrospective.md` の基準点のコミットの版で既に `done`/`dropped`（＝そうしたコミットが基準点の祖先にある。まとめての振り返りが読み終えた）。(b) は基準点の版のファイルを1回読むだけで済む。記録ファイルが無い・1行が無いときは (a) だけで判定し、1行のハッシュがリポジトリに無いときは `INVALID`
2. **契機**: `/next-task` の手順7（タスクの1コミット）の直後に `task prune` を打ち、`PRUNED` なら消したぶんを別の1コミットにして、手順8の同じ `task ship` で一緒に送る。`/loop` から回っているときも走る（消しても本文は履歴から読め、依存も壊れない）。いま完了にしたタスクは印が立っているので消えず、次のサイクルで消える。`/retrospect` は消さない（手で呼んだあとに消したければ人が `task prune` を打つ）
3. **コミットの単位**: 消したぶんは単独の1コミット。件名は `振り返り済みのタスクファイルを消す（N件）` でタスクIDを置かない（`scan.py` が件名のIDで割り付けるので、IDがあるとそのタスクの材料に削除が混ざる）。並行する作業ツリーが同じファイルを消していた場合は、rebase で両側の削除が一致して空になったコミットは git が落とす。消したファイルを相手が書き換えていたら（`done` のファイルを書き換える手順は無いので起きにくい）`task ship` が `CONFLICT` で止まるので、人に預ける（ほかの衝突と同じ）

作業（claude-skills の `feature/tsukumo-T-560` の作業ツリーで）:

- `task-workflow/scripts/task.py`: `prune [--dry-run]` を足す。clean でなければ `DIRTY`（4）。対象ごとに `PRUNE\tT-xxx\t<reviewed|retrospect>`、最後に `PRUNED\t<N>`（`git rm` して stage。コミットしない）／`PLAN\t<N>`（`--dry-run`）／対象が無ければ `NOTHING`
- `task-workflow/scripts/selftest_task.py`: `prune` のテスト（判定の2条件・印・`--dry-run`・DIRTY・依存が解決済みのまま）
- `task-workflow/WORKFLOW.md`: 「ファイル配置」の行、「1サイクル」、サブコマンドの参照の表と終了コードの表
- `next-task/SKILL.md`: 手順7の後に prune と別コミット、完了報告
- `retrospect/SKILL.md`: 「このスキルがしないこと」にタスクファイルを消さない（基準点を進めると次の `task prune` の対象になる）、材料の節に「消したものは最後にあった版」
- `docs/task-workflow-redesign.md`: 5章のサブコマンドと、アーカイブの段を持たないとしている箇所
- `task.py` の冒頭の使い方の行

## 注意

- 作業するのは claude-skills のリポジトリ（`~/ghq/github.com/sinnlosses/claude-skills`。`~/.claude/skills/` の各スキルはそこへの symlink で、書き換えは全プロジェクトへ即座に効く）。**着手時に `git -C ~/ghq/github.com/sinnlosses/claude-skills status --short` が空でなければ止めて預ける**
- claude-skills の枝は本体の作業ツリーで checkout せず、`git worktree add` で別の作業ツリーを切ってそこで作業する。枝の名前は `feature/tsukumo-<このタスクのID>`。検証コマンドは claude-skills の `CLAUDE.md` に従う（`./check.sh`）
- コミットは claude-skills の側に入る。tsukumo のタスクの結果には claude-skills のコミットハッシュを書く
- 着手時に T-558 の結果を読み、前提（基準点の持ち方）が変わっていたら論点1をそれに合わせて解く

## 結果

- claude-skills `7fce1b2`: `task prune [--dry-run]` を足した。消すのは HEAD で done/dropped・印なし・（`## 結果` に `- 振り返り:` がある か 基準点の版で既に done/dropped）のもの。`/next-task` に手順7a（タスクのコミットの直後に prune、別の1コミット、同じ ship で送る）を足した。自己テストに `test_prune` を足した
- tsukumo で初回を走らせ、17件（基準点で読み終えた16件＋1件ごとに振り返った T-558）を消した
- 検証コマンド: 2304 pass / 0 fail（+19）。claude-skills の `./check.sh` も通過
- 振り返り: 同じファイルを3回以上直した（タスクファイル T-560.md の書き足しで、作業の迷いではない）（ドラフト 0件）
