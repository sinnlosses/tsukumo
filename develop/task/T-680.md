---
id: T-680
summary: `question-scroll.tsx` か `turn-selection.tsx` の store を zustand に置き換えて形を決める
status: todo
difficulty: sonnet
loopable: Y
dependencies: []
---

## 目的

ブラウザ側の自作 store（`useSyncExternalStore` の定型と Context の Provider）を zustand に寄せる
最初の1件。小さい store を1つ置き換えて、残りへ広げるときの形（store の置き場所・セレクタの書き方・
テストでの差し込み方）を決める。

## 完了条件

- `src/browser/stores/question-scroll.tsx`（56行）か `src/browser/stores/turn-selection.tsx`（83行）の
  どちらか1つが zustand の store になり、`subscribe` / `getSnapshot` / listeners の定型が消えている
- `zustand` が `dependencies` に入っている
- `docs/coding-standards.md`「React」節に、zustand の store の書き方と `useShallow` の使いどころ
  （セレクタがその場で配列・オブジェクトを作るときだけ）が書かれている
- 後段（zustand の残りを置き換えるタスク）の本文を、決めた形に合わせて直している
- `bun run check` が通る

## 背景

- 自作 store は `useSyncExternalStore` で書かれ、`src/browser/stores/` に7ファイル
  （`session.tsx` 335行・`question-answer.tsx` 274行・`location-hash.ts` 178行・`screen.tsx` 110行・
  `turn-selection.tsx` 83行・`main-view-turn.ts` 61行・`question-scroll.tsx` 56行）と、
  `src/browser/domain/reveal/brush-tip.ts`・
  `src/browser/components/page/achievement/hooks/use-diary-book-open-request.ts` にある
- `session.tsx` の `useSessionSelector` のコメントどおり、セレクタがその場で作ったオブジェクトを
  返すと描き直しが止まらない。zustand でも同じで、`useShallow` で避ける
- `docs/research/architecture-rethink.md` の状態管理の行で「配り方が面倒になったら zustand」と
  控えにしていたものを本採用にする（2026-09-26 ユーザーの指示）

## 決まっていること（蒸し返さない）

- 状態管理は zustand にする（2026-09-26 ユーザーの判断）

## 注意

- `T-669` が `test/browser/stores` の単体テストを監査して消す。先に済んでいたら消えたテストを
  書き戻さない
