---
id: T-555
summary: CLAUDE.md や docs を縮めるタスクに、消した規則ごとの行き先を求める
status: todo
difficulty: haiku
loopable: Y
dependencies: []
---

## 目的

正典（`CLAUDE.md`・`docs/`）の節を削る・縮めるタスクで、規則が行き先の無いまま消えるのを防ぐ。

## 完了条件

- `docs/workflow.md`「タスクを書くとき・受け入れるとき」に、「CLAUDE.md や docs の節を削る・縮めるタスクは、消した規則ごとの行き先（移した節・捨てた理由）の表を完了条件に書かせ、受け入れでその表と差分を突き合わせる」旨の1項目がある
- `grep -c '^#\{2,3\} ' docs/workflow.md` の数が編集の前後で変わっていない
- `bun run check` が通る

## 背景

2026-09-24 の振り返り（`develop/retrospective.md` の `f1adaf96`..`7ef5a7ba`）で見つけたもの。T-528 で `CLAUDE.md` を 413行 → 347行に縮めたとき、「新しい作業ツリーの立ち上げは人がやる（`bun install` と `bun run build`。自動化はしない）」がどこにも移されずに消え、受け入れのときに見つけて戻した。完了条件で項目ごとの行き先の表を求めた T-527（`develop/progress.md` の89件）では、消えたものは見つかっていない。

## 注意

- 同じ趣旨の項目がすでに `docs/workflow.md` にあれば、足さずに `dropped` で閉じてよい（「正典の大掃除は移送先へ先に書く」の項は順序の話で、行き先の網羅とは別）
