---
id: T-538
summary: 成果の集計で、互いに依存しない git 呼び出しを同時に走らせる
status: todo
difficulty: haiku
loopable: Y
dependencies: []
---

## 背景

2026-09-24 に `/simplify` の4観点（再利用・単純化・効率・根本対処）でリポジトリ全体を洗った結果の1件（`docs/history/direction.md` の 2026-09-24「改修コストを下げる直し9件」）。

`src/server/adapter/main-history.ts` の成果の集計（`GET /achievement` が呼ぶ）は、今日と昨日の切り口（`cutoffCommitBefore` を2回）と、今日と昨日のタスクの読み取り（`readTaskSnapshotSource` を2回）を、それぞれ `await` で1つずつ順に待っている。どちらの組も片方の結果をもう片方に渡していないので、同時に走らせてよい。成果の画面は今日を見ているあいだ60秒ごとに取り直す（`src/browser/features/achievement/hooks/use-achievement.ts`）。

## やること

1. 2組をそれぞれ `Promise.all` にする。`unavailable` の判定と返す値は変えない

## 完了条件

- 2組の呼び出しがそれぞれ `Promise.all` の中にある
- `main-history.ts` の既存テストが変更なしで通る
- `bun run check` が通る
