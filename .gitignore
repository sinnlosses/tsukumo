# 依存
node_modules/

# macOS
.DS_Store

.idea/

# ブラウザ側の成果物（bun run build で作る。2.6MB の生成物を履歴に入れない）
dist/

# キャラクター素材（権利のあるものを公開リポジトリに入れないため）
characters/local/

# Claude Code のセッションが作るロック（設定ファイルだけを履歴に入れる）
.claude/scheduled_tasks.lock
