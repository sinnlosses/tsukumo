node_modules/

# 上流（mattpocock/skills）由来のスキル。取り直したときに差分が汚れるので整形しない
.claude/

# 過去の記録。oxfmt の折り返しが強調記法とコード中の `*` を壊すので整形しない
docs/history/

# E2E の期待値。足場（test/e2e/scenario-run.ts）が書いたままの形にし、走らせた結果
# （/tmp/tsukumo-e2e/）とそのまま diff できるようにする
test/e2e/expected/
